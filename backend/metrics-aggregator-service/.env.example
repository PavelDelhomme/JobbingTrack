# Prisma - metrics-aggregator-service
# Copier en .env si vous lancez npx prisma depuis ce dossier (ex: npx prisma db push).
# Depuis la racine du repo, préférer : make db-push-metrics (charge le .env racine).

# Postgres local (Docker : port 5432 mappé sur l'hôte)
DATABASE_URL=postgresql://jobbingtrack:jobbingtrack123@localhost:5432/jobbingtrack?schema=public
