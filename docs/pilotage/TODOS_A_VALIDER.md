# TODOs à valider par le porteur

Dernière mise à jour : 19 juin 2026 (batterie mobile 19/19 + smokes suite Lot D)

## Règle

Ce fichier bloque la suite produit. Tant qu’une ligne **bloquante** est ouverte ici, l’agent ne doit pas avancer vers une nouvelle fonctionnalité.

Règle d’ordre : l’agent et le porteur traitent **la première ligne ouverte uniquement**. Pas de validation suivante, pas de préparation parallèle, pas de “suite” tant que cette ligne n’est pas validée explicitement ou transformée en problème à corriger.

Quand une ligne est validée par le porteur, la déplacer vers `TODOS_DONE.md` avec la date, l’environnement et la preuve.

## Priorités (échelle)

| Niveau | Sens |
|--------|------|
| **P0** | Bloquant produit local : sécurité, rapports, menaces, HTTPS — à valider avant le reste. |
| **P1A** | Sécurité / opérations sensibles : alertes, archives, restauration, tests offensifs contrôlés, actions avant purge. |
| **P1B** | Observabilité métier : Statistics, logs, corrélation, chiffres cohérents. |
| **P1C** | UX backoffice et lisibilité sécurité : thème, popup, graphes, navigation, pages de sécurité non destructives. |
| **P1D** | Gate de fin de journée / avant gros merge : suite complète et lecture rapports. |
| **P2** | Utile mais non bloquant pour la file courte ; peut attendre la fin des P0/P1 ou aller dans `docs/BACKLOG.md` si reporté. |

## Comment valider

Le porteur valide la première ligne ouverte soit en répondant dans le chat avec `OK` ou `KO` + détail, soit en renseignant les colonnes de cette même ligne :

- `Notes porteur` : observations libres, ressenti UI, problème vu, amélioration souhaitée.
- `Preuves porteur` : capture, chemin de rapport, URL testée, valeur observée, compteur avant/après.
- `Décision porteur` : `OK <nom exact>` ou `KO <nom exact>` + détail.

L’agent ne coche pas à la place du porteur : après un `OK` explicite, il archive la ligne dans `TODOS_DONE.md` ; après un `KO`, il corrige ou crée la tâche de correction avant toute suite.

## Mode opératoire détaillé pour valider

Guide pas à pas : **où cliquer**, **quelles commandes lancer**, **quoi regarder**, **quoi répondre**. Une ligne du tableau = une validation à la fois.

### Prérequis communs (toutes les validations UI)

| Étape | Action |
|-------|--------|
| 1 | Stack locale up : conteneurs `jobbingtrack-postgres` et `jobbingtrack-frontend` **healthy** (`docker ps --filter name=jobbingtrack`). Si down : cible Make documentée **`up-full`** (équivalent `docker compose up -d` depuis la racine du repo). |
| 2 | Connexion : [https://jobbingtrack.localhost:5443/login](https://jobbingtrack.localhost:5443/login) (ou [http://localhost:5003/login](http://localhost:5003/login)) avec `ADMIN_EMAIL` / `ADMIN_PASSWORD` du `.env`. |
| 3 | Hub backoffice : [https://jobbingtrack.localhost:5443/b4ck0ff1ce](https://jobbingtrack.localhost:5443/b4ck0ff1ce) — menu latéral gauche. |
| 4 | Terminal : se placer à la racine du repo (`cd …/JobbingTrack`). Préférer `/usr/bin/env node …` si la commande `node` échoue silencieusement (shell `lazynvm`). |

---

### P1A — Archive logs sécurité sans purge

**But** : exporter des logs en archive gzip + manifest, **sans** `DELETE` ni baisse du compteur `security_logs`.

> **Important — pas de bouton dans l’interface** : l’archivage ne se fait **pas** depuis `/b4ck0ff1ce/security/logs`. C’est un **script terminal** (`security-logs-archive-export.cjs`). L’UI sert seulement à **vérifier** que les logs sont toujours visibles après l’export. La page Statistics → Sécurité mentionne les scripts mais ne les lance pas.

#### A. Interface (contrôle visuel uniquement — pas d’action « Archiver »)

| # | Où aller | URL directe | Quoi vérifier |
|---|----------|-------------|---------------|
| 1 | Menu **Sécurité** → **Logs** | [https://jobbingtrack.localhost:5443/b4ck0ff1ce/security/logs](https://jobbingtrack.localhost:5443/b4ck0ff1ce/security/logs) | La liste des logs s’affiche (pagination OK). **Aucun bouton « Archiver » attendu ici.** |
| 2 | Menu **Sécurité** → **Vue d’ensemble** | [https://jobbingtrack.localhost:5443/b4ck0ff1ce/security](https://jobbingtrack.localhost:5443/b4ck0ff1ce/security) | Bandeau éventuel « résultat tronqué » si > 2000 lignes sur 30 j — normal. |
| 3 | Menu **Statistics** → **Sécurité** | [https://jobbingtrack.localhost:5443/b4ck0ff1ce/statistics/security](https://jobbingtrack.localhost:5443/b4ck0ff1ce/statistics/security) | Carte ambre « Volume logs sécurité » + **noms** des scripts (info seulement). |

#### B. Terminal (export sans purge)

Copier-coller depuis la racine du projet :

```bash
# 0 — Postgres accessible
docker ps --filter name=jobbingtrack-postgres --format '{{.Names}} {{.Status}}'

# 1 — Compteur AVANT export (noter le chiffre)
docker exec jobbingtrack-postgres sh -lc \
  'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -At -c "SELECT COUNT(*) FROM security_logs;"'

# 2 — Dry-run lecture seule (optionnel, aucune écriture)
/usr/bin/env node scripts/security/security-logs-retention-dry-run.cjs

# 3 — Export archive SANS purge (commencer petit si erreur ENOBUFS)
/usr/bin/env node scripts/security/security-logs-archive-export.cjs --class=noise --limit=50

# 4 — Compteur APRÈS export (doit être IDENTIQUE à l’étape 1)
docker exec jobbingtrack-postgres sh -lc \
  'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -At -c "SELECT COUNT(*) FROM security_logs;"'

# 5 — Lire le manifest produit aujourd’hui
cat "data/archives/security-logs/$(date -I)/manifest.json"
ls -la "data/archives/security-logs/$(date -I)/"
```

**Fichiers attendus** (gitignorés, sur disque local) :

- Dossier : `data/archives/security-logs/YYYY-MM-DD/`
- `manifest.json` — champ `"note": "Export only — no rows deleted from security_logs"`
- `noise.jsonl.gz` (ou autre classe : `critical.jsonl.gz`, `high.jsonl.gz`, `standard.jsonl.gz`)

**Preuve agent déjà prête (10/06)** : export `noise` 50 lignes → `data/archives/security-logs/2026-06-10/` ; compteur `security_logs` **42311** avant et après.

#### C. Critères OK / KO

| OK si | KO si |
|-------|-------|
| `manifest.json` lisible, `exportedRows` > 0 | Pas de dossier `data/archives/security-logs/…` |
| Fichier `.jsonl.gz` présent | `manifest` illisible ou vide |
| Compteur étape 1 = compteur étape 4 | Compteur `security_logs` a baissé |
| UI logs toujours accessibles après export | Erreur script sans archive utilisable |

#### D. Réponse à copier dans le chat

- OK : `OK Archive logs sécurité sans purge`
- KO : `KO Archive logs sécurité` + chemin manquant ou compteur avant/après

### P1A — Restauration logs sécurité en staging

**But** : recharger une archive dans **`security_logs_restore_staging` uniquement**, jamais dans `security_logs`.

#### A. Interface (contrôle visuel)

| # | Où aller | URL | Quoi vérifier |
|---|----------|-----|---------------|
| 1 | **Sécurité** → **Logs** | [http://localhost:5003/b4ck0ff1ce/security/logs](http://localhost:5003/b4ck0ff1ce/security/logs) | Les logs normaux sont toujours là, inchangés. |

#### B. Terminal (restauration staging)

**Prérequis** : avoir fait l’export P1A (dossier `data/archives/security-logs/YYYY-MM-DD/`).

```bash
# 1 — Compteur table réelle (ne doit PAS changer après restore)
docker exec jobbingtrack-postgres sh -lc \
  'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -At -c "SELECT COUNT(*) FROM security_logs;"'

# 2 — Restauration en staging uniquement
/usr/bin/env node scripts/security/security-logs-archive-restore.cjs \
  --class=noise --load-staging

# 3 — Compteur staging (doit être > 0 si export non vide)
docker exec jobbingtrack-postgres sh -lc \
  'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -At -c "SELECT COUNT(*) FROM security_logs_restore_staging;"'

# 4 — Recompter table réelle (identique à l’étape 1)
docker exec jobbingtrack-postgres sh -lc \
  'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -At -c "SELECT COUNT(*) FROM security_logs;"'

# 5 — Aperçu staging (optionnel)
docker exec jobbingtrack-postgres sh -lc \
  'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "SELECT row_id, retention_class, source_file FROM security_logs_restore_staging LIMIT 3;"'
```

**Sortie attendue du script** : lignes `No write to public.security_logs` et `Target table: public.security_logs_restore_staging`.

**Preuve agent déjà prête (10/06)** : `staging_loaded=50`, `security_logs` reste **42311**.

#### C. Critères OK / KO

| OK si | KO si |
|-------|-------|
| Script affiche « No write to public.security_logs » | Message d’insert dans `security_logs` |
| `security_logs_restore_staging` > 0 | Staging vide après export non vide |
| Compteur `security_logs` inchangé | Compteur `security_logs` modifié |
| UI logs inchangée | Logs manquants ou différents en UI |

#### D. Réponse

- OK : `OK Restauration staging logs sécurité`
- KO : `KO Restauration staging` + compteur ou table concernée

### P1A — Alertes email critiques JobbingTrack

**But** : alias publics visibles, réauth admin pour modifier, email de test reçu dans MailHog.

#### A. Interface

| # | Où aller | URL / chemin | Action |
|---|----------|--------------|--------|
| 1 | **Menu latéral Sécurité → Alertes email** (ou sous-nav sur une page Sécurité) | [https://jobbingtrack.localhost:5443/b4ck0ff1ce/security/alerts](https://jobbingtrack.localhost:5443/b4ck0ff1ce/security/alerts) | Page dédiée avec le bloc **Alertes email — menaces & disponibilité**. Accès aussi via **Paramètres** (popup ou `/settings`) → onglet **Notifications** → bouton rouge **Ouvrir Alertes email sécurité**. |
| 2 | Bloc **Destinataires** | même page | Vérifier destinataires (`@jobbingtrack.test` ou alias publics, pas d’adresse perso). |
| 3 | Modifier / tester | champ **Mot de passe actuel** obligatoire | Saisir `ADMIN_PASSWORD`, **Enregistrer les alertes sécurité** puis **Envoyer un email de test**. |
| 4 | Boîte mail locale | [http://localhost:8025](http://localhost:8025) (MailHog) | Email reçu ; expéditeur = alias (`SECURITY_ALERT_FROM` / `SMTP_FROM`). |
| 5 | Boîtes réelles si miroir SMTP activé | SMTP OVH / fournisseur | Vérifier aussi la réception sur les destinataires réels attendus (`dev@…`, `admin@…`) ; MailHog reste la preuve locale principale. |
| 6 | Diagnostic admin | [https://jobbingtrack.localhost:5443/b4ck0ff1ce/email-monitor?type=NOTIFICATION](https://jobbingtrack.localhost:5443/b4ck0ff1ce/email-monitor?type=NOTIFICATION) | Filtre **Notification** actif ; vérifier statut, destinataire, date et bouton **Voir le contenu** si la boîte réelle ne reçoit pas. |
| 7 | Panneau diagnostic page Alertes | [https://jobbingtrack.localhost:5443/b4ck0ff1ce/security/alerts](https://jobbingtrack.localhost:5443/b4ck0ff1ce/security/alerts) — bloc **Derniers envois alertes** | Encart **Dernier envoi vers admin** avec sujet/horodatage/miroir ; cartes `admin@delhomme.ovh` surlignées ; rappel spam/délai fournisseur. |

**Note** : l’ancien accès via `/settings` onglet Notifications reste possible, mais la validation porteur doit utiliser la page dédiée Sécurité → Alertes email pour éviter toute ambiguïté.

**Limite connue** : ces alertes passent encore par la stack JobbingTrack (`security-service` / `notification-service`). Elles ne suffisent pas si toute la stack tombe. Un watcher externe léger, séparé de Docker/JobbingTrack, doit être prévu pour vérifier l’indisponibilité complète et envoyer une alerte mail directement via SMTP/fournisseur.

#### B. Critères OK / KO

| OK si | KO si |
|-------|-------|
| Réauth demandée avant sauvegarde/test | Modification sans mot de passe |
| Email visible dans MailHog :8025 | Pas d’email / erreur SMTP |
| Diagnostic visible dans **Gestion des emails → Historique** filtre **Notification** | Impossible de retrouver l’email dans l’admin |
| Expéditeur = alias public JobbingTrack | Adresse perso dans Git/UI/rapport |
| Si miroir SMTP activé : logs `miroir SMTP envoyé` et réception réelle confirmée | Miroir bloquant ou erreur SMTP réelle non expliquée |
| Email d’alerte enrichi avec contexte utile : service touché, sévérité, horodatage, derniers logs ciblés redigés, lien diagnostic admin | Email trop vague sans élément d’enquête ou avec secrets/payloads sensibles bruts |

#### C. Réponse

- OK : `OK Alertes email critiques`
- KO : `KO Alertes email critiques` + capture ou erreur UI

### P1A — Tests offensifs contrôlés par conteneur

**But** : valider le **cadrage** (matrice + bornes), pas lancer une campagne agressive.

#### A. Documents à lire (5–10 min)

- `docs/security/SECURITY_TESTING_MATRIX.md`
- `docs/security/COMPOSE_RUNTIME_HARDENING.md`

#### B. Interface (consultation)

| # | Où aller | URL |
|---|----------|-----|
| 1 | **Développement** → **Tests** | [http://localhost:5003/b4ck0ff1ce/tests](http://localhost:5003/b4ck0ff1ce/tests) |
| 2 | Carte **Tests offensifs contrôlés** | même page — vérifier le bouton **Périmètre offensif lab** ; il génère un rapport plan-only, sans payload |
| 3 | **Développement** → **Tests** → **Tests Sécurité** | [http://localhost:5003/b4ck0ff1ce/tests-security](http://localhost:5003/b4ck0ff1ce/tests-security) |
| 4 | **Développement** → **Rapports de tests** | [http://localhost:5003/b4ck0ff1ce/test-reports](http://localhost:5003/b4ck0ff1ce/test-reports) — vérifier `controlled-offensive-20260612-013308` |

**Interdit** : ZAP actif, nmap, SYN flood, spoofing sur prod. Cibles lab : `localhost:5002` uniquement.

**Preuve agent fraîche (12/06 01h33)** : `bash scripts/security/run-controlled-offensive-lab-scope-with-report.sh` → `tests/results/controlled-offensive/20260612-013308`, `dryRun=true`, `willRunPayload=false`, Jest préflight/lab-scope **9/9 OK**.

#### C. Réponse

- OK : `OK Tests offensifs contrôlés`
- KO : `KO Tests offensifs` + menace ou cible problématique

### P1A — Leurres / désinformation contrôlée VPS-Portainer

**But** : valider le **design** de réduction d’exposition — **pas** déployer de honeypot.

#### A. Documents à lire

- `docs/deployment/VPS_PORTAINER_NPM_OVH.md`
- `docs/security/COMPOSE_RUNTIME_HARDENING.md`
- `A_VALIDER_AVANT_PRODUCTION.md` (gate « Masquage infos infra / leurres »)

#### B. Vérif rapide

- Admin : [http://localhost:5003/b4ck0ff1ce/security](http://localhost:5003/b4ck0ff1ce/security) — les vraies alertes restent visibles.
- Public : Portainer / Postgres / metrics ne doivent pas être accessibles hors ports prévus.

**Preuve agent fraîche (12/06 01h40)** : documents relus (`docs/security/VPS_EXPOSURE_REDUCTION.md`, `docs/security/COMPOSE_RUNTIME_HARDENING.md`, `docs/deployment/VPS_PORTAINER_NPM_OVH.md`) ; conclusion conservatrice : réduction d’exposition d’abord, Portainer/NPM admin via IP allowlist/VPN/tunnel, NPM réseau partagé seulement avec `frontend`/`api-gateway`, pas de leurre/honeypot sur domaine utilisateur réel. Aucune action offensive ou déploiement de leurre lancé.

#### C. Réponse

- OK : `OK Leurres/désinformation design`
- KO : `KO Leurres/désinformation` + risque identifié

### P1B — Corrélation performances

| # | Menu | URL | Action |
|---|------|-----|--------|
| 1 | **Performances** → **Corrélation** | [http://localhost:5003/b4ck0ff1ce/performances/correlation](http://localhost:5003/b4ck0ff1ce/performances/correlation) | Choisir un service (ex. `api-gateway`). |
| 2 | Même page | — | KPI logs/erreurs non tous à 0 si stack active ; sinon message « pas de données » explicite. |

**Preuve agent fraîche (12/06 01h51)** : `scripts/ops/run-performance-correlation-validation-with-report.sh` → `tests/results/performance-correlation/20260612-015102` (**5/5 étapes OK**) ; central logging runtime **15/15** ; `aggregated_logs` contient **27604** lignes / **18** services ; endpoints metrics et page `/b4ck0ff1ce/performances/correlation` répondent **200** ; rapport ouvrable via `performance-correlation-20260612-015102`.

**Réponse** : `OK Corrélation performances` ou `KO Corrélation performances` + service + détail.

### P1B — Statistics (4 validations séparées)

Menu **Statistics** → sous-onglets en haut de page.

| Validation | URL | Vérifications |
|------------|-----|---------------|
| Sécurité | [http://localhost:5003/b4ck0ff1ce/statistics/security](http://localhost:5003/b4ck0ff1ce/statistics/security) | Cohérent avec [http://localhost:5003/b4ck0ff1ce/security](http://localhost:5003/b4ck0ff1ce/security). |
| Logs | [http://localhost:5003/b4ck0ff1ce/statistics/log-stats](http://localhost:5003/b4ck0ff1ce/statistics/log-stats) | Filtres service/niveau ; compteurs cohérents. |
| Données applicatives | [http://localhost:5003/b4ck0ff1ce/statistics/app-data](http://localhost:5003/b4ck0ff1ce/statistics/app-data) | Pas de `undefined`. |
| Vue d’ensemble | [http://localhost:5003/b4ck0ff1ce/statistics](http://localhost:5003/b4ck0ff1ce/statistics) | Graphes dispo/erreur ; bascule 24 h ↔ 7 j. |
| Temps de réponse endpoints | Statistics / Performances selon route exposée | Services HTTP critiques visibles ou état explicite : `auth-service`, `deployment-service`, `call-service`, `notification-service`, `followup-service`, `application-service` ; source claire `monitoring-agent-rs` / `metrics-aggregator`, moyenne instantanée/agrégée non trompeuse. Postgres reste une dépendance santé Docker, hors panneau temps de réponse P1B. |

**Preuve agent fraîche Statistics Sécurité (12/06 01h56)** : endpoints `persistence/security/metrics`, `persistence/security/summary`, `persistence/stats` répondent **200** ; `security/metrics` = **2000** points, `summary.dataPoints=2000`, `security_logs=42296` et `security_metrics=97779` confirmés en BDD ; `/b4ck0ff1ce/statistics/security` et `/b4ck0ff1ce/security` répondent **200**.

**Preuve agent fraîche Statistics log-stats (12/06 02h08)** : `scripts/ops/run-statistics-log-stats-validation-with-report.sh` → `tests/results/statistics-log-stats/20260612-020820` (**4/4 étapes OK**) ; `smoke-persistence-stats.cjs` OK ; `/persistence/stats` → `aggregatedLogs=27606` confirmé BDD ; `/persistence/logs` 14j → **799** lignes, filtre WARN → **50**, filtre `jobbingtrack-api-gateway` → **3** ; `/b4ck0ff1ce/statistics/log-stats` HTTP **200** ; rapport `statistics-log-stats-20260612-020820` listé dans test-reports.

**Preuve agent fraîche Statistics app-data (12/06 13h22)** : correctif ports `event/followup` dashboard-service ; `scripts/ops/run-statistics-app-data-validation-with-report.sh` → `tests/results/statistics-app-data/20260612-132256` (**4/4 OK**) ; `/api/v1/statistics` sans `undefined` ; `followups=152`, `events=1011` cohérents BDD ; timeline fallback 1 point + message explicite ; `/b4ck0ff1ce/statistics/app-data` HTTP **200** ; rapport `statistics-app-data-20260612-132256`.

**Revalidation agent Statistics app-data (16/06 23h34)** : login admin migré Node (`load-root-env.cjs`) ; précheck `ensure-dashboard-service-ready.sh` ; rapport `statistics-app-data/20260616-233410` **4/4 OK** ; `/api/v1/statistics` **200**, `users=146`, `undefinedHits=0` ; pages `/backoffice/statistics/app-data` smoke OK.

**Preuve agent fraîche Statistics vue d’ensemble (12/06 13h27)** : `scripts/ops/run-statistics-overview-validation-with-report.sh` → `tests/results/statistics-overview/20260612-132737` (**4/4 OK**) ; `system/metrics` 24h et 7j → **51** points / disponibilité présente ; Jest séries **3/3 OK** ; `/b4ck0ff1ce/statistics` HTTP **200** ; rapport `statistics-overview-20260612-132737`.

**Revalidation agent Statistics vue d’ensemble (16/06 23h34)** : rapport `statistics-overview/20260616-233410` **4/4 OK** ; history 7 j **45** points ; ranges API **24h=91**, **7d=174** ; Jest `statisticsTimeSeries` OK ; pages `/backoffice/statistics` + sous-routes smoke OK.

**Preuve agent fraîche temps de réponse endpoints (15/06 02h)** : `responseTime.per_service` exposé ; le panneau P1B Vue d’ensemble affiche uniquement les services HTTP prioritaires (`auth`, `deployment`, `call`, `notification`, `followup`, `application`) et n’affiche plus Postgres / `Santé Docker`. Vérification runtime Playwright headless : panneau présent, `postgres=false`, `Santé Docker=false`. Jest ciblé P1B **2 suites OK** dans le lot **3 suites / 15 tests OK** ; Jest CI frontend complet **40 suites / 175 tests OK** ; `type-check`, `lint --quiet`, `format:check` OK.

**Réponses** : `OK Statistics Sécurité` · `OK Statistics log-stats` · `OK Statistics app-data` · `OK Statistics vue d’ensemble` · `OK Statistics temps de réponse endpoints` · etc. (ou `KO` + route).

### P1C — UX backoffice (une validation = une réponse)

| Validation | Navigation | Étapes |
|------------|------------|--------|
| Mode sombre | Barre haute → icône 🌙/☀️ | Passer sombre → F5 sur `/b4ck0ff1ce` → reste sombre. |
| Popup paramètres | Bouton **Paramètres** barre haute | Clic hors popup / `Escape` ferme ; clic dedans OK. |
| Graphes conteneurs | **Performances** → **Conteneurs** [lien](http://localhost:5003/b4ck0ff1ce/performances/containers) | « Tous les conteneurs » : couleurs CPU/RAM distinctes. |
| Plages temporelles | Sous-pages Performances | 24 h → 7 j → personnalisé : pas de vide durable. |
| 1re navigation perf. | Hub → **Performances** | Délai + graphes présents. |
| Sécurité FR | Sous-onglets Sécurité | [Politiques](http://localhost:5003/b4ck0ff1ce/security/policies) [Menaces](http://localhost:5003/b4ck0ff1ce/security/threats) [Firewall](http://localhost:5003/b4ck0ff1ce/security/firewall) [Logs](http://localhost:5003/b4ck0ff1ce/security/logs) [Analyse](http://localhost:5003/b4ck0ff1ce/security/analysis) [Réseau](http://localhost:5003/b4ck0ff1ce/security/network) |
| Comparaison rapports | [test-reports](http://localhost:5003/b4ck0ff1ce/test-reports) | 2 rapports non sécurité → **Comparer** → tableau. |
| Menu Tests | **Développement** → **Tests** | Clic = vue d’ensemble ; **Rapports de tests** dans sous-menu. |
| Responsive backoffice complet | DevTools navigateur ou écran réel | Vérifier petit écran, écran moyen et largeur intermédiaire sur `/b4ck0ff1ce`, Email Monitor, Utilisateurs, Sécurité, Performances, Statistics et Rapports : menu utilisable, pas de débordement horizontal, tableaux/listes lisibles, filtres empilés, boutons accessibles. |

**Réponses** : `OK <nom exact ligne tableau>` ou `KO <nom exact>` + ce que tu vois.

## À valider maintenant (ordre strict — une ligne à la fois)

| Priorité | Validation porteur | Environnement | Preuve attendue | Notes porteur | Preuves porteur | Décision porteur | Statut | Notes agent |
|----------|--------------------|---------------|-----------------|---------------|------------------|------------------|--------|-------------|
| P1B | Statistics — onglet Logs (`log-stats`) | local | Suivre § “P1B — Statistics sécurité/logs/données applicatives/vue d’ensemble” : filtres service/niveau, sources actives/historiques, compteurs cohérents. **Vérifier aussi** : pills période lisibles, chips niveau colorés, autocomplete services, graphe **Volume de logs dans le temps** avec plage d’échantillon affichée. | okay valide | OK Statistics log-stats | OK Statistics log-stats | [x] | **Preuve agent 15/06** : les options **Niveau** et **Service** restent stables grâce à un échantillon non filtré, même après application d’un filtre. Correctif backend associé : `/api/v1/persistence/stats` ferme son `PrismaClient` en `finally` pour éviter la saturation Postgres observée pendant les smokes (`too many clients`). Runtime : `/persistence/stats` `aggregatedLogs=28121`, `/persistence/logs` 14 j = **800**, filtre `WARN` = **800**, filtre `jobbingtrack-api-gateway` = **66** ; boucle `/persistence/stats` **20/20 OK** après restart `metrics-aggregator` healthy. Validations sans `make` : backend Jest persistence **2 suites / 3 tests OK**, frontend `format:check` OK, `type-check` OK, `lint --quiet` OK, Jest ciblé log-stats **2 suites / 6 tests OK**, Jest CI frontend **44 suites / 185 tests OK**, Playwright Statistics **5/5 OK**, perf HTTP moy. `/statistics/log-stats` ~135 ms, `/statistics` ~244 ms, `/services/logs` ~105 ms. Mail récap final `[JobbingTrack] Recap P1B Statistics log-stats FINAL 2026-06-15` : `EmailLog` **3/3 SENT**, miroir SMTP **3/3 true**. **Revalidation 16/06 23:21** : rapport `statistics-log-stats/20260616-232133` **4/4 OK** ; `aggregatedLogs=26645` ; filtres WARN/gateway **50/50** ; Playwright `security-logs-filters` **1/1**. **Revalidation 17/06 branche `feat/p1b-validations-performances-e2e`** : rapport `statistics-log-stats/20260617-120015` **4/4 OK** ; `aggregatedLogs=26761`. **Post-merge PR #17** : `statistics-log-stats/20260617-142627` **4/4 OK** ; `aggregatedLogs=26770`. **Post-merge PR #19 + branche log-stats (17/06)** : `statistics-log-stats/20260617-150040` **4/4 OK** ; filtres multi Niveau/Service ; Jest logStatsFilters **5/5**. **Post-commit Performances Disque (17/06 16h08)** : `statistics-log-stats/20260617-160815` **4/4 OK** ; `aggregatedLogs=26829`, **18** services. **Refonte UI filtres + graphe temporel (17/06)** : pills période ; chips niveau sémantiques ; `FacetMultiValueField` services ; graphe volume logs + `ChartPeriodCaption` ; Jest logStatsTimeSeries **2/2** ; `tsc` + `lint --quiet` OK. **Validation porteur 17/06** : « okay valide ». |
| P1B | Statistics — onglet Données applicatives (`app-data`) | local | Suivre § “P1B — Statistics sécurité/logs/données applicatives/vue d’ensemble” : totaux, timeline, états vides lisibles, pas de `undefined`. **Vérifier aussi** : pills période timeline 7/14/30 j, toggle séries sur le graphe, `ChartPeriodCaption`, snapshot explicite si un seul point API. | ouais ca me vas | OK Statistics app-data | OK Statistics app-data | [x] | **Preuve agent 15/06** : la note API de timeline fallback est maintenant conservée par `statisticsService.getStatisticsTimelineResult()` et affichée dans `/statistics/app-data` quand la timeline renvoie un seul snapshot. Runtime : `/api/v1/statistics` HTTP 200 (`applications=1043`, `users=146`, `companies=761`, `contacts=2`, `interviews=128`, `calls=200`, `followups=140`, `events=973`) ; `/api/v1/statistics/timeline?time_range=7d&limit=500` HTTP 200, **1 point**, note `Timeline simplifiée (fallback)…`, aucun `undefined` brut dans les payloads. Validations sans `make` : `node --check` controller statistics OK, frontend `format:check` OK, `type-check` OK, `lint --quiet` OK, Jest app-data **2 suites / 5 tests OK**, Jest CI frontend **45 suites / 187 tests OK**, Playwright Statistics **5/5 OK**, perf HTTP moy. `/statistics/app-data` ~222 ms, `/statistics` ~49 ms, `/statistics/log-stats` ~54 ms. Mail récap `[JobbingTrack] Recap P1B Statistics app-data 2026-06-15` : `EmailLog` **3/3 SENT**, miroir SMTP **3/3 true**. **Revalidation 16/06 23:34** : script validation migré Node (`load-root-env.cjs`, `smoke-statistics-app-data-api.cjs`, `smoke-backoffice-page-urls.cjs`, `ensure-dashboard-service-ready.sh`) ; rapport `statistics-app-data/20260616-233410` **4/4 OK** ; `undefinedHits=0`, timeline **1** point + note fallback ; pages `/backoffice/statistics/app-data` smoke OK. **Revalidation 17/06 branche `feat/p1b-validations-performances-e2e`** : rapport `statistics-app-data/20260617-120331` **4/4 OK** ; `undefinedHits=0`. **Post-merge PR #17** : `statistics-app-data/20260617-142705` **4/4 OK** ; `undefinedHits=0`. **Refonte UI timeline (17/06)** : pills 7/14/30 j, toggle séries, `ChartPeriodCaption`, domaine X sticky ; Jest `appDataTimeline` **4/4** ; `tsc` + `lint --quiet` OK. **Validation porteur 17/06** : « ouais ca me vas ». |
| P1B | Statistics — graphes disponibilité / erreur (vue d’ensemble) | local | Suivre § “P1B — Statistics sécurité/logs/données applicatives/vue d’ensemble” : courbes chargées, légende source visible, plage 24h↔7j OK. Vérifier que **Disponibilité dans le temps** et **Taux d’erreur dans le temps** sont sur deux lignes larges, pas en deux colonnes. Changer la plage (ex. 24 h → 7 j) : l’axe X doit couvrir toute la fenêtre sticky (pas seulement les points présents) ; tooltips avec date/heure locale. **Vérifier aussi** : `ChartPeriodCaption`, source `system_metrics` et mention dérivation erreur visibles. | partiel OK hors graphes vides | KO graphes vides | OK Statistics vue d'ensemble | [x] | **Validation porteur 17/06** : « OK Statistics vue d'ensemble même si partiel ». Correctifs préalables merge `dev` : marqueurs sparse, retrait panneau P1B. Réorientation mobile. |
| Lot D | Mobile — auth + parcours candidatures | local + appareil/émulateur | Sur l’app Flutter (`mobile/`) : (1) cold start → splash → **reste connecté** si session valide ; (2) login `testuser@jobbingtrack.test` → home ; (3) onglet **Candidatures** : liste non vide (JWT transmis) ; (4) **Nouvelle candidature** → création OK → retour liste avec la nouvelle entrée ; (5) logout → retour login. Gateway dev `127.0.0.1:5002` (adb reverse) ou `10.0.2.2:5002` (émulateur). Voir `docs/mobile/AUDIT_AUTH_APPLICATIONS.md`. | liste OK ; déconnexion = login sans fermer app + purge locale | Samsung R5CT7263YJL | OK auth candidatures | [x] | **Preuve agent 18/06** : smokes **19/19** + shell ADB **OK**. **Fix logout 18/06** : `UserSessionCleanup` purge session chiffrée, files offline/télémétrie, caches providers, `keepLoggedIn=false` ; navigation `/login` sans reload app. **Validation porteur 18/06** : OK (liste non vide, clarification déconnexion vs hors-ligne). Archivé `TODOS_DONE.md`. |
| Lot D | Mobile — Paramètres : télémétrie anonyme + Aide/retours | appareil + local | Dans **Paramètres** : (1) toggle “Partager des données anonymes” active/désactive (RGPD) ; (2) toggles “Performances anonymes” + “Trace d’activité” ; (3) “Diagnostic local” affiche un résumé ; (4) Aide & retours : **Bug / Suggestion / Signalement** → formulaire, option “Joindre diagnostic”, envoi OK. Vérifier qu’en mode **sans consentement**, seuls les retours manuels sont envoyés (pas de traces auto). | clavier OK ; toggles OK | Samsung | OK paramètres télémétrie aide retours | [x] | **Preuve agent 17–18/06** : `MobileAnalyticsService` ; formulaire retour clavier corrigé ; `smoke-feedback-adb.js` **OK**. **Validation porteur 18/06** : OK (déjà validé clavier + paramètres). Archivé `TODOS_DONE.md`. |
| Lot D | Mobile — déconnexion drawer/menu (régression 19/06) | Samsung R5CT7263YJL | (1) Drawer → **Déconnexion** → confirmer → écran **Connexion** en ~2 s (session effacée). (2) Menu **⋮** → **Déconnexion** → idem. (3) Écran empreinte → **Se déconnecter** → idem. (4) Après déconnexion : cold start → login (pas d’accueil direct). | OK logout | Samsung R5CT7263YJL | OK déconnexion drawer menu | [x] | **Validation porteur 19/06** : OK. Archivé `TODOS_DONE.md`. **Preuve agent 19/06** : `appNavigatorKey`, smoke ADB drawer **OK**. |
| Lot D | Mobile — biométrie (login, déverrouillage, reconnexion empreinte) | Samsung R5CT7263YJL | (1) Login : cocher biométrie → déverrouillage immédiat sans fermer app. (2) Cold start : empreinte → accueil. (3) Après déconnexion : compte enregistré + **Connexion par empreinte**. (4) Paramètres : activation biométrie. (5) **Fallback mot de passe** : formulaire visible + « Se connecter avec le mot de passe » si empreinte refusée/changée. | biométrie OK | Samsung R5CT7263YJL | OK biométrie mobile | [x] | **Validation porteur 19/06** : « biométrie carrément OK ». Archivé `TODOS_DONE.md`. **Suite agent 19/06** : fallback mot de passe + smokes `TEST_USER_*` **OK** ADB sans empreinte. |
| Lot D | Mobile — Inscription + télémétrie obligatoire + vérif email | appareil + local | **Inscription** : case télémétrie cochée par défaut, refus bloque la création ; après OK → écran « Vérifiez votre email » + bouton renvoi. **Deep link** : `make mobile-verify-email TOKEN=…` ou lien mail → écran vérification → login. **Admin rapide** : `make mobile-login-admin` (`.env` `TEST_ADMIN_*`). | | | | [ ] | **Preuve agent 19/06** : `smoke-register-adb.js` + `smoke-verify-email-adb.js` **OK** ; `smoke-register-telemetry-refuse-adb.js` **OK** — refus télémétrie bloque. Doc `docs/mobile/INSCRIPTION_VERIFICATION_EMAIL.md`. **Reste porteur** : renvoi email ; deep link manuel. |
| Lot D | Mobile — navigation retour, admin, relances, ajouts candidature | appareil + local | (1) **Retour** : depuis Profil/Paramètres/Admin/Événements → retour = page précédente (pas accueil forcé). (2) **Admin** : compte USER → section Administration invisible ou accès refusé ; compte ADMIN → hub admin + utilisateurs/analytics/logs/statistiques/corbeille OK. (3) **Relances** : liste sans crash date ; pas de bouton + global ; création depuis **détail candidature** uniquement. (4) **Appel** : choix contact existant ou création rapide (nom seul ou prénom nom). (5) **Entretien** : date + lieu + notes depuis détail candidature. | | | | [ ] | **Preuve agent 19/06** : `smoke-mobile-navigation-adb.js` **OK** (retour Paramètres→Profil) ; `smoke-mobile-application-detail-fab-adb.js` **OK** ; `smoke-mobile-fab-relance-adb.js` **OK** — création relance depuis FAB. **Reste porteur** : retour depuis Admin/Événements ; appel/entretien depuis FAB.
| Lot D | Mobile — intérim, biométrie, admin sécurisé | appareil + local | (1) **Paramètres** → activer « Mode intérim » : menu Intérim visible, champ agence sur nouvelle candidature, calendrier ambre pour événements intérim. (2) **Connexion** : cocher « Garder la connexion » + « Déverrouiller biométrie » → fermer app → rouvrir → écran empreinte puis accueil. (3) **Admin** : user normal ne voit pas ADMIN ; admin voit hub + sous-pages. (4) Sans « garder connexion » : cold start → écran login. | biométrie OK porteur 19/06 | | | [ ] | **Preuve agent 19/06** : `smoke-mobile-settings-interim-adb.js` **OK** ; `smoke-mobile-interim-home-adb.js` **OK** ; `smoke-mobile-interim-calendar-adb.js` **OK** — event API `#F59E0B` visible calendrier. **Reste porteur** : confirmer visuellement icône ambre sur l’appareil.
| Lot D | Mobile — entreprises, contacts, édition profil | appareil + local | (1) **Entreprises** : liste non vide (si données), recherche, tap → détail. (2) **Contacts** : liste, recherche, **+** créer contact minimal, tap → détail. (3) **Profil** → Modifier : changer prénom/nom/téléphone → Enregistrer → retour profil à jour. (4) **Admin → Données de test** : message renvoi backoffice (pas de générateur sur mobile). | | | | [ ] | **Preuve agent 19/06** : `smoke-mobile-entities-adb.js` enrichi — recherche, création contact, détails ; `smoke-mobile-accounts-adb.js` — Données de test → backoffice. **Preuve agent 19/06** : `smoke-mobile-profile-save-adb.js` **OK** — téléphone persisté. **Reste porteur** : validation visuelle profil.
| Lot D | Mobile — notifications, à venir, biométrie D6, création entreprise | appareil + local | (1) **Cloche** : liste notifications **métier uniquement** (candidatures, relances, entretiens, appels, rappels push) — **pas** de crash/erreurs techniques ; marquer lu / tout marquer lu / supprimer (swipe ou ✕) ; badge **uniquement** si non-lues (icône toujours visible). Même règle compte **admin** sur mobile. (2) **Accueil** : bloc « À venir » (entretiens + relances). (3) **Entretiens** : onglets À venir / Passés. (4) **Biométrie** : activer dans Paramètres (mot de passe) → kill app → empreinte → accueil ; si session expirée, reconnexion auto. (5) **Entreprises** : **+** créer entreprise (option agence intérim si mode intérim). | biométrie OK porteur 19/06 | OK ligne 321 centre notifs | connexion OK ; cloche sans crash | OK ligne 321 (centre) | [ ] | **Preuve agent 19/06** : `smoke-mobile-notification-nav-adb.js` **OK** ; `smoke-mobile-company-create-adb.js` **OK** ; cold-start **OK**. **Preuve agent 19/06** : `smoke-mobile-home-upcoming-adb.js` **OK** — bloc « À venir » visible. **Reste porteur** : validation visuelle bloc À venir.
| Lot D | Mobile — autocomplete entreprise + parcours live complet | appareil + API | (1) **Nouvelle candidature** : champ entreprise unique — taper, choisir existante ou « Créer « nom » ». (2) **Détail candidature** : contact, entretien, relance, appel avec contact, appel **sans contact**. (3) **API** : `node scripts/mobile/smoke-full-journey-api.js` → 19/19 OK. (4) **Calendrier**, **Paramètres** retours, **déconnexion/reconnexion**. (5) Safe area bas écran. **Pas de push/rappels locaux** (backlog). **Agent email** : compte dev perso uniquement. | | | | [ ] | **Preuve agent 19/06** : `smoke-full-journey-api.js` **19/19 OK**. **Preuve agent 22/06** : `smoke-mobile-live-journey-adb.js` **OK** Samsung ; login émulateur **OK**. **Preuve agent 22/06** : re-run `smoke-full-journey-api.js` **19/19** post-fix FK analytics (session/event/perf/crash). **Reste porteur** : autocomplete tap ; détail appel sans contact ; safe area.
| Lot D | Mobile — télémétrie offline (appareil / perte réseau) | appareil + local | (1) Activer télémétrie dans Paramètres. (2) Couper réseau → naviguer. (3) Rouvrir réseau → vérifier `/user-analytics` + Diagnostic : `telemetryQueuePending=0`. | | | | [ ] | **Preuve agent 19/06** : `smoke-offline-business-adb.js` **OK** ; `smoke-mobile-offline-telemetry-adb.js` **OK** — navigation hors-ligne + Diagnostic local après retour réseau. **Reste porteur** : `telemetryQueuePending=0` dans backoffice.
| Lot D | Mobile — télémétrie erreurs login/CRUD (FK analytics corrigée) | appareil + local | (1) Login `TEST_USER_*` → parcourir candidatures/détail (CRUD). (2) Logs Docker : **aucun** `user_errors_sessionId_fkey` sur `POST /analytics/errors`. (3) `/backoffice/user-analytics` : erreurs/latence API visibles (7 j, user test). (4) Paramètres → Diagnostic : flush sans erreur réseau. | | | | [ ] | **Preuve agent 22/06** : `ensureAnalyticsSession()` avant insert erreurs/perf ; smokes analytics + parcours API **19/19** ; login émulateur **OK** ; doc `EMULATEUR_ADB.md`. **Reste porteur** : vérif visuelle backoffice.
| Lot D7 | Mobile — workflow API + push dev + offline métier | appareil + API | (1) `node scripts/mobile/smoke-workflow-api.js` → status + trigger dev OK. (2) Login app → token push enregistré (`smoke-push-register-api.js` OK). (3) **Offline métier** : réseau OFF → nouvelle candidature → message file → réseau ON → candidature visible API + liste app. (4) Réseau toujours rétabli après test. | | | | [ ] | **Preuve agent 19/06 (re-run)** : `smoke-workflow-api.js` + `smoke-push-register-api.js` + `smoke-offline-business-adb.js` **OK**.
| Lot D | Mobile — FAB détail candidature + navigation liens entités | appareil | (1) **Détail candidature** : FAB « Ajouter » → contact / relance / entretien / appel (plus de boutons par section). (2) **Détail contact** : entreprises, candidatures liées (API), relances et appels. (3) **Détail entretien/relance/appel** : candidature, entreprise, contacts navigables. (4) Analytics test user : sessions actives visibles pour `TEST_USER_EMAIL`. | | | | [ ] | **Preuve agent 19/06** : FAB + analytics **OK** ; `smoke-mobile-entity-links-adb.js` **OK** ; `smoke-mobile-followup-nav-adb.js` **OK** — relance → candidature ; `smoke-mobile-interview-nav-adb.js` **OK** — entretien → candidature. **Reste porteur** : navigation appel → candidature.
| Lot D | Mobile — popup candidature, statuts FR, contacts, édition entités | appareil | (1) **Nouvelle candidature** : FAB ou bouton vide → **popup** (pas page pleine, pas de drawer) → création → liste rafraîchie. (2) **Statut candidature** : libellés **français** ; bouton « Changer statut » → choix manuel → automatismes désactivés (`statusEngineOptOut`). (3) **Contact** : création avec **notes** depuis candidature/entreprise ; visible sur détail entreprise ; archiver / corbeille. (4) **Détail relance/entretien/appel** : bouton modifier conserve les données. (5) Parcours candidature → contact → retour OK. | | | | [ ] | **Preuve agent 19/06** : `smoke-mobile-application-sheet-adb.js` **OK** — popup accueil, statuts FR liste + picker « Résultat / statut ». **Reste porteur** : création complète, notes contact, édition relance.
| Lot D | Mobile — FAB accueil, plateforme, safe area, lien contact | appareil | (1) **Accueil** : FAB + ouvre popup nouvelle candidature. (2) **Plateforme utilisée** : liste + « + Ajouter une plateforme » (perso utilisateur). (3) Bouton **Créer** visible au-dessus de la barre de navigation basse. (4) Date candidature affiche l’**heure locale** (~14:06 si créée à 14:06). (5) **Lier un contact existant** sur détail candidature : plus d’erreur 500. | | | | [ ] | **Preuve agent 18/06** : fix `getContactsByCompany` ; `PlatformPickerField` ; `shell_layout.dart` ; heure UTC corrigée ; APK install Samsung **Success**. |
| Lot D | Mobile — statuts auto, entreprise picker, appel, analytics réseau | appareil | (1) Statut manuel = **refus/retenue/offre** uniquement ; pas « 1 relance » sans relance créée. (2) **Relance créée** → statut candidature mis à jour auto. (3) **Entretien** → « Entretien à venir/imminent ». (4) Entreprise : sheet avec sélection visible. (5) Appel : même format dialog que relance. (6) Erreurs réseau visibles dans analytics. | | | | [ ] | **Preuve agent 18/06** : cascade backend followup/interview ; labels FR cycle candidature ; analytics `connection_refused`. |
| Lot D | Mobile/Backoffice — appareils analytics UUID + version Android | appareil + local | (1) Relancer l’app mobile (télémétrie ON) → login. (2) `/backoffice/user-analytics` → onglet **Versions & App mobile** : colonne **ID appareil** (UUID tronqué, survol = complet) + **Version OS** ex. `Android 14 (API 34)`. (3) Erreurs réseau : même ID appareil corrélable. (4) Ancien `mob-*` remplacé par UUID au prochain lancement. | | | | [ ] | **Preuve agent 17/06** : `DeviceId.generateUuidV4()` ; `MobileDeviceInfo` Android API level ; table backoffice enrichie. Tests Flutter **5/5** ; `tsc` direct OK. Rebuild APK pour migration ID sur Samsung. |
| Lot D | Mobile/Backoffice — analytics app (perf live, retours, erreurs) | local + appareil | (1) **Performances live** : bouton **Actualiser** ; KPI RSS/snapshots/appareils cohérents. (2) **Retours** : **1 envoi = 1 ligne** ; clic → diagnostic complet. (3) **Erreurs auto** : tri/filtre + détail + **Marquer traité** (sans ManualReport retour). (4) Email sur `CRASH_REPORT_EMAIL` + visible Email Monitor (**Crash / retour mobile**). (5) Formulaire bug : **clavier reste ouvert**, champ description visible, bouton **Envoyer** dans l’AppBar (icône en haut à droite). (6) Actualiser via **Actions** ou icône recherche ; email HTML lisible ; snapshots compressés. | clavier OK formulaire bug | Samsung R5CT7263YJL — saisie description + Envoyer AppBar | OK analytics app perf retours erreurs | [x] | **Preuve agent 18/06** : refresh global AdminLayout ; email retour enrichi ; `smoke-feedback-adb.js` **OK** ; fix clavier 18/06 22h (`resizeToAvoidBottomInset: false`, arbre stable). **Validation porteur 18/06** : OK clavier formulaire bug. Archivé `TODOS_DONE.md`. |
| Lot D | Backoffice — Analytics utilisateur + Activité mobile | local | (1) `/backoffice/user-analytics` : page charge sans erreur ; vue d'ensemble avec 4 aperçus ; onglet **Événements** avec filtres source (mobile/backoffice/API) + type ; pagination + clic détail. (2) `/analytics/application/activity` : table **Événements** (pas « récents ») avec filtres type/nom ; performances paginées + détail. (3) App mobile connectée + télémétrie : naviguer quelques écrans → données visibles dans Activité (scope application). (4) Erreurs API mobile (≥400) visibles dans onglet Erreurs utilisateur après action provoquant une erreur. (5) **Top pages** : libellés lisibles (Accueil, Candidatures…) et compteurs/barres cohérents avec filtre 7 j. (6) **Sélecteur utilisateur** admin + sessions actives temps réel (refresh 30 s). (7) App debug : bannière bas d'écran « Télémétrie ON · file … · prochain flush … ». | | | | [ ] | **Preuve agent 18–19/06** : fix backend `days=` + `countForGroupBy` + `userId` query ; frontend `pageLabels.ts` + sélecteur user ; `smoke-analytics-api.js` **OK** ; Flutter **5/5** ; bannière dev flush. **Suite 18/06 soir** : KPI **Latence API moy.** fetch `api_latency` (500 lignes) ; crash Flutter (Hero FAB, setState build, dropdown intérim) corrigés mobile ; `GET /followups/:id` + `contacts/company` **200** ; `smoke-mobile-shell-adb.js` **OK** (4 onglets shell, 0 FlutterError logcat) ; APK Samsung reinstallé. |
| P1C | Lot A graphes — Statistics axes temps + lazy charts | local | Ouvrir `/b4ck0ff1ce/statistics` (onglet Vue d’ensemble) et onglet **Santé technique** : vérifier chargement fluide (spinner court puis graphes), axe X cohérent avec la barre période (24 h / 7 j), ticks date si plage > 24 h, tooltips date/heure sur survol. | | | | [ ] | **Preuve agent 15/06** : `StatisticsErrorAvailabilityCharts` — `timeMs` + domaine X ; `page.tsx` — `lazy` + `Suspense` + props `chartXMin/Max` sur Overview et Security. Validations sans `make` : `tsc --noEmit --pretty false` OK ; Jest **3/3** ; Playwright Statistics **5/5 OK**. |
| P1B | Statistics — moteur UI partagé shell | local | Ouvrir `/b4ck0ff1ce/statistics`, `/statistics/security`, `/statistics/log-stats` et `/statistics/app-data` : vérifier que le layout admin, la sous-navigation Statistics, l’en-tête, les actions de rafraîchissement, les filtres et les graphes restent cohérents sans double marge ni rupture visuelle. | | | | [ ] | **Preuve agent 15/06** : les états chargement/erreur de la vue d’ensemble utilisent maintenant `StatisticsPageShell`, donc la sous-navigation et l’en-tête restent cohérents aussi hors état nominal. Le smoke Playwright vérifie la sous-navigation partagée `Vue d’ensemble / App data / Sécurité / Logs (stats)` sur les 4 routes Statistics. API smokes : `/statistics` OK, `/statistics/timeline` **1** point OK, `/persistence/system/metrics` **50** points OK. Validations sans `make` : frontend `format:check` OK, `type-check` OK, `lint --quiet` OK, Jest ciblé **4 suites / 11 tests OK**, Jest CI frontend **45 suites / 187 tests OK**, Playwright Statistics **5/5 OK**, perf HTTP moy. `/statistics` ~70 ms, `/statistics/security` ~48 ms, `/statistics/log-stats` ~48 ms, `/statistics/app-data` ~85 ms. Mail récap `[JobbingTrack] Recap P1B Statistics shell 2026-06-15` : `EmailLog` **3/3 SENT**, miroir SMTP **3/3 true**. |
| P1B | Performances — Réseau et Corrélation visibles | local | Ouvrir `/b4ck0ff1ce/performances/network` puis `/b4ck0ff1ce/performances/correlation` : vérifier que Réseau affiche des courbes RX/TX et que Corrélation montre un chargement explicite puis des graphes, pas une page vide durable. Sur Corrélation : auto-refresh en pause par défaut, démarrage sur un seul service utile, clic nom service = focus + filtre, bouton **Tout charger (filtre)** charge explicitement le sous-ensemble filtré, mode complet peut monter jusqu’à 24 services sans recharge de toute la page. Vérifier aussi `/b4ck0ff1ce` : le panneau “Temps de réponse — services prioritaires P1B” ne doit plus afficher Postgres / `Santé Docker`. | | | | [ ] | **Preuve agent 15/06 (suite porteur)** : bootstrap Corrélation réduit à un service préféré (`security-service`/`auth-service`/`contact-service`/`api-gateway` si présents), auto-refresh lourd désactivé par défaut avec bouton explicite, cache historique par période pour éviter de refetch tous les services déjà chargés lors d’un clic, clic nom service → focus + filtre. Mode complet : plafond **24** services, historique par service réduit pour garder la page fluide. Validations sans `make` : `format:check`, `type-check`, `lint --quiet` OK ; Jest monitoring/metrics **2 suites / 12 tests OK** ; Playwright Statistics + Performances **11/11 OK** dont Corrélation **5,8 s** sur ce run. |
| P1B | Performances Corrélation — contexte endpoint incidents | local | Ouvrir `/b4ck0ff1ce/performances/correlation`, choisir un service avec logs WARN/ERROR, puis vérifier le tableau **Corrélation fine incidents ↔ points métriques** : colonnes **Méthode**, **Endpoint**, **HTTP**, **Proto**, **Port** visibles ; recherche texte sur méthode/endpoint ; clic ligne affiche le diagnostic contexte avec source technique pour méthode, endpoint, protocole et port. Si un champ reste `—`, vérifier que le diagnostic indique clairement que la donnée source n’a pas été persistée, sans valeur inventée. | | | | [ ] | **Preuve agent 15/06** : parsing renforcé côté UI depuis metadata/message (`method/httpMethod/request.method`, `endpoint/originalUrl/path/url/request.url`, URL complète, `host:port`, `port/localPort/serverPort/remotePort`) + colonne Méthode et diagnostic Méthode HTTP. Validations sans `make` : Prettier ciblé OK, `type-check` OK, `lint --quiet` OK, Jest `performanceCorrelationModel` **1 suite / 5 tests OK**. Playwright Performances **7/7 OK** via `playwright.standalone.config.ts` + frontend Docker **5003** (`npm run test:e2e:performances`, **48,3 s**). |
| P1C | Corrélation — sous-page Signaux conteneurs (pics ressources) | local | Ouvrir `/b4ck0ff1ce/performances/correlation/containers` (onglet **Signaux conteneurs** ou lien depuis Corrélation). Charger **postgres** + **frontend** via « Tout charger (filtre) » : vérifier le **tableau synthèse pics** (CPU/mém max + horodatage, Δ réseau/I/O), la **comparaison superposée** (qui pic CPU/mém/réseau/I/O à quel moment), et le **détail conteneur actif** avec brush synchronisé sur CPU, mémoire, réseau et Block I/O. La page incidents `/correlation` doit rester inchangée (logs, WAF, incidents). | | | | [ ] | **Preuve agent 17/06** : route `/correlation/containers`, `CorrelationSubNav`, lib `correlationContainerMetrics`, graphes lazy. Validations sans `make` : Jest **2/2** ; `tsc --noEmit` OK. **Revalidation 17/06 branche `feat/p1b-validations-performances-e2e`** : Playwright `correlation-containers-smoke.spec.ts` **2/2** (~39 s) — navigation sous-nav + panneaux synthèse/comparaison/détail ; lien depuis page incidents via `CorrelationSubNav`. |
| P1C | Backoffice — cartes vue d’ensemble cliquables | local | Sur `/b4ck0ff1ce`, cliquer chaque carte synthèse et vérifier la destination : **Sessions actives** → Utilisateurs filtrés actifs ; **Signaux sécurité** → Incidents sécurité ; **Santé système** → Performances synthèse ; **Temps de réponse** → Performances latence ; **CPU conteneurs** et **Mémoire conteneurs** → Performances CPU & Mémoire. Vérifier la flèche → et le survol (carte cliquable). | | | | [ ] | **Preuve agent 17/06** : `MetricCard` en `Link` ; filtre `status=active` sur `/users`. Jest `userFilters` **4/4** ; `tsc` OK. **Revalidation 17/06 branche `feat/p1b-validations-performances-e2e`** : Playwright `backoffice-hub-kpi-links.spec.ts` **6/6** (~39 s) — chaque carte KPI navigue vers la destination métier attendue. |
| P1C | Lot A graphes — brush synchronisé page Performances Réseau | local | Ouvrir `/b4ck0ff1ce/performances/network` : glisser la barre brush sous un des graphes (RX/TX, débit, temps de réponse ou corrélation CPU & mémoire) et vérifier que **tous** les graphes de la page zooment sur la même fenêtre temporelle. Vérifier que le graphe de corrélation affiche aussi **Mémoire %** en plus de CPU %, RX et TX. Cliquer **Réinitialiser le zoom** et vérifier le retour à la fenêtre récente par défaut. | | | | [ ] | **Preuve agent 15/06** : hook `useSyncedChartBrushRange` + brush partagé sur 4 graphes Réseau. **Suite retour porteur 15/06** : `SystemCpuNetworkCorrelationChart` trace aussi **Mémoire %** sur l’axe pourcentage ; titre “Corrélation CPU & mémoire (%) vs débit réseau”. Validations sans `make` : TypeScript direct OK ; Jest charts **2 suites / 6 tests OK** ; ESLint ciblé **0 erreur** ; Playwright Performances direct **7/7 OK** en **44,0 s**. Wrappers npm type-check/lint : dette connue code 1 sans sortie. |
| P1C | Lot A graphes — brush synchronisé page Performances Disque | local | Ouvrir `/b4ck0ff1ce/performances/disk` : vérifier que **Block I/O cumulé — lecture / écriture** et **Débit Block I/O observé — Mo/min** affichent des valeurs/courbes quand la plage contient des snapshots Block I/O (ex. 24 h local actuel). Vérifier que le graphe **Volume disque — utilisé / total** trace bien les deux courbes sur toute la plage (ex. ~680 Go utilisé, ~900 Go total) et que les cartes affichent **utilisé / total** + espace libre. Glisser la barre brush sous le graphe volume (ou débit I/O si pas d’historique système) ; vérifier que **tous** les graphes visibles zooment sur la même fenêtre. Cliquer **Réinitialiser le zoom** et vérifier le retour à la fenêtre complète. Pas d’overlay Next.js « issues » (NaN Recharts). | | OK Performances Disque (porteur 17/06) | [x] | **Preuve agent 17/06** : fix `diskMetricsModel.ts`, série complète charts, brush pleine plage, domaine X zoom, cartes volume, anti-hydratation layouts. `tsc` + `lint --quiet` + Jest `diskMetricsModel` **3/3** ; smoke navigateur OK. Archivé `TODOS_DONE.md`. |
| P1C | Lot A graphes — brush synchronisé page Performances Conteneurs | local | Ouvrir `/b4ck0ff1ce/performances/containers` en mode **Tous les conteneurs** : glisser la barre brush sous le graphe CPU ou mémoire et vérifier que **les deux** graphes zooment sur la même fenêtre. Tester aussi un conteneur unique (ex. `postgres` ou `frontend`) : vérifier les graphes **Block I/O cumulé** et **débit Mo/min** (alignés `docker stats Block I/O`) en plus de CPU/mémoire ; le brush doit zoomer les courbes visibles. **Réinitialiser le zoom** doit restaurer la fenêtre récente. | | | | [ ] | **Preuve agent 15/06** : `useSyncedChartBrushRange` dans `AnalyticsContainersChartsBundle` ; brush partagé modes multi et single. **Revalidation 17/06 Block I/O** : agent cgroup `io.stat` (plus `/proc`) ; graphes Block I/O mode single ; Jest `containerBlockIoChartModel` **4/4** ; alignement postgres agent vs `docker stats`. Validations sans `make` : `tsc --noEmit` OK ; ESLint ciblé **0 erreur** ; Jest hook **2/2 OK** ; Playwright ciblé Conteneurs **2/2 OK**. |
| P1C | Lot A graphes — brush page Performances Latence | local | Ouvrir `/b4ck0ff1ce/performances/latency` : glisser le brush sous l’historique agrégé et vérifier que la **moyenne sur la plage sélectionnée** se met à jour. Cliquer **Réinitialiser le zoom** et vérifier le retour à la fenêtre récente par défaut. | | | | [ ] | **Preuve agent 15/06** : migration `useSyncedChartBrushRange` ; moyenne brush alignée sur points rendus (`chartRows`). Validations sans `make` : `tsc --noEmit` OK ; ESLint ciblé **0 erreur** ; Playwright ciblé Latence **2/2 OK**. |
| P1C | Lot A graphes — export CSV/JSON séries affichées | local | Ouvrir `/b4ck0ff1ce/performances/network` : cliquer **CSV** puis **JSON** dans “Exporter les séries affichées” et vérifier que les fichiers contiennent les points visibles (timestamp, RX/TX cumulés, débit Mo/min, CPU/mémoire, temps de réponse si présent). Ouvrir ensuite une fiche `/b4ck0ff1ce/services/<service>` : vérifier que le bloc “Historique des Performances” propose aussi CSV/JSON et exporte CPU, mémoire, réseau, Block I/O cumul + débit observé. | | | | [ ] | **Preuve agent 15/06** : `SeriesExportButtons` + modèle `seriesExport` ; intégration Performances Réseau et détail service. Validations sans `make` : `npm run type-check` OK ; `npm run lint -- --quiet` OK ; Jest export **1 suite / 3 tests OK** ; Jest fiche service **1 suite / 33 tests OK**. Playwright Performances **7/7 OK** via `PLAYWRIGHT_BASE_URL=http://localhost:5003` (script `npm run test:e2e:performances`). |
| P1C | Lot A graphes — mini-série historique liste Services | local | Ouvrir `/b4ck0ff1ce/services` : vérifier que les lignes des services actifs affichent une petite courbe CPU historique sous le nom quand l’historique existe, et une jauge instantanée seulement en fallback. Vérifier que la page reste fluide pendant le chargement et que les actions Logs/Démarrer/Redémarrer/Arrêter ne changent pas. Point noté pour plus tard : si une vue affiche une “somme CPU” >100 %, elle doit être renommée/corrigée pour ne pas être comprise comme CPU global machine. | | | | [ ] | **Preuve agent 15/06** : mini-série SVG sans Recharts ; chargement initialement limité aux 24 services visibles, concurrence 3, 36 points/service, puis réduit par le correctif CPU/RAM Services à **12 services**, concurrence **2**, **24 points/service** via `/docker/service/:name/history`; fallback jauge instantanée si historique absent. Validations sans `make` : Jest modèle sparkline **1 suite / 3 tests OK** puis Playwright Services **3/3 OK**. |
| P1C | Frontend CPU/RAM — Services en mode sobre par défaut | local | Ouvrir `/b4ck0ff1ce/services` : vérifier que le bouton indique **Auto-refresh pause** au chargement, que la liste reste visible, que **Rafraîchir** fonctionne manuellement, puis activer l’auto-refresh et vérifier le passage **Auto-refresh actif**. Ouvrir `/b4ck0ff1ce/services/api-gateway` : vérifier que la case **Auto** est décochée par défaut, que les graphes/logs s’affichent, que **Actualiser** fonctionne, puis cocher **Auto** si besoin. Observer si le conteneur `frontend` redescend mieux en CPU/RAM pendant quelques minutes sans interaction. | | | | [ ] | **Preuve agent 15/06** : liste Services sans polling par défaut, auto-refresh explicite 60 s, mini-séries réduites (12 services, 24 points, concurrence 2), fiche détail sans auto-refresh par défaut, logs 60 lignes, historique réduit. Validations sans `make` : TypeScript direct OK ; ESLint ciblé OK ; Jest fiche service **33/33 OK** ; Playwright Services **3/3 OK** sur frontend Docker 5003. |
| P1A | Sécurité login backoffice — campagne contrôlée | local | Ouvrir `/login`, vérifier qu’aucun compte de test/secret n’est visible, puis relire le rapport `docs/security/BACKOFFICE_LOGIN_SECURITY_AUDIT_2026-06-15.md`. Si besoin, relancer le spec `backoffice-login-security.spec.ts` via Playwright local. | | | | [ ] | **Preuve agent 15/06 (revalidée)** : Playwright `backoffice-login-security.spec.ts` **5/5 passed** ; mail récap `RAPPORT AGENT JOBBINGTRACK — Sécurité login backoffice 2026-06-15` déjà **3/3 SENT**. Rapport : `docs/security/BACKOFFICE_LOGIN_SECURITY_AUDIT_2026-06-15.md`. |
| P1A | WAF gateway — remote host, shell et URL injection | local | Relire `docs/security/GATEWAY_INJECTION_HOST_AUDIT_2026-06-15.md`, puis vérifier dans le backoffice Sécurité → Logs que les événements `waf_blocked_request` récents apparaissent avec `isBlocked=true` pour les IPs de test `203.0.113.52/53/54` ou relancer le test WAF ciblé si besoin. | | | | [ ] | **Preuve agent 15/06 (revalidée)** : Jest `waf.test.js` **12/12 passed** (TMPDIR local) ; mail récap `RAPPORT AGENT JOBBINGTRACK — WAF remote host shell URL 2026-06-15` déjà **3/3 SENT**. Rapport : `docs/security/GATEWAY_INJECTION_HOST_AUDIT_2026-06-15.md`. |
| P1B | Analytics — moteur UI partagé shell | local | Ouvrir `/b4ck0ff1ce/analytics`, `/analytics/application/performance`, `/analytics/application/activity` et `/analytics/application/feedback` : vérifier que le layout admin, le retour hub, la sous-navigation Application, les titres, le sélecteur de période live et les cartes restent cohérents sans double marge ni rupture visuelle. | | | | [ ] | **Preuve agent 15/06 (revalidée)** : `AnalyticsPageShell` partagé via `ApplicationSubNav` sur hub + 3 pages Application ; Jest `backoffice/analytics/__tests__` **5 suites / 20 tests OK** ; routes `/analytics`, `/analytics/application/performance`, `/activity`, `/feedback` HTTP **307** sans session (redirect login attendu). Mail récap `[JobbingTrack] Recap P1B Analytics UI shell 2026-06-15` : **3/3 SENT**, miroir **3/3**. Branche `feat/p1b-analytics-ui-shell-validation`. |
| P1C | Sécurité — moteur UI partagé shell | local | Ouvrir `/b4ck0ff1ce/security`, `/security/analysis`, `/security/logs`, `/security/incidents`, `/security/threats`, `/security/firewall`, `/security/network`, `/security/alerts` et `/security/policies` : vérifier que le layout admin, la sous-navigation Sécurité, l’en-tête, les actions (rafraîchir, exports, lab) et les onglets Incidents/Menaces restent cohérents sans double marge ni rupture visuelle. Sur une fiche menace ou alerte incident, vérifier le retour sans sous-nav principale. | | | | [ ] | **Preuve agent 13/06** : nouveau `SecurityPageShell` + `SecuritySubNav` partagés ; migration de la vue d’ensemble, Analyse, Logs, Incidents, Menaces, Firewall, Réseau, Alertes email et Politiques ; fiches détail menace/alerte avec `showSubNav=false` et retour ; sans changement des appels API, filtres, tableaux ni calculs. Validations : `tsc --noEmit` direct OK ; ESLint ciblé **0 erreur**, 56 warnings historiques ; Jest sécurité lib **3 suites / 14 tests OK** ; routes `/security/*` HTTP **307** sans session (redirect login attendu, stack up) ; `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable, dette wrappers/globale déjà notée. |
| P1C | Administration — moteur UI partagé Services | local | Ouvrir `/b4ck0ff1ce/services`, `/services/logs` et une fiche `/services/<service>` : vérifier que le layout admin, la sous-navigation Services, l’en-tête, les actions **Services & Logs**, **Actualiser**, auto-refresh et les filtres logs restent cohérents sans double marge ni rupture visuelle. | | | | [ ] | **Preuve agent 13/06** : nouveau `ServicesPageShell` + `ServicesSubNav` partagés ; migration de la liste Services, des logs services et de la fiche détail service sans modifier les appels metrics-aggregator, filtres, tableaux, actions stop/restart ni auto-refresh. Validations : `tsc --noEmit` direct OK ; ESLint ciblé **0 erreur**, 24 warnings historiques ; lints IDE OK ; Jest fiche service **1 suite / 33 tests OK** ; routes `/services`, `/services/logs`, `/services/api-gateway` HTTP **307** sans session (redirect login attendu, stack up). `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable, dette wrappers/globale déjà notée. |
| P1C | Administration — moteur UI partagé Archives / Corbeille | local | Ouvrir `/b4ck0ff1ce/archives` et `/b4ck0ff1ce/trash` : vérifier que le layout admin, la sous-navigation Données, les en-têtes, les filtres, la liste, le bouton **Vider la corbeille** et les actions Restaurer/Désarchiver/Supprimer définitivement restent cohérents sans double marge ni rupture visuelle. | | | | [ ] | **Preuve agent 13/06** : nouveau `AdministrationDataPageShell` + `AdministrationDataSubNav` appliqué aux pages Archives et Corbeille ; les requêtes `adminService`, filtres, confirmations et actions de restauration/suppression ne changent pas. Validations : `tsc --noEmit` direct OK ; ESLint ciblé **0 erreur**, 6 warnings historiques ; lints IDE OK ; routes `/archives` et `/trash` HTTP **307** sans session (redirect login attendu, stack up). Jest ciblé non lancé : pas de test unitaire Archives/Corbeille existant ; couverture E2E existante mais session admin Playwright encore bloquée par credentials E2E. `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable, dette wrappers/globale déjà notée. |
| P1C | Administration — moteur UI partagé Données utilitaires | local | Ouvrir `/b4ck0ff1ce/test-data`, `/billing`, `/user-stats` et `/suivi-interim` : vérifier que la sous-navigation Données, les en-têtes, les actions du générateur de données, les contenus facturation/statistiques/suivi restent cohérents sans double marge ni rupture visuelle. | | | | [ ] | **Preuve agent 13/06** : pages Données de test, Facturation, Stats utilisateur et Suivi intérim migrées vers `AdministrationDataPageShell`, sans modifier les appels API, loaders auth, composants internes, actions sensibles ou formulaires. Validations : `tsc --noEmit` direct OK ; ESLint ciblé **0 erreur**, 6 warnings historiques sur `test-data`; lints IDE OK ; routes `/test-data`, `/billing`, `/user-stats`, `/suivi-interim` HTTP **307** sans session (redirect login attendu, stack up). Jest ciblé non lancé : pas de test unitaire dédié à ces wrappers ; E2E existants reportés au réalignement credentials admin Playwright. `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable, dette wrappers/globale déjà notée. |
| P1C | Administration — moteur UI partagé Utilisateurs | local | Ouvrir `/b4ck0ff1ce/users`, `/users/new` et une fiche `/users/<id>` : vérifier que l’en-tête, le retour, l’action **Nouvel utilisateur**, les boutons Modifier/Enregistrer/Supprimer, les filtres, la pagination et les actions email restent cohérents sans double marge ni rupture visuelle. | | | | [ ] | **Preuve agent 13/06** : nouveau `UsersPageShell` appliqué à la liste Utilisateurs et à la fiche création/détail/erreur/loading ; les appels auth API, cache, filtres, pagination, emails reset/vérification, rôles et suppressions ne changent pas. Validations : `tsc --noEmit` direct OK ; ESLint ciblé **0 erreur**, 18 warnings historiques ; lints IDE OK ; Jest `userFilters` **1 suite / 3 tests OK** ; routes `/users`, `/users/new`, `/users/test-id` HTTP **307** sans session (redirect login attendu, stack up). `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable, dette wrappers/globale déjà notée. |
| P1C | Administration — moteur UI partagé Hub Données | local | Ouvrir `/b4ck0ff1ce/datas` avec plusieurs onglets (`applications`, `companies`, `suivi-interim`) et `/b4ck0ff1ce/data-management` : vérifier que la sous-navigation Données, l’en-tête, les onglets lazy et les cartes export/import/nettoyage restent cohérents sans double marge ni rupture visuelle. | | | | [ ] | **Preuve agent 13/06** : `/datas` et `/data-management` migrés vers `AdministrationDataPageShell`, sans modifier les redirections `?tab=...`, onglets lazy, composants métiers, actions export/import/nettoyage ni appels API. Validations : `tsc --noEmit` direct OK ; ESLint ciblé **0 erreur** ; lints IDE OK ; routes `/datas`, `/datas?tab=applications`, `/data-management` HTTP **307** sans session (redirect login attendu, stack up). Jest ciblé non lancé : pas de test unitaire dédié au hub Données ; E2E existants reportés au réalignement credentials admin Playwright. `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable, dette wrappers/globale déjà notée. |
| P1C | Développement — moteur UI partagé Tests automatisés | local | Ouvrir `/b4ck0ff1ce/tests-api`, `/tests-backend`, `/tests-frontend` et `/tests-backoffice` : vérifier que la sous-navigation Tests, les en-têtes, les boutons **Lancer/Arrêter**, les listes de tests, la progression, les logs et les liens rapports restent cohérents sans double marge ni rupture visuelle. | | | | [ ] | **Preuve agent 13/06** : nouveau `DevelopmentPageShell` + `DevelopmentTestsSubNav` appliqués aux 4 runners automatisés, sans modifier les endpoints `/api/test/run-*`, sélections, logs, progression ni rapports. Validations : `tsc --noEmit` direct OK ; ESLint ciblé **0 erreur**, 18 warnings historiques ; lints IDE OK ; Jest `testsCatalog` **1 suite / 3 tests OK** ; routes `/tests-api`, `/tests-backend`, `/tests-frontend`, `/tests-backoffice` HTTP **307** sans session (redirect login attendu, stack up). `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable, dette wrappers/globale déjà notée. |
| P1C | Emails — moteur UI partagé Configuration / MailHog | local | Ouvrir `/b4ck0ff1ce/emails/settings` et `/b4ck0ff1ce/emails/mailhog` : vérifier que la sous-navigation Gestion des emails, les en-têtes, le bouton **Vérifier**, le lien **Ouvrir dans un onglet** et l’iframe MailHog restent cohérents sans double marge ni rupture visuelle. Vérifier aussi que l’onglet `/email-monitor` reste accessible depuis la sous-navigation. | | | | [ ] | **Preuve agent 13/06** : nouveau `EmailBackofficePageShell` + `EmailBackofficeSubNav` appliqués aux pages Configuration SMTP et MailHog, sans modifier l’appel `/api/v1/emails/test-smtp`, le lien externe MailHog ni l’iframe. Validations : `tsc --noEmit` direct OK ; ESLint ciblé **0 erreur**, 2 warnings historiques `any` sur `settings`; lints IDE OK ; routes `/emails/settings`, `/emails/mailhog`, `/emails` et `/email-monitor` HTTP **307** sans session (redirect login attendu, stack up). Playwright ciblé `page Configuration SMTP` tenté : bloqué au setup admin `401`, test page non exécuté, blocage E2E déjà connu. `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable, dette wrappers/globale déjà notée. |
| P1C | Emails — moteur UI partagé Dashboard / Templates | local | Ouvrir `/b4ck0ff1ce/emails` et `/b4ck0ff1ce/emails/templates` : vérifier que la sous-navigation Gestion des emails, les en-têtes, le lien vers **Email Monitor**, le bouton **Actualiser**, les onglets Email de Test/Reset et les onglets Templates Prévisualisation/Code HTML/Variables restent cohérents sans double marge ni rupture visuelle. | | | | [ ] | **Preuve agent 13/06** : Dashboard emails et Templates migrés vers `EmailBackofficePageShell`, sans modifier les appels `/api/v1/emails/stats`, `/api/v1/emails/test`, `/api/v1/auth/forgot-password`, `/api/v1/emails/templates`, ni les handlers preview/édition/sauvegarde variables. Validations : `tsc --noEmit` direct OK ; ESLint ciblé **0 erreur**, 19 warnings historiques sur ces grandes pages ; lints IDE OK ; routes `/emails`, `/emails/templates`, `/email-monitor` HTTP **307** sans session (redirect login attendu, stack up). Playwright ciblé Dashboard/Templates tenté : setup admin KO `401`, 4 tests page non exécutés, blocage E2E déjà connu. `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable, dette wrappers/globale déjà notée. |
| P1C | Emails — moteur UI partagé Email Monitor / Déliverabilité | local | Ouvrir `/b4ck0ff1ce/email-monitor` et `/b4ck0ff1ce/emails/deliverability` : vérifier que la sous-navigation Gestion des emails, les en-têtes, le mode temps réel, les actions Actualiser/Exporter/Supprimer, les filtres Email Monitor, les tests DNS/SMTP et les boutons d’envoi de test restent cohérents sans double marge ni rupture visuelle. | | | | [ ] | **Preuve agent 13/06** : Email Monitor et Déliverabilité migrés vers `EmailBackofficePageShell`, sans modifier polling, filtres, pagination, exports/suppressions, tests DNS/SMTP, endpoints d’envoi ni états de résultat. Validations : `tsc --noEmit` direct OK ; ESLint ciblé **0 erreur**, 9 warnings historiques ; lints IDE OK ; routes `/email-monitor`, `/emails/deliverability`, `/emails` HTTP **307** sans session (redirect login attendu, stack up). Playwright ciblé Monitor/Déliverabilité tenté : setup admin KO `401`, 5 tests page non exécutés, blocage E2E déjà connu. `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable, dette wrappers/globale déjà notée. |
| P1B | Statistics / Performances — temps de réponse endpoints services | local | Suivre § “P1B — Statistics” : vérifier temps de réponse instantanés/moyens par service, source `monitoring-agent-rs` / `metrics-aggregator`, services manquants visibles ou justifiés (`auth`, `deployment`, `call`, `notification`, `followup`, `application`, `postgres`). | | | | [ ] | **Preuve agent fraîche (12/06 15h30)** : page Latence alignée UX Performances (sous-nav + retour) ; `TimeRangeSelector` partagé (24h→7j, plage personnalisée) ; Playwright latence **OK** avec historique + instantané par service. |
| P1B | Backoffice — moyenne temps de réponse alignée endpoints affichés | local | Ouvrir `/b4ck0ff1ce` : vérifier que la carte **Temps de réponse** et le bloc **État du système** affichent la même moyenne que le panneau **Temps de réponse — services prioritaires P1B** juste en dessous. La moyenne doit être calculée seulement sur les endpoints P1B réellement affichés avec une mesure HTTP, pas sur Postgres/Redis ni sur l’agrégat historique `monitoringC`. | | | | [ ] | **Preuve agent 16/06** : retour porteur “134.4ms pas moyenne des endpoints dessous” corrigé. Frontend : `averagePriorityResponseTimeMs()` ignore non HTTP, hors P1B et valeurs nulles/0 ; carte hub + bloc système utilisent cette moyenne ; le panneau P1B affiche “Moyenne des endpoints affichés”. Validations sans `make` : Jest ciblé **2 suites / 13 tests OK**, TypeScript direct OK, ESLint ciblé OK. |
| P1B | Performances — historique temps de réponse par service multi-jours | local | Ouvrir `/b4ck0ff1ce/performances/correlation` ou `/b4ck0ff1ce/performances/latency` puis tester une plage 7 j / personnalisée : les séries de disponibilité/temps de réponse par service doivent couvrir toute la fenêtre disponible en BDD, pas seulement les dernières minutes/heures. Pour un service qui a changé de nom court (`auth-service` vs `jobbingtrack-auth-service`), l’historique ne doit pas disparaître. | | | | [ ] | **Preuve agent 16/06** : backend `service_availability_history` bucketisé sur `startDate/endDate/limit` et fusion des alias de nom dans une requête `IN`, aussi pour les stats `?hours`. Validations sans `make` : syntaxe backend OK ; Jest ciblé service availability **1 suite / 3 tests OK** ; `jobbingtrack-metrics-aggregator` redémarré healthy ; smoke proxy 7 j `auth-service` limit 12 → **12 points** couvrant **2026-06-09 → 2026-06-15**, stats 168 h `totalChecks=6940`, au lieu de seulement la fin de plage. |
| P1C | Graphes conteneurs multi-séries lisibles | local | Ouvrir Performances → Conteneurs → **Tous les conteneurs** : vérifier que les couleurs sont franchement différentes (pas de rouge clair/rouge foncé ou plusieurs verts proches en première lecture). Chaque conteneur doit avoir un code visible (`C1`, `C2`, etc.), une forme différente dans les premiers conteneurs (rond, carré, losange, triangle, croix, plus, hexagone, étoile, barre, chevron, capsule, anneau), un style de trait et des marqueurs `C#` espacés sur la courbe. Dans le graphique CPU, masquer/afficher quelques conteneurs via **Séries CPU** ; dans le graphique Mémoire, masquer/afficher une sélection différente via **Séries mémoire**. Les deux sélections doivent être indépendantes et modifiables à la volée, sans recharger toute la page. Tester aussi **Tout masquer** puis **Tout afficher** sur un seul graphique. Vérifier ensuite rapidement CPU & Mémoire : la palette partagée doit être plus contrastée qu’avant. | | | | [ ] | **Preuve agent 15/06 suite retour porteur** : `AnalyticsContainersChartsBundle` ajoute palette locale ordonnée très contrastée, codes `C#`, formes étendues, styles de ligne, marqueurs `C#` sur courbes + panneaux de sélection indépendants CPU/Mémoire ; mode conteneur unique : CPU plein, mémoire tirets. La palette partagée `seriesColors` utilisée par les autres graphes multi-séries passe aussi sur une palette ordonnée fortement contrastée. Validations sans `make` : TypeScript direct OK ; ESLint ciblé 0 erreur (1 warning hook préexistant page Conteneurs) ; Jest `chartTimeDomain` **1/1 OK** ; Playwright ciblé `/performances/containers` **2/2 OK**. Suite erreur frontend 15/06 : warning React/Recharts `key` sur les marqueurs `C#` corrigé ; test navigateur ad hoc boutons/console **OK** (`Tout masquer` CPU n'affecte pas Mémoire, `Tout afficher` restaure CPU, Statistics `Actualiser` + auto-refresh OK) ; Playwright Performances complet **7/7 OK** après correction. Validation porteur partielle 15/06 : nouveaux éléments + résolution frontend acceptés, mais lot complet explicitement non clôturé. Suite agent 15/06 : budget de rendu abaissé sur Conteneurs/CPU & Mémoire/Réseau/Disque et marqueurs `C#` espacés dynamiquement ; Playwright Performances **7/7 OK** en **40,8 s**, test navigateur rechargements 7j/30j sans erreur console. À revoir avant validation finale/déploiement avec les autres graphes longues plages 30 j. |
| P1C | A1G Performances — libellés sources historiques | local | Ouvrir `/b4ck0ff1ce/performances`, `/performances/cpu-memory`, `/performances/containers`, `/performances/network`, `/performances/disk` et `/performances/latency` : vérifier qu’un encart indique clairement la période, la source des séries (`system_metrics`, `container_metrics`, fallback Docker live ou séries dérivées) et, quand disponible, le nombre de points source vs points rendus. Sur 30 j, vérifier que le libellé aide à comprendre les trous éventuels sans donner l’impression que 30 j réels existent si la BDD locale n’a pas encore historisé 30 j. | | | | [ ] | **Preuve agent 15/06** : `PerformanceHistoryCaption` sur CPU & Mémoire, Conteneurs, Réseau, Disque, **Latence** et **Synthèse Performances** ; budget rendu latence/synthèse **160** points. Playwright Performances **7/7 OK** en **48,0 s**. |
| P1C | Performances — CPU & Mémoire dédié | local | Ouvrir `/b4ck0ff1ce/performances/cpu-memory` : entrée **CPU & Mémoire** visible sous Synthèse et avant Temps de réponse ; cartes CPU/mémoire live ; graph CPU global et mémoire globale ; sous-vues **CPU détaillé** / **Mémoire détaillée** ; cases services activables/masquables ; période 24h↔7j↔personnalisé conserve les graphes sans écran vide durable. En **Vue globale**, glisser le brush sous le graphique mémoire et vérifier que CPU et mémoire zooment ensemble ; **Réinitialiser le zoom** doit restaurer la fenêtre récente. Vérifier aussi Disque/Conteneurs : les graphes doivent afficher soit l’historique persisté, soit un fallback live court, sans écran vide durable. Vérifier enfin que les textes “Vue machine…” et “Journée civile locale…” ne s’affichent plus. | | | | [ ] | Implémentation agent 13/06 sur `feat/performance-cpu-memory-charts` ; optimisation suite retour porteur : vue globale en une requête système, détails limités aux services cochés (6 par défaut), fallback graphique live si historique vide, champs CPU/mémoire live aplatis depuis `service.metrics`. **Suite 15/06** : brush synchronisé vue globale CPU+mémoire + encart sources. Validations sans `make` : TypeScript direct OK ; ESLint ciblé OK ; Jest `useSyncedChartBrushRange` **2/2 OK** ; Playwright Performances **7/7 OK**. |
| P1C | Performances — moteur UI partagé pages réseau/latence | local | Ouvrir `/b4ck0ff1ce/performances/network` et `/b4ck0ff1ce/performances/latency` : retour Performances, sous-navigation, titre, sélecteur de période, cartes graphes et états vides/chargement restent identiques visuellement ; vérifier qu’aucun graphe n’a disparu et que les pages restent rapides. | | | | [ ] | **Preuve agent 13/06** : premier lot moteur UI créé (`PerformancePageShell`, `PerformanceChartCard`, états loading/empty/info) et appliqué à Réseau + Latence sans changer les requêtes ni calculs. Tests : `type-check` OK, lint OK, Jest UI partagé **3/3**, régressions metrics/charts **15/15**, routes `/performances/network`, `/performances/latency`, `/performances` HTTP 200. Playwright Performances retenté : bloqué par le 401 admin connu avant tests de page. |
| P1C | Performances — moteur UI partagé pages disque/conteneurs | local | Ouvrir `/b4ck0ff1ce/performances/disk` et `/b4ck0ff1ce/performances/containers` : retour Performances, sous-navigation, titre, sélecteur de période, état chargement/vide et cartes graphes doivent rester cohérents avec Réseau/Latence ; vérifier que les graphes disque, volume, Block I/O et conteneurs CPU/mémoire restent visibles et rapides. | | | | [ ] | **Preuve agent 13/06** : lot 2 moteur UI appliqué à Disque + Conteneurs via `PerformancePageShell`, `PerformanceChartCard`, `PerformanceLoadingState` et `PerformanceEmptyState`, sans changement de requêtes ni calculs. Validations : `tsc --noEmit` direct OK ; Jest UI/charts/metrics **4 suites / 19 tests OK** ; routes `/performances`, `/performances/disk`, `/performances/containers` HTTP 200 ; API système `limit=20` HTTP 200, 20 points ; API `docker/services/all` HTTP 200, 23 services ; temps HTTP local moyen Disque ~70 ms, Conteneurs ~70 ms. `npm run type-check` et `npm run lint` restent perturbés par wrappers/dette globale ; lint ciblé pages modifiées 0 erreur, 1 warning hook préexistant. |
| P1C | Performances — moteur UI partagé vue d’ensemble | local | Ouvrir `/b4ck0ff1ce/performances` : vérifier que le retour Tableau de bord, le lien Analytics, la sous-navigation Performances, la période sticky, les cartes CPU/mémoire, latence, réseau/corrélation et endpoints instantanés restent présents et cohérents avec les pages détaillées. | | | | [ ] | **Preuve agent 13/06** : lot 3 moteur UI appliqué à la vue d’ensemble avec `PerformancePageShell`, options `backHref/backLabel/topLinks`, `PerformanceChartCard` avec `id` d’ancre et états partagés, sans changer les fetchs ni les calculs. Validations : `tsc --noEmit` direct OK ; ESLint ciblé vue d’ensemble + composants UI 0 erreur ; Jest UI/charts/metrics **4 suites / 19 tests OK** ; route `/performances` HTTP 200 ; API système `limit=20` HTTP 200, 20 points ; API `docker/services/all` HTTP 200, 23 services ; temps HTTP local moyen vue d’ensemble ~142 ms. `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable, dette wrappers/globale déjà notée. |
| P1C | Performances — moteur UI partagé CPU & Mémoire | local | Ouvrir `/b4ck0ff1ce/performances/cpu-memory` : vérifier que le retour Performances, la sous-navigation, le titre, le sélecteur de période, les cartes live, les vues Vue globale/CPU détaillé/Mémoire détaillée, la sélection de services et les graphes restent présents et cohérents avec les autres pages Performances. | | | | [ ] | **Preuve agent 13/06** : lot 4 moteur UI appliqué à CPU & Mémoire via `PerformancePageShell`, `PerformanceChartCard`, `PerformanceLoadingState` et `PerformanceEmptyState`, sans changer les hooks de période, fetchs système/conteneurs, sélection de services ni fallbacks live. Validations : `tsc --noEmit` direct OK ; ESLint ciblé 0 erreur ; lints IDE OK ; Jest UI/charts/metrics **4 suites / 19 tests OK** ; route `/performances/cpu-memory` HTTP 200 ; API système `limit=20` HTTP 200, 20 points ; API `docker/services/all` HTTP 200, 23 services ; temps HTTP local moyen CPU & Mémoire ~187 ms, système metrics ~129 ms, Docker services ~868 ms. `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable, dette wrappers/globale déjà notée. |
| P1C | Performances — points historiques CPU/mémoire/conteneurs/réseau/disque/latence réactivés | local | Ouvrir `/b4ck0ff1ce/performances`, `/performances/cpu-memory`, `/performances/containers`, `/performances/network`, `/performances/disk`, `/performances/latency` et `/performances/correlation` : vérifier que les graphes affichent de nouveau des points dans le temps, que la période 24h montre des courbes système CPU/mémoire/disque/réseau/temps de réponse, que les conteneurs ont des courbes CPU/mémoire/réseau sans trou durable, et que Corrélation reste utilisable sans surcharge. Tester aussi une bascule 24h → 7j → 30j → retour 24h : sur 30j, l’axe doit refléter la plage demandée, même si la BDD locale ne contient des points que depuis le 12/06 (`system_metrics`) ou depuis l’après-midi du 15/06 (`container_metrics`). | | | | [ ] | **Preuve agent 15/06 suite retour porteur** : correction backend `metrics-aggregator` sur `/persistence/system/metrics` et `/persistence/containers/metrics` : longues périodes échantillonnées par buckets temporels sur toute la fenêtre, plus “les 2000 derniers points seulement”. Correction front : les graphes Performances ne recadrent plus silencieusement l’axe X sur les points présents. Constat local : `system_metrics` **2026-06-12 11:15 → 2026-06-15 15:19**, `container_metrics` **2026-06-15 14:48 → 15:19** ; donc 30j ne peut pas afficher 30j de données tant que la stack n’a pas historisé 30j. Validations sans `make` : syntaxe backend OK ; TypeScript direct OK ; Jest `chartTimeDomain` **1/1 OK** ; ESLint ciblé 0 erreur. Preuves précédentes conservées : historique conteneur lit `container_metrics`; alias courts disponibilité résolus ; Corrélation charge 8 services par défaut ; audits API système/conteneurs OK. |
| P1C | Metrics aggregator — historique multi-jours et alerte MailHog transitoire | local | Ouvrir MailHog puis vérifier qu’il n’y a pas de nouvelle alerte critique “Metrics aggregator indisponible” après redémarrage stabilisé. Ouvrir `/b4ck0ff1ce/performances` et tester 24 h puis 7 j : les graphes système doivent afficher des points de la veille si les snapshots existent, même si les tables brutes `system_metrics` / `container_metrics` ont redémarré aujourd’hui. | | | | [ ] | **Preuve agent 16/06** : l’email MailHog observé à 10:26 a bien été envoyé (`EmailLog` `SENT`, miroir SMTP `sent=true`) à cause d’un timeout 5 s sur `/docker/services/all` pendant le redémarrage. Correctif : `security-service` vérifie `/api/v1/health` avant de créer une alerte critique ; si le service répond `online/healthy/ok`, pas d’email critique. Correctif metrics : `/persistence/system/metrics` fusionne `system_metrics` brut + snapshots historiques, avec buckets snapshots sur toute la plage. Tests proxy frontend : 14/06 **60 pts**, 15/06 **53 pts**, 24 h glissantes **27 pts** couvrant 15/06→16/06. MailHog reste à **1** message après redémarrage ciblé. |
| P1C | Mémoire JobbingTrack — budget 8 Go et limites conteneurs | local + futur VPS | Après recreate des conteneurs, vérifier `/b4ck0ff1ce` et `/b4ck0ff1ce/services/<service>` : la mémoire projet affiche un budget JobbingTrack (~8192 MB en profil `full`), pas les ~48046 MB de la RAM hôte ; chaque service affiche une limite cohérente (`Budget JobbingTrack` ou `Limite Docker`) et aucun service ne présente 48 Go comme limite conteneur. | | | | [ ] | Correctif agent 12/06 : `mem_limit` Compose local/prod, budget stack `JOBBINGTRACK_STACK_MEMORY_LIMIT_MB=8192`, normalisation metrics-aggregator si Docker expose la RAM hôte. Recreate requis pour appliquer les limites Docker réelles. |
| P1C | Audit charge API mobile + mémoire frontend | local | Revoir le résultat agent : confirmer que les paliers API bornés suffisent pour l’instant ou demander une campagne authentifiée plus longue. Points à valider : gateway tient les tests locaux contrôlés, WAF bloque un payload XSS quand le trafic simule une IP externe/proxy, mais le frontend dev Next/Turbopack est saturé proche de **2 Go** et doit être traité en priorité avant de parler de “ultra performant”. Décider aussi si l’on fournit un token/test user pour charger les vrais endpoints mobiles authentifiés (`applications`, `companies`, `contacts`, `followups`, `interviews`, `calls`) au-delà du simple périmètre auth/401. | | | | [ ] | **Preuve agent 13/06** : stack `full` healthy ; WAF `X-Forwarded-For` externe **403** ; `test-performance.js PERF_LIGHT=1` **15/15** ; frontend **~2 GiB** saturé. **Preuve agent 14/06** : script `test-mobile-api-authenticated.js` — **9/9** endpoints métier USER (`testuser@jobbingtrack.test`) + charge **10/10** ; plafond graphes Statistics **2000 points** ; refresh Statistics visible-tab uniquement ; frontend ~**1.79 GiB / 2 GiB** après campagne Playwright. Reste : Recharts dynamiques, campagne rate-limit prod-like dédiée. |
| P1C | Frontend mémoire/performance — dev server stabilisé | local | Vérifier localement que `/b4ck0ff1ce/email-monitor`, `/b4ck0ff1ce/security`, `/b4ck0ff1ce/security/threats`, `/b4ck0ff1ce/statistics` et `/b4ck0ff1ce/performances/correlation` ouvrent toujours la page attendue après connexion admin. Sur Statistics et Menaces, vérifier que l’état initial indique un auto-refresh en pause/désactivé, que **Actualiser** fonctionne manuellement, puis que l’auto-refresh ne démarre qu’après activation explicite. Observer que le conteneur frontend ne remonte plus durablement au plafond **2 Go** après navigation entre ces pages. | | | | [ ] | **Preuve agent 15/06 suite CPU/RAM** : Services déjà sobres ; Sécurité Menaces auto-refresh off par défaut + 60 s si activé ; Sécurité Réseau/Incidents 60 s visible-tab ; Statistics chargement initial conservé mais polling off par défaut, bouton **Actualiser**, auto-refresh explicite. Validations sans `make` : TypeScript direct OK ; Jest `chartTimeDomain` **1/1 OK** ; ESLint ciblé 0 erreur (warnings historiques). Scripts `npm run type-check` / `npm run lint` reproduisent la dette connue code 1 sans sortie. Preuve agent 13/06 conservée : dev frontend Docker `next dev --webpack`, Email Monitor polling **10 s**, Sécurité logs aperçu **500** + refresh **15 s** visible-tab, `useMetrics` timer bootstrap annulé, mémoire repos ~**499 MiB** puis ~**984 MiB** après court idle. |
| P1C | Tooltips graphiques alignés couleurs séries | local | Sur Performances / Statistics : survoler plusieurs graphes multi-séries ; chaque ligne du tooltip doit reprendre la couleur de la courbe/barre correspondante, avec valeur lisible en clair/sombre. | | | | [ ] | **Preuve agent à finaliser (12/06)** : thème Recharts partagé ajusté pour ne plus forcer une couleur unique sur les items tooltip. |
| P1C | Logo JobbingTrack projet | local + mobile | Vérifier le nouveau logo sur login/register web, sidebar backoffice, favicon/PWA navigateur et splash/login Flutter ; pas de logo emoji résiduel sur ces entrées principales. | | | | [ ] | Nouveau logo enregistré dans `frontend/public/brand/jobbingtrack-logo.png` et `mobile/assets/branding/jobbingtrack-logo.png`. |
| P1C | Performances — plages temporelles sans flash vide | local | Suivre § “P1C — UX backoffice” : Synthèse/Réseau/Disque/Conteneurs/Latence, changement 24h→7j→personnalisé sans écran vide durable. | | | | [ ] | **Preuve agent fraîche (12/06 15h30)** : Playwright `performances-range-smoke.spec.ts` **7/7 OK** sur Synthèse, Réseau, Disque, Conteneurs, Latence, Corrélation (courbes réelles + bascule 24h↔7j/168h). |
| P1C | Performances — première navigation depuis hub | local | Suivre § “P1C — UX backoffice” : clic `/b4ck0ff1ce` → Performances, noter délai et présence des graphes. | | | | [ ] | |
| P1C | Performances Corrélation — champs forensics incidents | local | Sur `/b4ck0ff1ce/performances/correlation`, sélectionner `jobbingtrack-api-gateway` puis vérifier dans **Corrélation fine incidents ↔ points métriques** qu’un événement WAF lab récent affiche `requestId`, Méthode `GET`, Endpoint `/health?jt_corr=…`, IP `203.0.113.89`, HTTP `403`, Proto `http`, Port `3000`. Cliquer la ligne : le panneau diagnostic doit indiquer les sources techniques, pas seulement `—`. Vérifier aussi `jobbingtrack-security-service` : le log lié `/api/v1/logs` doit afficher le même `requestId` et le contexte disponible. | | | | [ ] | Preuve agent 16/06 : fusion `security_logs` + `aggregated_logs` dans la page corrélation ; WAF lab `lab-correlation-perfect-002` créé localement ; BDD vérifiée avec tous les champs côté gateway ; validations `tsc`, Jest corrélation/WAF/security filters OK. |
| P1C | Sécurité — logs triés et fiche menace score/corrélation | local | Sur `/backoffice/security/logs`, vérifier que le tri est explicite et que `Plus récent d’abord` affiche bien les événements récents avant les anciens ; tester recherche IP/message/type sans résultat trompeur lié à la page courante. Vérifier aussi que saisir une catégorie/type/recherche ne recharge pas le tableau avant clic sur **Appliquer les filtres**, et que les logs lab (`Lab autocomplete...`) alimentent les suggestions. Sur une fiche menace high/critical sans log corrélé, vérifier que le risque est affiché comme score retenu/estimé depuis la sévérité plutôt qu’un `Risque max 0` trompeur. Sur `/backoffice/security`, vérifier que la **Pondération du score sécurité** indique la persistance serveur (ou repli local explicite), affiche la formule, et que **Réinitialiser les poids** revient aux valeurs par défaut. **Suite 13/06** : clic sur un log lab avec lien menace ne doit plus mener à un 404 (`lab-autocomplete-threat` rejeté) ; menu **Incidents & menaces** avec onglets Synthèse / Menaces réseau ; pas de `SecuritySubNav` ni paragraphes d’aide retirés qui réapparaissent. **Suite 16/06** : dans la colonne **Liens**, un log non corrélé ne doit plus afficher un tiret silencieux ; il doit afficher une raison explicite (`Log lab sans menace rattachée`, `Aucune menace proche`, `IP source absente`, etc.). Redémarrer `security-service` si besoin. | | | | [ ] | Preuve agent 12–13/06 : CUID validé, auto-menace logs critical/error/warning, Jest backend **6/6** + frontend liens **3/3**, commit `022003f0` poussé ; **suite 16/06** : `resolveSecurityLogLink()` + `securityScore.ts` **3/3** ; **16/06 soir post-merge** : API `GET/PUT /api/v1/security/score-settings`, UI debounce serveur + migration localStorage ; Jest `score-settings` **3/3** ; `tsc` OK. Playwright setup admin **401** déjà connu pour cette ligne. |
| P1C | Sécurité réseau — lecture source connexion | local | Sur `/b4ck0ff1ce/security/network`, vérifier le tableau **Connexions observées (lecture source)** : colonnes Source, Nature, Destination, Confiance, Liens Menaces/Logs ; plus de libellés bruts `host-network` / `host-local` / `0.0.0.0` sans explication. Sur une fiche menace avec connexions, même tableau avec réputation VPN/proxy/Tor/ASN si sourcée. Vérifier aussi l’absence de grands pavés d’aide sur Firewall, Incidents, Alertes email. | | | | [ ] | Preuve agent 16/06 : `connectionSource.js` + `NetworkConnectionSourceTable` ; Jest backend **3/3**, frontend **3/3** ; `tsc` direct OK. |
| P1C | Emails backoffice — lisibilité et test SMTP | local | Sur Email Monitor en thème sombre : ouvrir un email HTML — contenu lisible (fond blanc dans l’aperçu). Sur un email `FAILED`, vérifier sujet/destinataire/expéditeur/date + raison d’échec SMTP. Sur `/b4ck0ff1ce/emails/settings`, vérifier host/port/from/reply-to/provider depuis l’API (pas de valeurs OVH codées en dur). Tester envoi depuis Déliverabilité (`POST /api/v1/emails/test`) — ne doit plus renvoyer 500 si SMTP/MailHog est configuré. | | | | [ ] | Preuve agent 16/06 : `EmailHtmlPreview`, settings dynamiques, `sendTestEmail` via Nodemailer ; `tsc` direct OK ; smoke runtime API test à confirmer avec session admin. |
| P1C | Politiques sécurité — posture globale B10/B11 | local | Sur `/b4ck0ff1ce/security/policies`, vérifier la synthèse en haut de page : WAF, Firewall, Réponse IP, Alertes critiques, Auto-blocage. Couper/réactiver WAF ou une règle WAF uniquement en environnement local/lab autorisé et vérifier que la carte reflète l’état. Vérifier que l’auto-blocage high/critical reste présenté comme borné/recommandé tant que la politique B12 n’est pas validée. | | | | [ ] | Preuve agent 16/06 : helper posture testé **2/2**, `npm run type-check` OK, ESLint ciblé OK. |
| P1C | Alertes critiques email — regroupement anti-spam | local | Déclencher deux alertes similaires high/critical en lab : la première doit créer un email, la seconde dans la fenêtre configurée doit être regroupée/rate-limitée. Au prochain email autorisé, vérifier la ligne **Alertes similaires regroupées** et l’absence de secrets dans le contexte technique. | | | | [ ] | Preuve agent 16/06 : `securityAlertEmailNotifier` rate-limit par famille d’alerte ; Jest notifier/payload/settings **10/10** ; `node --check` OK. |
| P1C | Réputation IP lab — VPN/proxy/Tor/ASN déterministes | local | Sur une fiche menace lab utilisant `198.51.100.*`, `203.0.113.*` ou `192.0.2.*`, vérifier que les champs ASN/organisation/VPN/proxy/Tor/source/confiance sont renseignés de façon cohérente et sourcée `rfc5737-lab-fixture`, sans dépendre d’un service externe. | | | | [ ] | Preuve agent 16/06 : fixtures RFC5737 dans `geoipProvider`; tests GeoIP + forensics **10/10**. |
| P1C | Menaces — statut blocage recommandé vs automatique | local | Sur `/b4ck0ff1ce/security/threats` et une fiche menace high/critical : vérifier les libellés **Blocage recommandé** (IP privée/lab ou auto-blocage non confirmé) vs **Bloqué automatiquement** vs **Bloqué manuellement** ; le détail sous le libellé doit expliquer pourquoi une menace high reste non bloquée. Après blocage manuel, vérifier le libellé manuel et l’absence du bouton **Bloquer**. Sur `/b4ck0ff1ce/security`, vérifier la mention `docs/ERRORS.md` dans la légende Lot sécurité. | | | | [ ] | Preuve agent 16/06 : `threatBlockPresentation.ts` testé **3/3** ; pages menaces + fiche détail ; `metadata.blockOrigin` backend auto/manuel ; `tsc` direct OK. |
| P1C | Menaces — timeline investigation multi-sources | local | Sur une fiche menace avec logs, intrusion ou DDoS corrélés : vérifier la section **Timeline d'investigation (24 h)** — badges Menace/Log/Intrusion/DDoS/Réseau, tri du plus récent au plus ancien, requestId si présent, lien **Voir le log** vers `/security/logs?highlight=…`. Menace sans corrélation : message empty state honnête. | | | | [ ] | Preuve agent 16/06 : `threatInvestigationTimeline.ts` **2/2** ; composant `ThreatInvestigationTimeline` ; `tsc` direct OK. |
| P1C | Logs sécurité — recherche requestId | local | Depuis une fiche menace avec un `requestId` visible dans la timeline, cliquer **Voir le log** puis vérifier que `/security/logs?highlight=…` retrouve le log. Si la page logs expose la barre de filtres, rechercher le `requestId` exact : le résultat doit inclure les logs dont `metadata.requestId`, `metadata.correlationId`, `metadata.xRequestId` ou `metadata["x-request-id"]` correspond. | | | | [ ] | Preuve agent 16/06 : API logs filtre `requestId` + recherche libre sur variantes de corrélation ; facettes `requestIds`; client frontend aligné ; Jest backend **4/4**, `tsc` direct OK. |
| P1C | Lot B3/B4 — nature incidents + réseau actionnable | local | Sur `/b4ck0ff1ce/security/incidents` : colonne **Nature** (Détection / Blocage manuel / auto) + origine si connue. Sur `/b4ck0ff1ce/security/network` : bannière **Corrélation conteneurs** avec % et hint actionnable si beaucoup de connexions non mappées ; liens tableau avec raison explicite si pas de menace/log. | | | | [ ] | Preuve agent 16/06 : `SecurityNatureBadge` + `correlationHint` API ; Jest `securityLabels` **8/8** ; `tsc` OK. |
| P1C | Lot B7/B8 — investigation + audit | local | Ouvrir `/b4ck0ff1ce/security/investigation` : onglets **Audit B7**, **Menaces & logs**, **Comptes impactés** ; filtres IP/requestId/service/type menace ; exports **CSV menaces** et **bundle complet** avec SHA-256 + mention audit `security_export`. Débloquer une IP lab puis vérifier une ligne `ip_unblock`. Sur fiche menace : section investigation inclut des `aggregated_logs` si un `requestId` gateway existe. | | | | [ ] | Preuve agent 16/06 suite : API `investigation/search` + `investigation/export` ; comptes impactés depuis audit auth + userId logs ; Jest investigation **4/4** ; `tsc` OK. **Prérequis local** : table `audit_logs` (migration ciblée security-service). **Re-vérif agent 16/06 soir** : `docker compose up -d jobbingtrack-metrics-aggregator` (healthy) ; navigateur `http://localhost:5003` — `/performances/network` **160 points** RX/TX ; `/security/network` **11 connexions** ; `/security/investigation` onglet Audit avec lignes audit + boutons export visibles. Cause zéro données perf : conteneur `metrics-aggregator` absent de la stack partielle. Badge Next.js « 1 Issue » = overlay dev hydration (attribut `data-cursor-ref` automation), pas bug applicatif constaté hors automation. |
| P1C | Lot B6/B9 — mobile corrélation | local | Depuis l’app Flutter : vérifier que les requêtes API portent `X-Request-Id` (logs gateway) et qu’un `POST /api/v1/mobile/security-events` (ex. `auth_failure`) crée un log `mobile_*` visible dans `/security/logs`. | | | | [ ] | Preuve agent 16/06 : headers Flutter + route gateway rate-limit 30/min ; `node --check` OK. |
| P1C | Playwright backoffice — navigateur et non-régression UI réactivés | local | Ouvrir les pages backoffice principales après login admin, notamment Sécurité, Statistics, Performances, Gestion des données > Relances, et Testeur d’API : vérifier qu’elles chargent sans écran d’erreur et que la navigation reste fluide. Point d’attention : la très grande suite backoffice 78 pages fait encore redémarrer brièvement le dev server Next en fin de campagne ; valider si le niveau actuel suffit pour continuer ou si on priorise un nouveau lot mémoire/dev-server. | | | | [ ] | **Preuve agent 14/06** : Playwright Chromium débloqué via `TMPDIR` local `frontend/.tmp-playwright`. Smokes ciblés OK (Sécurité **10/10**, Statistics **4/4**, Performances **7/7**, Relances/Événements/API tester **6/6**). Suite complète `backoffice.spec.ts` : warmup routes lourdes + `gotoBackofficePage` résilient + retry local **2** → relance fraîche **78/78 passed**, **0 flaky**, **0 failed** (~5,3 min) ; conteneur healthy, `restart=0`, `oomKilled=false`, ~**1.49 GiB / 2 GiB** après campagne. |
| P1C | Comparaison rapports tests agrégés (non sécurité) | local | Suivre § “P1C — UX backoffice” : comparer 2 rapports non sécurité de même catégorie ; tableau de comparaison visible. | | | | [ ] | |
| P1C | Backoffice Développement → Tests — navigation et regroupement rapports | local | Suivre § “P1C — UX backoffice” : clic Tests = vue d’ensemble ; sous-menu Rapports ; toutes les entrées Tests restent accessibles. | | | | [ ] | **09/06 porteur** : menu Tests trop long et pas assez sous-catégorisé ; à traiter après P1A/P1B selon ordre. |
| P2 | Backoffice Tests — suite Agent email / triage | local | Depuis `/b4ck0ff1ce/tests`, vérifier la carte **Agent email / triage**, le bouton rapide **Lancer suite agent email**, l’exécution sans erreur et l’ouverture du rapport dans `/b4ck0ff1ce/test-reports` catégorie **Agent email / triage**. | | | | [ ] | Suite ajoutée après demande porteur 12/06 pour rendre les tests email-triage lançables directement dans l’interface JobbingTrack. |
| P1C | Responsive backoffice complet petit/moyen écran | local | Suivre § “P1C — UX backoffice” : tester petit écran, écran moyen et largeurs intermédiaires sur les pages backoffice principales ; lister les routes avec débordement horizontal, filtres/tableaux cassés, menu inutilisable, boutons hors écran, ou contenu qui réserve encore une marge/colonne pour la sidebar/panneau rétractable alors qu’il devrait reprendre toute la largeur disponible. | | | | [ ] | **10/06 porteur** : interface pas encore assez optimisée sur petit et moyen écran. **12/06 porteur** : ne pas garder d’espace vide réservé au panneau JobbingTrack rétractable sur petit écran. **Preuve agent 14/06** : audit Playwright `backoffice-responsive-audit.spec.ts` — **4 viewports × ~70 routes** vert agent (petit-pc, smartphone, moyen, grand-ecran) ; correctif toolbar Email Monitor 1280px. **Validation visuelle porteur toujours requise** sur écran réel/DevTools. |
| P2 | Rapports tests — taille et compression | local | `/b4ck0ff1ce/test-reports` affiche la taille de chaque rapport (Ko/Mo) et le volume total filtré ; cadrer ensuite une compression/archivage des anciens rapports sans casser l’ouverture, le téléchargement ni les détails sensibles. | | | | [ ] | Taille affichée ajoutée côté agent ; compression à traiter après P0 CVE. |
| P2 | Mode clair backoffice — lisibilité page par page | local | Si une page est illisible en clair, noter la route exacte (ne bloque plus le lot global). | | | | [ ] | Acceptation provisoire 21/05. |
| P2 | Performances — Disque stockage BDD | local | Page Disque : cartes + Block I/O ; validation données réelles. | | | | [ ] | |
| P2 | Préférences refresh par graphique | local | Vérifier héritage zone globale vs override local (si UI exposée). | | | | [ ] | |
| P2 | Mobile — source officielle | décision | Choisir `mobile/` vs `flutter-mobile-app/` avant archivage doublon. | | | | [ ] | |
| P2 | Agent email produit — UI `/agent` + connexion boîtes | local | (1) Admin : activer **Agent recherche** sur un user test (`jobSearchAgentEnabled`). (2) User : `/agent` → consentement **Accès boîtes mail** → enregistrer. (3) Ajouter boîte IMAP ou Gmail OAuth (`GOOGLE_OAUTH_*`). (4) **Synchroniser** → file « À traiter ». (5) **Lier candidature** + **Tasks/Calendar** sur un email. (6) Révoquer boîte. | | | | [ ] | **Preuve agent 23/06** : API liaison/actions + UI `/agent` ; `tsc` OK. Prérequis porteur : `GOOGLE_OAUTH_*` ; IMAP OVH `EMAIL_TRIAGE_READ_PASSWORD` (guillemets si `#`/`^` dans le mot de passe). |
| P2 | AVD — BlueMail OVH + fin connexion Gmail | appareil `emulator-5554` | (1) Play Store → installer **BlueMail** ; compte IMAP `candidatures@delhomme.ovh` / `imap.mail.ovh.net:993`. (2) Paramètres Android → terminer connexion **Gmail pro** si écran Google encore ouvert. (3) Ajouter `EMAIL_TRIAGE_READ_PASSWORD=<mdp candidatures>` dans `.env` puis `node scripts/mobile/sync-test-env.js --write`. | | | | [ ] | **Preuve agent 23/06** : Play Store ouvert via `install-emulator-bluemail.js` ; Gmail auto-login partiel ; OVH IMAP **KO** sans mot de passe dédié. |
| P2 | Agent email / tâches recherche emploi — cadrage produit (suite) | local | Relire `docs/features/EMAIL_TRIAGE_AGENT.md` pour digest 18h, Tasks/Calendar, liaison candidature, mobile. | | | | [ ] | **09/06 porteur** ; phase 1 produit livrée 22/06 — suite après validation UI `/agent`. |

## Gate technique fin de journée / avant push majeur

À lancer **en fin de journée**, **avant un push complet sur `dev`** ou **avant une PR importante**. Exception acceptée le 21/05 : ne pas lancer cette campagne pendant la correction P0/P1 pour éviter de perdre du temps ; garder les tests ciblés par changement, puis faire la campagne complète quand la session de corrections est stabilisée.

| Étape | Commande / cible (référence) | Preuve attendue |
|-------|------------------------------|-----------------|
| 1. Stack + BDD | Cible Make documentée **`test-full-quick`** (= `up-full` + `db-push-all` + `seed-auth` + `status`) **ou** équivalent manuel si la stack tourne déjà | `make status` / conteneurs `jobbingtrack-*` healthy ; pas d’erreur `db-push-all` |
| 2. Suite complète | **`bash scripts/run-all-tests-with-reports.sh`** avec `TEST_NOPROMPT=1` et variables hôte (voir bloc ci-dessous) | Sortie script **code 0** ; dossier `tests/results/<horodatage>/` créé |
| 3. Lecture détaillée | Ouvrir `tests/results/<horodatage>/summary.json`, `report.html`, `report.txt` **et** `/b4ck0ff1ce/test-reports` | **0 échec bloquant** ; comprendre chaque ligne en échec avant de pousser |
| 4. Frontend rapide (complément) | Depuis `frontend/` : `npm run type-check` + `npm run lint` | Pas de nouvelle erreur introduite par le lot |

Bloc recommandé (équivalent validé le 13/05 — voir `docs/TODOS.md`) :

```bash
TEST_NOPROMPT=1 \
API_URL=http://127.0.0.1:5002 \
API_GATEWAY_URL=http://127.0.0.1:5002 \
PLAYWRIGHT_BASE_URL=http://localhost:5003 \
PLAYWRIGHT_FRONTEND_MODE=smoke \
PLAYWRIGHT_MOBILE_MODE=smoke \
PERF_LIGHT=1 \
bash scripts/run-all-tests-with-reports.sh
```

**Sans stack Docker** : des centaines d’échecs sont **normaux** (`ECONNREFUSED`, conteneurs absents) — voir `docs/ERRORS.md` et `docs/STATUS.md` § 11/04/2026. Ne pas interpréter comme régression si Postgres/gateway/front ne tournent pas.

**Durée** : ~10–20 min (smoke). Campagne Playwright **full** : variable `PLAYWRIGHT_FRONTEND_MODE=full` (plus long, hors agrégat par défaut).

**Checklist longue** : `docs/tests/TESTS_END.md` (tous les points manuels en fin de projet).

| Priorité | Validation porteur | Environnement | Preuve attendue | Notes porteur | Preuves porteur | Décision porteur | Statut | Notes agent |
|----------|--------------------|---------------|-----------------|---------------|------------------|------------------|--------|-------------|
| P1D | Gate suite complète tests fin de journée / avant push majeur | local | Dernière campagne `run-all-tests-with-reports.sh` **verte** (exit 0) + rapport lu dans `tests/results/<horodatage>/` ; noter date/heure dans décision/preuves porteur. | | | | [ ] | Dernière campagne complète : **à relancer en fin de journée**, pas pendant le P0/P1 courant. |
| P1D | Audit final A-Z avant déploiement / tests globaux finaux | local puis préprod dédiée | Reprendre chaque bouton, formulaire, écriture BDD, endpoint API, service backend, job/worker, email, configuration, Docker/HTTPS, sécurité, logs, métriques, performances, responsive, mobile et rapports. Produire un bilan détaillé : régressions, lenteurs, opportunités d’optimisation sans perte fonctionnelle, risques prod et décision GO/NO-GO. | | | | [ ] | À lancer seulement en fin de programme, après clôture/reclassement des P1 ouverts et avant préprod/prod. |

## File technique liée (pas de validation ici)

Les chantiers non encore prêts pour validation porteur restent dans `docs/TODOS.md` (env strictes, pentest, PQC, purge menaces après OK P0, etc.).

## À ne pas valider ici

- Préprod/prod réelle : utiliser `A_VALIDER_AVANT_PRODUCTION.md`.
- Déploiement serveur : utiliser `DEPLOIEMENT_PRODUCTION.md`.
- Validation production réelle : utiliser `VALIDATION_PRODUCTION.md`.
- Tâche technique non livrée : rester dans `docs/TODOS.md`.
