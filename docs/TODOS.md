# TODOS — chantier backoffice / API / doc (JobbingTrack)

**Dernière mise à jour : 11 juin 2026** — les tâches à faire passent en premier. Les succès techniques restent conservés plus bas, mais ils ne valent pas validation porteur. Le flux obligatoire est maintenant **`PILOTAGE.md` → `TODOS_A_VALIDER.md` → `TODOS_A_VERIFIER.md` → `docs/TODOS.md`**.

## Pilotage (comment utiliser ce fichier)

- **`PILOTAGE.md`** = point d’entrée obligatoire avant ce fichier.
- **`TODOS_A_VALIDER.md`** = bloque la suite si une validation porteur P0 est ouverte.
- **« Priorités à faire maintenant »** = backlog technique ordonné ; **une puce peut couvrir plusieurs PR ou sessions** (ne pas viser tout cocher en un seul merge).
- **Variables d’environnement strictes** = chantier **progressif** : la ligne détaille déjà ce qui est **amorcé** (gateway, politique, doc) et le **lot suivant** (autres services, compose, CI). Avancer **service par service** en gardant la stack démarrable entre deux étapes.
- **Validation porteur** : déplacer de **`TODOS_A_VALIDER.md`** vers **`TODOS_DONE.md`** ; reporter ensuite dans **`PLAN.md`** / **`STATUS.md`** quand c’est acté.
- **Lecture rapide 21/05** : suivre d’abord le tableau ci-dessous. Le reste du fichier garde le détail historique par lot, mais ne doit plus servir de liste linéaire à dérouler.

## Pilotage court — état actuel

| Bloc | Statut | Prochaine action concrète |
|------|--------|---------------------------|
| **CI / formatage frontend** | **21/05** run GitHub `26202796200` sur `finish-feat/central-logging-full` : pipeline **succès** (Prettier, type-check, Tests Frontend, Tests Backend, DB). | Branche archivée après merge ; rouvrir seulement si un nouveau push casse le workflow. |
| **Agent email / tâches recherche emploi** | **09/06 cadrage porteur** + **12/06 socle tests digest/classification** + récupération Apps Script v4 : boutons `FAIT` / `PAS PERTINENT` / `REPORTER +3j`, recherche offres Rennes via flux publics, Web App `doGet`; secrets sortis du script vers propriétés Apps Script. Fiche dédiée : `docs/features/EMAIL_TRIAGE_AGENT.md`. | Après validation P0/P1 bloquants : espace utilisateur `/` distinct du backoffice `/b4ck0ff1ce`, accès réservé au compte personnel non-admin explicitement autorisé, worker planifié, Gmail OAuth lecture seule multi-comptes, boîtes configurées hors Git, stockage interne, règles déterministes, digest 18h + récap hebdo via SMTP JobbingTrack (`EMAIL_TRIAGE_DIGEST_FROM` `@jobbingtrack.com`), Google Tasks/Calendar obligatoires sans horaire inventé à `00:00`, ni événement auto avant `05:00` ou après `23:00`, IA locale en renfort, suites de tests/rapports dédiées. |
| **Performances — plages temporelles** | Synthèse validée 21/05 ; sous-pages Réseau/Disque/Conteneurs/Latence validées en Playwright (`performances-range-smoke.spec.ts`, transitions 24 h ↔ 7 j, pas d’overlay Network Error). | Validation porteur navigateur optionnelle ; sticky période et disque BDD approfondi restent ouverts. |
| **Performances — UI graphes / logo** | **12/06** : tooltips Recharts alignés avec la couleur réelle des séries, lien sous-nav Latence corrigé vers `/performances/latency`, nouveau logo JobbingTrack branché sur web/backoffice/mobile. | Validation porteur dans `TODOS_A_VALIDER.md` : tooltips au survol + logo sur login/sidebar/mobile. |
| **Budget mémoire JobbingTrack** | **12/06** : budget stack local `full` fixé à **8192 MB** via `mem_limit` Compose ; `metrics-aggregator` normalise les limites Docker pour ne plus afficher la RAM hôte (~48 Go) comme limite par service. Dette reportée avant déploiement : rendre ce budget adaptatif à la RAM réellement disponible par serveur et aux autres systèmes présents, au lieu de considérer les valeurs `.env.example` comme profil universel. | Recreate conteneurs puis validation porteur : dashboard/services doivent afficher le budget JobbingTrack et non la capacité hôte. |
| **Mode clair backoffice** | Acceptation porteur provisoire 21/05 : lisibilité globale considérée validable pour l’instant. | Garder les cas résiduels dans “UX unifiée Statistics / Performances / Vue d’ensemble” plutôt que bloquer cette ligne. |
| **Statistics / backoffice** | **21/05** : vue d’ensemble affiche graphes disponibilité/erreur + `MetricsSeriesCaption` ; plage partagée avec Performances (`TimeRangeSelector` + localStorage) ; barre période sticky. | Valider porteur onglet Sécurité stats ; étendre couleurs multi-séries ; persister `error_rate` explicite si besoin. |
| **Sécurité** | Pagination/navigation/central logging déjà validés techniquement ; forensics, rapports, alertes mail restent des lots séparés. | Ne pas mélanger avec le formatage CI : reprendre par lot B10/B11 après le socle backoffice. |
| **Docs / organisation** | `TODOS.md` contient encore de l’historique utile mais trop long. | Transformer progressivement les gros blocs en liens vers `PLAN.md`, `STATUS.md`, `docs/BACKLOG.md`, sans perdre les preuves déjà notées. |

## Phase validation porteur (ordre strict — après `db-push-all` + `make recreate`)

> **Recreate** : si `docker ps` affiche les conteneurs `jobbingtrack-*` **Up** depuis peu (ex. ~1 h), le **`make recreate`** a bien tourné. Sinon : `make recreate` depuis la racine, puis `make status`.

### Checklist à cocher (dans l’ordre)

- [x] **1. HTTPS / login** — validé local 19/05 : `curl` login **200** + page `/login` **200** (archivé dans `TODOS_DONE.md`).
- [x] **2. Sécurité — navigation** — validé porteur 19/05 : **`SecuritySubNav`** ; titres FR **Logs** ; parcours Politiques, Menaces, Firewall, Analyse, Réseau.
- [x] **3. Sécurité — pagination** — validé porteur 19/05 : `?page=` / `blockedPage=` conservés au refresh ; logs / menaces / IPs bloquées paginés.
- [x] **4. Corrélation** — validé porteur 19/05 : `/b4ck0ff1ce/performances/correlation` — KPI logs / ERROR-WARN exploitables sur service focal.
- [x] **5. WAF — comprendre les alertes** — validé 19/05 : faux positif `consolidated=true` corrigé (`all=true`) ; payloads XSS/SQLi **403** ; trafic JWT + XFF legit **200** (archivé dans `TODOS_DONE.md`).
- [x] **6. Reporter** — validations locales 19/05 archivées dans `TODOS_DONE.md` ; la suite porteur ouverte est maintenant dans `TODOS_A_VALIDER.md`.

### Moteur personnalisation / apparence (dette technique — pas un vrai moteur aujourd’hui)

> **Constat porteur (19/05)** : la page **Paramètres** et l’apparence globale reposent sur un empilement ad hoc (`useCustomization` + variables CSS sur `:root` + `localStorage` + API `/users/customization` optionnelle), **sans** moteur de thème/layout unifié. La page **Statistics** a en plus sa propre clé `statistics-customization` (doublon). Résultat : reset fragile, couleurs incohérentes, options « disposition » peu fiables.
> **Constat porteur (20/05)** : le **mode clair backoffice** reste par endroits franchement illisible (contrastes, fonds/cartes, textes secondaires), malgré la migration structure frontend / moteur UI. Même si le mode sombre reste l’usage principal, le mode clair doit redevenir exploitable avant validation produit. Suite porteur : certaines cartes/badges colorés (vert, orange, bleu, violet, rouge…) étaient trop fades et pas assez identifiables.

- [x] **Spécifier un moteur UI** (branche `finish-feat/ui-motor`) : `docs/frontend/UI_MOTOR.md`, `UserUiPreferencesV1` + `CustomizationSettings` dans `frontend/src/lib/ui/preferences/`.
- [x] **Couche d’application** : tokens `--jt-primary` / `--jt-accent` (+ alias `--primary-color`), plus de génération `--primary-500/600` en JS.
- [x] **Persistance** : localStorage v1 + legacy + API ; panneaux `panels.statistics` / `panels.analytics` (migration auto des anciennes clés).
- [x] **Layouts backoffice** : `DashboardLayoutRegion` (metrics/section/dense/triple/split) sur `/backoffice` + Statistics ; `data-dashboard-layout` + classe `dashboard-content`.
- [x] **Fusion** : `statistics-customization` / `analytics-customization` → schéma v1 (`useStatisticsPanelPrefs` / `useAnalyticsPanelPrefs`).
- [x] **Tests** (partiel) : `preferences/__tests__/customization.test.ts` (merge, `customizationToV1`). Reste : reset DOM, round-trip API mock.
- [x] **Persistance thème sombre/clair — storage unifié (20/05)** : le toggle `useTheme` synchronise maintenant `theme`, `jobbingtrack-ui-preferences-v1` et `customization-settings` pour éviter qu’un ancien `theme:auto` du moteur UI réapplique le mode clair au refresh.
- [x] **Mode clair backoffice — lisibilité globale** : **20/05 partiel** : surfaces/cartes/champs renforcés, puis familles colorées (`blue`, `green/emerald`, `amber/orange/yellow`, `red/rose`, `violet/purple/indigo`, `sky/cyan`) rendues plus visibles en clair. **21/05** : validation porteur provisoire acceptée pour ne plus bloquer le lot apparence. Les cas résiduels restent suivis dans **“UX unifiée Statistics / Performances / Vue d’ensemble”** et dans `TODOS_A_VALIDER.md` si une page précise redevient illisible.

**Correctifs courts déjà faits (19/05)** : crash `duration`, reset opérationnel, couleurs hex validées — **ne remplace pas** le moteur ci-dessus.

### Suite après validation (ne pas mélanger avec la phase ci-dessus)

1. **Statistics (A1h)** — compteurs services + graphes erreur / disponibilité.
2. **Central logging** — runtime `aggregated_logs` validé **15/15** ; suite : brancher/fiabiliser l’usage UI (`log-stats`, granularité erreurs, période).
3. **B10 reste** — `document.title`, fallbacks noms API, homogénéisation libellés.
4. **Priorités techniques** — CI Prettier, env strictes, pentest, etc. (§ ci-dessous).
5. **Très fin de projet** — § **Vision données décentralisées (DHT)** en bas de fichier.

---

## WAF gateway — analyse logs (18/05 — diagnostic, pas de correctif dans ce lot)

**Où** : `backend/api-gateway/src/middleware/waf.js` (`wafCheck`, règles `WAF_RULES`).

**Règles actives (familles détectées)** :

| Règle | Gravité | Exemples de motifs |
|--------|---------|-------------------|
| `SQL_INJECTION` | high | `union select`, `--`, `drop table`, etc. |
| `XSS` | high | `<script>`, `javascript:`, **`on\w+\s*=`** (handlers HTML), iframes, `data:text/html` |
| `PATH_TRAVERSAL` | critical | `../`, encodages `%2e%2e` |
| `COMMAND_INJECTION` | critical | `\|`, `` ` ``, `; rm`, `cmd /c` |
| `LDAP_INJECTION` | medium | filtres LDAP mal formés |
| `SUSPICIOUS_USER_AGENTS` | high | sqlmap, nikto, burpsuite, nmap, etc. |
| `MALICIOUS_PATTERNS` | (voir fichier) | `eval(`, `base64_decode`, etc. |
| `SUSPICIOUS_HEADERS` | (voir fichier) | en-têtes anormaux |

**Ce que montrent les logs `api-gateway` en navigation backoffice sécurité (mai 2026)** :

- Alertes répétées **`Attaque détectée par WAF`** · règle **`XSS`** · motif **`on\w+\s*=`** sur  
  `GET /api/v1/security/firewall/blocked-ips?consolidated=true`
- **Ce n’est pas une vraie attaque XSS** : le paramètre query **`consolidated=true`** contient la sous-chaîne **`on`** + lettres + **`=`**, ce qui déclenche la regex des handlers du type `onclick=`.
- **Source côté UI** : page **Menaces** (auto-refresh ~5 s) appelle `blocked-ips?consolidated=true` pour croiser les IPs bloquées.
- **Impact** : la règle **XSS** est en gravité **high** → la gateway renvoie **403 `WAF_BLOCKED`** (pas seulement un log). La page **Menaces** peut échouer sur `blocked-ips?consolidated=true` toutes les ~5 s ; certaines requêtes sans ce paramètre passent (logs gateway : alternance warn / proxy OK).

**Correctif à planifier (après validation porteur, lot sécurité)** :

- [x] **WAF faux positif `consolidated=true`** — corrigé 19/05 : frontend `all=true` + regex XSS `(?<![a-zA-Z0-9])on[a-z]+=` ; `blocked-ips?consolidated=true` → **200** en test.
- [x] Affiner la règle **XSS** (durcissement supplementaire) et documenter dans **`ERRORS.md`** le distinguo « vraie attaque » vs faux positif query — **21/05** section ERRORS.md + regex `(?<![a-zA-Z0-9])on[a-z]+=` (19/05).
- [x] Documenter dans **`ERRORS.md`** le distinguo « vraie attaque » vs « faux positif consolidated » — **21/05** (fusionné avec entrée ci-dessus).

---

## Priorités à faire maintenant

- [ ] **Agent email, tâches et accompagnement recherche** : **09/06 cadrage porteur** — créer une interface utilisateur privée JobbingTrack sur `/`, distincte du backoffice admin `/b4ck0ff1ce`, utilisable par le compte personnel non-admin du porteur après activation explicite, mais non disponible automatiquement pour tout compte créé. Elle doit lire/triager les emails de recherche d’emploi depuis un ou plusieurs comptes Gmail OAuth et boîtes configurés hors Git, relier emails ↔ candidatures/entreprises/contacts, créer et planifier tâches/relances/appels/événements, préparer les entretiens, envoyer un digest quotidien à 18h et un récap hebdomadaire via le même socle SMTP JobbingTrack que reset/validation (`SMTP_*`, `SMTP_FROM`, `SMTP_REPLY_TO`, `EmailLog`), synchroniser Google Tasks/Calendar obligatoirement, et utiliser un moteur déterministe doublé ensuite d’IA locale si utile. **11/06 précision porteur** : l’intégration Google Tasks ne doit pas seulement créer des tâches ; elle doit aussi relire périodiquement les tâches Google, détecter celles marquées terminées / déplacées / modifiées côté Google, réconcilier ce statut avec JobbingTrack, planifier la suite (`à faire`, `fait`, `à relancer`, `à vérifier`) et afficher les conflits ou actions externes dans le digest. Calendar ne doit jamais inventer un horaire `00:00`, créer automatiquement avant `05:00` ni créer automatiquement après `23:00` : date seule = événement journée entière proposé ou tâche “horaire à confirmer”, horaire hors fenêtre = tâche “horaire à vérifier” ou validation utilisateur. Le périmètre inclut aussi : feature flag/droit utilisateur `JOB_SEARCH_AGENT_ENABLED`, activation/révocation admin auditée, séparation stricte rôle admin vs lecture emails personnels, base de composants frontend partagée entre espace utilisateur et backoffice, possibilité future de séparer `user-frontend` et `backoffice-frontend`, revalidation PIN + clavier numérique, dashboard responsive mobile, autocomplete poste/ville/plateforme avec navigation clavier ARIA, token agent email + doc endpoint API, adresse de transfert configurable, liste emails reçus triée par candidature/relance, recherche v2 utilisateur puis backoffice/admin, préparation/envoi relance-email depuis l’interface après validation explicite, calendrier agrégé, programmation manuelle d’appels/tâches/rappels/événements même sans email déclencheur, appels contact/entreprise préremplis, relances uniquement depuis fiche candidature, page détail entreprise complète, imports Google Contacts CSV/vCard, gestion intérim agence/mission, enrichissement entreprise, préparation entretien, sauvegarde PDF d’offre depuis URL, veille salons/job dating par ville/région, et suites de tests agent email avec rapports `tests/results/email-triage/<timestamp>`. Make.com/Zapier ne sont pas le socle. Source détaillée : `docs/features/EMAIL_TRIAGE_AGENT.md`, découpage : `docs/PLAN.md` lot I. **Important** : ne pas démarrer l’implémentation tant que la première ligne bloquante de `TODOS_A_VALIDER.md` n’est pas validée ou reclassée.
- [ ] **Tests agent email recherche — suite dédiée à créer après P0** : socle amorcé (`tests/email-triage/README.md`, moteur classification, politique horaire Calendar, digest programmé, identité expéditeur `@jobbingtrack.com`, `run-with-report.sh`). **12/06** : suite email-triage **8 suites / 45 passed, 1 skipped** (permissions `JOB_SEARCH_AGENT_ENABLED`, rendu digest, politique connexion Gmail/IMAP) ; rapport `tests/results/email-triage/20260612-011945` ; backoffice `/b4ck0ff1ce/tests` + section 34b `run-all-tests-with-reports.sh` alignés sur les 8 fichiers Jest. Reste produit : API permissions réelles, worker digest SMTP, OAuth Gmail lecture seule, UI `/`. Les vrais comptes/adresses/mots de passe restent uniquement dans `.env` gitignoré ; en absence de variable, les tests d’intégration doivent être skip explicite et lisible, pas fallback dangereux.
- [ ] **Backoffice Développement → Tests — navigation et rapports** : **09/06 demande porteur** — le menu Tests est trop long : clic direct sur **Tests** = vue d’ensemble ; créer un sous-menu **Rapports** regroupant **Rapports de tests** et **Rapports de parcours** ; réorganiser les entrées Playwright/API/Backend/Frontend/Backoffice/Sécurité/Performance, programmation et parcours sans casser l’accès existant. **11/06 agent** : sous-menu **Rapports** séparé, sections **Automatisés** / **Sécurité & charge** dans Tests, doublon « Rapports de parcours » retiré du menu Parcours (accès via Rapports). Validation navigateur porteur en attente (`TODOS_A_VALIDER.md` P1C).
- [ ] **Backoffice listes longues — pagination/recherche réutilisables** : **10/06 demande porteur** — généraliser le patron `Pagination` + recherche serveur sur toutes les listes volumineuses (emails, logs, rapports, utilisateurs, données applicatives, incidents). **Fait partiel** : Email Monitor utilise pagination serveur `page/limit`, recherche `q` sur destinataire/expéditeur/sujet et badge de recherche. **Reste** : auditer page par page, éviter les listes complètes en mémoire, normaliser `pagination.total/pages`, conserver filtres/page dans l’URL quand utile.
- [ ] **Backoffice responsive complet — petit et moyen écran** : **10/06 demande porteur** — auditer toute l’interface admin sur petits écrans, écrans moyens et largeurs intermédiaires, pas seulement mobile strict. **11/06 agent** : spec Playwright dédié ajouté (`backoffice-responsive-audit.spec.ts`) sur routes backoffice statiques et viewports smartphone/moyen/petit PC/grand écran ; correctifs layout appliqués sur `AdminLayout`, sécurité/firewall, services, tests, parcours utilisateur, API tester, délivrabilité emails, événements, mobile-emulator, corbeille, analytics utilisateur et utilisateurs. **12/06 porteur** : sur petit écran, le contenu ne doit pas réserver une marge/colonne fantôme pour le panneau/sidebar JobbingTrack rétractable ; quand le panneau est replié ou en overlay mobile, le reste de l’écran doit reprendre toute la largeur disponible comme sur grand écran. Validation agent : smartphone **2/2 OK**, moyen **2/2 OK**, petit PC + grand écran OK lors de l’audit complet précédent ; `tsc` OK ; ESLint ciblé 0 erreur / warnings historiques. **Reste** : validation visuelle porteur ligne P1C dans `TODOS_A_VALIDER.md`, puis traiter les pages dynamiques avec IDs réels (`users/[id]`, `applications/[id]`, `companies/[id]`, menaces/incidents détail) dans un lot données fixtures si besoin.
- [ ] **Performances — page dédiée CPU & Mémoire** : **13/06 demande porteur** — recréer une lecture métriques claire dans `/b4ck0ff1ce/performances` avec une entrée de navigation **CPU & Mémoire** sous **Synthèse** et au-dessus de **Temps de réponse**. **Fait technique 13/06** : page `/b4ck0ff1ce/performances/cpu-memory`, sélecteur temporel partagé, cartes CPU/mémoire live, sous-vues **Vue globale / CPU détaillé / Mémoire détaillée**, séries par service activables/masquables, couleurs stables et tooltips Recharts partagés. **Optimisation suite retour porteur** : la vue globale charge l’historique système en une requête ; les vues détaillées ne chargent que les services cochés (6 par défaut, bouton tout afficher si besoin), pour éviter le chargement de tous les historiques conteneurs au premier rendu. **Correctif “aucune information”** : conservation/aplatissement des champs live CPU/mémoire depuis `service.metrics` + fallback graphique live quand la persistance n’a pas encore de points ; points visibles pour les séries live plates/proches de zéro ; hints de période masqués sur les pages Performances. La page réutilise `metrics-aggregator` sans nouveau contrat API. **Reste** : validation visuelle porteur, stabilisation des vraies séries A5/A1h (sources live vs BDD, périodes, axes, gaps, empty states), puis extraction du **moteur graphique dédié** pour éviter de créer une abstraction avant d’avoir les cas CPU/mémoire/latence/conteneurs réellement validés.
- [ ] **Validation A-Z agent par lots** : **11/06** programme ajouté dans `docs/operations/VALIDATION_A_Z_AGENT.md` pour vérifier Docker, API, backend, emails, frontend, backoffice, mobile, sécurité non destructive et documentation. Première passe agent : health stack OK, emails validation envoyés/miroir OK, suites P1B/SMTP/security/metrics/mobile vertes. **Reste** : étendre aux parcours navigateur authentifiés, onglets Statistics P1B, responsive global, mobile login réel sur S21 FE (`scripts/mobile/setup-physical-device.sh` + `smoke-login-adb.js`) et gate fin de journée.
- [ ] **Lot M — mini-modèles prédictifs locaux sobres (à ouvrir très bientôt, après clôture/reclassement P1 en cours)** : cadrer puis implémenter progressivement de très petits modèles locaux, optionnels et explicables, sans appel cloud ni dépendance lourde par défaut. Objectifs possibles : détection d’anomalies/failles sécurité dans les logs (`security_logs`, `aggregated_logs`, WAF/gateway/auth), scoring bruit vs incident réel, regroupement d’erreurs répétées, suggestions de corrélation `requestId`/IP/endpoint/compte, extraction métier depuis emails/offres/CV libres (nom entreprise, nom/prénom contact, poste, ville, plateforme, date/heure, téléphone/email, statut candidature), aide à l’autocomplétion backoffice et pré-tri des tâches agent email. Premier incrément recommandé : **règles déterministes + jeux de tests + format d’explication**, puis seulement ensuite TF-IDF/logistic regression/Naive Bayes/arbre léger ou ONNX quantifié si le gain est mesuré. Contraintes : entraînement local sur données anonymisées/redigées ; feature flag par usage ; seuils conservateurs ; explication des signaux ; mesure faux positifs/faux négatifs ; aucun secret ni donnée personnelle brute dans les artefacts ; fallback 100 % déterministe si modèle absent. Branche cible future : `feat/local-predictive-models`, après stabilisation B10/B8/B11 et avant l’audit final A-Z.
- [ ] **Audit final A-Z avant déploiement / tests globaux finaux** : à ne pas lancer maintenant. En fin de programme, reprendre de manière exhaustive chaque action utilisateur, bouton, formulaire, écriture BDD, endpoint API, service backend, worker/job, email, configuration `.env`, Docker/Compose/HTTPS, sécurité, logs, métriques, rapports, responsive, mobile et performances. Inclure la refonte du budget mémoire : détection/capacité serveur, marge réservée aux systèmes hors JobbingTrack, profils adaptatifs dev/préprod/prod et affichage UI qui distingue limite configurée, limite Docker effective et capacité hôte. Produire un bilan détaillé avec preuves, lenteurs, régressions, risques, optimisations possibles sans perte fonctionnelle et décision GO/NO-GO avant préprod/prod. La ligne de validation porteur associée est dans `TODOS_A_VALIDER.md` section P1D.
- [x] **CI GitHub #556 — Prettier frontend / tests frontend / backend smoke** : **21/05** correctifs locaux (`.prettierignore`, retrait override `glob`, lockfile, seuil coverage global, timezone Jest, smoke security-service 70s + replis CI, `npx` via `PATH`) puis push ; run **`26202796200`** **succès** sur `finish-feat/central-logging-full`.
- [ ] **Frontend validation scripts muets** : diagnostiquer pourquoi `npm run type-check`, `npm run lint` et désormais `npm run test:metrics-p1b` peuvent sortir parfois code **1** sans sortie exploitable alors que les commandes directes sous-jacentes (`./node_modules/.bin/tsc --noEmit --pretty false`, ESLint ciblé, `./node_modules/.bin/jest --runTestsByPath ...`) passent. Objectif : retrouver une sortie d’erreur lisible, vérifier les wrappers/scripts npm, variables d’environnement, redirections, hooks éventuels et comportement terminal/CI avant de considérer les validations frontend comme fiables.
- [ ] **Tests hors frontend — credentials codés en dur historiques** : nettoyer `tests/` comme cela a été fait côté `frontend/` : supprimer les fallbacks `admin@jobbingtrack.test`, `password123`, `SecureP@ss123!`, `admin123` lorsqu’ils servent à se connecter ou créer des comptes réels de test, et lire `TEST_ADMIN_EMAIL`, `TEST_ADMIN_PASSWORD`, `ADMIN_EMAIL`, `ADMIN_PASSWORD`, `TEST_VERIFICATION_PASSWORD`, `TEST_REAL_EMAIL(S)` avec échec explicite si absent. Première passe faite sur les specs mobile email Gmail/Proton/BlueMail et `tests/email/run-inscription-gmail-email-check.js`; reste à reprendre les anciens specs/admin/helpers/fixtures listés par scan `rg`.
- [x] **Menaces réseau Docker — faux positifs `BRUTE_FORCE` (`172.19.0.x`)** : **21/05** le repli `SECURITY_NETWORK_RELAX_INTERNAL` est couvert par `backend/security-service/tests/network-threat-detector.test.js` : en mode relax, une anomalie interne Docker `172.19.0.x` ne crée ni menace, ni alerte, ni log sécurité, tout en conservant la connexion réseau utile. Test ciblé security-service **3/3 OK**. Les lignes historiques déjà en BDD peuvent rester visibles jusqu’à purge UI/lab validée.
- [x] **Roadmap sécurité IA + PQC** : **21/05** phases 6–8 documentées dans **`ROADMAP_SECURITE_API_ET_BACKOFFICE.md`** : offensive IA contrôlée, défense UEBA/DAST avec garde-fous explicables, PQC/TLS hybride + crypto-agilité. Implémentation produit à planifier dans les lots B11–B13.
- [ ] **Logs sécurité — compression & rétention** : **21/05 cadrage** dans `docs/security/SECURITY_LOGS_RETENTION.md` : classes de rétention par gravité, archive JSONL gzip sans perte, manifest SHA-256, dry-run obligatoire, restauration temporaire et purge après validation. **21/05 dry-run** : `scripts/security/security-logs-retention-dry-run.cjs` → **42 MB**, **42 344** logs, candidats `standard=228`, `noise=9654`, `critical/high=0`. **21/05 export** : `scripts/security/security-logs-archive-export.cjs` (JSONL gzip + manifest, sans DELETE). **21/05 restauration contrôlée** : `scripts/security/security-logs-archive-restore.cjs` vérifie manifest/SHA-256 et charge seulement `security_logs_restore_staging` via `--load-staging` (jamais `security_logs`). **21/05 UI volume** : compteur `security_logs` via `/persistence/stats` sur Statistics sécurité + onglet Logs. **18/05 (partiel UI)** : pagination logs sécurité + plafond `SECURITY_LOGS_FETCH_LIMIT=2000` signalé sur vue d’ensemble. **Reste** : purge validée après comparaison staging/source.
- [x] **Sécurité backoffice — pagination URL + refresh (18/05)** : hook `useUrlPagination` ; **menaces** (`?page=`, 50/ligne), **firewall IPs bloquées** (`blockedPage`, 25/ligne), **logs sécurité** (`page`, 25/ligne) ; auto-refresh silencieux conserve la page courante. **Reste** : autres listes sécurité (intrusions, DDoS, whitelist…) + audit cohérence `total` API.
- [x] **Sécurité backoffice — libellés & navigation (`/b4ck0ff1ce/security/**`) — B10** : **21/05** titres navigateur FR ajoutés via `useDocumentTitle` sur les routes sécurité principales. **12/06 suite** : helper `securityLabels` pour centraliser sévérités, types de menaces, événements, origines blocage et actions firewall ; page `/b4ck0ff1ce/security/logs` restaurée avec pagination/filtres/highlight ; sous-nav `Logs sécurité` + Incidents actif sur détail alerte ; Menaces/fiche menace/Incidents/Overview/Analyse/Firewall/Politiques alignés pour limiter les codes API bruts. Validation technique : `tsc` direct OK, Jest libellés **6/6 OK**, ESLint ciblé **0 erreur** sur nouveau helper/page/spec ; warnings historiques seulement sur anciennes pages sécurité. **Validation porteur 12/06 OK** : `OK Sécurité - titres FR, libellés et navigation`. **Reste** : sous-titres EN résiduels au fil des pages si constatés.
- [x] **Corrélation performances — central logging runtime** : BDD **`aggregated_logs`** alimentée post-recreate/rebuild sur les **15 services** centralisés. Validation **20/05** : `node scripts/ops/smoke-central-logging-runtime.cjs` → **15/15 OK** ; `METRICS_API_KEY` présent dans les 15 conteneurs ; Compose healthy. **Porteur** : ouvrir ensuite **`/b4ck0ff1ce/performances/correlation`** et confirmer KPI logs ≠ 0 sur un service focal.
- [x] **Corrélation performances — 0 logs / signaux (suite technique)** : `ENABLE_CENTRAL_LOGGING` + `METRICS_SERVICE_URL` + `METRICS_API_KEY` + `SERVICE_NAME=jobbingtrack-*` propagés sur les microservices `full` équipés de `centralLogger` ; `centralLogger.js` envoie `X-API-Key`; garde-fou `scripts/ops/validate-central-logging-compose.cjs` OK (15 services).
- [ ] **Suite sécurité (non fait)** : pentest stack dédiée (`INTRUSION_RELAX_HEURISTICS=false`), scans P0 complets (`nmap`, ZAP actif, tri CVE), env strictes tous services, forensics preuves complètes, alertes mail **B11**, et couverture explicite des menaces réseau avancées listées dans `docs/security/SECURITY_TESTING_MATRIX.md` (HTTP forgé, DNS poisoning, UPnP abuse, session hijack, IP spoofing, ICMP redirect, BGP hijack, ARP spoofing, MAC flooding, VLAN hopping).
- [ ] **GitGuardian / ggshield — prévention secrets avant push + incidents temps réel** : **10/06 demande porteur** — intégrer `ggshield` comme brique officielle anti-secrets, en complément de Gitleaks historique. Prévoir guide d’installation développeur (`brew install ggshield` sur macOS, alternative Linux via paquet/pipx si validée), `ggshield auth login`, scan repo (`ggshield secret scan repo /path/to/repo`), hooks Git `pre-commit`, `pre-push` et option serveur `pre-receive` pour bloquer les secrets avant partage. Ajouter aussi scan des images Docker avec le CLI quand disponible, en ciblant les images JobbingTrack produites localement/CI. Côté produit sécurité : étudier GitGuardian API/webhooks pour importer les incidents (secret détecté, statut, criticité, repo/commit, owner, timestamps, remediation), les afficher dans le backoffice sécurité, déclencher des emails périodiques ou immédiats via le système d’alertes, dédupliquer/anti-spam, et ne jamais stocker le secret brut. Variables futures uniquement hors Git (`GITGUARDIAN_API_KEY`, webhook secret, endpoint allowlist). À traiter après validation email P1A : d’abord cadrage doc + PoC local hook, puis CI, puis API/incidents.
- [ ] **Alertes email critiques JobbingTrack — aliases publics et routage privé** : configurer les reports/alertes `critical/high` avec des alias publics du domaine JobbingTrack côté application (`SECURITY_ALERT_EMAIL`, `SECURITY_ALERT_FROM`, `SECURITY_ALERT_REPLY_TO`) et une redirection fournisseur vers la boîte privée réelle, jamais écrite dans Git. Catégoriser proprement les emails (`CVE`, `WAF/intrusion`, `service down`, `firewall`, `backup/restore`, `security report`) ; vérifier MailHog/local puis SMTP réel ; ne jamais afficher les secrets SMTP ni les boîtes privées dans les logs/rapports ; audit/réauth à prévoir pour modification des destinataires. **10/06** : local corrigé avec MailHog primaire + miroir SMTP réel async ; Email Monitor affiche destinataire, statut, date, contenu et miroir. **11/06** : validation porteur **OK Alertes email critiques**, réception réelle confirmée, non-spam ; le diagnostic spécifique admin a été retiré de `/b4ck0ff1ce/security/alerts`, qui garde seulement la liste claire des derniers emails `NOTIFICATION`. `From` provisoire `noreply@maily.ovh` accepté car plus fiable côté SMTP authentifié. **12/06** : documentation/env/gates alignés + contrôle automatisé `env-validate-runtime.cjs --production` : MailHog est local/dev/test uniquement ; en préprod/prod le transport principal doit être un SMTP réel, `SMTP_HOST` ne doit pas valoir `mailhog`/`localhost`, `NOTIFICATION_SMTP_HOST` ne doit pas forcer MailHog, et les smokes doivent prouver réception réelle + `EmailLog` `SENT`. Suite fin de programme : utiliser réellement le domaine possédé `jobbingtrack.com` pour afficher/envoyer depuis `security@jobbingtrack.com` si SPF/DKIM/DMARC et politique fournisseur le permettent, sinon conserver `From` authentifié + `Reply-To` sécurité. Suite demandée : enrichir les emails d’alerte avec contexte d’enquête utile (service/composant touché, sévérité, horodatage, requestId/correlationId si disponible, derniers logs ciblés redigés, statut disponibilité, lien vers Email Monitor / page sécurité), avec plafond de taille, compression ou pièce jointe si nécessaire, et redaction stricte des secrets, tokens, mots de passe, adresses privées non nécessaires et payloads sensibles.
- [ ] **Watchdog externe hors stack — alerte si JobbingTrack tombe totalement** : **10/06 demande porteur** — compléter les alertes internes par un mécanisme indépendant de la stack JobbingTrack, capable d’envoyer une alerte même si Docker, le frontend, l’API gateway, `security-service` et `notification-service` sont indisponibles. Option cible : petit script/service hors Compose ou timer `systemd` sur le VPS, ou supervision externe légère, qui vérifie les URLs publiques critiques, la réponse TLS, le login/health minimal, éventuellement l’état Docker si disponible, et envoie directement via SMTP/fournisseur indépendant. Contraintes : faible consommation CPU/RAM, pas de dépendance à la BDD JobbingTrack, secrets hors Git, anti-flap/dédup/backoff, alerte recovery, heartbeat “je suis vivant”, logs locaux compacts, test de coupure réelle contrôlée, et documentation claire de ce qui reste interne vs externe. Les emails du watchdog doivent aussi embarquer un contexte minimal : cible testée, code HTTP/erreur réseau/TLS, durée, nombre d’échecs consécutifs, derniers logs locaux du watchdog, état recovery, et consignes de diagnostic. À traiter après validation email P1A : d’abord cadrage architecture, puis PoC minimal.
- [x] **CVE applicatives — localisation réelle dans JobbingTrack** : P0 validé localement le 10/06. `CVE-2026-21710` est localisée comme CVE runtime Node via les Dockerfiles `node:20.18.0` (`node-runtime`, correctif `20.20.2+`, surface HTTP Node) ; `cve-locate` couvre rapports archivés, npm audit et runtimes Dockerfile connus. Test ajouté : `tests/security/cve-locate-runtime.test.js` **1/1 OK**. Suite non bloquante : enrichir les rapports sécurité avec liens vers fiches et poursuivre le tri finding par finding dans `docs/security/STATS.md`.
- [ ] **Tests offensifs contrôlés par conteneur/service** : vérifier chaque conteneur JobbingTrack exposé ou sensible contre tentatives de remote host / shell injection / command injection / URL injection / headers spoofing / requêtes HTTP forgées / path traversal / SSRF-like selon surface. **11/06 agent** : encart + tableau périmètre lecture seule sur `/b4ck0ff1ce/tests-security` ; préflight non destructif `scripts/security/controlled-offensive-preflight.cjs` ajouté et testé (**5/5 OK**) pour bloquer prod/cibles externes non approuvées avant tout scan actif ; manifeste plan-only `scripts/security/controlled-offensive-lab-scope.cjs` ajouté pour lister services/scénarios sans payload (`willRunPayload=false`). **Reste** : validation porteur cadrage (`TODOS_A_VALIDER.md` P1A), puis seulement ensuite runner lab explicite par scénario.
- [ ] **Leurres / réduction d’exposition VPS-Portainer** : **11/06** design `docs/security/VPS_EXPOSURE_REDUCTION.md` (réduction ports/réseaux d’abord, leurres en phase ultérieure isolée). Validation porteur design en attente.
- [ ] **VPS/Portainer — réduction d’exposition et leurres contrôlés** : concevoir une défense qui empêche un attaquant de récupérer de vraies informations serveur/conteneurs/réseau : minimiser bannières/versions/erreurs, réponses génériques publiques, pas d’exposition Docker/Portainer/metrics internes, segmentation réseau, honeypot/leurres seulement isolés et audités. Ne pas polluer les logs internes ni tromper l’admin ; documenter clairement ce qui est masqué côté public vs visible côté backoffice sécurisé.
- [ ] **Préprod VPS sans sync Portainer payante** : déploiement via **registry Docker** + stack Compose manuelle / webhook déploiement backoffice — voir **`docs/deployment/VPS_PORTAINER_NPM_OVH.md`**, **`DEPLOIEMENT_FINAL.md`** (pas de dépendance Portainer Business).
- [x] **WAF gateway — faux positif XSS sur `consolidated=true`** — validé local 19/05 (`TODOS_A_VALIDER.md`).
- [x] **Intrusion gateway — rafale backoffice HTTPS (dev)** — validé local 19/05 : `INTRUSION_RELAX_HEURISTICS=true` + bypass detection entiere en runtime relax ; **0** spam intrusion apres rafale API authentifiee. **Reste** : pentest avec `INTRUSION_RELAX_HEURISTICS=false` sur stack dediee.
- [ ] **HTTPS local bloqué — `ERR_CERT_AUTHORITY_INVALID` sur `/login`** : **étape 1** depuis la racine : `DEV_HTTPS_INSTALL_CA=1 bash scripts/ops/dev-https-certs.sh` ; si besoin : `sudo trust anchor --store .local/dev-certs/ca/jobbingtrack-dev-root-ca.pem` puis `sudo trust extract-compat` ; **fermer tout le navigateur** puis rouvrir **`https://jobbingtrack.localhost:5443/login`**. **Critère hôte** : `curl -fsS -o /dev/null -w '%{http_code}\n' https://jobbingtrack.localhost:5443/login` → **200** sans `--cacert` = **TLS + CA système déjà OK** : **ne pas reboucler** sur le script ; **Brave** (Chromium) : **`brave://settings/security`** → **Gérer les certificats** → **Autorités** → importer `.local/dev-certs/ca/jobbingtrack-dev-root-ca.pem` (filtre **Tous les fichiers** si besoin), puis **quitter tout Brave** ; idem Chrome avec `chrome://settings/security`. Flatpak/Snap = magasin isolé. Test **Firefox** ; recours **`mkcert`** (Option D dans **`docs/operations/DEV_HTTPS.md`**). **Ne pas** contourner par TLS/WAF off ni fallback HTTP silencieux.
- **11/06 — accès téléphone LAN dev** : `https://<IP_PC>:5443` est pris en charge en développement uniquement : certificat SAN IP, frontend API same-origin `/api/*`, proxy Nginx vers gateway et CORS LAN privé HTTP/HTTPS hors production. Validation agent sur `https://192.168.1.134:5443` OK ; validation visuelle téléphone encore dépendante d’ADB/appareil.
- [ ] **Variables d’environnement strictes (monorepo)** : supprimer progressivement les `${VAR:-défaut}` dans **tous** les `docker-compose*.yml`, les manifests CI et les `process.env.X || '…'` / `|| 3000` côté services ; réutiliser la sentinelle **`__JT_ENV_INCOMPLETE__`** et `config/jt-env-policy.cjs` ; faire échouer le démarrage avec le code **JT-CFG-001** en prod (message générique) et le nom de variable en dev. **15/05** : politique centralisée + doc `docs/configuration/STRICT_ENV.md` ; validation `.env` étendue ; boot **api-gateway** + bloc **api-gateway** dans `docker-compose.yml` alignés (`:?`, `PORT=3000`, `REDIS_URL` réseau Docker). **15/05 WAF dev** : jeton bypass tests uniquement si format **`jtbypass1-` + 32+ car.** (`config/dev-test-bypass-format.cjs`) ; helpers regroupés sous **`scripts/env/`** (`dev-test-bypass-fetch.cjs`, `dev-test-bypass-curl.inc.sh`, voir `scripts/env/README.md`). **15/05 auth-service** : boot **`src/bootstrap/strictAuthEnv.js`** + `PORT` résolu comme la gateway ; bloc **`auth-service`** Compose en **`${VAR:?…}`** ; **`REDIS_URL`** = `redis://${REDIS_HOST}:${REDIS_INTERNAL_PORT}`. **Suite** : autres microservices, `frontend` (bloc Compose `:-`), `docker-compose.monitoring.yml`, `production/*`, `backend/docker-compose.prod.yml`, scripts shell ; **évolution** : WAF edge dédié (ModSecurity / sidecar / managé) — cadrage **`docs/security/ACTIVATION_WAF.md`** (architecture + chantier WAF edge).
- [ ] **Validation porteur rapports sécurité backoffice** : vérifier en navigateur que `/b4ck0ff1ce/test-reports` liste la catégorie `Sécurité`, affiche les `summary.md`/`summary.json` et télécharge un fichier lisible depuis `reports/security/**` et `tests/results/security/**`. **21/05** : rapport frais généré localement via `python3 scripts/security/cve-scan.py --output-dir tests/results/security --timeout-sec 60` → `tests/results/security/cve-20260521-201336/summary.md`, listé par l’API sous `security-results-cve-20260521-201336`.
- [ ] **Rapports tests — volume disque et compression** : afficher dans `/b4ck0ff1ce/test-reports` la taille de chaque rapport (Ko/Mo) et le volume total de la liste filtrée. Ensuite cadrer une compression des anciens rapports (`.tar.gz`/gzip ou archive par dossier) avec manifest/hash, conservation des derniers rapports non compressés, ouverture/téléchargement compatibles, exclusion des données sensibles sans step-up, et procédure de restauration avant suppression. Ne pas activer de purge automatique tant que les validations P0 CVE ne sont pas closes.
- [x] **UX rapports sécurité CVE/PDF — première passe affichage** : **21/05** rendu backoffice des `summary.md` CVE amélioré dans `/api/test-reports/view` : tableau responsive, synthèse en cartes, surfaces critiques/hautes priorisées, détails repliables et Markdown brut conservé. **Reste éventuel** : export PDF dédié si besoin après validation porteur.
- [x] **Rapports sécurité — comparaison CVE** : P0 validé porteur le 10/06. Résolution IDs sécurité alignée, parsing `summary.md`, synthèse Critical/High/Medium/Low/Info, tableau par surface, écarts, garde-fou détails sensibles, filtres/tri exploitabilité et statut dérivé des compteurs (`critical/high` ⇒ à traiter/vulnerable). Suite non bloquante : continuer à enrichir les rapports avec liens vers fiches findings.
- [ ] **Rapports sécurité — détails bruts sous réauth forte** : ajouter un flux séparé “voir détails sensibles” pour notes brutes, payloads, preuves CVE et éventuels chemins exploitables. Accès uniquement après step-up auth : mot de passe récent ou réauth dédiée, jeton court non rejouable et lié à la session/device, rôle élevé, audit append-only, expiration rapide, `Cache-Control: no-store`, pas de téléchargement massif sans confirmation. La comparaison standard doit rester résumée et non sensible.
- [ ] **Tri P0 sécurité final** : récupérer ou régénérer les rapports datés absents (`reports/security/secrets-...`, `tests/results/security/cve-...`), puis classer les findings `critical/high` finding par finding selon le gabarit de `docs/security/STATS.md`.
- [ ] **Forensics menaces — preuves exploitables** : collecter et afficher des preuves techniques utiles par menace (`requestId`, endpoint, IP, payload/request redige si sensible, service touche, compte impacte si fiable, logs correles, decision WAF/firewall, export horodate). **20/05 ajout porteur** : si une IP source est externe au réseau privé/local, tenter un enrichissement technique contrôlé (reverse DNS, RDAP/whois, ASN/organisation réseau, pays/région approximatifs, réputation, indicateurs VPN/proxy/Tor/datacenter) avec cache/rate-limit/timeout, sans proxys gratuits non fiables et sans données personnelles/doxxing ; marquer la source et la confiance de chaque donnée. **21/05 constat porteur** : `198.51.100.42` est une IP de documentation `TEST-NET-2` (RFC 5737) et ne doit pas être présentée comme une vraie menace localisée en Roumanie/Bucarest ; traiter les plages RFC 5737 comme lab/documentation avant enrichissement public. Les menaces lab `10.0.0.x` critiques/non bloquées doivent afficher clairement leur statut test, le service réellement impacté quand disponible, et la télémétrie manquante.
- [x] **Menaces historiques/lab/IP privées — tri avant nettoyage** : validé porteur le 10/06 comme bruit/lab. Les plages `10.0.0.x`, `198.51.100.42`, `172.19.x` et `172.20.x` peuvent être traitées par une purge contrôlée, avec sauvegarde/procédure dédiée et sans suppression implicite.
- [ ] **Backoffice post-login — HTTPS local / CA dev** : vérifier après login que les pages admin utilisent les URLs HTTPS locales documentées (`https://jobbingtrack.localhost:5443` et `https://api.jobbingtrack.localhost:5443`) avec une CA locale installée et reconnue par le navigateur. Ne pas ajouter de fallback HTTP silencieux, ne pas désactiver TLS et ne pas bypasser les contrôles WAF/auth/CORS. **13/05 début** : suppression du downgrade HTTP dans `FRONTEND_URLS.api`, réalignement de `/backoffice` sur la configuration centralisée, et diagnostic ajouté dans `docs/operations/DEV_HTTPS.md`. **13/05 diagnostic `/api/v1/auth/profile`** : l'erreur console `Critical service unavailable: Network Error` masque un refus TLS navigateur (`ERR_CERT_AUTHORITY_INVALID`) avant toute réponse HTTP ; la CA locale a été générée/réinstallée dans NSS (`~/.pki/nssdb` + profils Firefox) via `DEV_HTTPS_INSTALL_CA=1 bash scripts/ops/dev-https-certs.sh`, puis `https://api.jobbingtrack.localhost:5443/health` a été validé en navigateur et par `curl --cacert`. **13/05 suite** : les pages navigateur `frontend/src/app/(admin)/**`, `frontend/src/app/(security)/**`, `frontend/src/app/(development)/**`, `frontend/src/app/(auth)/verify-email`, `frontend/src/app/(public)/reset-password`, `frontend/src/components/**`, `frontend/src/hooks/**` et `frontend/src/lib/**` n'utilisent plus directement `NEXT_PUBLIC_API_URL` / `NEXT_PUBLIC_API_GATEWAY_URL`; elles passent par `FRONTEND_URLS.api`. Les occurrences restantes sont limitées à `frontend/src/config/ports.config.ts` (source centralisée) et à des routes serveur `frontend/src/app/api/**` qui privilégient `API_GATEWAY_URL` / `API_URL` internes. Validations : `git diff --check`, `frontend` type-check complet, lints Cursor `frontend/src`, `npm run lint` OK. **13/05 validation porteur** : connexion réussie via `https://jobbingtrack.localhost:5443/login`. **14/05 correctif CA** : l'ancienne CA OpenSSL était acceptée par `curl --cacert` mais rejetée par des clients TLS stricts (`CA cert does not include key usage extension`) ; `scripts/ops/dev-https-certs.sh` génère maintenant une vraie CA locale (`CA:true`, `keyCertSign`, `cRLSign`), réinstalle l'entrée NSS et le proxy HTTPS a été redémarré. Validation stricte : login API HTTPS `200`, profil HTTPS `200`, rôle `SUPER_ADMIN`. Doc de connexion normale ajoutée dans `docs/operations/DEV_HTTPS.md`. **15/05 outillage** : `make up-full` enchaîne par défaut `dev-https-up` ; Compose inclut `docker-compose.https.yml` sur les étapes monitoring pour supprimer le WARN « orphan » sur `dev-https-proxy` ; `trust` + `sudo -n` documenté pour Chrome natif. **15/05 TLS navigateur** : Nginx sert un **`fullchain`** (feuille + CA) ; après `trust anchor` réussi, le script tente **`trust extract-compat`** ; option **`mkcert`** documentée (Option D) si le navigateur refuse encore la CA OpenSSL. **Critère hôte** : `curl -fsS -o /dev/null -w '%{http_code}\n' https://jobbingtrack.localhost:5443/login` → **200** confirme TLS + CA **système** OK (sans `--cacert`). **Reste porteur** : smoke navigateur post-login sur quelques écrans admin en HTTPS uniquement (ou import PEM si Chrome Flatpak/Snap) ; **reste prod** : certificats publics réels + HSTS sur domaine public seulement.
- [ ] **Backoffice navigation Performances — coût premier chargement** : investiguer et réduire le délai au clic depuis `/b4ck0ff1ce` vers `/b4ck0ff1ce/performances` observé en dev autour de **6,2 s** (`Fast Refresh` / compilation chunk). Ne pas retirer de graphes, de contrôles auth ou de protections HTTPS/WAF. **13/05 début** : `loading.tsx` squelette. **18/05** : imports ES modules corrigés sur `performances/page.tsx` ; Recharts extraits dans `performancesHeavyCharts.tsx` + `next/dynamic`. **Reste porteur** : mesurer en navigateur après hard refresh ; valider tous les graphiques.
- [x] **Performances réseau — ChunkLoadError graphique corrélation (18/05)** : `/backoffice/performances/network` plantait avec `Loading chunk …SystemCpuNetworkCorrelationChart… failed` et requête `/_next/undefined` (lazy chunk orphelin avec `distDir: .next-local`). **Correctif** : import statique du graphique sur la page réseau ; sur `/performances`, lazy load via le barrel `performancesHeavyCharts` (même chunk que les autres graphes lourds). **Reste** : hard refresh navigateur après déploiement ; si récidive, vider `.next-local` et redémarrer le conteneur `frontend`.
- [ ] **Monitoring Rust — benchmark long post-bascule** : refaire un contrôle CPU/RAM/IO p95 40-60 min dans le gate tests complets/préprod avec `monitoring-agent-rs` et `log-collector-rs`; conserver `monitoring-c` uniquement comme baseline historique.
- [ ] **Durcissement compose prod** : réduire les ports internes publiés sur `0.0.0.0`, documenter les exceptions et ajouter une validation de configuration.
- [x] **Validation porteur centralisée — nouveau flux** : `PILOTAGE.md` devient le point d’entrée ; `TODOS_A_VALIDER.md` bloque la suite côté porteur ; `TODOS_A_VERIFIER.md` suit les preuves agent ; `TODOS_DONE.md` archive ce qui est accepté ; les gates préprod/prod sont séparés dans `A_VALIDER_AVANT_PRODUCTION.md`, `DEPLOIEMENT_PRODUCTION.md` et `VALIDATION_PRODUCTION.md`.
- [ ] **Mobile** : décider la source officielle entre `mobile/` et `flutter-mobile-app/`, puis archiver/supprimer le doublon restant après vérification des scripts.
- [ ] **Conventions Git** : appliquer `BRANCHES.md` pour les prochaines branches et commits ; documenter ensuite les triggers GitHub exacts par workflow.

## Chantier Statistiques & backoffice (backlog — après priorités immédiates ci-dessus)

> **Constat porteur (18/05)** : onglet **Vue d’ensemble** de **`/backoffice/statistics`** — compteurs services incohérents (ex. **~22 hors ligne**, **1 seul sain**) alors que la stack tourne ; graphes **taux d’erreur dans le temps** et **disponibilité dans le temps** peu exploitables ; bloc **Statistiques & monitoring global** à reprendre. Même chantier pour **vue d’ensemble `/backoffice`**, onglets **Sécurité** et **Logs** sous Statistiques, et pages admin **non encore alignées** sur le socle actuel (HTTPS centralisé, `aggregated_logs`, log-collector Rust, pagination sécurité).

- [x] **Statistics / `/backoffice` — état des services (P0 UX)** : KPI **Sains / Dégradés / En cours / Arrêtés** alignés sur `centralMetricsService.getAllServices()` + `summarizeDockerServiceHealth`, avec validation porteur **20/05** : section **État du système** cohérente et onglet **Vue d’ensemble** avec les mêmes ordres de grandeur.
- [ ] **Tests métriques conteneurs** : **20/05 frontend OK** (`serviceHealthOverview.test.ts`) **8/8** ; **20/05 backend OK** (`serviceHealthModel.test.js`) **4/4** ; **20/05 smoke API OK** (`smoke-persistence-stats.cjs`, `smoke-statistics-history-api.cjs`, correctif `docker exec` sans `-T`). **11/06 suite P1B** : `PriorityResponseServicesSummary.test.tsx` + enrichissements `serviceHealthOverview.test.ts`, commande directe Jest **2 suites / 12 tests OK**, script `frontend:test:metrics-p1b` ajouté, bouton `/b4ck0ff1ce/tests` **Lancer suite P1B latence** + API `run-metrics-p1b` validés (rapport `20260610-231609`). **Reste** : smoke navigateur Statistics/backoffice après login porteur.
- [x] **Statistics — séries temporelles erreurs & disponibilité (P0)** : **21/05** graphes sur onglet Vue d’ensemble + onglet Sécurité (`StatisticsErrorAvailabilityCharts`), légende `MetricsSeriesCaption` (source `system_metrics`, dérivé 100−dispo), plage alignée Performances, barre sticky. Playwright `statistics-smoke` vérifie le titre « Disponibilité dans le temps ». **Reste** : validation porteur ; persister `error_rate` explicite si besoin produit.
- [ ] **Statistics — onglet Sécurité (`/backoffice/statistics/security`)** : revoir avec le **nouveau pipeline** — `aggregated_logs`, logs sécurité paginés, `security-service` summary ; éviter doublon trompeur avec **`/backoffice/security`** ; KPI et tendances doivent refléter la **persistance**, pas seulement le live. **20/05 suite** : fallback ajouté sur la table brute `security_metrics` quand `security_metrics_aggregated` est vide, avec source affichée côté UI. **21/05** : `useDocumentTitle`, lien `/b4ck0ff1ce/security`, bandeau volume `security_logs`, colonnes intrusion/DDoS dans agrégation journalière. **Reste** : séries WAF/menaces dédiées + validation navigateur porteur.
- [x] **Statistics — onglet Logs (`/backoffice/statistics/log-stats`) — persistance** : filtres service/niveau ajoutés et validés porteur **20/05** ; **20/05 suite** : `/api/v1/persistence/stats` compte les vraies tables (`aggregated_logs`, `log_collector_logs`, `container_logs`, `system_metrics`, `container_metrics`, snapshots, disponibilité, sécurité, événements, réseau services) ; UI avec période sélectionnable, labels FR, synthèse **sources actives / historiques / à brancher** et badges de statut. Clarification : `container_logs` = historique conteneurs de l’ancien collecteur, `log_collector_logs` = flux Docker Rust actuel.
- [x] **Statistics — onglet Logs (`/backoffice/statistics/log-stats`) — `system_events` + `service_network_history`** : alimentés le **20/05** côté `metrics-aggregator` : événements sur première observation / transition de statut service, et historique réseau services basé sur les probes santé (succès/échec, temps de réponse, endpoint health). Smoke : `node scripts/ops/smoke-persistence-stats.cjs` OK (`events=23`, `serviceNetwork=46` au premier cycle validé).
- [ ] **Statistics — onglet Logs (`/backoffice/statistics/log-stats`) — suite UI/tests** : ajouter tests API/UI automatisés sur `/persistence/stats`, vérifier en navigateur après login, puis contrôler la cohérence fine avec **`/backoffice/services/logs`** + granularité erreurs.
- [ ] **Statistics — onglet Données applicatives (`app-data`)** : **20/05 cadrage** : page centrée sur **totaux** + **timeline globale**. **20/05 suite** : détails ajoutés côté API/UI pour actifs vs total, nouveaux sur période, candidatures par statut, utilisateurs par rôle, entretiens/relances par statut, appels, événements et états vides par source. **20/05 correction** : valeurs `undefined`/vides regroupées en **Non renseigné** au lieu d’être affichées brutes. **Reste** : valider en navigateur, puis décider si la timeline doit être persistée historiquement (aujourd’hui snapshot courant enrichi).
- [ ] **Statistics / Performances — temps de réponse endpoints par service** : **10/06 retour porteur** — la vue des temps de réponse instantanés/moyens doit refléter correctement les endpoints et services réels, avec source claire `monitoring-agent-rs` / `metrics-aggregator` / agrégat persistant. **11/06 agent** : source unique `serviceHealthEndpoints.js`, mount dev metrics-aggregator, `PriorityResponseServicesSummary` sur Statistics + hub `/b4ck0ff1ce`, rebuild `monitoring-agent-rs`, smoke API OK. **12/06 agent** : `centralMetricsService` utilise maintenant `/api/metrics-aggregator` côté navigateur comme `analyticsService` (plus d’appel direct `localhost:5004` par défaut), mapping historique `services` corrigé, rapport `statistics-endpoint-latency/20260612-143538` **3/3 OK**, navigateur `/performances` + `/performances/latency` OK. **Reste** : p95/p99 si exposés par monitoring-agent, validation navigateur porteur (`TODOS_A_VALIDER.md` P1B).
- [x] **Vue d’ensemble `/backoffice` — état services** : KPI et libellés alignés avec Statistics et `/backoffice/services` ; validation porteur **20/05** sur la cohérence des ordres de grandeur.
- [ ] **Vue d’ensemble `/backoffice` — suite** : reprendre cartes **débit erreurs**, **incidents sécurité**, **performance** avec le même contrat métriques que Statistics + Performances.
- [ ] **Performances — Disque (`/backoffice/performances/disk`)** : aligner la page sur le standard Performances (retour, sous-nav, période partagée, cartes/surfaces, états vides) et exposer le détail Block I/O. **20/05 suite** : page reprise avec sélecteur de période commun, usage disque, volume utilisé/total, Block I/O cumulé lecture/écriture et débit estimé Mo/min agrégé depuis les historiques conteneurs. **Reste** : valider navigateur avec données réelles et étendre si besoin au détail par conteneur.
- [x] **Performances — sélection de période sans flash vide** : **21/05** les pages Synthèse, Réseau, Disque, Conteneurs et Latence attendent l’hydratation de la plage partagée, annulent les fetchs obsolètes et conservent les graphes existants visibles pendant le chargement de la nouvelle plage. Validation navigateur locale avec login `.env` : `/b4ck0ff1ce/performances` conserve **22 graphes** sur 24 h → 7 j → 3 j → Aujourd’hui → plage personnalisée → période actuelle, sans écran “Chargement…” intermédiaire ni erreur console.
- [ ] **Performances — Disque / stockage BDD approfondi** : ajouter une vue stockage PostgreSQL dans `/backoffice/performances/disk` : taille totale base/index/TOAST, détail par table (`pg_total_relation_size`, table, index, toast, lignes estimées), regroupement métier par familles (**logs** : `aggregated_logs`, `log_collector_logs`, `container_logs` ; **metrics** : `system_metrics`, `container_metrics_snapshots`, disponibilité, sécurité ; **métier** : candidatures, contacts, entreprises, événements, rapports tests). Prévoir tendances de croissance, top tables, projection saturation disque, liens vers rétention par famille, et stratégie de compression/archivage (partitionnement temps, purge contrôlée, export compressé, compression anciens logs/rapports, éventuellement hypertables/Timescale ou partitions PostgreSQL si pertinent). Chantier à traiter séparément et en profondeur avant activation d’actions destructives.
- [ ] **Stockage emails/logs/métriques/rapports — compression sans perte** : définir une stratégie globale pour limiter l’espace disque sans perdre les données : conserver les enregistrements actifs en base, compresser/archiver les anciens contenus lourds (corps emails `EmailLog.emailContent`, logs agrégés, métriques historiques, rapports tests, captures/traces Playwright, rapports parcours utilisateur, rapports sécurité), garder index/metadata requêtables, restaurer à la demande, et documenter les délais de rétention par famille. Priorité : mesurer par table/dossier avant toute purge ; actions destructives interdites sans validation porteur. **10/06 nettoyage local validé par le porteur** : `EmailLog` mesuré avant purge à `8971` lignes / `19 MB`; suppression contrôlée des anciens `FAILED`/`PENDING` avant aujourd’hui (`8722` lignes), conservation des `SENT` et notifications récentes, `VACUUM FULL ANALYZE`, résultat `249` lignes / `720 kB`. **Reste** : remplacer les purges ponctuelles par export compressé horodaté + manifest/hash + politique de rétention configurable.
- [ ] **Nettoyage données métier de test — dry-run puis purge validée** : inventorier les données générées par tests/seeds dans les tables métier (**users**, companies, applications, contacts, interviews, calls, followups, events, notifications/emails) via critères sûrs (`TEST_PREFIX`, emails `@example.com`, noms fixtures, dates de campagnes tests, ownership test). Étendre le helper `cleanupTestData` qui ne couvre aujourd’hui que contacts/companies, produire un rapport avant/après, préserver les comptes/admins/données seed nécessaires aux tests, puis autoriser la suppression uniquement avec validation explicite et sauvegarde préalable. **10/06** : nettoyage utilisateurs E2E demandé par le porteur testé en navigateur sur `/b4ck0ff1ce/users` ; bouton **Nettoyer les utilisateurs de test** a supprimé `2441` comptes `isTestData`/`@jobbingtrack.test`, compteur BDD `0` après. Reste à traiter les autres tables métier avant une purge globale.
- [ ] **Alertes email sécurité — validation notification-service** : vérifier le chemin `security-service` → `notification-service` → SMTP/MailHog pour les alertes `critical/high`. **20/05 suite** : notification-service reçoit maintenant `SMTP_USER`/`SMTP_PASS`/`SMTP_SECURE` via Compose et n’envoie plus `AUTH PLAIN` vide si les credentials sont absents. **État UI** : `SecurityAlertEmailSettings` existe dans Paramètres → Notifications pour gérer activation, niveaux et destinataires. **10/06** : page dédiée `/b4ck0ff1ce/security/alerts`, MailHog primaire, miroir SMTP réel async, diagnostic `EmailLog` via Email Monitor filtre Notification, réauth UI et test d’envoi validés techniquement. Correctif délivrabilité admin : miroir explicitement activé en local, `From` réel du miroir sur l’utilisateur SMTP, `Reply-To` alias sécurité, test Jest SMTP `4/4` OK. **Reste** : confirmation porteur de réception réelle admin/dev et audit renforcé complet si exigé avant prod.
- [x] **Performances / Statistics / Analytics — barre période sticky** : **21/05** composant `StickyTimeRangeToolbar` branché sur `/b4ck0ff1ce/performances` et `/b4ck0ff1ce/statistics`. **Reste** : étendre aux sous-pages Performances et Analytics si besoin porteur.
- [ ] **Graphes multi-séries — couleurs uniques, stables et tooltips cohérents** : toute vue qui affiche plusieurs services/conteneurs dans un même graphe doit utiliser une couleur unique par série, stable entre CPU/mémoire et indépendante de l’ordre reçu. **20/05 suite** : socle `seriesColors` ajouté et branché sur `/backoffice/performances/containers` en mode “Tous les conteneurs”. **12/06 suite** : `rechartsTooltipProps` ne force plus une couleur globale sur les items, afin que les libellés/valeurs des tooltips reprennent la couleur réelle des lignes/barres. **Reste** : étendre les palettes stables aux graphes Statistics/Analytics/Services qui utilisent encore des palettes locales ou `index % palette.length`.
- [ ] **Identité visuelle JobbingTrack — logo officiel** : le logo porteur est enregistré dans `frontend/public/brand/jobbingtrack-logo.png` et décliné en icônes PWA `frontend/public/icons/icon-*.png`; backoffice, login/register, page racine et émulateur mobile utilisent le composant `JobbingTrackLogo`. **Reste** : vérifier rendu navigateur + mobile réel après hard refresh/cache icônes.
- [ ] **Préférences refresh par graphique / emplacement** : les réglages actuels sont par grande zone (`logs`, `analytics`, `metrics`, `dashboard`, `services`). Ajouter une granularité par graphique/emplacement : ex. **Performances → Synthèse → temps de réponse**, CPU/RAM/réseau/conteneurs, Statistics sécurité/logs/app-data, Analytics application/utilisateurs. Prévoir héritage depuis la zone globale, override local, libellé source du refresh et validation que chaque graphe consomme bien son intervalle.
- [ ] **Analytics application & utilisateurs — reprise produit** : séparer clairement **activité/traces mobile/app** (retours, signalements, événements utilisateur, parcours) des **analytics utilisateurs admin** (comptes, activité, cohortes, rôles, rétention, états vides). Ne pas mélanger avec les performances live infra ; reprendre après Statistics/app-data.
- [ ] **Discipline tests & rapports au fil du dev** : à chaque lot, continuer uniquement les tests ciblés liés au changement (type-check, Jest ciblé, scripts smoke API/UI selon surface) et noter les commandes dans `STATUS.md`. Les suites complètes, rapports complets, rapports sécurité globaux et campagnes parcours utilisateur sont lancés par le porteur uniquement ; après génération, analyser `tests/results/<timestamp>/summary.json`, `report.html` et rapports parcours/sécurité fournis, puis reporter les vrais échecs dans `TODOS.md` / `TODOS_A_VALIDER.md`.
- [ ] **Audit backoffice — pages à réaligner sur le socle 2026** (HTTPS `FRONTEND_URLS.api`, persistance, monitoring Rust, UX commune) — traiter **par lot** après Statistics :
  - [ ] **`/backoffice/services`** + **`/backoffice/services/[serviceName]`** (liste vs détail — référence socle graphes **A1**).
  - [ ] **`/backoffice/analytics/**`** (hub + application ; redirects conteneurs/réseau déjà faits).
  - [ ] **`/backoffice/user-stats`**, **`/backoffice/user-analytics`** (sources et libellés).
  - [ ] **`/backoffice/security`** (vue d’ensemble — hors sous-pages déjà paginées **18/05**).
  - [ ] **`/backoffice/data-management`**, **`/backoffice/test-reports`**, **`/backoffice/tests-*`** (smoke après changements infra).
  - [ ] Inventaire complet : **73** routes sous `frontend/src/app/(admin)/backoffice/**/page.tsx` — cocher au fil des reprises dans le tableau **§ Lot A — inventaire Recharts** ci-dessous.
- [ ] **UX unifiée Statistics / Performances / Vue d’ensemble** : **20/05 partiel** : `app-data`, `security` et `log-stats` utilisent un shell Statistics commun (largeur, header, action refresh, surfaces). **20/05 mode clair** : garde-fous globaux ajoutés sur les surfaces/cartes/champs backoffice pour améliorer bordures, ombres et textes secondaires. **Reste** : rapprocher encore la vue d’ensemble Statistics du standard Performances (barre période partagée, empty states, légende **live vs persisté**) et valider la cohérence sur navigateur.

### UX Sécurité — inventaire routes `/b4ck0ff1ce/security` (libellés à reprendre)

| Route | Fichier | Problème constaté / action |
|-------|---------|----------------------------|
| `/b4ck0ff1ce/security` | `security/page.tsx` | Cartes KPI — vérifier sous-titres et intitulés des liens |
| `/b4ck0ff1ce/security/logs` | `security/logs/page.tsx` | Titre **EN** (« Security Logs ») → **FR** + **`SecuritySubNav`** |
| `/b4ck0ff1ce/security/policies` | `security/policies/page.tsx` | **`SecuritySubNav`** ; règles WAF/firewall si `name` API vide |
| `/b4ck0ff1ce/security/analysis` | `security/analysis/page.tsx` | **`SecuritySubNav`** + titres cartes cohérents |
| `/b4ck0ff1ce/security/firewall` | `security/firewall/page.tsx` | Tableaux règles / IP — libellés + noms règles |
| `/b4ck0ff1ce/security/network` | `security/network/page.tsx` | Corrélation conteneurs / IP sans libellé lisible |
| `/b4ck0ff1ce/security/threats` | `security/threats/page.tsx` | Colonnes type/IP — fallbacks si champs vides |
| `/b4ck0ff1ce/security/threats/[id]` | `security/threats/[id]/page.tsx` | Titre fiche (type + IP ou id court) |

## À valider manuellement par le porteur

- [ ] **Backoffice avec le mot de passe `.env`** : ouvrir **`https://jobbingtrack.localhost:5443/login`**, utiliser uniquement `ADMIN_EMAIL` et `ADMIN_PASSWORD` du `.env` (copier-coller), puis confirmer l'accès au backoffice. Si **401** : `bash backend/scripts/database/create-admin-user.sh` (voir **`docs/troubleshooting/TROUBLESHOOTING_LOGIN.md`**). Ne jamais copier le mot de passe dans un ticket, un commit, un log ou une capture.
- [ ] **Workflow GitHub CI/CD** : observer le prochain run après push pour confirmer côté GitHub que les jobs Node 24 et validation DB passent réellement.
- [ ] **Acceptation produit** : reporter les validations manuelles confirmées dans `PLAN.md` colonne `Validé (porteur)` ou dans `STATUS.md` avec date.

## Réalisé récemment — à relire si besoin

- [x] **Login HTTPS + URL API centralisée (18/05)** : `ports.config.ts` / `api.ts` pointent vers `https://api.jobbingtrack.localhost:5443` en dev HTTPS ; plus de `ERR_SSL_PROTOCOL_ERROR` sur `:5002`. Admin resynchronisé via Prisma (`create-admin-user.sh` corrigé). Validation technique : login API **200** + token.
- [x] **Auth-service — env stricte (boot + Compose)** : `src/bootstrap/strictAuthEnv.js` + `server.js` (PORT comme gateway) ; bloc **`auth-service`** en **`${VAR:?…}`** ; **`REDIS_URL`** aligné sur **`REDIS_INTERNAL_PORT`**. `env-generate-secrets.cjs --with-postgres` réécrit **`DATABASE_URL`** pour Prisma/scripts (`POSTGRES_CLIENT_HOST` / `localhost` + `POSTGRES_PORT`) ; Compose injecte toujours `@postgres:5432` dans les conteneurs. Doc **`scripts/env/README.md`**.
- [x] **`.env.example` / alertes critiques** : ports collecteurs Rust vs legacy C sans collision hôte ; `EMULATOR_CONTROLLER_URL` documenté (hôte vs conteneur) ; `SECURITY_CRITICAL_SERVICES` inclut **`jobbingtrack-monitoring-agent-rs`** et **`jobbingtrack-log-collector-rs`** ; défaut **`securityScheduler`** aligné.
- [x] **`AUDIT_SEC_PROJECT.md`** rangé sous **`docs/security/`** ; liens **`docs/README.md`**, **`TODOS_A_VALIDER.md`**, index **`docs/security/README.md`**.
- [x] **Bypass tests WAF — organisation scripts** : logique hors `scripts/dev/` ; tout regroupé sous **`scripts/env/`** (`dev-test-bypass-fetch.cjs`, `dev-test-bypass-curl.inc.sh`) avec doc **`scripts/env/README.md`** ; dossier `scripts/dev/` supprimé après déplacement. Le format reste **`config/dev-test-bypass-format.cjs`**.
- [x] **BDD runtime réalignée** : schéma Postgres synchronisé, table `log_collector_logs` présente, colonnes critiques disponibles (`Application.isTestData`, `Application.isArchived`, `Application.thankYouEmailSentAt`, `Company.isTestData`) et logs Postgres sans erreurs `isTestData` / `log_collector_logs` / `does not exist` après contrôle.
- [x] **Validation DB locale renforcée** : suite `test-database.js` + `test-postgresql-config.js` + `test-migrations.js` alignée sur le flux actuel, avec contrôles Prisma, contraintes, index et test archive/corbeille candidature **21/21**.
- [x] **CI/CD Node 24 + validation DB** : workflows GitHub alignés sur actions Node 24, `DATABASE_URL` de test disponible au niveau job/workflow, validation Prisma adaptée au flux `db push` et format Prisma non bloquant pour un simple écart de format.
- [x] **CI `npm ci` backend réparé** : `backend/package-lock.json` resynchronisé avec `backend/package.json` pour inclure les dépendances ESLint manquantes ; validation locale `/usr/bin/npm ci --dry-run` OK.
- [x] **Suite complète stabilisée sans exécution directe de Make par l'agent** : orchestration via script de tests avec gateway locale, Playwright smoke et `PERF_LIGHT=1` ; résultat **436/436 réussis**, **1 ignoré**.
- [x] **Scripts metrics modernisés** : `scripts/testing/verify-all-metrics.sh` lit les ports depuis `.env`, normalise les hostnames Docker vers les ports hôte quand lancé localement, lit `METRICS_API_KEY` sans exposer le secret et reste validé sur stack active.
- [x] **Rapports sécurité branchés au backoffice** : les routes `frontend/src/app/api/test-reports/*` scannent maintenant `reports/security/**/summary.md|summary.json|report.html` et `tests/results/security/**/summary.md|summary.json|report.html`; la page `/b4ck0ff1ce/test-reports` accepte le type `security` pour consulter, télécharger et supprimer ces rapports. Reste validation porteur navigateur.
- [x] **Tri P0 sécurité initial cadré** : `docs/security/STATS.md` distingue maintenant le tri initial possible sans rapports bruts du tri final finding par finding. État réel : les rapports datés du 11/05 ne sont pas présents localement dans le workspace, donc il faut les récupérer depuis les artefacts CI ou les régénérer avant conclusion exploitable.
- [x] **Connexion backoffice sécurisée côté technique** : `/login` n'affiche plus d'identifiants de test ni `password123`, l'année du footer est dynamique, l'admin est synchronisé depuis `.env` sans afficher le mot de passe, et le login gateway répond **200** avec rôle `SUPER_ADMIN`.
- [x] **Connexion backoffice — diagnostic porteur 13/05** : après resynchronisation admin, le login finit par réussir et redirige vers `/b4ck0ff1ce`. Les premiers `401` vus avant succès correspondent à des tentatives avec identifiants non encore alignés ou saisie précédente ; le problème restant observé est post-login (`ERR_CERT_AUTHORITY_INVALID` sur des appels HTTPS locaux), traité séparément dans la priorité "URLs API locales".
- [x] **Connexion backoffice HTTPS validée porteur** : le parcours normal de développement local passe par `https://jobbingtrack.localhost:5443/login` après installation de la CA locale ; validation porteur confirmée le 13/05. La procédure est documentée dans `docs/operations/DEV_HTTPS.md`.
- [x] **Forensics menaces — vocabulaire clarifié** : `ASN` / `VPN` / `proxy` / `Tor` est documente comme enrichissement reseau optionnel, pas comme preuve d'attaque. La suite porte sur les preuves techniques exploitables (`requestId`, endpoint, payload redige, logs, compte impacte fiable, action WAF/firewall).
- [x] **Monitoring Rust restructuré** : les crates `monitoring-agent`, `log-collector` et `metrics-aggregator` ont des `main.rs` minimalistes et des modules dédiés ; `cargo fmt --all` et `cargo check --workspace` OK.

## Priorité immédiate 11 mai 2026 — sécurité exploitable, rapports et suite forensics

- [x] **Documentation B15** : créer et raccorder la matrice tests sécurité offensifs contrôlés (`docs/security/SECURITY_TESTING_MATRIX.md`) : URL enumeration, injections, XSS, auth/JWT/IDOR, CORS, Docker/réseau, secrets, TLS, spoofing, mobile/reverse engineering.
- [x] **Outillage sécurité passif initial** : ajouter `.github/workflows/security-audit.yml`, `makefiles/security/Makefile`, scripts `secrets-scan.sh`, `ports-scan.sh`, `jwt-audit.sh`, `zap-active-scan.sh`, et `reports/security/README.md`.
- [x] **Performance sécurité** : documenter explicitement que la détection runtime ne doit pas inspecter le trafic inter-conteneurs ; audits lourds en CI/commande, corrélation asynchrone dans `security-service`.
- [x] **Validation outillage initial** : `bash -n` scripts, `make -n security-*`, `git diff --check`, lints Cursor et `frontend npm run type-check` OK.
- [x] **Blocage scan prod identifié** : `make security-scan-ports` produit un rapport non bloquant et note que `docker-compose.prod.yml` seul est incomplet (`auth-service` sans `image`/`build`).
- [x] **Corriger la cible prod scannable** : scripts sécurité alignés sur le merge `docker-compose.yml + docker-compose.prod.yml` avec profile `full` par défaut (`SECURITY_COMPOSE_FILES`, `SECURITY_COMPOSE_PROFILES`) ; correction `docker-compose.prod.yml` (`jobbingtrack_postgres_data`, `replicas: 1` tant que `container_name` existe) ; `ports-scan` génère un rapport lisible des ports publiés.
- [ ] **Exécuter les scans P0 en environnement autorisé** : **11/05 partiel** `gitleaks` historique complet exécuté (717 findings bruts), `trivy`/CVE images compose prod fusionné exécuté ; reste `nmap` sur cible préprod/prod autorisée, `jwt_tool` avec token lab, ZAP active scan local via gateway (`SECURITY_ACTIVE_SCAN=1` uniquement), `truffleHog` si installé.
- [ ] **Traiter les rapports sécurité** : classer findings `critical/high`, ouvrir tâches ciblées, justifier faux positifs avec date/environnement/commit, et reporter les résultats synthétiques dans `STATUS.md` / `security/STATS.md`. **13/05** : tri initial documenté dans `docs/security/STATS.md`, mais tri final bloqué tant que les rapports datés bruts ne sont pas récupérés/régénérés. **Findings bruts 11/05** : scan ports compose prod fusionné OK mais trop de ports internes sont publiés sur `0.0.0.0`; `gitleaks` remonte surtout JWT/headers dans artefacts de tests ; Trivy/CVE remonte des vulnérabilités critiques/hautes sur dépendances Node et images Docker, notamment `deployment-service`, `mailhog`, `frontend`, `api-gateway`, `postgres`, `redis`.
- [x] **Suite sécurité runtime sans surcoût — WAF lab** : audit ciblé `api-gateway` après HTTPS dev. Correctif appliqué : un trafic navigateur arrivant via reverse proxy Docker (`X-Forwarded-*`) n’est plus traité comme trafic interne à bypass WAF sauf secret machine explicite. Ajout `make security-waf-lab` (`scripts/security/waf-lab-check.sh`) pour vérifier un payload XSS borné sur `/health` via HTTPS dev, sans blocage IP admin persistant.
- [ ] **Suite forensics menaces** : brancher les preuves techniques exploitables dans les fiches menaces et exports (request/payload rediges si sensible, `requestId`, endpoint, compte impacte fiable, logs correles, action WAF/firewall), puis ajouter seulement ensuite un enrichissement reseau optionnel (ASN/VPN/proxy/Tor) libelle comme contexte.
- [ ] **Suite alertes mail** : UI admin déjà amorcée dans les paramètres backoffice ; compléter par réauth/audit pour adresse d’alerte, anti-flap/dédup, digest `high`, et validation des alertes `service down` via metrics-aggregator/monitoring Rust. **10/06** : faux positif identifié — `metrics-aggregator` était healthy, mais le check `security-service` recevait `401` faute de transmettre `X-API-Key`; correctif à valider pour éviter les emails “Metrics aggregator indisponible” quand il répond bien.
- [ ] **Préprod / release / conformité** : suivre le nouveau **lot H** (`PLAN.md`) et **`docs/operations/RELEASE_PREPROD_PRODUCTION_PLAN.md`** : branche tests complets, branche préprod, serveur préprod, beta mobile, prod, licences, RGPD, retours utilisateurs, déploiements et stratégie mono-repo vs multi-repo.
- [ ] **Fluidité backoffice admin** : auditer toutes les pages `/backoffice/**` (navigation, fetchs, charts, polling, bundles, loading states), garder les clics perçus instantanés, ajouter des loaders/skeletons par route, précharger les routes fréquentes, lazy-loader les composants lourds, réduire animations/polling coûteux, et valider au minimum par `npm run type-check` + parcours manuel des pages clés.
- [x] **Connexion backoffice — sécurisation technique admin .env** : admin runtime réaligné sur `.env` (`ADMIN_EMAIL` / `ADMIN_PASSWORD`), hash admin mis à jour même si le compte existe déjà, utilisateur de test seedé sans afficher de secret, proxy gateway corrigé pour ne plus transmettre `Content-Length` invalide sur login, et page `/login` nettoyée (plus de compte de test visible, plus de `password123`, footer année courante). Validation technique faite : stack active, `ADMIN_PASSWORD` `.env` non faible, login gateway `POST /api/v1/auth/login` = `200`, token reçu, rôle `SUPER_ADMIN`.
- [ ] **Connexion backoffice — validation navigateur porteur** : même critère que la ligne « Backoffice avec le mot de passe `.env` » (HTTPS **5443**). Doc : **`docs/troubleshooting/TROUBLESHOOTING_LOGIN.md`** + **`docs/operations/DEV_HTTPS.md`**.
- [x] **HTTPS développement réaliste** : proxy Nginx dev `dev-https-proxy` + certificats locaux hors Git, CA installée dans les magasins navigateur NSS disponibles, URLs publiques front/gateway en HTTPS (`jobbingtrack.localhost`, `api.jobbingtrack.localhost`), validation curl avec CA locale. Doc : `docs/operations/DEV_HTTPS.md`. **Reste** : en prod/préprod, utiliser certificats publics réels + HSTS strict sur domaine public seulement.
- [x] **CI sécurité GitHub — correction Trivy/Node 24** : `security-audit.yml` ne référence plus `aquasecurity/trivy-action@0.28.0` introuvable ; passage à `aquasecurity/trivy-action@v0.36.0`, actions GitHub officielles Node 24 (`checkout` v6, `setup-node` v6, `upload-artifact` v7), `gitleaks-action@v2.3.9` remplacé par installation du binaire Gitleaks + `scripts/security/secrets-scan.sh`, et upload SARIF Trivy migré `github/codeql-action/upload-sarif@v4`. **21/05 correction run `26232354842`** : la découverte dynamique de la dernière release Gitleaks appelait l’API GitHub non authentifiée et a pris `403 rate limit exceeded` ; version figée `GITLEAKS_VERSION=v8.30.1` + URL d’asset directe vérifiée. **21/05 correction run `26234201957`** : Gitleaks s’exécute et remonte **718 findings bruts** historiques ; CI passée en rapport non bloquant via `GITLEAKS_FINDINGS_EXIT_CODE=0`, artefact conservé. **Validation GitHub** : Security Audit `26234510539` **succès** sur `dev`.
- [x] **Docs production — Security Audit / Trivy images prod** : procédure ajoutée dans `docs/ci-cd/README.md`, `docs/operations/PREPROD_PRODUCTION_CHECKLIST.md`, `docs/operations/RELEASE_PREPROD_PRODUCTION_PLAN.md` et `docs/deployment/DEPLOIEMENT_FINAL.md` : lancer **Security Audit** manuellement avec `scan_prod_images=true`, récupérer `trivy-prod-image-reports`, trier les `HIGH`/`CRITICAL` dans `docs/security/STATS.md` avant préprod/prod.
- [x] **Audit doc sécurité/déploiement — cartographie et sources de vérité** : **21/05** `docs/security/README.md` et `docs/project/CHANTIER_SECURITE_DATA_DOCS.md` clarifient les rôles de `FIREWALL_PLAN`, `ACTIVATION_WAF`, `DEPENDABOT_ALERTS_INTEGRATION`, `CVE_CONTINUOUS_MONITORING`, `AUDIT_SEC_PROJECT`, `SECURITY_LOGS_RETENTION`, `SECURITY_TESTING_MATRIX`, `STATS.md` et les docs prod. Les exemples CVE/WAF/matrice ont été réalignés sur les workflows actuels, URLs HTTPS locales et la règle “cibles Make documentées, pas commandes directes”.
- [x] **Dependabot npm — première passe réelle** : lockfiles existants traités pour `frontend`, `backend/api-gateway`, `backend/application-service`, `backend/auth-service`, `backend/deployment-service`, `backend/workflow-service`, `tests`; mises à jour directes/transitives principales (`next` 16, `axios` 1.16, `nodemailer` 8, `express` 4.22, `jsonwebtoken` 9.0.3, `postcss` 8.5, `jspdf` 4) et audits runtime `npm audit --omit=dev` verts sur `frontend`, `api-gateway`, `application-service`, `auth-service`, `deployment-service`, `workflow-service`. Validation : `frontend` type-check/lint/build OK après corrections Next 16 (`Suspense`, `params: Promise`, config), `api-gateway` Jest **27/27**, `application-service` Jest **3/3**. **12/05 suite** : `auth-service` Jest corrigé et vert **18/18** avec DB Docker locale; `security-service` retire `geoip-lite` vulnérable au profit d'un provider neutre et repasse à audit runtime **0 vulnérabilité** + Jest **18/18**.
- [x] **Dependabot alerts → BDD sécurité** : import GitHub Dependabot implémenté dans `security-service` (`analyzeDependabotAlerts`, endpoint `POST /api/v1/vulnerabilities/dependabot/import`, job cron optionnel `DEPENDABOT_ALERTS_ENABLED=false` par défaut). Mapping vers `Vulnerability` + `SecurityAlert` pour nouvelles `critical/high` ouvertes, token serveur uniquement (`DEPENDABOT_ALERTS_TOKEN` ou `GITHUB_TOKEN`), variables ajoutées à `.env.example` / `.env.production.example`. Validation ciblée : Jest `security-cve-alerts` **5/5**.
- [x] **Scripts — restructuration sûre par lots** : `make scripts-inventory` reste utilisable et signale **0 script racine** après déplacements sûrs vers `scripts/env/`, `scripts/setup/`, `scripts/db/`, `scripts/docker/`, `scripts/reports/`, `scripts/performance/`, `scripts/ops/`, `scripts/health/`, `scripts/testing/` et `scripts/utils/`. Make dry-run validés : `setup-ports`, `test-mobile-report`, `status-watch`, `logs-watch`, `menu`, `fix-all`, `git-checkout`, `test-performance-backend`; syntaxe `bash -n` OK sur les scripts déplacés. **13/05 suite** : audit des **21 scripts sans référence automatique** documenté dans `docs/scripts/NON_REFERENCED_SCRIPTS_AUDIT.md`; `make scripts-inventory` signale maintenant **0 script non référencé**. **13/05 suite 2** : `scripts/testing/test-reset-password.sh` modernisé pour `API_GATEWAY_URL`/`API_GATEWAY_PORT`, `METRICS_URL` et `METRICS_API_KEY` optionnelle ; **13/05 suite 3** : anciens scripts racine test/Playwright/rapports rangés sous `scripts/testing/`, `scripts/monitoring/`, `scripts/performance/`, `scripts/reports/` et `scripts/setup/`, avec chemins Make/routes/docs migrés ; validation réelle stack active OK après correction auth-service (`passwordResetToken` absent du Prisma Client → fallback `User.resetToken/resetTokenExpiry`). **13/05 suite 3** : `scripts/testing/verify-all-metrics.sh` modernisé pour `METRICS_URL`/`METRICS_AGGREGATOR_PORT`, `API_GATEWAY_URL`/`API_GATEWAY_PORT`, auth metrics optionnelle et contrats JSON actuels ; validation réelle stack active OK (**52/52**, sortie `0`). **Reste à traiter séparément** : archiver/supprimer progressivement les scripts classés legacy, sans supprimer les outils de dépannage ponctuels.
- [ ] **Audit documentation complet** : suivre **`docs/operations/DOCUMENTATION_AUDIT_PLAN.md`** pour vérifier/restructurer les dossiers `docs/core/architecture`, `database`, `deployment`, `emails`, `features`, `frontend`, `getting-started`, `mobile`, `monitoring`, `operations`, `performance`, `project`, `scripts`, `security`, `tests`, `todo`, `troubleshooting`, `user-journey`, fichiers racine `docs/*` et `monitoring/MIGRATION_RUST.md` avant fusions/déplacements.

## Validation sécurité/backoffice 12 mai 2026 — état des tests applicatifs

- [x] **Frontend sécurité ciblé** : `npm run type-check`, lints Cursor, `git diff --check` OK après correctifs `/backoffice/security/**`.
- [x] **Tests backend forensics ciblés** : `backend/security-service` → `npm test -- --runTestsByPath tests/firewall-threat-forensics.test.js --runInBand` OK (**6/6**).
- [x] **Tests applicatifs Playwright sécurité** : `PLAYWRIGHT_BASE_URL=http://localhost:5003 npx playwright test tests/e2e/security-e2e.spec.ts --project=chromium --reporter=line` OK (**16/16**) avec frontend `5003` et gateway `5002/health` joignables.
- [x] **Lint frontend global** : `npm run lint` OK après correction des erreurs bloquantes (`rules-of-hooks`, `prefer-const`, types UI vides, import docs, `@ts-ignore`). Il reste des warnings historiques non bloquants à traiter par lots dédiés.
- [x] **Suite Jest frontend complète** : `npm test -- --runInBand --passWithNoTests` OK (**86/86**) après réalignement de l’encart service-detail “Réutilisation monitoring”.
- [x] **Suite Jest `security-service` complète** : `npm test -- --runInBand` OK (**15/15**) après correction de `tests/setup.js` pour utiliser la base Docker locale exposée sur `POSTGRES_PORT` (`5000` par défaut) au lieu de forcer `localhost:5432`, et alignement du test `/security/logs` sur le contrat actuel `data + pagination`.
- [x] **Redis runtime vérifié** : `jobbingtrack-redis` healthy sur le port hôte `5001`; `redis-cli PING` OK, `DBSIZE=7`, clés `intrusion:*` présentes et `blocked_ip:*` présent. Redis est utilisé côté gateway pour intrusion/rate-limit/blocage temporaire; absence de clé `rate_limit:*` au moment du check = aucune fenêtre active, pas une preuve de non-utilisation.
- [x] **Tests réels HTTPS / WAF / metrics / backoffice** : stack recréée avec `dev-https-proxy`, `frontend`, `api-gateway`, `metrics-aggregator` healthy. Vérifié en réel : frontend HTTPS `200`, gateway `/health` HTTPS `200`, payload XSS externe via proxy `403`, bypass interne avec `X-Internal-Secret` valide `200`, metrics-aggregator direct sans clé `401`, direct avec `X-API-Key` `200`, proxy Next `/api/metrics-aggregator/docker/services/all` `200`, gateway `/api/v1/metrics` `200`, smoke navigateur HTTPS avec JWT/cookie réel sur `/backoffice/security`, `/backoffice/security/threats` et `/backoffice/services` sans `401/403/500`.
- [x] **Suite complète agrégée sans `make` (13/05)** : `TEST_NOPROMPT=1 API_URL=http://127.0.0.1:5002 API_GATEWAY_URL=http://127.0.0.1:5002 PLAYWRIGHT_BASE_URL=http://localhost:5003 PLAYWRIGHT_FRONTEND_MODE=smoke PLAYWRIGHT_MOBILE_MODE=smoke PERF_LIGHT=1 bash scripts/run-all-tests-with-reports.sh` OK : **436/436 tests réussis**, **1 ignoré**, sortie `0`. Correctifs faits avant validation : `Application.isTestData`, routes `/:id/status*`, statut candidature enrichi, auth admin/env fiable, firewall/WAF JWT, et runner qui échoue vraiment si le rapport contient des échecs.
- [x] **Correctif accès admin post-tests (13/05)** : Redis `blocked_ip:172.19.0.1` supprimé après blocage par les tests d’injection ; `intrusionDetector` ne bannit plus durablement les IP privées/locales hors production. Validation : injection dev = `403`, puis `blocked_ip:172.19.0.1` absent ; login admin `200`, profil `200`, `/b4ck0ff1ce` `200`, `/backoffice` `307 → /b4ck0ff1ce`. La CA dev a été réinstallée en NSS ; pour `http://localhost:5003`, le front bascule la gateway vers `http://127.0.0.1:5002` afin d’éviter `ERR_CERT_AUTHORITY_INVALID`.
- [x] **Correctif CI/CD Node 20 déprécié + DB (13/05)** : tous les workflows utilisent des actions compatibles runtime Node 24 (`actions/checkout@v6.0.2`, `actions/setup-node@v6.4.0`, `actions/upload-artifact@v7.0.1`) et exportent `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true`. `database-schema-validation` a une `DATABASE_URL` job-level ; `database-validation.yml` passe ses jobs Prisma sur Node 22, utilise `prisma db push --accept-data-loss --skip-generate`, et remplace `prisma format --check` par `prisma format`. Validations locales : YAML OK, plus aucune référence `@v4`/`node-version: '20'`, `prisma validate` + `generate` OK, `db push` OK sur Postgres temporaire (**58 tables**).
- [x] **Validation BDD complète post-erreurs `Application.isTestData` / `log_collector_logs` (13/05)** : `make db-push-all`, `DATABASE_URL=... npm run test:database`, `make test-database`, `prisma validate/generate`, checks SQL colonnes critiques, et Jest ciblé `api/test-archive-trash.test.js` OK (**21/21**). `test-postgresql-config.js` et `test-migrations.js` ont été réalignés sur le flux actuel `db push` + correctifs SQL `db-push-all` + `init-key-tables.sql`; `make test-database` lance maintenant les tests en local pour accéder aux fichiers racine et échoue vraiment si la suite DB échoue.

## Priorité précédente 6 mai 2026 — perf ressources + corrélation

- [x] Corrélation UI: afficher un chargement animé dans le tableau incidents pendant le fetch (au lieu de laisser des cellules ambiguës).
- [x] Corrélation UI: loader colonne par colonne (skeleton par `<td>` au chargement des logs) + colonnes métriques (CPU / mémoire / TR / écart) en attente tant que le conteneur focal n’est pas dans `loadedOrder` ; KPI logs vs score sécurité chargés séparément (`correlation/page.tsx`).
- [x] Corrélation data: tableau diagnostic par champ (source technique, détail si absent, correctif) pour requestId / endpoint / IP / HTTP / proto / port — sélection d’une ligne incidents sur **`/backoffice/performances/correlation`** (`parseIncidentContextFull` + panneau sous le tableau).
- [x] Metrics-aggregator: profiler `collectAllMetrics` — **`METRICS_AGGREGATOR_PROFILE_COLLECT=1`** (`server.js` : `createCollectProfiler`, phases chronométrées + meta `monitoringC` / `dockerFallbackRan` / compteurs).
- [x] Frontend: diagnostiquer le pic CPU (>300%) sous charge et réduire le coût des rafraîchissements/perf pages monitoring. (06/05: WATCHPACK polling off + cap mémoire Node + healthcheck simplifié)
- [x] Redis: mesurer la pression mémoire réelle (dataset, RSS/buffers, fragmentation) + budget/actions — **`make redis-memory-report`** (`scripts/monitoring/redis-memory-report.sh`) ; budget local **128 MB**, warn **70%**, critical **85%**, fragmentation ignorée sous **10 MB** utilisés, actions affichées si `maxmemory=0`, fragmentation haute ou clients bloqués.
- [x] Log collector C: rotation/troncature + découverte dynamique + lecture non bloquante — **`collector.c`** passe à `inotify_init1(IN_NONBLOCK)` + `poll`, traite les événements fichier même avec `len=0`, rescane `/var/lib/docker/containers` toutes les 10s, évite les watches en double, retire les watches sur `IN_MOVE_SELF`/`IN_DELETE_SELF`, option **`LOG_COLLECTOR_READ_EXISTING=0`** pour éviter d’ingérer tout l’historique au démarrage ; compilation C OK.
- [x] Monitoring C: remplacer les `popen` coûteux (`docker stats`/`inspect`/`curl`) par collecte non-forkée et checks parallèles. **07/05** : inventaire conteneurs via Docker socket Unix sans shell, métriques CPU/mémoire/réseau via cgroups/proc, health checks via libcurl multi parallèle ; plus aucun `popen` dans `monitoring-c`.
- [ ] Validation dédiée **coût collecte métriques post-bascule Rust** : benchmark CPU/RAM/IO p95 sur 40-60 min avec `monitoring-agent-rs`, `log-collector-rs` et `jobbingtrack-metrics-aggregator` dans le gate tests complets/préprod. **Baseline historique C du 07/05** (`tests/results/resource-budget/20260507-123612/summary.md`) : `metrics-aggregator` **CPU p95 93.04% / max 121.75% / RAM p95 588.4 MB**, `frontend` **CPU p95 19.61% / max 79.68% / RAM p95 868.4 MB**, `monitoring-c` **CPU p95 1.22% / RAM p95 2.7 MB**, `log-collector-c` **CPU p95 0% / RAM p95 5.1 MB**, Redis **CPU p95 2.48% / RAM p95 29.7 MB**. **Contrôle court Rust post-bascule** : voir `monitoring/MIGRATION_RUST.md` (`tests/results/resource-budget/20260507-161634/summary.md`, 5 min) avec `monitoring-agent-rs` et `log-collector-rs` tres bas en CPU/RAM. Budget cible p95 : aggregator < **5% CPU / 250 MB**, frontend dev < **8% CPU / 700 MB** (prod cible < **3% / 300 MB**), collecteurs Rust < **2% CPU / 20 MB**, Redis < **2% CPU / 64 MB**. **Décision** : ne pas relancer à chaque itération ; refaire le contrôle long uniquement dans le gate tests complets/préprod avant prod.

Liste opérationnelle alignée sur **`PLAN.md`** (lots A–H) et **`STATUS.md`**. Sujets reportés : **`docs/BACKLOG.md`** et « Plus tard » dans **`STATUS.md`**.

## Lire ce fichier (validation vs inventaire)

- **Inventaire / cases `[x]` ici** : le code ou la procédure **existe dans le dépôt** ; ce n’est **pas** automatiquement « validé par le porteur ». L’acceptation produit reste **`PLAN.md`** (colonne **Validé**) ou une mention datée dans **`STATUS.md`**.
- **Où regarder dans le projet** : les tableaux du lot **A** (Recharts, métriques) indiquent d’abord le **chemin fichier** (`frontend/src/app/(admin)/backoffice/...`, composants sous `frontend/src/components/...`). L’URL navigateur (**`/backoffice/...`**) sert surtout à la QA manuelle une fois le fichier identifié.
- **À ne pas confondre** : une mention du lot **A2** dans **`PLAN.md`** ou dans **« Fait récemment »** ne veut pas dire **A2** terminée : la case **A2** reste **`[ ]` partielle** tant que **gateway `admin/logs/*`** + **Loki** (ou décision explicite) ne sont pas réglés.
- **Next.js (RSC) + baril `@/components/features`** : tout fichier exporté par ce baril et qui utilise des **hooks React** doit commencer par **`'use client'`**. Sinon, une page **serveur** qui importe seulement `AdminLayout` depuis le baril peut quand même faire analyser le graphe d’exports et **échouer à la compile** (ex. **`SettingsPopup.tsx`**, corrigé **07/04**). Vérification recommandée : **`./node_modules/.bin/next build`** dans **`frontend/`** (ou **`npm run dev`** sur les routes touchées).

---

## Suite & suivi explicite (post-livraisons, hors cœur des lots A–H)

**Rôle de ce bloc** : noter ici ce qui a été **évoqué en fin de chantier** ou **« à faire ensuite »** sans mériter tout de suite une case dans **A1–H** détaillée. Ce n’est **pas** le backlog long terme (**`docs/BACKLOG.md`** reste la file « officielle » reportée). **Règle** : une ligne = une intention ; cocher quand c’est fait ; si le sujet grossit, **créer** une entrée dans le lot concerné (**A–H** ou **`PLAN.md`**) et **retirer** la ligne d’ici pour éviter le double compte.

- [x] **Front — lot A1 (partiel)** : lazy + `rechartsTooltipProps` sur **`/backoffice/performances/containers`** (`AnalyticsContainersChartsBundle`) ; hub **`/backoffice/analytics`** recentré **application + utilisateurs** (plus de CPU système sur cette route, **07/04**). *Case = code en dépôt ; QA navigateur / build = à faire côté porteur (voir § RSC ci-dessus).*
- [ ] **Front — lot A1 (suite)** : **`/backoffice/statistics`** — **fait** : **`rechartsTooltipProps`** (**07/04**) ; **reste technique** : lazy gros graphes — **bloqué métier** par **§ Chantier Statistiques & backoffice** (compteurs services, séries erreur/disponibilité à refaire avant polish UI).
- [ ] **UX unifiée (Statistics / Performances / Analytics)** : gabarit commun KPI + période + empty states + légende **live vs persisté** — **après** fiabilisation **A1h** / § **Chantier Statistiques & backoffice** (sinon homogénéiser une UI encore fausse).
- [ ] **Graphes fenêtre “24h” intelligente** : quand la période demandée inclut majoritairement du vide (service récemment démarré), proposer un mode “auto-fit aux données disponibles” (zoom initial sur premier/dernier point) tout en gardant l’option d’afficher la fenêtre complète 24h.
- [ ] **Graphe corrélation “pic” (Performances)** : sous-page dédiée (`/backoffice/performances/correlation`) — toggles machine + conteneurs, axes % / Mo, **mode Léger / Complet** (session), limites points + concurrence fetch + timeout liste conteneurs ; **fait (30/04 + 07/05, partiel)** : route historique disponibilité consolidée (`availability?history=1`) + parsing front robuste alias **réseau / I/O / TR** (évite séries vides selon variantes de champs) + bloc corrélation incidents du service focalisé (logs persistés `ERROR/WARN`, signaux sécurité heuristiques, score sécurité moyen fenêtre) + **tableau de corrélation fine** incidents (`requestId`/endpoint/IP/message) alignés au point métrique le plus proche (CPU/mémoire/TR + écart secondes, borne max d’alignement pour éviter les rapprochements hors plage) + **tri colonnes** sur la synthèse **et** sur le tableau incidents (asc/desc/reset) + filtres rapides incidents (niveau, requestId présent, recherche texte) + liste services triée (**chargés en mémoire d’abord**, puis alphabétique) + deltas **Réseau/I-O** robustes aux resets compteur (**et au cas 1 seul point**) + fallback TR via **stats service** si historique vide + **07/05** : fusion **`metadata.metadata`** pour colonnes incidents (Winston imbriqué) + annulation explicite des fetch corrélation (**`AbortController`**, moins de bruit reload) + **brush/zoom partagé** sur les graphes + **icône d’état de tri** visible sur en-têtes ; **console** : moins de spam **ECONNABORTED** au F5 ; reste : corrélation causalité “pic” plus stricte (règles métier/requestId bout-en-bout), enrichissement logs sécurité (IP/port/endpoint/requestId systématiques à la source) ; **07/05 suite** : mapping persistance conteneurs enrichi pour pousser `blockIO.read/write` même via source monitoring-C (plus de `null` systématiques) ; **QA** : vérifier **I/O bloc / cumul** affichés vs persistance réelle (agrégateur / champs `block_io`) et documenter si Docker retourne réellement `0/0` sur l’hôte.
- [ ] **Corrélation fine — colonnes incomplètes en réel** : sur `/backoffice/performances/correlation`, traiter en priorité les lignes où `CPU % proche`, `Mémoire % proche`, `TR ms proche`, `Écart (s)`, `endpoint`, `IP`, `requestId`, `Proto`, `Port` restent vides malgré logs présents ; livrer une analyse par champ (absence source vs parsing vs fenêtre d’alignement) + correctifs ciblés. **07/05 (partiel)** : remplacement des `—` **dans les colonnes** par une raison explicite (`source absente`, `champ manquant (contexte)`, `champ manquant (métriques)`, `hors fenêtre`).
- [ ] **Synthèse Performances — endpoints instantanés** : fiabiliser le bloc `Temps de réponse des endpoints (instantané)` pour afficher toutes les mesures attendues de façon stable (pas d’éléments manquants/intermittents), clarifier le tri/source et gérer proprement les états de données partielles. **Partiel** : affichage explicite des services sans mesure instantanée ajouté dans la synthèse.
- [ ] **Pont Perf ↔ Sécurité ↔ Logs** : depuis la vue sécurité, afficher un graphe corrélé des séries perf/services/conteneurs + événements logs (IP, endpoint, code HTTP, menaces) pour expliquer “quel log a causé quel pic”.
- [ ] **Sécurité — notifications critiques par mail** : toute menace `CRITICAL` (ou équivalent risque élevé confirmé) doit déclencher une notification mail vers une adresse configurable dans l’interface admin. La modification de cette adresse doit exiger une réauthentification récente et être journalisée. **11/05 partiel** : socle backend en place — `security-service` notifie `notification-service` via route interne `X-Internal-Secret` pour alertes `critical/high`; destinataires env `SECURITY_ALERT_EMAIL(S)` avec fallback `CRASH_REPORT_EMAIL`; logs `EmailLog` si disponibles. **12/05 suite** : alertes disponibilité `SERVICE_DOWN` créées par le `security-service` depuis `/api/v1/docker/services/all` du metrics-aggregator, avec dédup anti-flap en mémoire et liste `SECURITY_CRITICAL_SERVICES`. **Reste** : écran admin + réauth + audit + confirmation + MailHog/SMTP réel.
- [ ] **Sécurité — actions sensibles avec réauth** : blocages firewall critiques, changement WAF/politiques, purge/export massif de données, modification email d’alerte et paramètres super admin doivent demander une reconnexion ou confirmation forte avant exécution.
- [ ] **Sécurité — confirmation email super admin / gestion données** : pour les actions destructrices ou très sensibles dans la gestion des données, envoyer un mail de confirmation avec jeton court et usage unique à l’adresse déclarée ; l’action ne doit s’exécuter qu’après validation explicite.
- [ ] **Sécurité — logs sans plafond arbitraire** : **18/05 partiel** — logs sécurité + menaces + IPs bloquées paginés (voir priorité « pagination URL »). **Reste** : autres écrans, curseur serveur si volume très élevé, vue d’ensemble 2000 événements / 30 j (indiquer explicitement la troncature).
- [ ] **Sécurité — détails forensics menaces avant CVE** : prioritaire sur la suite CVE. Pour chaque menace détectée (DDoS, WAF, intrusion, firewall), afficher IP source/destination, route/flux réseau, requêtes et payloads dangereux rediges si sensibles, logs applicatifs corrélés, services et comptes impactés. **11/05 partiel** : page menaces responsive + auto-refresh silencieux + fiche menace enrichie avec GeoIP, logs sécurité 24h, intrusions/DDoS liés, ports/protocoles/services et télémétrie manquante explicite. **12/05 suite** : fallback sur `network_connections` quand `network_threats.destIp` / `metadata.connectionDetails` sont vides ; corrélation logs via `metadata.sourceIp` / `metadata.threatId` en plus de `sourceIP`; cartes Analyse cliquables vers menaces filtrées ; auto-refresh protégé contre les requêtes concurrentes ; vue réseau avec IPs surveillées actionnables. **Reste** : preuves request/payload conservables, export horodate, tests WAF on/off en dev/prod-like, puis enrichissement reseau optionnel (ASN/VPN/proxy/Tor) clairement libelle comme contexte IP et non comme preuve d'attaque.
- [x] **Sécurité — WAF : ne pas filtrer le trafic interne inter-services** : le WAF protège le trafic externe entrant (gateway/public) sans inspecter/bruiter les appels internes de confiance entre conteneurs JobbingTrack. **12/05 renforcé** : `WAF_INTERNAL_BYPASS_ENABLED` + `WAF_INTERNAL_BYPASS_CIDRS`, mais le bypass interne est refusé si la requête porte des headers de reverse proxy (`X-Forwarded-*`) sans `X-Internal-Secret`; cela évite qu’un proxy Docker transforme tout trafic navigateur en trafic interne. `make security-waf-lab` valide le cas externe bloqué via HTTPS dev et le bypass interne de confiance si le secret est exporté. **Reste** : vérifier les CIDR réels du serveur/VPS, remote host / reverse proxy, et affiner les logs séparés en production.
- [ ] **Sécurité — supprimer les tokens mock runtime** : les bypass dev doivent être des valeurs exactes d’environnement (`DEV_AUTH_BYPASS_TOKEN`) ou de vrais JWT signés (`JWT_SECRET`), jamais des préfixes codés en dur type `mock-jwt-token*`. **11/05 partiel** : gateway test JWT signé, application/company/dashboard passent sur token env exact ; secret interne runtime sans fallback hardcodé.
- [ ] **Sécurité — alertes mail incidents critiques et services down** : envoyer un email via le système mail existant quand une menace confirmée `CRITICAL`, une CVE `critical`, un blocage firewall majeur, ou un service/conteneur critique passe `down` selon le monitoring Rust/metrics-aggregator. Adresse configurable en admin avec réauth + audit ; tests MailHog + test SMTP réel optionnel. **11/05 partiel** : alertes `SecurityAlert` `critical/high` branchées sur notification-service + tests ciblés. **12/05 suite** : transitions `service down` couvertes via job périodique `security-service` (`SECURITY_SERVICE_DOWN_ALERT_CRON`, timeout, dédup `SECURITY_SERVICE_DOWN_DEDUP_MINUTES`, services critiques configurables). **Reste** : digest `high`, UI admin réauth/audit, validation MailHog/SMTP réel et scénario manuel arrêt/redémarrage d’un conteneur critique.
- [ ] **Sécurité — CVE supply-chain continue** : exécuter régulièrement **`make test-cve-scan`** (Node/Rust + images des conteneurs en cours avec `CVE_SCAN_DOCKER=1`) et traiter/justifier toute CVE `high`/`critical` avant release ; idéal CI : `CVE_SCAN_STRICT=1 CVE_SCAN_FAIL_ON=high`.
- [ ] **Sécurité — CVE en production + alertes mail** : brancher `security-service` sur le vrai scanner CVE avec verrou, cadence légère, déduplication par CVE/surface, alertes mail immédiates pour `critical`, digest `high`, configuration email protégée par réauthentification et confirmation.
- [ ] **Sécurité — rétention/protection logs** : formaliser la rétention par classe de données (90 jours logs bruts, 12–24 mois agrégats, 1–3 ans actions critiques selon besoin légal), protéger export/purge/lecture payload brut par rôle dédié + réauth + audit trail.
- [ ] **Contrat logs forensics (gateway + services)** : propager et persister systématiquement `requestId`/`correlationId`, IP client, endpoint, méthode, protocole/port, code HTTP sur les logs `WARN/ERROR` pour rendre la corrélation incidents réellement exploitable (anti-DDoS, brute force, usurpation, injection). **05–06/2026 partiel** : mêmes services qu’avant + **`api-gateway`** (cf. **`STATUS.md`** § 6 mai ; complément **HTTP** amont sur erreurs proxy + métadonnées central logger) + **`workflow-service`** (**`requestContext`**, Winston, **`centralLogger`**, CORS en-têtes corrélation, **`TRUST_PROXY_HOPS`**, cron → Winston) ; **reste** : **validation bout-en-bout** vue **`/backoffice/performances/correlation`** (QA porteur ; colonne **HTTP** + parsing métadonnées côté front).
- [ ] **Observabilité qualité données corrélation** : afficher explicitement, par service, la couverture réelle des points **I/O** et la source **TR** (historique vs fallback stats) pour éviter de masquer les trous de collecte derrière des `--` ou des hypothèses. **30/04 partiel** : panneau “Qualité des données”, parsing alias mémoire/réseau/I/O, multi-chargement services, deltas robustes (cumuls / un seul point), fallback **live container stats**, résumé même sans historique persistant.
- [ ] **Statistiques utilisateurs — clarification métier** : remplacer la logique actuelle `activeUsers ~= totalUsers` par une définition explicite et mesurée (ex. sessions actives récentes), puis harmoniser libellés “Total / Actifs / Nouveaux” dans les cartes et graphiques.
- [ ] **Statistiques utilisateurs — lisibilité camemberts** : éviter le chevauchement des labels (labels externes/masqués + légende chiffrée), normaliser les noms de rôles (`ADMIN`, `USER`, etc.) et garder les valeurs absolues visibles.
- [ ] **Performances Disque** : compléter la page `backoffice/performances/disk` avec snapshots historisés, usage %, volume utilisé/total, puis ajouter la suite Block I/O par service (corrélation avec détail service).
- [x] **Doc perf** — **`docs/performance/RAPPORT_PERFORMANCE.md`** : titres de section alignés sur **`/backoffice/statistics`** (**24/04**).
- [x] **`tools/README.md`** : index **`adb-lib`** + **`emulator-controller`** (**24/04**, lot H2).
- [x] **`mobile/test/widget_test.dart`** : smoke **app → écran Connexion** (**24/04**).
- [ ] **Mobile (`mobile/`)** : renommage optionnel `home_screen` → `dashboard_screen` ; barrel **`lib/screens/screens.dart`** si tu veux des imports courts.
- [x] **`flutter-mobile-app/lib/screens/`** : même logique **`jobbing/*`** + **`routes`** nommées pour **`/applications`** etc. (**24/04** ; vérifier **Provider** si tu branches l’API sur cet exemple).
- [x] **Scripts** — table *script → Makefile / doc* dans **`scripts/README.md`** (suite lot **H1**, **07/04**).
- [x] **Env sans défauts codés — lot config 12/05** : voir **HX1** en bas de fichier (audit `|| '…'` / ports magiques sur tout le monorepo). `make env-check` aligne `.env` / `.env.example` ; `make env-validate` signale sans afficher les valeurs les secrets faibles/placeholders, `NEXT_PUBLIC_*` sensibles, SMTP requis si alertes activées, et erreurs prod (`ALLOWED_ORIGINS` HTTPS-only, `WAF_ENABLED`, `ENABLE_METRICS_AUTH`, absence de bypass dev). Correctifs appliqués : secrets locaux régénérables sans affichage, `make waf-enable` / `waf-disable` / `waf-status`, `.env.production.example` séparé et validable, `ENV_FILE=.env.production make env-validate-prod`, scripts env déplacés sous `scripts/env/` avec wrappers historiques, `make scripts-inventory`, metrics auth active en dev via route Next serveur `/api/metrics-aggregator`, clé `METRICS_API_KEY` uniquement serveur, `metrics-aggregator` protège aussi `docker` / `persistence`, et `NEXT_PUBLIC_MOBILE_TEST_USER_PASSWORD` reste retiré. **Reste B14 large** : Redis auth, docker.sock proxy/limitation, ports prod non publiés, non-root/read-only/tmpfs et tri des CVE/images.
- [x] **Synthèse `/backoffice`** : stats **CPU & mémoire** explicites **conteneurs JobbingTrack** (cartes + tableau par `jobbingtrack-*`, lien **Performances → Conteneurs**) ; encart **Performance** enrichi (**07/04**).
- [x] **Statistiques `/backoffice/statistics/security`** : **uniquement persistance** (résumé `…/security/summary` + tendance score + **agrégat par jour** + extrait lignes) — **sans** doublon des cartes live de **`/backoffice/security`** (**07/04**).

---

## Règles de travail (produit / Git / tests)

- **Pull requests** : **pas de PR** tant que le porteur ne l’a **pas demandé explicitement** dans la conversation.
- **Conventions de commit (obligatoire)** : utiliser un préfixe cohérent selon la nature du changement — `feat:`, `err:`, `test:`/`tests:`, `misc:` ; regrouper par lots logiques (pas de mélange front/back/docs/tests sans lien direct), puis vérifier `git status` avant commit.
- **Tâches « terminées » côté code** : les cases `[x]` ci-dessous reflètent surtout l’**implémentation** ; l’**acceptation produit** suit **`PLAN.md`** (colonne **Validé (porteur)** = **Oui (date)** après **votre** vérification manuelle, ou mention équivalente dans **`STATUS.md`**).
- **Agent IA / validations locales** : ne pas lancer directement de commandes `make ...` sauf demande explicite du porteur ; utiliser les scripts sous-jacents ou commandes directes pour éviter que Cursor les interprète comme ouvertures de fichiers.
- **Cibles Make de référence** : `tests`, `test-suite-full`, `up-full`, `db-push-all` restent documentées pour le porteur, mais les validations agent doivent indiquer l'équivalent direct réellement exécuté.
- **Suite complète** : la cible `tests` correspond à la suite complète + rapports dans `tests/results/<horodatage>/`. Prérequis porteur : stack complète, schéma BDD synchronisé, seed auth si besoin, MailHog si tests mail — sinon échecs massifs normaux (voir `ERRORS.md` et `STATUS.md` § 11/04/2026). **Gate fin de journée / avant push majeur** : section dédiée dans `TODOS_A_VALIDER.md` (commande `bash scripts/run-all-tests-with-reports.sh`, lecture `summary.json` / `report.html`) ; cible Make de référence **`test-full-quick`** (= stack + BDD + seed + `test-all`). Ne pas lancer cette campagne au milieu d’un P0/P1 sauf demande explicite ; privilégier les validations ciblées pendant les corrections.

---

## Alignement `ERRORS.md` → suivi (actions hors environnement vide)

À traiter dans le code / les lots concernés ; cocher ici seulement quand **corrigé et vérifiable** (et **Validé** dans **`PLAN.md`** si produit).

- [ ] Table **`deployments`** manquante — deployment-service (`ERRORS.md`)
- [ ] Table **`user_events`** manquante — User Analytics (`ERRORS.md`)
- [ ] **API versioning** 404 — `GET .../analytics/stats/:userId/versions` (`ERRORS.md`)
- [ ] **Sync mobile** — endpoints `POST /sync/push`, etc. (`ERRORS.md`)
- [ ] (Optionnel) Build APK **`flutter_local_notifications`** (`ERRORS.md`)

---

## Fait récemment (à ne pas re-faire)

- [x] **07/05 — Corrélation / observabilité dev** : **`logs-watch`** ne traite plus **141** comme arrêt utilisateur ; **`analytics.service.ts`** filtre les interruptions axios bénignes (**reload**) ; **`correlation/page.tsx`** — **`mergeAggLogMetadata`** pour incidents ; **`frontend/package.json`** — **`browserslist:update`** ; **`make logs`** aide mémo vers **`logs-watch`**.
- [x] **29/04 — Front boot bloqué** : correction de `frontend/next.config.js` pour forcer un `distDir` utilisateur (`.next-local`) quand `frontend/.next` appartient à `root` (cas fréquent après exécution Docker). Effet : suppression de l’erreur navigateur `layout.js ... literal not terminated` liée à des assets `.next` corrompus/partiellement écrits.
- [x] **06/05 — Forensics `workflow-service` (lot A3/B6)** : **`requestContext`**, Winston, **`centralLogger`** (off sous Jest), CORS **`X-Request-Id` / `X-Correlation-Id`**, **`TRUST_PROXY_HOPS`** ; cron / moteur via **`logger`** (plus de **`console.*`** sur les chemins principaux).
- [x] **06/05 — Forensics `api-gateway` (lot A3/B6)** : middleware corrélation (**AsyncLocalStorage**, aligné microservices), logger Winston + contexte, **`CentralLoggerTransport`** (désactivé sous Jest), **`TRUST_PROXY_HOPS`** ; erreurs proxy enrichies (**`upstreamHttpStatus`**, **`httpStatus`**) ; Jest gateway **22/22** verts (fallbacks **`NODE_ENV=test`** auth/logs, CORS OPTIONS avec **Origin**, Bearer sur route logs).
- [x] **05/05 — Forensics microservices (lot A3/B6)** : `interview`, `call`, `followup`, `event`, `profile`, `notification`, `deployment` — alignement sur auth/company (requestContext, centralLogger, logger Winston) ; **axios** ajouté à call/followup/event pour le flush HTTP.
- [x] **04/05 — Vue d’ensemble `/backoffice`** : retrait des encarts texte (explication métriques, sous-titre carte Performance, note débit erreurs) — allègement UI ; suivi infra Redis **HX5** + séquence **`db-push-all` → `up-full`** en fin de fichier.
- [x] **04/05 — Backoffice « Chargement… » + Postgres `Company.isTestData`** : alignement **schéma maître auth** (`Company.isTestData` + index, comme `backend/prisma`) pour que `make db-push-all` crée la colonne ; script **`scripts/db/fix-company-isTestData.sql`** dans **`db-push-all.sh`** (idempotent). **Gateway** : **`GET /api/v1/services`** ne renvoie plus **503** si metrics-aggregator est down — **200 + fallback** + logs **`warn`** pour erreurs transitoires (**`logMetricsAggregatorFailure`**).
- [x] **30/04 — Corrélation Performances** : historique disponibilité service passé sur `.../persistence/services/:serviceName/availability?history=1` (plus de sous-route `/history`) ; front `correlation/page.tsx` renforcé pour lire plusieurs alias de champs (`network*`, `block*`, `responseTime*`) afin d’afficher correctement **Réseau / I/O / TR** malgré les différences de payload entre sources.
- [x] Vue d’ensemble `/backoffice` : carte incidents sécurité, grille 2 lignes, légendes CPU total, état services (En ligne / ~ms), débit erreurs en `/min`, reset compteurs à 0 (`frontend/src/app/(admin)/backoffice/page.tsx`).
- [x] Panneau **Performance** : ligne disponibilité %, légende des sources, lien vers `/services/backoffice`, texte d’aide bas de carte (avril 2026).
- [x] Doc : **ERRORS.md** (pièges dashboard + pipeline synthèse), **FONCTIONNALITES.md** § 4.1, **RESOLUTIONS.md** (avril 2026), **STATUS.md** (structure + tableau lots), **docs/CHANTIER_…**, **docs/INDEX.md**.
- [x] **21/04** — **STATUS** / **PLAN** / **ERRORS** / **TODOS** / **README** : **`make status-watch`** & **`status-live`** (= **`make status`**, **`CLEAR`**, résumé stack vide) ; section logs backoffice (metrics-aggregator) ; **mise à jour des textes `PLAN.md`** pour cadrer les lots **A2**, **C1**, **E1** (≠ livraison complète de ces lots) ; pièges **ERRORS** (`make status` DOWN, ancien **status-live**).
- [x] **24/04** — **`make status` vs backoffice** : le statut Makefile reflète l’**état des conteneurs** (UP / healthy / ports), pas l’activité d’une page Next.js (ex. Statistiques) ni le volume de requêtes utilisateur. **`status-watch` / `status-live`** : script **`scripts/ops/status-watch-loop.sh`** — **buffer terminal alternatif par défaut** (`ALTSCREEN=1`) pour réafficher le bloc courant **sans empiler** les cycles dans l’historique du terminal ; `ALTSCREEN=0` pour l’ancien défilement ; **`CLEAR=1`** + `ALTSCREEN=0` pour `clear` classique. **UI logs détail service** : grille date / message, couleurs (HTTP, firewall, erreurs), bouton masquer bruit firewall/WAF.
- [x] **07/04 (complément)** — **`make status`** : légende longue des ports (**`STATUS_LEGEND=1`** pour l’afficher ; **désactivée par défaut**). **`status-watch-loop.sh`** : **`STATUS_FOLD=1`** (défaut) — sortie de **`make status`** passée dans **`fold -s -w $(tput cols)`** ; **`STATUS_FOLD=0`** pour désactiver. **Front** : **`GlobalSearch.tsx`** avec **`'use client'`** (build RSC + baril **`features`**), chevron drawer **Performances** ouvert si sous-route active, **Synthèse Performances** : scroll vers **`#latence`** une seule fois par navigation vers l’ancre (pas à chaque reload des graphes). Test **`GlobalSearch.client-boundary.test.ts`** ; **`ERRORS.md`** § baril RSC.
- [x] **07/04** — **`SettingsPopup.tsx`** : **`'use client'`** en tête (hooks + réexport baril **`@/components/features`** ; pages RSC comme **`/backoffice/performances/containers`**).
- [x] **13/06 — Performances graphes “vides”** : vérification `metrics-aggregator` = données historiques non perdues (`systemMetrics` ~244k, `containerMetrics` ~5,4M, plage 06/03→13/06). Front renforcé : `getContainersList()` aplatit les métriques live CPU/mémoire depuis `service.metrics`; pages **CPU & Mémoire**, **Conteneurs** et **Disque** affichent un fallback graphique live court quand la plage historique ne renvoie rien/est inexploitable, sans ajouter de nouvelle API lourde.

---

## Lot A — Monitoring services + logs multi-sources (+ persistance)

### Socle graphes / données partagés (cible lot A, élargie au backoffice)

- **Vision** : le **socle** (fonctions pures de dérivation, hooks, composants Recharts réutilisables, tooltips / axes / légendes alignés) a démarré sur le **monitoring** (détail service), mais la **cible** est **toute page backoffice** qui affiche des **graphiques** ou des **vues tableau + séries** — dès qu’on standardise, sans casser l’existant.
- **Règle porteur** : **ne pas supprimer** un graphe, une série ou un bloc de données **sans validation explicite**. Un refactor « socle » = **déplacement / factorisation** dans des modules dédiés, **pas** retrait fonctionnel tant que tu n’as pas validé écran par écran.
- **Navigation (préambule lot A — avril 2026)** : drawer **Tableau de bord** — **Vue d’ensemble** ; **Performances** (`/backoffice/performances`, sous-entrées **Synthèse**, **Temps de réponse** → route canonique `/backoffice/performances/latency`, **Conteneurs** (graphes Docker), **Disque**, **Réseau (détail)** → **`/backoffice/performances/network`**) ; **Statistiques** : canon **`/backoffice/statistics`** (+ sous-routes **app-data**, **security**, **`log-stats`** — motif `logs/` **gitignoré**) ; l’URL **`/backoffice/statistique`** redirige vers la canonique ; **Analytics** (hub **application + utilisateurs** + sous-routes applicatives ; **pas** de conteneurs / CPU système ici — anciennes URLs **`/backoffice/analytics/containers`** et **`/backoffice/analytics/network`** → **redirect** vers Performances). **Annulation** : `git revert` / commit parent si besoin.

### Inventaire — **Metrics système & conteneurs** : ce qui est déjà sur le nouveau socle

- **Périmètre** : ici on parle du volet **métriques système & conteneurs** (charge, conteneurs Docker, séries agrégées), **pas** d’un cas isolé « un service au hasard ». Le **premier écran** déjà branché sur le socle (modèle + hook + composant) est le **détail d’un service** — parce que c’est là que vit l’**historique** exploitable (CPU, mémoire, réseau, Block I/O).
- **Référence graphes** : le rendu **CPU / mémoire** (et le reste du bloc) dans **`MonitoringServiceHistoryCharts`** sur **`/backoffice/services/[nom]`** est **nettement plus abouti** que les graphes **CPU** (et voisins) des pages **performances / analytics** actuelles. **Objectif** : **réutiliser ce patron** (axes zoomés, tooltips, dérivés) sur les vues **metrics système & conteneurs** plus larges — **sans retirer** les graphes existants tant que tu n’as pas validé la bascule (règle porteur ci-dessus).

| Zone (metrics système & conteneurs) | Fichiers / pièces *(chemins relatifs au dépôt)* | Validé par toi *(relecture visuelle / prod)* |
|-------------------------------------|-----------------------------------------------|--------------------------------|
| **Historique** détail conteneur / service : CPU, mémoire, réseau, Block I/O cumul, débit Block I/O | `frontend/src/lib/monitoring/serviceHistoryChartModel.ts`, `useServiceHistoryChartData.ts`, `serviceHistorySources.ts`, `frontend/src/components/monitoring/MonitoringServiceHistoryCharts.tsx`, `frontend/src/app/(admin)/backoffice/services/[serviceName]/page.tsx` — URL QA : **`/backoffice/services/<nom>`** | [ ] |
| **Performances** — CPU & mémoire % (système) | `frontend/src/lib/charts/systemMetricsSeriesModel.ts`, `frontend/src/components/charts/SystemCpuMemoryAreaCharts.tsx`, `frontend/src/app/(admin)/backoffice/performances/page.tsx` — **`/backoffice/performances`** ; alias `analytics/performances` → canonique | [ ] |

### Inventaire — pages backoffice avec Recharts **hors** socle unifié (A1c / A1d / A5)

*(Composant **`recharts`**. **A1c** : lazy = **`next/dynamic`** + tooltips **`rechartsTooltipProps`** quand c’est fait.)*

| URL (QA navigateur) | Fichier source principal | Lazy / tooltips *(état 24/04)* |
|--------------------|---------------------------|--------------------------------|
| **`/backoffice/analytics`** | `frontend/src/app/(admin)/backoffice/analytics/page.tsx` | Hub cartes (application, utilisateurs, lien Performances) — **sans** Recharts système |
| **`/backoffice/performances`** | `frontend/src/app/(admin)/backoffice/performances/page.tsx` | **Partiel** : `SystemCpuMemoryAreaCharts` + `SystemCpuNetworkCorrelationChart` en `dynamic` ; graphes inline sur la page |
| **`/backoffice/performances/network`** | `frontend/src/app/(admin)/backoffice/performances/network/page.tsx` | **Partiel** : graphique corrélation CPU/réseau (import statique, plus de lazy chunk — **18/05**) ; ex-`/backoffice/analytics/network` |
| **`/backoffice/performances/containers`** | `.../performances/containers/page.tsx` + `components/charts/AnalyticsContainersChartsBundle.tsx` | **A1c** : `rechartsTooltipProps` + lazy bundle Recharts (**07/04**, déplacé depuis Analytics) |
| **`/backoffice/analytics/application/*`** | `frontend/src/app/(admin)/backoffice/analytics/application/**/page.tsx` | À inventorier au fil des écrans (perf / activité / feedback) |
| **`/backoffice/statistics`** (+ sous-pages) | `frontend/src/app/(admin)/backoffice/statistics/page.tsx`, `app-data/page.tsx`, `security/page.tsx`, `log-stats/page.tsx`, `StatisticsSubNav.tsx` | **À refaire (18/05)** : compteurs services faux positifs offline, séries erreur/disponibilité — voir **§ Chantier Statistiques & backoffice** ; tooltips **`rechartsTooltipProps`** (**07/04**) ; lazy gros graphes = **reste A1e** |
| **`/backoffice/statistique`** | `frontend/src/app/(admin)/backoffice/statistique/page.tsx` | **Redirection serveur** → **`/backoffice/statistics`** (plus de doublon de contenu) |
| **`/backoffice/tests-performance`** | `frontend/src/app/(admin)/backoffice/tests-performance/page.tsx` | Graphes Recharts inline — **reste** alignement thème / lazy si besoin |
| **`/backoffice/performance-tests`** | `frontend/src/app/(admin)/backoffice/performance-tests/page.tsx` | Pas Recharts sur la même logique ; option **Infrastructure** ajoutée **24/04** |

| **`/statistics`** (hors `backoffice/`) | `frontend/src/app/(admin)/statistics/page.tsx` | **Redirect** → **`/backoffice/statistics`** (**24/04**) |

### Feuille de route — réseau cumul vs activité, hub Analytics, application « métriques », sécurité

- **Réseau (snapshots agrégateur)** : les compteurs **RX/TX** sont souvent des **cumuls** → courbe monotone peu parlante seule. **Fait (Performances)** : **débit Mo/min** (`buildSystemNetworkMbRateRows`) + **graphique combiné** CPU % vs RX/TX Mo/min (`SystemCpuNetworkCorrelationChart.tsx`, deux axes Y). **Suite** : **brush** / zoom aligné ; **fenêtre glissante** (moyenne) côté API ; corrélation **mémoire** optionnelle ; **graphiques supplémentaires** sur **`/backoffice/performances/network`** (histogrammes / répartition par conteneur si données disponibles).
- **Hub `/backoffice/analytics` vs `/backoffice/performances`** : **Analytics** = entrée **métier** (application, utilisateurs, lien Statistiques) ; **Performances** = **historique infra** système (CPU, mémoire, **temps de réponse agrégé** `responseTimeAvg` quand la persistance le remplit, réseau cumul + débit, **conteneurs**). **Fait (07/04)** : retrait du test CPU et des cartes infra sur le hub Analytics.
- **Sous-menu Performances** : **Fait (07/04)** — entrées drawer + **`PerformancesSubNav`** : synthèse unique (cartes), **conteneurs** (graphes sur **`/backoffice/performances/containers`**), **stub disque**, **réseau** sur **`/backoffice/performances/network`**. Anciennes routes sous **`/backoffice/analytics/containers|network`** = **redirect** Next vers Performances. **Suite** : routes dédiées CPU-only / mémoire-only seulement si le porteur impose le découpage (sinon ancrages internes).
- **Application** (drawer **Analytics**) : **sous-routes** `/application/performance` (ex-page live), `/application/activity`, `/application/feedback` + **`ApplicationSubNav`** ; redirect `/application` → **performance**. Périmètre cible inchangé : **perf mobile**, **traces** (resets, etc.), **retours** (emails / signalements) — pages **activité** et **retours** = emplacements réservés (chantier données **user-analytics**, **event-service**, **mail**).
- **Sécurité × observabilité** : exploiter **charge réseau**, **métriques**, **analytics** pour **surcharge**, **menaces**, **politiques** (lots **B** + **A**) — gros chantier : règles de corrélation, seuils, pas seulement réafficher des graphes dans la vue sécurité.

### Souhaits graphes / données / tableaux (priorité porteur — compléter ici)

*(Ajoute une puce `[ ]` par idée ; l’équipe ne devine pas les priorités métier.)*

- [x] **CPU & mémoire (premier pas)** : **`/backoffice/performances`** — **`SystemCpuMemoryAreaCharts`** + **`systemMetricsSeriesModel`**. **Suite possible** : **`/backoffice/performances/containers`**, **`/backoffice/statistics`**, etc.
- [x] **Réseau** : débit **Mo/min** sur **Performances** ; page détail **`/backoffice/performances/network`**.
- [x] **Temps de réponse** : persistance + instantané par service sur **Performances**.
- [x] **Statistiques** : sous-pages **App data** / **Sécurité** / **Logs (stats)** branchées.
- [ ] **Liste services** : **vraie** sparkline / mini-série **historique** (API légère ou cache) — aujourd’hui **jauge CPU** instantanée + **`title`** ligne (voir **A1d**).
- [ ] **Export** : CSV / JSON des séries affichées (performances, réseau, détail service) pour analyse externe.
- [ ] **Brush / plage** : zoom synchronisé entre plusieurs graphes d’une même page (**A1e**).
- [ ] **Seuils & alertes visuelles** : bandes ou lignes de référence (CPU %, latence ms) configurables.
- [ ] **Tableaux** : colonnes triables + mémorisation préférences (localStorage) sur vues « lourdes » (logs agrégés, stats).
- [ ] **Heatmap** : charge par service × tranche horaire (données agrégateur / BDD à dimensionner).
- [ ] **Comparatif** : superposer deux périodes ou deux conteneurs sur un même graphe (hors simple multi-line actuel).
- [ ] **PIDs historisés** : courbe ou tableau si **A1f** (contrat API) validé produit.

---

#### A1 — Que faire concrètement ? (vue détail service + socle graphes)

**Périmètre** : page **`/backoffice/services/[nom]`** + modules **`frontend/src/lib/monitoring/*`**, **`MonitoringServiceHistoryCharts.tsx`**, API **`metrics-aggregator`** (**`docker.routes.js`** — **`/history`**, métriques live).

| Statut | Signification |
|--------|-----------------|
| **Fait (code)** | Implémenté dans le dépôt ; **≠** « validé par toi » → colonne **Validé** dans **`PLAN.md`**. |
| **Reste** | Pas fini ou volontairement basse prio ; tu peux cocher quand c’est livré ou reporter dans **Souhaits** ci-dessus. |

**Déjà en place (ne pas refaire sans nouveau besoin)**  
CPU / mémoire / réseau / **Block I/O** (cumul + débit) sur l’historique ; **PIDs** dans les métriques live ; **disque hôte** (encart via **`GET /api/v1/metrics`**) ; fusion **fichiers + `container_metrics_snapshots` + session** via **`loadServerHistoryPoints`** ; graphes extraits dans **`MonitoringServiceHistoryCharts`** ; tests **`serviceHistorySources`**, **`serviceHistoryChartModel`**, **`page.test.tsx`** + mock **`/api/v1/metrics`** (`npm run test:service-detail-page`, `npm run test:service-history-model`).

**À faire ensuite (ordre raisonnable)**  

- [ ] **A1 — lot parent** : cocher la case principale **A1** seulement quand **A1d / A1e / A1g** auront avancé selon **ton** objectif produit (ou quand tu auras explicitement décidé de reporter le reste dans **`docs/BACKLOG.md`**).

- [x] **A1c — partiel (étendu 07/04)** : **`next/dynamic`** pour **`MonitoringServiceHistoryCharts`** (détail service) ; **`SystemCpuMemoryAreaCharts`** + **`SystemCpuNetworkCorrelationChart`** (**Performances**) ; **`SystemCpuNetworkCorrelationChart`** (**Analytics réseau**). **Reste** : extraire / lazy les gros **`LineChart`** encore inline (**hub analytics**, **containers**, **statistiques**) si le bundle reste trop lourd.

- [ ] **A1d** — Réutiliser le **patron** détail service sur : **`/backoffice`** (mini-séries si API OK) ; **`analytics/*`**, **`/backoffice/statistics`** (axes, gaps, légendes) ; liste **`/backoffice/services`** : **fait (partiel)** — **`title`** ligne + **jauge CPU** sous le nom (pas de Recharts) ; suite = sparkline **historique** si tu valides l’API.

- [ ] **A1e** — Brush / zoom ; couleurs + légendes + **`rechartsTooltipTheme`** partout ; aligner **`maxPoints`** / sous-échantillonnage avec **Performances**. **Partiel** : tooltips **`performances/containers`** (graph mémoire multi-conteneurs) alignés thème ; **`/backoffice/statistics`** vue d’ensemble (**`page.tsx`**, **07/04**).

- [ ] **A1f** — Contrat API **PIDs** stable (ex. `docker top` ou champ agrégateur versionné) **si** tu en as besoin côté produit ; aujourd’hui **instantané** liste services via **`GET …/metrics`** / services.

- [x] **A1g — partiel (07/04/2026)** : encart **sources d’historique** (session / fichiers agrégateur / Postgres) sur **`services/[serviceName]/page.tsx`**. **Reste A1g** : même logique de libellés sur **autres** pages monitoring (**A5** global).

**Qualité / CI (hors « feature » mais bloque parfois)**  
Dette **`npm run type-check`** : **`ERRORS.md`** ; journal : **`make type-check-frontend-log`**.
- [x] **Type-check frontend (07/05)** : prédicats `ms != null` corrigés sur **`performances/latency/page.tsx`** et **`performances/page.tsx`** (tri sûr), typage serveur ajusté sur **`frontend/src/instrumentation.ts`** (`self` côté Node), puis **`tsc --noEmit`** repassé **vert**.

**Roadmap sécurité** (liée mais hors A1) : **B11** / **B12** — **`PLAN.md`** / **`STATUS.md`**.

- [ ] A2 — Logs tous services + filtres — **partiel (21–22/04)** : **`/backoffice/services/logs`** + **`centralMetricsService.getServiceLogs`** + agrégateur **`since`/`until`** ; filtres **niveau / type / texte** ; **`(development)/services/applications`** (onglet logs) + **`(development)/services/backoffice/[serviceName]`** → **`/api/v1/docker/service/…/logs`**. **Suite A2 (22/04)** : **`api-gateway`** — **`dockerLogsQuery.js`** + **`normalizeDockerLogsQuery`** (proxy + réponse **`query`**) ; **`admin/logs/*`** ; **Jest** `tests/dockerLogsQuery.test.js` (incl. construction query proxy — **complément** : smoke curl / **`make tests`** avec stack). **Infra** : **log-collector-c** écoute **3019** dans le conteneur (**5099** hôte) ; **`make rebuild-log-collector-c`** après pull. **Reste** : smoke / E2E admin ; **Loki** → **`docs/BACKLOG.md`** si hors sprint.
  - [ ] **A2 suite persistance fichiers** : en plus de la **BDD** (`aggregatedLog` / persistance agrégateur), **archives compressées rotatives** (gzip) par service ou par jour, index minimal pour recherche rapide ; exposition ou doc **`make`** / UI **`/backoffice/statistics/log-stats`**.
- [ ] A3 — Vues détail service : corrélation logs techniques × sécurité (**partiel** : encart liens sécurité + logs centralisés sur la page détail ; reste : vue unifiée / timeline / API si besoin).
- [x] A4 — Synthèse pipeline dans `ERRORS.md` (§ Pièges + pipeline) ; **à réviser** après A2–A3.
- [ ] A5 — **Historique enregistré** : UI qui distingue **temps réel Docker** / **snapshots fichiers** / **persistence BDD** ; brancher les séries déjà stockées sur détail service + pages monitoring liées (analytics, stats, liste services) ; **suite** (non pressé) : encore plus de panneaux sur détail service + pages « performances ». **Suite 07/04 (partiel)** : timeout historiques + clamp `limit` + **localStorage** période partagée performances/réseau/conteneurs ; **affichage heure** : `formatLocalDateTime` dernier point ; **`normalizeMetricRows`** (`analytics.service.ts`) = **`timestampMs`** dérivé de l’**ISO** quand parseable (corrige JSON incohérent) ; tests **`date-metrics-display.test.ts`** + **`analytics-metric-rows-normalize.test.ts`** ; **`timestampMs`** API + **`metricRowToTimeMs`** / **`timeMs`** sur graphes ; **`parseChartTimestamp`** `{ value }` ; **`injectMetricTimeGaps`** ; **`docker-compose.yml`** `postgres` **TZ/PGTZ** ; **SQL `system_metrics`** : **`AT TIME ZONE`** = **`POSTGRES_SYSTEM_METRICS_TZ`** (comme Postgres) + **`make restart-metrics-recreate`** / **`monitoring-clock-refresh`** si besoin — **à valider** sur ta machine (graph aligné horloge locale) ; étendre **user-analytics** / **application** si besoin.

---

## Lot B — Sécurité

**Vision d’ensemble (tout le périmètre « sécurité » — où ça vit dans le repo)** :

| Thème | Rôle / implémentation typique | Lots / doc |
|--------|-------------------------------|--------------|
| **Firewall, WAF, règles, iptables** | `security-service`, routes proxy **`/api/v1/security/*`** via **gateway** ; UI **`/backoffice/security/firewall`** ; vérif **`make security-live-check`** | **B5**, **B10** ; `scripts/security/` |
| **Politiques (IPs, WAF)** | Données **security-service** + écrans **Politiques / Menaces** ; application : règles persistées puis **application** côté service (firewall controller) + trafic entrant filtré **gateway** / service selon archi | **B1–B3**, **B10** |
| **Menaces & analyse temps réel** | Logs **`eventType`**, table menaces, **`networkThreatDetector`** ; pages **Analyse**, **Réseau**, vue d’ensemble | **B3**, **B4**, **B6**, **B10** |
| **Logs techniques utiles enquête** | **`/backoffice/services/logs`** (metrics-aggregator) + **corrélation** avec sécurité | **A2**, **A3**, **B8** |
| **Mot de passe (complexité, reset)** | **`auth-service`** (ex. `min: 6` aujourd’hui), emails reset ; durcissement = évolution produit + **B7** audit si besoin | Auth + **B7** ; **`.env.example`** |
| **Déploiement / secrets / prod** | Compose, **`PREPROD_PRODUCTION_CHECKLIST.md`**, **`INTRUSION_DETECTION_ENABLED`**, secrets forts ; **B14** durcissement **`docker-compose`** / **`docker.sock`** / Redis / non-root | **B5**, **B6**, **B14** ; lot **G** ; **`docs/security/COMPOSE_RUNTIME_HARDENING.md`** |
| **Sauvegardes & continuité** | Pas encore le même code que le firewall — **lot G** | **G1–G7** |

- [x] B1 — Cohérence : `blockOrigin` sur IPs bloquées + affichage firewall / analyse (affiner si besoin).
- [x] B2 — Test IP lab + **refus blocage de sa propre IP** côté API ; messages UI test vue sécurité.
- [ ] B3 — **Partiel (`PLAN.md`)** — Légende vue sécurité + panneaux Analyse (détections / manuels+lab / auto) ; **reste** : badges **origine** partout (firewall), distinction détection vs blocage **homogène** sur toutes les vues.
- [ ] B4 — **Partiel (`PLAN.md`)** — Réseau : corrélation % + hint actionnable ; **reste** : cas **« unknown »** résiduels, messages API / empty states.
- [x] B5 — `make security-live-check` : auth firewall/WAF côté security-service + secret interne scripts ; types menaces `generate-test-threats` alignés sur l’API (avril 2026).
- [ ] **B6** — **Corrélation** : `X-Request-Id` / `X-Correlation-Id` — **partiel 22/04 + 06/05** : middleware gateway **`requestCorrelation.js`** (ALS + contexte requête dans les logs WARN/ERROR, central logging optionnel), proxy + CORS + **`frontend/src/lib/api.ts`** ; **intrusionDetector** enrichi si réactivé. **Reste** : morgan / middleware sur **chaque** microservice (hors déjà livrés) + recherche côté **security-service** + mobile + NTP prod.
- [ ] **B7** — **Audit append-only** : actions sensibles (admin login, exports, IP unblock, test-data destructif, rôles) ; pas de secrets en clair ; lecture réservée rôle élevé.
- [ ] **B8** — **Backoffice** : vue **investigation / post-incident** sous **Sécurité** (filtres acteur, période, type) ; croisement menaces + audit + logs ; export horodaté + **hash** pour chaîne interne ; doc **`docs/security/`** (procédure, rôles). **Suite demandée 12/06** : sur une fiche menace (ex. brute force lab `198.51.100.42`), corréler vraiment `security_logs`, `aggregated_logs`, tentatives intrusion, connexions réseau, `requestId`/`correlationId`, endpoints touchés, comptes de test impactés, conteneur/service, IP source/destination, ports/protocoles et score max ; ne plus afficher un `Risque max 0` sans explication quand la menace est high/critical ; afficher la raison exacte quand endpoints/comptes/logs restent non corrélés. **12/06 partiel** : score retenu/source du score + endpoints/méthodes/risques issus de `intrusionAttempt`/`dDoSAttack`; reste : corrélation réelle gateway/auth/app logs, comptes et timeline multi-sources.
- [ ] **B9** — **Mobile** : erreurs + **événements sécurité** (session révoquée, échecs auth, etc.) vers API **rate-limit** ; corrélation user/device/IP ; rappel limite appareil compromis — croiser **lot D1** / **D2**.
- [ ] **B10** — **UX backoffice sécurité** : reprendre **`/backoffice/security/**`** (vue d’ensemble, firewall, analyse, réseau, etc.) — lisible, métriques **alignées API**, empty states, unités / fenêtres temporelles explicites ; préparer emplacements **investigation (B8)** ; politique mots de passe **côté UI** (texte d’aide + lien doc) une fois règles auth figées.
- [ ] **B10 suite — incidents temps réel + score** : texte d’encart déjà posé (données BDD / démo / tests) ; **compléter** : lien vers **`ERRORS.md`** (faux positifs résiduels, **`ENOTFOUND`** si stack partielle) ; **exposer proprement la pondération du score** (aujourd’hui **`localStorage`** **`securityScoreWeights`** dans **`page.tsx`** vue sécurité) — idéalement UI « poids par famille » + persistance serveur ou doc admin si refus du localStorage seul. **Suite demandée 12/06** : rendre le tri des logs explicite dans l’UI (`timestamp desc` côté API = plus récent d’abord, plus ancien ensuite), ajouter un vrai contrôle/label de tri si besoin, éviter les filtres locaux trompeurs sur une seule page paginée, et préparer les emplacements forensics B8 sur les fiches menace/incidents. **12/06 point porteur à traiter avant la suite** : dans `/b4ck0ff1ce/security/logs`, la colonne **Liens** ne doit pas afficher `—` pour des événements qui représentent une menace/incidence corrélable ; afficher un lien **Menace liée** / **Voir menace** vers la fiche précise dès qu’un `metadata.threatId`, une menace créée par le flux lab/firewall, ou une corrélation fiable IP+endpoint+fenêtre temporelle existe. Pour les logs non corrélés, afficher une raison explicite (`Aucune menace liée`, `log lab non rattaché`, `corrélation absente`) au lieu d’un tiret silencieux. **12/06 partiel branche `fix/security-b10-forensics-score`** : filtres serveur `eventType`/`q`/`order`, label de tri UI, bouton `Date` triable, facettes API `/security/logs/facets` issues des logs récents, filtre Logs appliqué explicitement par bouton pour éviter le rechargement à chaque frappe, 6 logs lab générés pour test, et score retenu `effectiveRiskScore`/`riskSource` sur fiche menace ; reste : timeline B8 complète, endpoints/comptes réellement corrélés, politique de pondération persistée. **13/06 partiel `fix/security-logs-threat-links`** : validation CUID + existence BDD, auto-menace pour logs `critical`/`error`/`warning`, fusion Incidents/Menaces, suppression double nav `SecuritySubNav` ; Jest **6/6** + **3/3** ; reste validation porteur 404 lab et tests E2E/proxy-VPN lab.
- [ ] **B10 UX filtres — refonte backoffice complète des filtres/autocomplétions** : **12/06 branche mergée dans `dev`** : base `useAppliedFilters` + `FilterBar` + `FacetAutocompleteField` + options centralisées. Pages migrées : Logs sécurité, Menaces, Email Monitor, Incidents, Statistics log-stats, **Réseau sécurité**, **Utilisateurs**, **Rapports de tests**, **Logs services** (`/services/logs` → `service-logs` + détail service). **Reste** : liste Services (filtres CPU/mémoire), sync URL optionnelle, tests Playwright par patron partagé, puis Lot M prédictif.
- [ ] **B10/P1C — nettoyage UX sécurité et réseau** : **13/06 demande porteur** — supprimer les textes d’aide non demandés sur Incidents/Menaces, Firewall, Réseau et Alertes email ; remplacer les tableaux réseau peu actionnables par une vraie lecture source de connexion (IP publique réelle, reverse proxy, Docker/interne, VPN/proxy/Tor/datacenter si sourcé, confiance, lien menace/log) au lieu de simples `host-network`, `host-local`, ports éphémères et `0.0.0.0`. Vérifier que les libellés restent lisibles clair/sombre sans grands pavés explicatifs.
- [ ] **B10/B8 — enrichissement menace et télémétrie de réputation IP** : vérifier que les champs localisation/RDAP/ASN/VPN/proxy/Tor sont fiables, sourcés, cache/rate-limités et testables sans dépendre d’un service gratuit instable. Ajouter jeux de données lab/mocks pour RFC5737 (`198.51.100.0/24`, `203.0.113.0/24`, `192.0.2.0/24`) et cas VPN/proxy/Tor/ASN connus ; préciser `confiance`, `source`, horodatage d’enrichissement et raison d’inconnu. Tests actifs via proxy/Tor réels seulement sur lab/préprod autorisée, jamais en prod ni contre tiers non autorisés.
- [ ] **B12/B8 — réponse automatique high/critical bornée** : définir la politique de blocage automatique des menaces `critical/high` (seuils, durée, source de confiance, exemptions IP privées/lab, rollback, audit B7, email B11, bouton manuel `Bloquer cette menace`). Tant que la politique n’est pas validée, l’UI doit distinguer clairement `Blocage recommandé` vs `Bloqué automatiquement` et expliquer pourquoi une menace high reste `Non bloqué`.
- [ ] **B10/B11 — WAF et Politiques sécurité réels** : vérifier en fin de lot/préproduction que l’activation/désactivation des règles WAF/firewall produit bien l’impact attendu (payload bloqué, IP bloquée/débloquée, règle désactivée sans effet, logs/menaces/alertes cohérents). Repenser **Politiques** comme couche plus large que Firewall/WAF/IP bloquées : posture sécurité, seuils, réponses automatiques, exemptions, notification, audit, réauth et validation porteur.
- [ ] **B11 (spec / backlog)** — **Emails de rapport sur incidents critiques** : configuration (**SMTP** déjà cadré préprod **`PREPROD_PRODUCTION_CHECKLIST.md`**, **`CRASH_REPORT_EMAIL`**, etc.) étendue aux **alertes métier** — failles / menaces **très graves**, événements **firewall** majeurs, **indisponibilité** d’un service ou d’une partie du projet ; destinataires, **seuil de gravité**, **rate-limit** / regroupement pour éviter le spam ; templates ; traçabilité **B7** / investigation **B8** ; pas d’exposition de secrets dans le corps du mail.
- [ ] **B11/P1C — emails backoffice lisibles et testables** : reprendre Email Monitor / détails emails / contexte technique en clair et sombre (contenu HTML email blanc dans interface sombre, textes gris/blanc illisibles, zones redigées). Les emails `FAILED` doivent afficher les mêmes informations utiles que les `SENT` plus la raison d’échec fournisseur/SMTP. La configuration SMTP doit afficher les valeurs réellement utilisées depuis l’environnement (host, port, secure, from/reply-to, provider) sans valeurs OVH codées en dur. Déliverabilité : rétablir les tests d’envoi (test, reset password, vérification) via le pipeline réel `maily.ovh`/SMTP et diagnostiquer le `POST /api/v1/emails/test` 500.
- [ ] **P1C — navigation Tests/Rapports et routes HTTPS** : refactoriser la hiérarchie Développement pour que les URLs suivent le menu (`/b4ck0ff1ce/tests` vue d’ensemble, sous-routes cohérentes, rapports tests/parcours groupés). Correctif partiel 13/06 : en HTTPS dev, `/api/test-reports/*`, `/api/test/*` et `/api/security/cve-*` doivent rester proxifiés vers le frontend Next, pas vers l’API Gateway, sinon `/api/test-reports/all` renvoie 404.
- [ ] **P1C — MailHog intégré backoffice** : remplacer l’entrée externe “MailHog (interface)” par une page backoffice qui embarque MailHog dans la zone de contenu (iframe/proxy dev sécurisé), sans ouvrir hors interface ni surcharger la navigation gauche.
- [ ] **P1B — historiques agrégés hors période courante** : sur Temps de réponse et les autres pages Performances/Statistics, vérifier pourquoi les plages personnalisées / navigation dans le temps ne chargent pas l’historique agrégé attendu. Couvrir API metrics-aggregator, query params, timezone, bornes date et rendu Recharts.
- [ ] **B14/A1 — audit cAdvisor/Prometheus vs monitoring-agent-rs** : vérifier si cAdvisor et Prometheus sont encore nécessaires depuis que le système Rust/metrics-aggregator couvre métriques conteneurs/services. Si doublon confirmé, retirer proprement services, dépendances, docs, dashboards et compose sans casser les rapports historiques.
- [ ] **B12 (spec / backlog)** — **Analyse sécurité en quasi temps réel** mais **très peu coûteuse** : boucles légères (intervalles **adaptatifs**, **backpressure**, limites mémoire sur tampons / fenêtres d’événements), éviter le **polling** agressif sur grosses tables ; option **SSE / WebSocket** **throttled** ; cohérence avec **B3–B4–B10** et charge côté **security-service** + gateway.
- [ ] **B13 (spec / backlog)** — **Préparation sécurité post-quantique (PQC)** : cadrage initial documenté dans `ROADMAP_SECURITE_API_ET_BACKOFFICE.md` (phase 8) ; lancer ensuite la crypto-agilité projet.
  - [ ] **Inventaire crypto** : TLS (entrant/sortant), JWT/JWS, hash/signatures, secrets de service, sauvegardes chiffrées, mobile/API.
  - [ ] **Classification des données** : prioriser les flux “longue confidentialité” (risque **harvest now, decrypt later**).
  - [ ] **Plan de transition** : cibles algorithmiques hybrides/PQC (quand supportées), rotation certs/clés, compatibilité clients et rollback.
  - [ ] **Tests dédiés** : non-régression fonctionnelle, perf/latence handshake, interop environnements, procédures incident.
  - [ ] **Documentation ops** : runbook migration crypto + checklists préprod/prod.

- [ ] **B14 — Durcissement Docker Compose & runtime** : feuille de route **`docs/security/COMPOSE_RUNTIME_HARDENING.md`** (priorisation **BX1–BX14**). **Ne pas** casser **`make up-full`** sans migration documentée (secrets, Redis, clients `REDIS_URL`, proxy socket). **Fait (6 mai 2026, partiel)** : WAF gateway piloté par **`${WAF_ENABLED:-true}`** + **`.env.example`** **`WAF_ENABLED=true`** (comportement type prod ; **`false`** seulement pour diagnostic local) ; **`METRICS_API_KEY`** via variable ; **`JWT_SECRET`** sur **profile-service** ; healthcheck **postgres** aligné **`POSTGRES_USER`** / **`POSTGRES_DB`** ; **frontend** fallback **`HOST_IP=localhost`** ; **security-service** **`no-new-privileges`** ; suppression **`server.js.backup.*`** + **gitignore** / **`backend/.dockerignore`**. **Reste** :
  - [ ] **BX1** — Retirer les **fallbacks secrets** dangereux du chemin **prod** (fichier compose dédié ou variables **`${VAR:?message}`** après pipeline secrets) ; garder un chemin **dev** explicite documenté.
  - [ ] **BX1a** — Secret tooling : ajouter une cible Makefile qui génère/rotate `JWT_SECRET`, `JWT_REFRESH_SECRET`, `SECURITY_INTERNAL_SECRET`, `METRICS_API_KEY`, mots de passe admin/test, sans affichage clair en prod ; sortie dev uniquement avec confirmation explicite.
  - [ ] **BX1b** — Credentials mobiles/tests : retirer les mots de passe de `NEXT_PUBLIC_*` et passer par un canal test côté serveur/runner, car tout `NEXT_PUBLIC_*` est exposé au navigateur/build.
  - [ ] **BX2** — Remplacer le montage direct **`/var/run/docker.sock`** par **socket proxy** (ou API limitée) pour les services qui n’en ont pas besoin au niveau root-hôte.
  - [ ] **BX3** — **Non-root** pour **monitoring-c** / **metrics-aggregator** (images + permissions) ; **security-service** : documenter l’exception **iptables** ou alternative.
  - [ ] **BX4** — (Suivi) WAF gateway : valider en **`make tests`** / E2E avec **`WAF_ENABLED=true`** et ajuster règles si besoin.
  - [ ] **BX5** — **Redis** : **`requirepass`** / ACL + mise à jour de **tous** les **`REDIS_URL`** / clients ; tests de non-régression session / intrusion detector.
  - [ ] **BX6** — (Suite) **`.dockerignore`** aux autres contextes de build si besoin ; interdire tout nouveau `*.backup.*` versionné.
  - [ ] **BX7** — **ADMIN_*** bootstrap **auth-service** : réduire exposition (secret Docker / init one-shot / doc prod).
  - [ ] **BX8** — (Suivi) Vérifier **tous** les services avec **`jwt.verify`** : même **`JWT_SECRET`** que l’émetteur ; pas de service « oublié » dans les profils compose.
  - [ ] **BX9** — (Suivi) **`no-new-privileges`** sur **security-service** : valider **`make security-live-check`** + firewall réel après durcissement.
  - [ ] **BX10** — (Suivi) **Frontend** : aucune IP privée « maison » en fallback dans les autres fichiers (grep périodique).
  - [ ] **BX11** — **`read_only: true`** + **`tmpfs`** là où l’écriture disque n’est pas requise (service par service).
  - [ ] **BX12** — Limites **CPU/RAM** (Swarm **`deploy.resources`**, Portainer, systemd, ou doc équivalente).
  - [ ] **BX13** — (Fait partiel postgres) — vérifier les **autres** healthchecks avec identifiants hardcodés.
  - [ ] **BX14** — Politique de **redémarrage** (backoff / max attempts) en orchestration prod ; documenter le risque **crash loop** avec **`unless-stopped`**.

---

## Lot C — Suivi-intérim & données test

- [ ] C1 — `/backoffice/suivi-interim` : données utiles, flux agences ↔ candidatures — **21/04** : erreur API affichée + **Rafraîchir** + lien **test data** (`SuiviInterimContent.tsx`) ; **reste** données / filtres / validation porteur.
- [ ] C2 — Procédure claire base principale vs base test (admin préservé) — **partiel** : **`make env-check`** / **`make env-append-missing`** ; doc **`docs/database/MIGRATIONS_ET_BASES.md`** à compléter.
- [x] C3 — `generate-test-data` / clear / marquage : **partiel livré** — endpoints **`tag-likely`**, **`summary`**, clear **Document** + **EmailLog** ; UI **TestDataTab** + **`/backoffice/test-data`** (balanced) ; **`--balanced`** / **`_balanced`** ; validation **porteur** + éventuels compteurs « stats uniquement test » encore **à faire**.

---

## Lot D — Mobile & observabilité

- [ ] D1 — Schéma d’événement crash / erreur normalisé (champs obligatoires) ; **aligner** champs avec **B9** (événements sécurité mobile si même pipeline).
- [ ] D2 — Chaîne complète jusqu’aux vues analytics / logs admin.
- [ ] D3 — Libellés et filtres compréhensibles dans stats / monitoring.
- [ ] **D4 — Analytics utilisateur (événements) : périmètre & récupération** — **cadrage produit + technique** (voir **`PLAN.md`** lot **D**, note analytics) : (1) **qui** est tracké — **admins / navigateur backoffice** (parcours UI admin, pas confondre avec l’app mobile candidat) **vs** **utilisateurs finaux sur l’app mobile** ; (2) **quoi** — interactions **au-delà du seul binaire mobile** : parcours **email** (lien d’inscription, **code / token** de validation compte, **demande et confirmation de reset mot de passe**) = événements **côté serveur** (auth-service, logs mail, tables dédiées) exploitables pour stats / audit sans dépendre uniquement du SDK mobile ; (3) **où en UI** — **`/backoffice/user-analytics`** et pages **Statistiques & Monitoring** cohérentes avec ce périmètre (libellés « web admin » vs « app mobile » vs « parcours email / auth ») ; (4) **alignement** tables **`user_events`** / API (voir **`ERRORS.md`** si table ou route manquante) + croisement **B7** (audit actions sensibles admin). **Priorité** : stack **locale** (`make up-full`, Postgres OK au **`make status`**) — pas de livrable **prod** pour l’instant.
- [ ] **D5 — Traces et signalements bout-en-bout** : instrumenter les parcours critiques mobile/web (activité utilisateur, retours, signalements), stocker les événements normalisés, puis exposer des vues actionnables en backoffice (analytics + sécurité + logs corrélés).

---

## Lot E — Documentation

- [x] `STATUS.md` — structure de lecture + tableau lots A–F + liens (avril 2026).
- [x] `ERRORS.md` — § Pièges dashboard + pipeline + lignes chantier A/B (avril 2026).
- [x] `project/FONCTIONNALITES.md` — § 4.1 dashboard détaillé + date avril 2026.
- [x] `RESOLUTIONS.md` — entrée avril 2026 (vue d’ensemble observabilité).
- [x] **`security/STATS.md`** — gabarit **CVE / dépendances** (services, front, mobile, Docker) + script boucle `npm audit` — **22/04** ; remplir tableaux après audits.
- [ ] `ERRORS.md` — relecture complète après lots A/C (échecs tests, nouvelles erreurs actives).
- [ ] `RESOLUTIONS.md` — derniers correctifs sécurité (lot **B**) / monitoring & logs (lot **A**) / intérim.
- [ ] `mobile/PROCESSUS_APPLICATION_MOBILE_ET_API.md` — synchro avec l’état API + mobile.
- [ ] `project/FONCTIONNALITES.md` — ajuster ce qui est livré vs prévu (y compris § **4.4** lot **G** quand implémenté).
- [ ] `docs/BACKLOG.md` — éviter doublons avec ce fichier ; renvoyer vers PLAN pour le chantier structuré.
- [ ] Revue ciblée des sous-dossiers `docs/` (architecture, API, DB, sécurité, tests).

---

## Lot F — Validation

- [ ] F1 — Rejouer **`make tests`** avec **`make up-full`** + **`.env`** (**`API_GATEWAY_URL=http://127.0.0.1:5002`** ou port réel) ; analyser **`tests/results/<ts>/report.html`**. **17/04** : doc **`ERRORS`/`STATUS`/`FONCTIONNALITES`/`RESOLUTIONS`** ; code **`dockerHostUrl.js`**, **`test-api-specific.sh`**, perf, gateway health — **à confirmer** sur ta machine.
- [ ] **F1 bis — run 05/05/2026 (`20260505-113157`)** : corriger d’abord `application-service` (Prisma `application.create/findFirst`), puis rejouer `make tests` pour valider la disparition des 4 KO `api-backend-script` + KO `user-journey` + KO `playwright-data-crud`.
- [x] **F1 bis — run 05/05/2026 (`20260505-113157`) (partie backend candidatures)** : fallback legacy `application-service` appliqué ; `Create Application` repasse sur les scripts ciblés. **Reste** : KO liés à `dashboard-service` indisponible (503/ENOTFOUND) et suites Playwright/Jest.
- [ ] **F1 bis.1 — disponibilité dashboard en campagne tests** : fiabiliser `dashboard-service` pendant `make tests` (ordre de démarrage, health, DNS compose, dépendances) pour supprimer les 503 sur `Get Statistics`, `Dashboard Statistics`, `Analytics Events/Stats`.
- [x] **F1 bis.1 — disponibilité dashboard en campagne tests (socle)** : `dashboard-service` en `restart: unless-stopped` + précheck `ensure_dashboard_service_ready` dans `run-all-tests-with-reports.sh` (auto-start + attente `/health`).
- [x] **F1 bis.1.a — validation post-correctif dashboard** : run complet relancé (`tests/results/20260505-162341`) ; disparition confirmée des `ENOTFOUND dashboard-service` et des `503` dashboard/analytics sur les scripts API (`api-backend-script` 51/51, `user-journey` 14/14).
- [ ] **F1 bis.1.b — remise à niveau BDD locale (`Application.isTestData`)** : synchroniser le schéma (`make db-push-all`) puis vérifier que les logs Postgres n’émettent plus `column Application.isTestData does not exist` durant create/delete candidature.
- [ ] **F1 bis.1.c — validation archives/corbeille admin après fallback legacy** : rejouer les scénarios backoffice `/backoffice/archives` et `/backoffice/trash` (UI + API gateway) et confirmer disparition des `500` liés à `Application.isTestData` dans `archive.controller.js`.
- [ ] **B12 bis — calibration anti faux-positifs intrusion** : mesurer le bruit sécurité réel après ajustements (seuil brute-force=20, exclusion headless, admin authentifié) ; définir seuils par endpoint (`login` vs `register`) + fenêtre temporelle adaptée trafic NAT.
- [x] **F1 bis.1.b — remise à niveau BDD locale (`Application.isTestData`)** : `make db-push-all` + ajout explicite colonne/index `Application.isTestData` en Postgres ; plus d’erreur `isTestData` sur logs récents.
- [x] **B12 bis — calibration anti faux-positifs intrusion (itération 1)** : brute-force limité à `/auth/login`, seuil défaut relevé (`BRUTE_FORCE_THRESHOLD=40`), exclusion headless/tests, skip unauthorized_access pour requêtes authentifiées.
- [ ] **B12 ter — calibration anti faux-positifs intrusion (itération 2 terrain)** : observer 24-48h en charge réelle (NAT/proxy partagés), puis ajuster `BRUTE_FORCE_THRESHOLD` et éventuelle fenêtre `window` selon ratio faux positifs / vrais incidents.
- [x] **F1 ter — API Gateway Jest** : assertions CORS/auth/logs alignées ; vérification locale **`backend/api-gateway`** = **22/22** verts (`/usr/bin/npm test -- --runInBand --watch=false`, 07/05).
- [x] **F1 quater — Frontend Jest analytics** : `tab-components.test.tsx` aligné sur le hub actuel (`analytics/page.tsx`) ; vérification locale `npm run test:analytics-pages -- --runInBand` = 5 suites / 20 tests verts.
- [x] **F1 quinquies — Playwright E2E login + suivi-intérim** : stabilisation `detectLoginOutcome` (tolérance recréation de contexte JS pendant navigation), sélecteurs menu suivi-intérim déjà robustifiés ; vérification locale `npx playwright test tests/e2e/login.spec.ts tests/e2e/suivi-interim.spec.ts` = **10 pass / 3 skip / 0 fail**.
- [x] **F1 sexies — Mobile E2E** : mode **smoke** activé par défaut dans l’agrégat (`PLAYWRIGHT_MOBILE_MODE=smoke`, timeout 240s) + suite **full** conservée à la demande ; validation locale smoke: `scripts/testing/playwright-mobile-e2e.sh` = **3 pass** (iPhone 13 Pro, auth mobile).
- [x] **F1 septies — Frontend E2E smoke dans l’agrégat** : `scripts/testing/playwright-frontend-e2e.sh` supporte `PLAYWRIGHT_FRONTEND_MODE=smoke|full` (smoke par défaut) + timeout agrégat réduit à 420s ; validation smoke locale = **31 pass / 3 skip / 0 fail** (`login + api-e2e + suivi-intérim`).
- [x] F1b (partiel) — **`Status: 000`** script API : **`mktemp`** + normalisation URL ; perf : **`exit 1`** si échecs ; reste : **intégration / sécurité** tolérants **`ENOTFOUND`** (durcir plus tard).
- [ ] F2 — Rédiger le récap : fait / reste / risques / prochaines priorités (peut aller en fin de `PLAN.md` ou `STATUS.md`).
- [x] F2 — Rédiger le récap : fait / reste / risques / prochaines priorités (mise à jour **`STATUS.md`** avec run complet `tests/results/20260505-171106`, succès smoke frontend/mobile, et note d’écart de comptage agrégé).
- [x] F3 — **Couverture `tests/services/` gateway + infra directe** : ajout de `tests/services/smoke-gateway-services.js` (health gateway/auth + routes typiques applications, companies, contacts, interviews, calls, events, followups, notifications, dashboard, profile, workflows, security), puis `tests/services/test-metrics-aggregator.js` et `tests/services/test-deployment-service.js` pour les exceptions non-gateway. Validations réelles 12–13/05 : gateway **14/14**, metrics-aggregator **4/4** (`/health`, auth `401`, `X-API-Key`, docker services), deployment-service **3/3** (`/health`, `/health/detailed`, `/api/v1/deployments`). Branché automatiquement dans `run-all-tests-with-reports.sh`.
- [x] F3b — **`tests/performance/test-load-advanced.js`** : scénarios **companies**, **applications**, auth et spike alignés sur la gateway via `normalizeGatewayUrlForHost` / `API_GATEWAY_URL`; suppression des bases `localhost:300x`; `run-all-tests-with-reports.sh` lance maintenant aussi le test en `PERF_LIGHT=1`. Validation réelle 12/05 : `API_GATEWAY_URL=http://127.0.0.1:5002 PERF_LIGHT=1 node tests/performance/test-load-advanced.js` = **46/46 succès**, rapport `tests/reports/load-test-advanced.json`.

---

## Lot H — Hygiène dépôt : `scripts/`, `tests/`, `tools/`, `services/`, mobile

Chantier **transversal** (hors périmètre fonctionnel A–G seul) : rangement, usage réel, doc. Cocher au fil de l’eau.

### H1 — Dossier **`scripts/`** (~90 fichiers à la racine du dossier + sous-dossiers `db/`, `monitoring/`, `testing/`, etc.)

- [ ] **Cartographie** : pour chaque script référencé par **`Makefile`** / **`package.json`** / CI / doc, noter l’appelant dans un **`scripts/README.md`** (table *script → usage*).
- [ ] **Sous-dossiers** : regrouper les scripts « one-shot » ou par domaine (**`scripts/legacy/`**, **`scripts/one-off/`**) sans casser les chemins **`Makefile`** ; grep des références avant déplacement.
- [ ] **Morts ou doublons** : scripts jamais cités — archiver ou supprimer après confirmation (ne pas supprimer à l’aveugle).

### H2 — Dossier **`tools/`** (`adb-lib`, `emulator-controller`, etc.)

- [x] **Usage réel** : parcours mobile / ADB documentés dans **`STATUS.md`** et **`tests/user-journey/README.md`** ; **`make up-full`** peut lancer **`tools/emulator-controller`** (voir README du sous-dossier).
- [x] **README racine `tools/`** : **`tools/README.md`** — index vers **`adb-lib`** et **`emulator-controller`** (**24/04**).

### H2 bis — Dossier racine **`tests/`**

- [x] **Structure source (13/05)** : scripts racine non contractuels rangés par rôle — `tests/runners/` (`run-tests`, ancien runner complet), `tests/system/` (`setup`, `verify`), `tests/ci/` (checks CI/CD ponctuels), `tests/email/` (test Gmail manuel). Les configs contractuelles restent à la racine (`package.json`, `jest.config.js`, `jest.setup.js`, `playwright.config.ts`, `docker-compose.test.yml`).
- [x] **Tests email auth-service (13/05)** : les scripts manuels `test-email-diagnostic.js`, `test-email-python.js`, `test-email-verification.js` ne vivent plus dans `backend/auth-service/tests/email`; ils sont centralisés sous `tests/email/auth-service/`. Le conteneur `auth-service` monte ce dossier sur `/app/tests/email` pour garder les commandes `make test-email-*` compatibles.
- [x] **Legacy test runner supprimé (13/05)** : l’ancien `backend/test-runner-service.js` n’était référencé par aucun compose/CI/route et devenait ambigu avec `tests/services/`; il est retiré du suivi Git.
- [x] **Test d’intégration ad hoc rangé (13/05)** : `backend/scripts/test-new-features.js` est déplacé vers `tests/integration/test-new-features.js`.
- [x] **Anomalie `tests/tests/`** : ancien rapport sécurité déplacé vers `tests/results/security-report-20260224-160806.json`, suppression du dossier imbriqué.
- [ ] **Tests Jest colocés services** : les vrais tests unitaires/intégration `backend/*-service/tests/*.test.js` restent temporairement colocés car `npm test` de chaque service dépend de son `jest.config.js` local. Migration possible ensuite vers `tests/backend/<service>/` avec mise à jour des configs Jest service par service.
- [ ] **Artefacts historiques** : `tests/results/` et `tests/performance-benchmark/` contiennent beaucoup de rapports suivis Git ; ne pas supprimer en vrac. Prochaine étape : décider archive externe, conservation limitée ou purge contrôlée avec accord.

### H3 — Dossier racine **`services/`** (microservices Python FastAPI)

- [x] **Audit + archivage contrôlé (13/05)** : les anciens services Python de statistiques ne sont pas référencés par le compose/gateway actifs et ne contiennent pas d’app complète ; ils sont sortis de la racine vers `docs/archive/legacy-python-services/services/`. Le backend actif reste `backend/*-service`.
- [ ] **Suite** : décider si ces endpoints Python legacy doivent être supprimés définitivement ou réécrits côté services Node actifs avant suppression de l’archive.

### H4 — **`docs/archive/mobile/mobile-native-app/`** (ancien `mobile-native-app/`)

- [x] **Archivage contrôlé (13/05)** : le dossier racine `mobile-native-app/` ne contenait qu’un Dockerfile React Native sans app associée ; il est archivé sous `docs/archive/mobile/mobile-native-app/`.
- [ ] **Suite** : statuer séparément sur `flutter-mobile-app/` versus `mobile/` avant fusion/suppression, car `frontend/scripts/test-mobile.sh` pointe encore vers `flutter-mobile-app`.

### H4 bis — Racine `backend/src/`

- [x] **Archivage contrôlé (13/05)** : `backend/src/services/cache.service.js` était le seul fichier sous `backend/src/`, non importé, avec références internes obsolètes ; il est archivé sous `docs/archive/legacy-backend-root-src/services/cache.service.js`.

### H4 ter — Scripts sensibles legacy

- [x] **Suppression contrôlée (13/05)** : l’ancien `backend/scripts/database/create-admin-user.sh` contenait un compte personnel et un mot de passe faible codés en dur ; il est supprimé du suivi Git. La cible `create-admin-user` reste neutralisée et renvoie vers `create-admin-user` avec `ADMIN_EMAIL` / `ADMIN_PASSWORD`.

### H5 — Application Flutter **`mobile/lib/screens/`**

- [x] **Structure dossiers (24/04)** : `screens/admin/admin_screen.dart` ; `screens/jobbing/auth/*` ; `screens/jobbing/dashboard/` (`home_screen`, `analytics_screen`, `statistics_screen`) ; `screens/jobbing/applications/*` ; `screens/jobbing/calendar/events_screen.dart` ; `screens/jobbing/users/` ; dossiers dédiés **companies**, **contacts**, **interviews**, **followups**, **calls**, **search**, **trash**, **logs**, **test_data** — **`main.dart`** et imports internes (candidatures / auth) mis à jour.
- [ ] **Suite** : voir **« Suite & suivi explicite »** (renommages, barrel) ; smoke **`widget_test.dart`** y est suivi.

### H6 — Vision produit **dashboard** (mobile)

- **Utilisateur standard** : après connexion, **un** écran d’accueil type **dashboard** = **ses** statistiques, relances, entretiens à venir (données **scopées utilisateur**).
- **Administrateur** : **même point d’entrée** (même navigation de base) mais données et actions **étendues** (vue « tous les utilisateurs » / équivalent **backoffice** côté API — rôles **`ADMIN`** / **`SUPER_ADMIN`**). À cadrer avec l’API auth et les écrans **`admin_*`** existants.

---

## Lot G — Sauvegardes sécurisées, API, délocalisation, continuité (PCA/PRI)

Spec détaillée : **`PLAN.md`** § **G** ; fonctionnel : **`project/FONCTIONNALITES.md`** § **4.4** ; statut projet : **`STATUS.md`** § *Sauvegardes…*.

- [ ] G1 — Cadrage sécurité : modèle de menaces, clés (vault/KMS/fichier), rotation, rôles (`SUPER_ADMIN` + service interne) ; doc `docs/operations/BACKUP_AND_DR.md` (ou équivalent).
- [ ] G2 — API backup sous gateway : jobs, statut, historique, audit, rate limit, **non publique** sans contrôle réseau.
- [ ] G3 — Pipeline **chiffrement** des dumps (pas de clair durable sur disque partagé) + vérification d’intégrité.
- [ ] G4 — **Délocalisation** (S3-compatible, autre hôte) ; secrets uniquement serveur ; option lien téléchargement **TTL court**.
- [ ] G5 — **UI backoffice admin** : déclenchement manuel, état des jobs, messages sans fuite ; restauration **sandbox** / runbook avant prod.
- [ ] G6 — **RPO/RTO**, runbook de reprise, exercices de restauration documentés.
- [ ] G7 — Durcissement Docker/réseau/logs sécurité pour le worker backup.

---

## Vue d’ensemble `/backoffice` — améliorations futures (optionnel)

- [ ] Afficher un **horodatage** ou état « connecté au metrics-aggregator » sur la carte Performance.
- [ ] Exposer un **taux d’erreurs HTTP %** si le backend fournit ce ratio (en complément du débit /min).
- [ ] Clarifier encore **sessions vs utilisateurs actifs** selon le contrat exact de l’endpoint auth (libellé + tooltip ou doc API).

## Makefile `status` / `status-watch` (implémenté 21/04 — à valider chez le porteur)

- [x] **`make status-watch`** / **`make status-live`** : boucle = **`make status`** ; défaut **sans `clear`** ; **`CLEAR=1`** pour plein écran ; séparateur gris entre cycles si pas clear ; pied de cycle coloré ; résumé **0/0** explicite.
- [ ] Relire sur ta machine : légende **5098 → 8015** (**monitoring-c**), couleurs terminal, **`INTERVAL=30`** si besoin.

## Monitoring transversal (optionnel — aligné **PLAN A5**, non pressé)

- [ ] Même **légende live / snapshots / BDD** que le détail service sur **analytics**, **statistiques**, **liste services**.
- [ ] **Détail service** : panneaux supplémentaires (redémarrages, limites cgroup, comparaison à la moyenne stack) quand l’API le permet.
- [ ] Pages **« performances »** : enrichissements UX + données (voir **PLAN.md** § Améliorations futures lot A).

---

## Rappels produit (hors lots mais prioritaires — voir STATUS.md)

Ne pas confondre avec le chantier ci-dessus ; ce sont les **P0** globaux du projet :

- [ ] Mobile utilisable quotidien (parcours inscription → CRUD → relances).
- [ ] Suivi intérim côté **mobile** (toggle utilisateur) + polish backoffice.
- [ ] Déploiement VPS simple (P1) — guide **Portainer + Nginx Proxy Manager + OVH** : **`docs/deployment/VPS_PORTAINER_NPM_OVH.md`** (complète **`docs/deployment/portainer/README.md`**).

---

## Comment utiliser ce fichier

1. Cocher `[x]` quand la tâche est **réellement** mergée et vérifiable ; la validation porteur reste **`PLAN.md`** / **`STATUS.md`** (voir **« Lire ce fichier »** en tête).
2. Si une tâche devient du « plus tard », la **déplacer** vers **`docs/BACKLOG.md`** avec une courte justification.
3. Critères détaillés des lots : **`PLAN.md`**.

---

## Actions **manuelles** (porteur / infra — l’IA ne peut pas les valider à votre place)

Checklist détaillée et cohérente : **`docs/operations/PREPROD_PRODUCTION_CHECKLIST.md`**.

- [ ] **NTP** (ou sync équivalente) sur les hôtes **avant** de s’appuyer sur les logs pour enquête (**B6**).
- [ ] **Secrets** production : pas de défauts dev (`SECURITY_INTERNAL_SECRET`, JWT, etc.).
- [ ] **Vérification terrain** : le **security-service** permet bien de **retrouver** une trace par `requestId` / métadonnées (selon votre stockage final).
- [ ] **Mobile** : envoi d’un **identifiant de corrélation** sur les appels API (build réel, pas seulement émulateur).
- [ ] **Intrusion gateway** : Redis disponible ; relever les **faux positifs** après activation ; `INTRUSION_DETECTION_ENABLED=false` uniquement en **dépannage** ciblé.
- [ ] **SMTP prod** : **`SMTP_USER`** / **`SMTP_PASS`** réels ; **TLS** (port + `SMTP_SECURE` / `SMTP_USE_SSL`) selon le fournisseur — voir **`docs/operations/PREPROD_PRODUCTION_CHECKLIST.md`** § F.
- [ ] **Crash reports** : **`CRASH_REPORT_EMAIL`** = boîte **dédiée** et exploitable (tri, alertes) ; parcours d’essai après bascule SMTP (**on verra** quand tu activeras l’envoi réel).

---

## Analyse CVE & supply chain (voir **`security/STATS.md`**)

Remplir **`security/STATS.md`** après chaque passe d'outils ; cocher ici quand le **processus** ou la **CI** est en place (les chiffres CVE restent dans **STATS**).

- [ ] **`npm audit --omit=dev`** (ou équivalent) sur **chaque** microservice listé dans **STATS.md** § 2.1 + **frontend** + racine — reporter date et severités dans **STATS.md**.
- [ ] **Docker Scout** ou **Trivy** sur les images **jobbingtrack-*** et bases **postgres** / **redis** — résumer dans **STATS.md** § 2.5.
- [ ] Mobile : **`flutter pub outdated`** (et audit pub si disponible) sur **`mobile/`** et **`flutter-mobile-app/`** — noter dans **STATS.md** § 2.4.
- [ ] (Optionnel) Étape **audit** en CI (seuil **high** minimum) sans bloquer indéfiniment le dev local.

---

## Tests sécurité offensifs contrôlés (PLAN B15)

Source de cadrage : **`docs/security/SECURITY_TESTING_MATRIX.md`**. À exécuter uniquement sur local/test/préprod autorisée, avec comptes et jeux de données dédiés.

- [ ] **Énumération URL/endpoints** : ajouter une commande projet (`make security-scan-api` ou équivalent) pour `ffuf` / `gobuster` / `nikto` / ZAP contre la gateway ; produire un rapport endpoints trouvés, statut attendu, authz OK/KO.
- [ ] **Fuzzing paramètres et mass assignment** : couvrir `arjun` / `wfuzz` / Burp ; vérifier whitelist DTO, rejet champs sensibles (`role`, `isAdmin`, `userId`, `tenantId`, etc.) et absence de modification non autorisée.
- [ ] **Injections entrées** : scénarios SQL/NoSQL (`sqlmap` + tests API), XSS (`dalfox`/ZAP), command injection (`commix`) ; rapporter protection ORM, validation, CSP/sanitisation, absence de shell non maîtrisé.
- [ ] **Auth/JWT/IDOR/privilege escalation** : tester brute force borné (`hydra`/`medusa` en lab), `jwt_tool`, matrix rôle → route → statut, accès ressources d’autres utilisateurs, session fixation/hijack.
- [ ] **API REST** : vérifier rate limiting, CORS, endpoints debug/health, erreurs non verbeuses, payload volumineux, limites body et corrélation logs.
- [ ] **Docker/réseau** : `nmap` local/préprod, `trivy` images, Docker Scout si disponible, audit `docker.sock`, ports Redis/Postgres non publics, compose prod sans secrets fallback. **Outillage initial** : `make security-scan-ports`, `make security-scan-images`.
- [ ] **Compose prod scannable** : corriger ou documenter la commande de fusion compose pour que `docker compose -f docker-compose.prod.yml config` soit exploitable (11/05 : `auth-service` sans `image` ni `build` dans le fichier prod seul).
- [ ] **Secrets/TLS/logs sensibles** : `gitleaks` / `truffleHog`, `sslscan` / `testssl.sh` en préprod/prod autorisée, redaction logs tokens/passwords/email. **Outillage initial** : `make security-scan-secrets`.
- [ ] **Contrôles prioritaires demandés** : `gitleaks` sur historique Git complet, `trivy` sur images Docker de prod, `nmap` sur l’exposition effective de `docker-compose.prod.yml`, `jwt_tool` sur les JWT, OWASP ZAP active scan sur API locale via gateway. **11/05 outillé** : workflow `.github/workflows/security-audit.yml` + cibles `make security-*`; reste à exécuter en lab/préprod et traiter les rapports.
- [ ] **DoS contrôlé et scans massifs** : tests bornés (`slowloris`, load testing) avec limites gateway/Nginx, timeouts, 429, circuit breaker et métriques ressources.
- [ ] **Spoofing IP/headers** : vérifier `TRUST_PROXY_HOPS`, `X-Forwarded-For`, extraction IP fiable, séparation trafic interne/externe WAF.
- [ ] **Performance / trafic interne** : ne pas analyser tout le trafic inter-conteneurs ; limiter la détection runtime au trafic entrant gateway/public, avec événements asynchrones vers `security-service`.
- [ ] **Architecture par couches** : runtime léger dans `api-gateway`, audits CI/CD hors chemin critique, forensics/alerting dans `security-service`, métriques sécurité agrégées pour backoffice/Grafana.
- [ ] **Mobile plus tard** : audit reverse engineering (MobSF, jadx/apktool), absence de secrets dans l’app, pinning TLS à évaluer, signaux root/jailbreak non utilisés comme unique preuve.
- [ ] **Backoffice sécurité** : prévoir une page de tests non destructifs (WAF, rate limit, CORS, authz endpoints, santé sécurité) avec historique, statut et lien vers rapports.
- [ ] **Rapports** : normaliser les sorties sous `reports/security/` ou `tests/results/security/` avec date, environnement, commit, outil, statut et actions restantes. **11/05 début** : scripts locaux écrivent sous `reports/security/` (ignoré par Git).

---

## Release, préprod, conformité et distribution (PLAN lot H)

Source : **`docs/operations/RELEASE_PREPROD_PRODUCTION_PLAN.md`**. Ces tâches sont majoritairement à traiter plus tard, avant merge `dev` → prod, mais elles doivent rester visibles dès maintenant.

- [ ] **Branche tests complets** : créer une branche dédiée depuis `dev` pour campagne complète backend, frontend, API, mobile, services, BDD, sécurité, performances, Playwright, qualité, erreurs, délivrables.
- [ ] **Reporter le benchmark long 40–60 min au gate préprod/prod** : garder `make resource-budget-sample` comme contrôle obligatoire avant prod, avec baseline 07/05 et budgets p95 déjà documentés ; ne pas le refaire à chaque petite itération.
- [ ] **Branche / environnement préprod** : définir branche préprod, serveur préprod, domaines, secrets, base, stockage, monitoring, alertes, sauvegardes et jeux de données distincts de la prod.
- [ ] **Bêta mobile** : préparer builds Android/iOS pointant vers préprod, canaux beta, signature, versioning, collecte retours et crash reports.
- [ ] **Gate prod** : tests complets verts ou exceptions validées, préprod validée, scans sécurité P0 traités, sauvegarde/restauration testée, monitoring/alerting actif, rollback documenté.
- [ ] **Licences** : inventorier licences packages Node/Rust/Flutter/Docker/outils/services externes ; décider licence projet ou licences par sous-partie ; produire notices si publication.
- [ ] **RGPD / conformité** : cadrer données collectées, consentement analytics/crash, minimisation, pseudonymisation, export/suppression utilisateur, rétention et accès aux logs.
- [ ] **Retours utilisateurs / rapports erreurs** : centraliser feedback, crash reports, requestId/correlationId, version app, device/service, statut de traitement et exploitation backoffice.
- [ ] **Déploiements automatisés** : définir quelles branches déclenchent build/test/deploy backend/API/front/web/mobile, registry/images, migrations, release notes et rollback.
- [ ] **Mono-repo vs multi-repo** : décider si garder le mono-repo actuel ou séparer backend/frontend/mobile/infra selon cadence release, droits, CI, contrats API et complexité de rollback.
- [ ] **Plateformes futures** : prioriser Android/iOS/Web/Linux ; Windows/macOS seulement plus tard si besoin produit validé.

---

## Fichiers « dot » à la racine & déploiement (à garder en tête)

| Fichier | Rôle | Git / CI / prod |
|--------|------|------------------|
| **`.jobbingtrack-stack-mode`** | Écrit par **`make up`**, **`make up-full`**, **`make up-dev`** : contient une ligne (`up-essential`, `up-full`, `up-dev`) pour que **`make restart`** affiche le **dernier mode** de stack. Supprimé par **`make down`** / **`down-clean`** / **`restart-clean`**. | **Ignoré** (`.gitignore`) — **ne pas déployer** comme config ; recréé localement par Make. |
| **`.nettoyage_effectue`** | Ancien **marqueur manuel** (note nov. 2025 sur fusion de docs) ; **aucune** lecture par le Makefile actuel. | **Ignoré** (`.gitignore`) — supprimable sur ta machine ; **ne pas pousser**. |
| **`.node-version`** | Version Node attendue (ex. **20.19.5**) pour **asdf / nodenv / certains hébergeurs** ; en parallèle de **`.nvmrc`** (souvent identique). | **Versionné** — utile en **CI** (actions `node-version` peuvent lire le fichier) et pour les contributeurs ; en **prod** Docker, la version réelle est l’**image** (`Dockerfile`), pas ce fichier. |
| **`.nvmrc`** | Même intention que **`.node-version`** pour **`nvm use`**. | **Versionné** — idem ; aligner avec l’image Node des Dockerfiles si tu changes la majeure. |
| **`.env`**, **`.env.*`** (hors exemple) | Secrets et URLs locales. | **Ignorés** — en déploiement : variables d’environnement **injectées** (fichier env du PaaS, secrets manager), **jamais** commit du `.env` prod. |

**Déploiement (plus tard)** — pense-bête : ne pas copier les marqueurs dev (`.jobbingtrack-stack-mode`, `.nettoyage_effectue`) sur les serveurs ; documenter dans le runbook quelles variables d’environnement remplacent **`.env.example`** ; vérifier que **`.dockerignore`** n’exclut pas des fichiers nécessaires au **build** ; option : **`.deployignore`** ou équivalent si tu synchronises le dépôt par **rsync** (hors Git) pour exclure `node_modules/`, `.next/`, rapports de tests. **À trancher** avec **`docs/operations/PREPROD_PRODUCTION_CHECKLIST.md`** (lot **G** / procédure release).

---

## Dernière section du fichier — méta-chantiers (ne rien ajouter *après* sans déplacer ce bloc)

**Règle** : toute nouvelle tâche « transverse » (doc globale, audit BDD, validation porteur) s’ajoute **dans cette section** ou juste **au-dessus du tiret `---` qui précède**, pour garder **en bas de fichier** l’historique des chantiers structurels. Les **petites suites** post-chantier vont plutôt dans **« Suite & suivi explicite »** (haut du fichier, après *Lire ce fichier*).

- [ ] **Validation produit (porteur)** : pour chaque lot livré techniquement, **vous** cochez **`PLAN.md`** (**Validé (porteur)**) ou une mention datée dans **`STATUS.md`** ; tant que ce n’est pas fait, le livrable reste « en attente d’acceptation » même si le code est mergé.
- [ ] **Revue base de données avant grosse passe de tests** : aligner schéma maître (**auth-service** / `db-push-all`), scripts **`scripts/db/*.sql`**, tables listées dans **`ERRORS.md`**, et attentes des services (déploiements, logs agrégés, etc.) — objectif : éviter les écarts « table absente » / doubles définitions lors de **`make tests`** ou **`make test-suite-full`**. Croiser **lot A2**, **A5**, **C** (données test) dans ce fichier.
- [ ] **Trafic répété `GET /api/v1/security/*` via la gateway** : ce flux correspond en général à un **client** (UI backoffice sécurité / firewall en rafraîchissement, onglet ouvert) — **ce n’est pas** une erreur isolée « mystérieuse » ; si le volume pose problème (logs, charge), **diagnostiquer la source** (quel onglet, quel intervalle) puis réduire le polling ou mutualiser les requêtes — voir **`STATUS.md`** § journalisation / diagnostic et **`ERRORS.md`** § homonyme.
- [ ] **Refonte documentation (racine + `docs/`)** : reprendre **tous** les **`.md` à la racine** et **tous les `.md` sous `docs/`** de façon **structurée** (rôles, doublons, liens, ordre de lecture, index unique ou fil d’Ariane clair) ; aligner **PLAN**, **STATUS**, **TODOS**, **ERRORS**, **RESOLUTIONS**, **STATS**, **FONCTIONNALITES**, **`docs/BACKLOG.md`**, **`docs/project/CHANTIER_SECURITE_DATA_DOCS.md`**. *À planifier explicitement avant grosses réécritures.*
- [ ] **HX1 — Variables d’environnement sans « fallback magique »** : le porteur souhaite **ne pas** coder de valeurs par défaut du type **`process.env.X || '3000'`** dans le dépôt (risque : prod silencieusement mal configurée). **Périmètre** : audit **`next.config.js`**, **frontend**, **backend** (chaque service), **mobile** (`mobile/`, `flutter-mobile-app/`), scripts — remplacer par **variables obligatoires** documentées dans **`.env.example`** / runbooks, ou échec explicite au démarrage si critique.
- [ ] **HX2 — Audit transverse « sécurité × doc × config »** : parcourir **toutes** les zones du dépôt (front, back, mobile, Docker, CI) pour repérer **URLs / secrets / schémas internes** dans la doc ou les commentaires **trop exposants** (faille d’ingénierie sociale ou fuite d’architecture) ; **niveau de détail** des guides : suffisant pour exploiter, pas de cookbook d’attaque. Croiser **`docs/operations/PREPROD_PRODUCTION_CHECKLIST.md`**, **`docs/BACKLOG.md`**.
- [ ] **HX3 — Environnements prod / préprod / tests conteneurs** : formaliser une **matrice** (compose profiles, fichiers env, noms d’images, jeux de données) pour éviter les dérives « même stack que le dev » ; cadrage initial dans **`docs/operations/RELEASE_PREPROD_PRODUCTION_PLAN.md`**, à détailler quand le modèle préprod sera figé.
- [ ] **HX4 — Session backoffice : reconnexion / expiration** : lorsqu’une **reconnexion** est demandée sans message clair (JWT expiré, middleware, refresh, WebSocket metrics), **instrumenter** (message UI + corrélation `requestId` / logs gateway) et documenter le comportement attendu dans **`ERRORS.md`** ou **`RESOLUTIONS.md`** — diagnostic reproductible pour le porteur.
- [ ] **HX5 — Hôte Linux / Redis : `vm.overcommit_memory`** : le conteneur **`jobbingtrack-redis`** peut logger *« WARNING Memory overcommit must be enabled »* — à traiter sur **l’hôte** (pas dans l’image Redis) : `sudo sysctl vm.overcommit_memory=1` et persistance dans **`/etc/sysctl.conf`** (`vm.overcommit_memory = 1`) puis reboot ou rechargement sysctl ; voir [redis / jemalloc](https://github.com/jemalloc/jemalloc/issues/1328). Objectif : éviter échecs **BGSAVE** / réplication sous pression mémoire.
- [ ] **Séquence post-`git pull` (stack locale)** : typiquement **`make db-push-all`** (schéma BDD + scripts SQL) puis **`make up-full`** (ou **`make up-dev`** si besoin seed + tests) — validé côté porteur **mai 2026** ; documenter tout écart machine (WSL, rootless Docker) dans **`ERRORS.md`** si besoin.

### Suite chantier (rappel — après nettoyage UI vue d’ensemble **04/05**)

- Poursuivre **contrat logs forensics** (**`workflow-service`** si pertinent, QA corrélation) et **corrélation perf** (brush, causalité, icônes tri) listés en haut de ce fichier (**§ Suite & suivi explicite**).
- **Après priorités immédiates** : enchaîner **§ Chantier Statistiques & backoffice** + **`PLAN.md`** lot **A1h** (Statistics, vue d’ensemble `/backoffice`, onglets sécurité/logs stats, audit ~73 pages).
- **Rapide (UX)** : **Sécurité — libellés & `SecuritySubNav`** sur **`/b4ck0ff1ce/security/**`** (voir priorité en tête de fichier + tableau § UX Sécurité).

---

## Vision données décentralisées (DHT) — **très fin de projet, hors périmètre actuel**

> **Idée technique notée pour plus tard** — ne **pas** démarrer tant que lots **validation porteur**, **Statistics (A1h)**, **central logging**, **B10** et **sécurité prod-like** ne sont pas stabilisés. Revoir en **dernière phase** du chantier global (après backoffice / API / déploiement cohérents).

**Concept (brouillon)** :

- **Table de hachage distribuée (DHT)** : annuaire éclaté entre les nœuds du réseau (pas un annuaire central unique).
- **Routage** : chaque nœud relaie du trafic **chiffré** (contenu non lisible par les relais).
- **Pairs** : échange de données **sans révéler l’IP** publique des participants (objectif privacy / résilience — modèle type overlay, pas le stack Docker actuel).
- **Rapport avec JobbingTrack aujourd’hui** : architecture **centralisée** (Postgres, metrics-aggregator, gateway, logs `aggregated_logs`) — la vision DHT serait un **chantier séparé** (recherche + PoC), pas une évolution du backoffice courant.

**Quand reouvrir** : fin de **`TODOS.md`** / **`PLAN.md` lot G+** ou roadmap produit dédiée « réseau P2P / confidentialité » ; cadrage légal et menaces (abus, conformité) obligatoire avant tout code.

