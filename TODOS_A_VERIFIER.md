# TODOs à vérifier par l’agent

Dernière mise à jour : 17 juin 2026

## Rôle

Ce fichier liste ce que l’agent doit vérifier techniquement avant de demander une validation porteur. Une ligne vérifiée par l’agent ne vaut pas validation produit.

## Vérifications ouvertes

| Priorité | Vérification agent | Preuve attendue | Statut |
|----------|--------------------|-----------------|--------|
| Mobile | Auth + parcours candidatures — audit et correctifs | Branche `feat/mobile-auth-applications-audit` : JWT sur `ApplicationProvider`, persistance session splash, import `foundation.dart`. **Appareil Samsung SM_G990B2 (R5CT7263YJL)** : APK debug installé 17/06 (`~/flutter-sdk` car Flutter système cassé) ; `adb reverse` 5002/5003 ; smoke login ADB OK ; session restaurée au cold start ; liste candidatures affiche `MobTest*` après création API (JWT OK, bouton Voir) ; scénarios ADB `verify_candidatures`, `navigation_bottom_bar`, `verify_search_hub` OK. API `test-mobile-api-authenticated.js` **9/9 + 27/27 OK**. | [x] |
| Mobile | Télémétrie anonyme + page Aide/retours dans Paramètres | Flutter : `MobileAnalyticsService` initialisée au splash et après login ; `MobileAnalyticsRouteObserver` sur `MaterialApp`. **Settings** : toggles consentement + performances + trace + formulaires bug/suggestion/signalement. Preuves : `flutter test` ciblés **9/9 OK** ; build APK debug **OK** ; install Samsung **Success**. API smoke curl gateway **5002** : `POST /analytics/sessions|events|performance` + `POST /crashes` **200/success**. | [x] |
| Mobile | Backoffice Application — Activité & Retours pleine largeur | Pages `/backoffice/analytics/application/activity` et `/feedback` alignées sur **Performances live** (shell pleine largeur, `TimeRangeSelector`, KPI + tableaux). Backend : `GET /analytics/events?scope=application&platform=mobile` (admin) + `GET /analytics/performance`. Jest smoke analytics **7/7 OK** ; `tsc` ciblé OK. | [x] |
| Mobile | Candidatures — liste enrichie entreprise/statut | Page Candidatures : noms entreprise + badges statut (ex. NO_RESPONSE) ; limite API 100 ; erreurs affichées ; onglets isolés si relances/contacts échouent. Backend : enrichissement company/status sur liste raw SQL. Tests : `application_model_test.dart` **2/2 OK** ; API `GET /applications?limit=3` renvoie `company.name` + `status.code`. | [x] |
| Mobile | Inscription — télémétrie obligatoire + vérif email | Inscription : case télémétrie **cochée par défaut**, refus = compte non créé ; consentement persisté (`enableTelemetryOnSignup`). Écran attente email : **renvoyer vérification** ; deep link Android `jobbingtrack://verify-email?token=`. Cibles Make : `mobile-login-admin`, `mobile-device-ready`, `mobile-verify-email TOKEN=`, `mobile-verify-email-smoke`, `mobile-sync-test-env WRITE=1`. **Preuves agent** : E2E `smoke-verify-email-adb.js` (EmailLog → deep link → login ADB) ; `smoke-register-adb.js` + `smoke-login-admin-adb.js` OK Samsung. | [x] |
| Mobile | Navigation retour + admin + relances + ajouts depuis candidature | **Locale** : `initAppLocale()` + `formatSmartEventDate` (plus de `LocaleDataException` sur Relances/Appels/Entretiens). **Admin** : routes `/admin`, `/users`, `/analytics`, `/logs`, `/statistics`, `/trash`, `/test-data` wrappées `AdminGuard` ; drawer ADMIN masqué si non-admin. **Retour** : suppression `_goToHome` / `popUntil(/home)` sur Profil, Entreprises, Contacts, Événements ; `BackToHomeScope` pass-through. **Relances** : pas de FAB global ; ajout relance/entretien/appel uniquement depuis **détail candidature** ; appel avec `contact_picker_sheet` (sélection ou création nom/prénom). **Écrans admin** : `logs_screen` créé ; `events_screen` branché `getCalendarEvents` ; analytics/statistics/users/trash chargent API. Tests : `flutter analyze` ciblé **0 erreur** (infos seulement) ; `flutter test` application_model + mobile_analytics **4/4 OK**. | [x] |
| Mobile | Autocomplete entreprise + parcours live API | **Fix 500 détail candidature** : `contactApplications` → `applications` (contact-service + application-service). **Safe area** : `scrollSafePadding`, bottom nav `SafeArea`. **Appels** : libellé Contact vs Entreprise. **Téléphone local** : import contacts + journal appels (`LocalPhoneIntegrationsService`, Paramètres). **Smoke API** : **19/19 OK** 18/06 (GET contacts/candidature inclus). **Appareil** : dual comptes ADB **OK** ; login user OK ; APK Samsung installé. **Email récap** : `[JobbingTrack] Recap mobile fix 500 + safe area 2026-06-18` → **6/6 HTTP 202**. Commit `6bf2c451`. | [x] |
| Mobile | Shell 4 onglets + navigation drawer + formulaires enrichis (18/06) | **ShellNavigation** : drawer Candidatures/Entreprises/Contacts/… bascule l’onglet shell (barre basse synchronisée) ; retour matériel → Accueil. **Menu ⋮** : recherche, paramètres, déconnexion ; cloche retirée login/inscription. **Relance/entretien** : canal, contact, notes multiligne, visio, durée. **Appels** : popup détail + `PUT /calls/:id`. **Backend** : `FollowUpContact` à la création ; table `audit_logs` (`scripts/db/ensure-audit-logs.sql`). **Analytics** : traces routes nommées uniquement ; diagnostic local + compteurs erreurs. `flutter analyze lib/` **0 erreur** ; `flutter test test/mobile_analytics_test.dart` **2/2 OK**. | [x] |
| Mobile/Backoffice | Analytics mobile — pagination, détail échantillons, enregistrement données | **Backend** : `ensureAnalyticsSession` (plus de 404 events) ; filtre plateforme `mobile|android|ios` ; performances versions enrichies (200 lignes). **Mobile** : JWT transmis aux POST analytics ; `POST /analytics/device` à la session. **Frontend** : `/analytics/application/activity` pagination 15 + clic détail ; `/user-analytics` vue d’ensemble avec aperçus événements/erreurs/perf/appareils + pagination onglets. `tsc --noEmit` OK. | [x] |
| Mobile | File télémétrie offline — analytics, erreurs, retours | **18/06 suite** : persistance **gzip** (`TelemetryPayloadCodec`) ; **JWT jamais sur disque** (`TelemetrySanitize`) ; flush **batch** événements `POST /analytics/events/batch` (max 20) ; corrélation `X-Request-Id` via `HttpCorrelation`. File `AnalyticsTelemetryQueue` + lifecycle + timer 5 min. Tests Flutter **10/10 OK** (`telemetry_codec_sanitize_test` inclus). Smokes : `smoke-analytics-api.js` + `smoke-analytics-batch-api.js` **OK**. | [x] |
| Mobile/Backoffice | Analytics utilisateur — userId admin, sessions actives, bannière dev flush | **19/06** : backend filtre `userId` sur GET events/errors/performance ; `activeSessionsList` dans stats ; frontend sélecteur utilisateur + refresh 30 s + colonnes appareil erreurs ; bannière debug mobile flush télémétrie. `smoke-analytics-api.js` **OK** ; Flutter ciblé **5/5** ; `tsc` OK. **Suite 18/06** : `device_info_plus` → `deviceModel` sessions ; `pageViews` sur `screen_view` ; bannière **en haut** (MaterialApp.builder) ; biométrie message FR + coller presse-papier ; `copy-test-password-clipboard-adb.js` ; APK debug réinstallé Samsung **Success**. | [x] |
| Mobile/Backoffice | Analytics utilisateur — recherche sélecteur utilisateur | **18/06** : autocomplete nom/email/rôle. **Suite** : recherche **serveur** `GET /auth/users?search=&limit=&offset=` + pagination « Charger plus » ; `fetchAnalyticsUsers.ts` ; smoke `smoke-auth-users-search-api.js`. Jest userPicker **3/3 OK**. | [x] |
| Mobile | Sync métier offline — CRUD candidatures, appels, relances, entretiens, contacts, entreprises | **18/06** : `OfflineBusinessSyncQueue` (gzip, sans JWT disque) + `OfflineMutationHelper` sur tous les writes métier (`create/update/delete` applications, companies, contacts, followups, interviews, calls). Flush au retour réseau / reprise app / requête API OK. Diagnostic Paramètres : `offlineSyncPending`. Exception `OfflineMutationQueued` pour UI. Tests **10/10 OK**. Smoke parcours online : `smoke-full-journey-api.js` (retry si 429 login). | [x] |
| Mobile | Auth session offline-first + stockage chiffré | **18/06 suite** : JWT + refresh token + profil dans `SecureAuthSessionStore` ; offline-first (pas de logout sur 401 hors ligne). **Blocage login 18/06** : rate-limit auth **5/min** (Redis) + **adb reverse absent** après mode avion → 429 / erreur réseau. Correctifs : purge Redis auth + `ensure-device-api-ready.js` ; auth limit **60/min en dev** ; `ApiService.prepareForLogin()` ; message 429 explicite mobile. | [x] |
| Infra | Nettoyage stack obsolète Prometheus / Loki / Grafana | **Constat** : stack active = monitoring-agent-rs + metrics-aggregator + backoffice Performances. **Nettoyage 17/06** : archive `docs/archive/monitoring-old/` supprimée ; services `prometheus.service.js` / `loki.service.js` retirés du metrics-aggregator ; routes metrics/logs basculées Docker ; tests prometheus/loki/cAdvisor supprimés ; `make metrics` → Performances BO ; `monitoring-test` → `/api/v1/health` metrics-aggregator. Validations : `docker restart jobbingtrack-metrics-aggregator` ; curl `/api/v1/health` OK ; curl `/api/v1/docker/jobbingtrack/aggregated` avec `X-API-Key` → `success=true`, métriques conteneurs. | [x] |
| P1B | Statistics overview — graphes dispo/erreur UI | Branche `feat/p1b-statistics-overview-graphs` mergée : `ChartPeriodCaption`, cartes alignées log-stats, `statisticsSampleRangeLabel`, doublon Santé technique retiré. | [x] |
| P1B | Statistics app-data — période timeline + séries | Suite validation log-stats 17/06 : aligner `/statistics/app-data` sur le patron Performances. Correctifs : pills période 7/14/30 j (`periodDaysToTimeRange`), toggle séries via `FilterMultiSelectField` variant statistics, `ChartPeriodCaption`, axe `timeMs` + `chartXDomainFromDataRange`, lib `buildAppDataChartRows` / `appDataSampleRangeLabel`. Validations sans `make` : `tsc --noEmit` OK ; `lint --quiet` OK ; Jest `appDataTimeline` **4/4 OK**. Validation porteur : « ouais ca me vas ». | [x] |
| P1B | Statistics log-stats — refonte filtres + graphe temporel | Retour porteur 17/06 : zone filtres jugée peu lisible ; demande d’afficher l’échantillon dans le temps comme Performances. Correctifs : pills période 24 h / 7 j / 14 j / 30 j ; `FilterMultiSelectField` variant `statistics` + `logLevelChipTone` ; services via `FacetMultiValueField` ; graphe **Volume de logs dans le temps**. Validations sans `make` : `tsc` OK ; `lint --quiet` OK ; Jest **7/7 OK**. Validation porteur : « okay valide ». | [x] |
| P1C | B10 sécurité réseau — lecture source connexion | Suite Lot B/B10 16/06 : les tableaux réseau affichaient `host-network`, `host-local`, `0.0.0.0` peu actionnables. Correctif : `backend/security-service/src/utils/connectionSource.js` structure source/destination/confiance ; `/api/v1/security/firewall/network/stats` renvoie connexions enrichies ; frontend `NetworkConnectionSourceTable` sur `/b4ck0ff1ce/security/network` et fiche menace (nature, destination, réputation, liens Menaces/Logs). Pavés d’aide retirés sur Firewall, Incidents empty state, Alertes email. Validations sans `make` : `tsc` direct OK ; Jest backend `connectionSource.test.js` **3/3** ; Jest frontend `connectionSourcePresentation.test.ts` **3/3** ; `node --check` firewallController OK. | [x] |
| P1C | B11 emails backoffice — lisibilité et test SMTP Node | Suite Lot B/B11 16/06 : Email Monitor HTML illisible en thème sombre ; Configuration SMTP avec valeurs OVH codées en dur ; `POST /api/v1/emails/test` passait par Python et renvoyait 500. Correctifs : composant `EmailHtmlPreview` (iframe fond blanc) ; emails `FAILED` avec métadonnées + raison SMTP/miroir ; page `/emails/settings` lit uniquement l’API test-smtp (host/port/from/reply-to/user/provider) ; envoi test générique via `emailService.sendGenericEmail`. Validations sans `make` : `tsc` direct OK ; `node --check` email.controller OK. Reste : smoke runtime `POST /api/v1/emails/test` avec token admin + validation porteur visuelle. | [x] |
| P1C/B11 | Emails incidents critiques — rate-limit et regroupement | Suite Lot B/B11 16/06 : `securityAlertEmailNotifier` évite maintenant le spam en regroupant les alertes similaires par clé stable `level/category/type/source` pendant `SECURITY_ALERT_EMAIL_RATE_LIMIT_WINDOW_MS` (15 min par défaut, `0` pour désactiver en test). Les répétitions ne déclenchent pas d’appel notification-service et sont comptées ; le prochain email autorisé affiche **Alertes similaires regroupées**. Les métadonnées restent redigées (`smtp_pass`, token, secret, etc.). Validations sans `make` : Jest `security-alert-email-notifier`, `security-alert-email-payload`, `notification-settings` **3 suites / 10 tests OK** ; `node --check securityAlertEmailNotifier.js` OK. | [x] |
| P1C/B10/B11 | Politiques sécurité — posture globale lisible | Suite Lot B/B10-B11 16/06 : `/b4ck0ff1ce/security/policies` n’est plus seulement un doublon WAF/firewall/IP ; ajout d’une synthèse de posture : WAF, Firewall, Réponse IP, Alertes critiques, Auto-blocage borné. Le statut des alertes critiques se base sur `/api/v1/security/notification-settings`; l’auto-blocage high/critical reste explicitement borné tant que B12 n’est pas validé. Validations sans `make` : Jest `securityPolicyPresentation.test.ts` **2/2** ; `npm run type-check` OK ; ESLint ciblé politiques/helper **0 erreur**. | [x] |
| P1C/B10/B8 | Enrichissement IP — fixtures RFC5737 sûres | Suite Lot B/B10-B8 16/06 : `geoipProvider` fournit des profils lab déterministes pour `198.51.100.0/24` (public lab), `203.0.113.0/24` (datacenter/proxy/VPN) et `192.0.2.0/24` (Tor), sans appel externe. Chaque résultat contient `sources=['rfc5737-lab-fixture']`, `confidence=high`, `enrichedAt`, RDAP et reverse DNS synthétiques, ce qui rend les tests VPN/proxy/Tor/ASN reproductibles. Validations sans `make` : Jest `geoip-provider` + `firewall-threat-forensics` **2 suites / 10 tests OK** ; `node --check geoipProvider.js` OK. | [x] |
| P1C | B10 sécurité logs — liens menace sans tiret silencieux | Suite Lot B/B10 16/06 : `/b4ck0ff1ce/security/logs` avait déjà les filtres serveur, tri explicite et corrélation menace/log, mais `resolveSecurityLogLink()` renvoyait encore le label `—` quand aucun `href` n’était disponible. Correctif frontend : en absence de fiche menace, le label reprend maintenant la raison explicite calculée (`Log lab sans menace rattachée`, `Menace référencée introuvable`, `Aucune menace proche dans la fenêtre`, `IP source absente`, etc.) ; la colonne **Liens** n’est donc plus muette. Validations sans `make` : `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé `security/logs/page.tsx`, `securityLogLinks.ts`, `securityLogLinks.test.ts` OK ; Jest ciblé `securityLogLinks.test.ts` + `securityLabels.test.ts` **2 suites / 10 tests OK** ; lints IDE OK. Vérification navigateur tentée sur `http://localhost:5003/b4ck0ff1ce/security/logs` : redirection `/login`, donc rendu authentifié à valider par le porteur/session admin. Wrappers `npm run type-check` et `npm run lint -- --quiet` reproduisent encore code 1 sans sortie. | [x] |
| P1C | MailHog intégré backoffice — déjà couvert | Retour porteur 16/06 : la tâche `P1C — MailHog intégré backoffice` est déjà résolue. Vérification code : `frontend/src/app/(admin)/backoffice/emails/mailhog/page.tsx` utilise `EmailBackofficePageShell`, embarque MailHog en iframe (`NEXT_PUBLIC_MAILHOG_UI_URL` ou `http://localhost:8025`) dans la zone de contenu et la sous-navigation Emails contient l’onglet `/b4ck0ff1ce/emails/mailhog`. Le menu latéral pointe vers cette route interne ; le bouton “Ouvrir dans un onglet” reste seulement une action secondaire. `docs/TODOS.md` reclassé en `[x]`. | [x] |
| P1B | Temps de réponse historique par service — plages multi-jours complètes | Suite `docs/TODOS.md` P1B “historiques agrégés hors période courante” : l’endpoint `GET /persistence/services/:serviceName/availability?history=1&startDate&endDate&limit` ne fait plus `ORDER BY timestamp DESC LIMIT` sur un seul alias de service. Correctif backend : si une plage bornée est fournie, `service_availability_history` est échantillonné par buckets sur toute la fenêtre demandée ; les alias (`auth-service` / `jobbingtrack-auth-service`) sont fusionnés dans une seule requête `IN (...)` pour ne pas masquer l’historique ancien quand un alias récent existe. Même fusion appliquée aux stats agrégées `?hours=...`. Validations sans `make` : `/usr/bin/node --check src/services/persistence.service.js` OK ; `/usr/bin/npm exec -- jest tests/serviceAvailabilityHistory.test.js --runInBand --verbose` **1 suite / 3 tests OK** ; frontend direct `./node_modules/.bin/tsc --noEmit --pretty false` OK (API consommée par le front) ; wrappers `npm run type-check` / `npm run lint` échouent encore code 1 sans sortie, dette déjà ouverte ; redémarrage ciblé `jobbingtrack-metrics-aggregator` healthy ; smoke proxy 7 j `auth-service` limit 12 → **12 points**, `2026-06-09T11:47:30Z → 2026-06-15T21:47:33Z`; stats 168 h → `totalChecks=6940`, `avgResponseTime≈1.95 ms` (avant reload, l’alias court ne renvoyait que les dernières minutes). | [x] |
| P1B | Temps de réponse P1B — moyenne alignée sur endpoints affichés | Retour porteur 16/06 : la carte “Temps de réponse” affichait la moyenne globale `monitoringC.avg_response_time_ms` (~134 ms), alors que le panneau en dessous liste uniquement les endpoints P1B affichés. Correctif frontend : ajout `averagePriorityResponseTimeMs()` et utilisation de cette moyenne dans la carte hub + bloc “État du système”; le panneau `PriorityResponseServicesSummary` affiche aussi **Moyenne des endpoints affichés**. La moyenne ignore les dépendances non HTTP (Postgres/Redis), les services hors liste P1B et les valeurs nulles/0. Validations sans `make` : Jest ciblé `serviceHealthOverview.test.ts` + `PriorityResponseServicesSummary.test.tsx` **2 suites / 13 tests OK** ; `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé fichiers frontend modifiés **0 erreur**. | [x] |
| P2 | Backlog produit — enrichissement automatique contacts | Demande porteur 16/06 notée dans `docs/TODOS.md` : future aide à la complétion des contacts depuis nom/prénom + domaine entreprise, proposition/vérification email, rapprochement LinkedIn/page publique si possible, sources + niveau de confiance, validation humaine avant écriture et garde-fous RGPD/anti-scraping. Aucun développement lancé. | [x] |
| P1C | Metrics aggregator — alerte MailHog + historique multi-jours après redémarrage | Suite retour porteur 16/06 : l’email MailHog **“Metrics aggregator indisponible”** a bien été envoyé (`EmailLog` `SENT`, miroir SMTP `sent=true`) à 10:26, mais il correspondait à un timeout 5 s de l’endpoint lourd `/api/v1/docker/services/all` pendant le redémarrage, pas à un service durablement down. Correctif sécurité : avant alerte critique, `security-service` vérifie maintenant `/api/v1/health`; si le healthcheck répond `online/healthy/ok`, il journalise une dégradation transitoire sans créer d’email critique. Côté métriques : les tables brutes `system_metrics` / `container_metrics` repartent à l’heure du redémarrage, mais les snapshots persistés sont conservés (`system_metrics_snapshots` depuis 2026-03-06, `container_metrics_snapshots` > 5,4 M lignes). Correctif backend : `/persistence/system/metrics` fusionne désormais `system_metrics` + snapshots sur la plage demandée et échantillonne les snapshots sur toute la fenêtre. Tests sans `make` : syntaxe backend OK ; Jest `service-availability-alerts` **4/4 OK** ; services `metrics-aggregator` et `security-service` redémarrés healthy ; proxy frontend testé sur 14/06 (**60 pts, 00:00→23:36**), 15/06 (**53 pts, 00:00→21:36**) et 24 h glissantes (**27 pts, 2026-06-15 10:48→2026-06-16 10:24**). MailHog reste à **1** message (pas de nouvelle alerte après correctif). Browserslist : `npm`/`npx`/`npm install --package-lock-only` échouent encore code 1 sans sortie, dette wrapper déjà ouverte ; aucun lockfile modifié à la main. | [x] |
| P1C | A1c — lazy Recharts legacy `/analytics` + `tests-performance` | Suite Lot A 15/06 : extraction des graphes inline vers `LegacyAnalyticsCharts.tsx` (CPU, mémoire, réseau, disponibilité) et `TestsPerformanceCharts.tsx` (courbes temps réel) ; chargement via `next/dynamic` (`ssr: false`) sur `/analytics` legacy et `/backoffice/tests-performance` ; tooltips alignés `rechartsTooltipProps`. Validations sans `make` : `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé **0 erreur** sur les 4 fichiers modifiés. | [x] |
| P1C | Lot A — Block I/O observé + mémoire corrélation + retrait cAdvisor/Prometheus actifs | Suite retour porteur 15/06 : `/performances/disk` priorise désormais les conteneurs avec Block I/O réel récent avant de limiter à 8 historiques, et renomme le graphe en **Débit Block I/O observé — Mo/min** (dérivé des compteurs Docker persistés, plus “estimé”). Vérification BDD locale : `container_metrics_snapshots` 24 h contient des cumuls Block I/O non nuls ; calcul SQL 24 h montre des débits non nuls sur services actifs (ex. frontend/postgres/API). `/performances/network` ajoute la série **Mémoire %** au graphe **Corrélation CPU & mémoire (%) vs débit réseau**. Nettoyage runtime : `docker ps` local = `postgres`, `metrics-aggregator`, `monitoring-agent-rs`, pas de `cadvisor/prometheus`; retrait `jobbingtrack-cadvisor/prometheus` des probes santé, configs services/profils, route frontend cAdvisor, proxy maintenance Prometheus et compose production Prometheus. Validations sans `make` : JSON configs OK ; `node --check` backend ciblé OK ; `docker compose config --quiet` OK ; TypeScript direct frontend OK ; Jest charts **2 suites / 6 tests OK** ; ESLint ciblé **0 erreur** (warnings historiques) ; Playwright Performances direct **7/7 OK** en **44,0 s**. Wrappers `npm run type-check` et `npm run lint -- --quiet` reproduisent code 1 sans sortie, dette connue. | [x] |
| P1C | A1G Performances — libellés sources historiques étendus | Suite A1G 15/06 : `PerformanceHistoryCaption` sur cpu-memory, containers, network, disk, latency et synthèse `/performances` ; budget rendu latence/synthèse **160**. **Suite A1e 15/06** : brush synchronisé vue globale **CPU & Mémoire** (`SystemCpuMemoryAreaCharts` + `useSyncedChartBrushRange`, bouton **Réinitialiser le zoom**). Validations sans `make` : TypeScript direct OK ; ESLint ciblé **0 erreur** ; Jest `useSyncedChartBrushRange` **2/2 OK** ; Playwright Performances **7/7 OK** (dernier run après brush cpu-memory). | [x] |
| P1C | A1d/A1e Statistics — axes temps alignés Performances + lazy charts | Branche `fix/frontend-security-performance` : `StatisticsErrorAvailabilityCharts` passe l’axe X en `timeMs` (number) avec `domain={[xDomainMin, xDomainMax]}` via `chartXDomainFromDataRange` (fenêtre sticky + points rendus) ; tooltips `formatLocalDateTime` ; ticks avec date si plage > 24 h. `/statistics` (`page.tsx`) calcule `chartXMin/chartXMax/chartAxisShowDate` et les propage à **Vue d’ensemble** et **Santé technique** ; graphes chargés en `lazy()` + `Suspense` (`SectionLoader`). Validations sans `make` : `./node_modules/.bin/tsc --noEmit --pretty false` OK ; Jest `statisticsTimeSeries` **3/3 OK** ; Playwright Statistics **5/5 OK** en **26,6 s** ; wrappers `npm run type-check` / `npm run lint` : code 1 sans sortie, dette connue. | [x] |
| P1C | Lot A graphes — brush synchronisé page Performances Disque | **Correctif porteur 17/06** : graphes volume utilisé/total vides + erreurs Recharts `NaN` (overlay Next.js ~4 issues). Causes : indices brush calculés sur la série complète (`systemRows` ~860 pts) mais données chart filtrées (`displaySystemRows` ~80 pts) ; brush par défaut 80 pts sur axe 24 h ; `dataKey="timeMs"` sur Brush avec axe numérique. Correctifs : charts sur série complète ; brush pleine plage par défaut ; zoom brush → domaine X partagé ; lib `diskMetricsModel.ts` (normalisation octets/Go, dérivation used/total, axe volume) ; cartes **679.6 / 900.0 Go** + espace libre ; layouts admin/sécurité `mounted` anti-hydratation. Validations sans `make` : `tsc --noEmit` OK ; `npm run lint -- --quiet` OK ; Jest `diskMetricsModel` **3/3 OK** ; smoke navigateur `/performances/disk` — courbes volume sur 24 h, Block I/O inchangé, badge issues Next.js disparu après reload. | [x] |
| P1C | Performances Disque — Block I/O réel restauré | Retour porteur 15/06 : la page Disque affichait le graphe mais ne détectait pas vraiment la lecture/écriture Block I/O. Cause : `getContainerMetricsHistory()` privilégiait `container_metrics`, table sans colonnes Block I/O, puis renvoyait `blockReadBytes/blockWriteBytes = null`, ce qui masquait les valeurs existantes dans `container_metrics_snapshots`. Correctif : fusion `container_metrics` + `container_metrics_snapshots` sur l’endpoint historique conteneur, avec enrichissement par point proche et ajout des points snapshot-only dans la plage. Preuves sans `make` : `node --check persistence.service.js` OK ; `tsc --noEmit` direct frontend OK ; `npm run type-check` / `npm run lint` reproduisent code 1 sans sortie (dette connue) ; ESLint ciblé Disque OK ; API proxy 24 h sur les 8 conteneurs Disque → **16 points Block I/O non nuls** ; Playwright `/performances/disk` **2/2 OK**. | [x] |
| P1C | Lot A graphes — brush synchronisé page Performances Conteneurs | Branche `fix/frontend-security-performance` : `AnalyticsContainersChartsBundle` synchronise le zoom brush entre graphes CPU et mémoire (mode **Tous les conteneurs** et mode conteneur unique) via `useSyncedChartBrushRange` ; brush sur chaque graphe partageant `chartData`, bouton **Réinitialiser le zoom**. Validations sans `make` : `tsc --noEmit` OK ; ESLint ciblé bundle **0 erreur** ; Jest hook **2/2 OK** ; Playwright ciblé `/performances/containers` **2/2 OK** en **17,1 s**. | [x] |
| P1C | Lot A graphes — brush page Performances Latence | Branche `fix/frontend-security-performance` : `/performances/latency` migre vers `useSyncedChartBrushRange` (fenêtre récente par défaut, **Réinitialiser le zoom**) ; moyenne sur plage brush calculée sur `chartRows` rendus (corrige l’écart indices bruts vs points affichés). Validations sans `make` : `tsc --noEmit` OK ; ESLint ciblé latence **0 erreur** ; Playwright ciblé `/performances/latency` **2/2 OK** en **10,4 s**. | [x] |
| P1C | Nettoyage données app — audit lecture seule, aucune suppression | Demande porteur 15/06 : “nettoyer les app data” (entretiens, candidatures, utilisateurs hors admin, entreprises, contacts, appels, relances, événements). Action agent sûre : audit SQL lecture seule par agrégats séparés, après arrêt d’une première requête trop lourde. Constat local : `admin@jobbingtrack.com` concentre le volume métier (`applications=1043`, `companies=764`, `contacts=2`, `interviews=128`, `calls=200`, `followups=140`, `events=973`) ; nombreux comptes `e2e-noverify-*.mailhog.local` visibles avec 0 donnée métier. Décision : pas de suppression sans validation explicite ; `docs/TODOS.md` mis à jour pour dry-run + sauvegarde/export avant purge. | [x] |
| P1C | A1E Statistics — budget de points rendus aligné Performances | Suite A1E 15/06 : `/statistics` borne désormais les points rendus par `buildStatisticsChartData` à **160** sur 21/30 j, **140** sur 3/7/14 j, **120** sur 24 h et **100** sur les plages live courtes, au lieu de monter à 500/300/200. Objectif : réduire le coût Recharts sur les graphes réutilisés plusieurs fois dans la page sans changer les requêtes ni les données source. Validations sans `make` : TypeScript direct OK ; ESLint ciblé **0 erreur** (66 warnings historiques sur `statistics/page.tsx`) ; Jest `statisticsTimeSeries` **3/3 OK** ; Playwright Statistics **5/5 OK**. Wrappers `npm run type-check` / `npm run lint` : code 1 sans sortie, dette connue reproduite. | [x] |
| P1C | Rapport A/B frontend graphes Performances envoyé | Demande porteur 15/06 : comparaison sans/avec dernière optimisation de rendu Recharts. Test sans optimisation de rendu sur commit `2ccb6d19` : Playwright Performances **7/7 OK**, **42,7 s**. Test avec optimisation actuelle `d1bb789e` : Playwright Performances **7/7 OK**, **41,8 s**. Conclusion agent : gain faible en durée globale smoke (~2,1 %) car auth/navigation/API dominent, mais charge Recharts bornée côté points rendus et rechargements graphes sans erreur console. Rapport email `[JobbingTrack] Rapport A/B frontend graphes Performances 2026-06-15` envoyé via `notification-service` aux destinataires habituels : `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification `EmailLog` : **3/3 SENT**, miroir SMTP **3/3 sent=true**. | [x] |
| P1C | Performances — budget de rendu graphes lourds | Suite validation partielle porteur 15/06 : les nouveaux éléments et la résolution frontend sont validés, mais le lot complet reste ouvert. Réduction ciblée des points **rendus** sans supprimer l'historique demandé : Conteneurs détail **160** points, Conteneurs multi-séries **80/120/160** selon le nombre de séries ; marqueurs `C#` espacés dynamiquement selon points × séries ; CPU & Mémoire global **160** points, détail multi-services **120** si beaucoup de séries ; Réseau **160** points ; Disque **160** points. Validations sans `make` : TypeScript direct OK ; ESLint ciblé 0 erreur (1 warning hook préexistant Conteneurs) ; Jest `chartTimeDomain` **1/1 OK** ; Playwright Performances complet **7/7 OK** en **40,8 s** ; test navigateur ad hoc rechargements 7j/30j sur Conteneurs, CPU & Mémoire, Réseau, Disque **OK**, sans `Network Error`, `Unhandled Runtime Error`, `Application error` ni erreur console. | [x] |
| P1C | Graphes conteneurs multi-séries — distinction + sélection par graphique | Branche `fix/frontend-security-performance` : `/performances/containers` mode **Tous les conteneurs** utilise maintenant une palette locale ordonnée très contrastée (plus de couleur choisie par hash pouvant rapprocher deux verts/rouges), des codes courts visibles (`C1`, `C2`, etc.), des formes distinctives (rond, carré, losange, triangle, croix, plus, hexagone, étoile, barre, chevron, capsule, anneau), des styles de ligne et des marqueurs `C#` espacés directement sur les courbes. Les badges de sélection reprennent la vraie couleur de la série avec contraste texte auto. Panneaux de sélection indépendants pour **Séries CPU** et **Séries mémoire** : chaque graphique peut masquer/afficher ses conteneurs à la volée, sans affecter l’autre graphique. Mode conteneur unique : CPU plein, mémoire tirets. Suite appliquée : la palette partagée `seriesColors` utilisée par les autres graphes multi-séries passe aussi sur une palette ordonnée fortement contrastée au lieu d’un départ par hash. Validations sans `make` : `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé `seriesColors.ts`, `AnalyticsContainersChartsBundle.tsx`, `CpuMemoryServiceLinesChart.tsx` + page Conteneurs **0 erreur** (1 warning hook préexistant page Conteneurs) ; Jest `chartTimeDomain` **1/1 OK** ; Playwright ciblé `/performances/containers` **2/2 OK** (setup admin + page). Suite retour erreur frontend 15/06 : warning React/Recharts `key` corrigé sur `SeriesPointDot` (clé passée directement, plus de spread de `key`), test navigateur ad hoc **OK** : `/performances/containers` charge sans erreur console, `Tout masquer` CPU passe **23/23 → 0/23** sans modifier Mémoire, `Tout afficher` restaure CPU, `/statistics` bouton **Actualiser** + bascule **Auto-refresh pause → actif** OK. Revalidation après correctif : TypeScript direct OK, ESLint ciblé OK, Jest `chartTimeDomain` **1/1 OK**, Playwright Performances complet **7/7 OK**. Wrappers `npm run type-check` / `npm run lint` : code 1 sans sortie, dette connue. | [x] |
| P1C | Frontend CPU/RAM — Statistics en mode sobre par défaut | Branche `fix/frontend-security-performance` : `/statistics` garde le chargement initial mais n’installe plus de polling automatique tant que l’utilisateur n’active pas **Auto-refresh actif**. Bouton **Actualiser** ajouté pour rafraîchir manuellement stats + historique selon l’onglet courant ; auto-refresh explicite conserve l’intervalle de préférence (60 s par défaut) et reste ignoré si l’onglet navigateur est masqué. Validations sans `make` : `./node_modules/.bin/tsc --noEmit --pretty false` OK ; Jest `chartTimeDomain` **1/1 OK** ; ESLint ciblé **0 erreur** (warnings historiques nombreux sur `statistics/page.tsx`, dont `any`, imports inutilisés et deps hooks déjà présents). Scripts `npm run type-check` / `npm run lint` : code 1 sans sortie, dette connue. | [x] |
| P1C | Performances — plages longues 30 j non recadrées sur les derniers points | Branche `fix/frontend-security-performance` : correction backend `metrics-aggregator` sur `/persistence/system/metrics` et `/persistence/containers/metrics` pour échantillonner toute la fenêtre demandée au lieu de renvoyer seulement les derniers points `LIMIT`. Correction front : domaine X des graphes Performances respecte la plage sélectionnée (ex. 30 j), sans zoom silencieux sur les seuls points disponibles. Constat BDD local : `system_metrics` couvre **2026-06-12 11:15 → 2026-06-15 15:19** (donc pas 30 j réels), `container_metrics` couvre **2026-06-15 14:48 → 15:19** ; le test direct backend 30 j renvoie **210 buckets** de `2026-06-12T11:15:13Z` à `2026-06-15T15:01:33Z`. Validations sans `make` : `/usr/bin/node --check backend/metrics-aggregator-service/src/services/persistence.service.js` OK ; `./node_modules/.bin/tsc --noEmit --pretty false` OK ; Jest `chartTimeDomain` **1/1 OK** ; ESLint ciblé **0 erreur**, 5 warnings `any` préexistants sur pages Sécurité. Scripts `npm run type-check` / `npm run lint` reproduisent la dette connue : code 1 sans sortie. | [x] |
| P1D | Bootstrap admin `up-full` — cadrage dette pré-déploiement | Constats porteur 15/06 consignés dans `docs/TODOS.md` : double cible `create-admin-user` dans `makefiles/database/Makefile` (l.289–306 vs stub OBSOLETE l.314–317) ; `up-full` appelle la cible cassée après vérif SQL sur `admin@jobbingtrack.test` ; script `backend/scripts/database/create-admin-user.sh` réécrit mais Make bloque. Pas de correctif code dans ce lot — traitement prévu avec gate `A_VALIDER_AVANT_PRODUCTION.md` / tests globaux finaux. Contournement documenté : script direct + `ADMIN_EMAIL` / `ADMIN_PASSWORD` dans `.env`. | [x] |
| P1C | Frontend CPU/RAM — Sécurité (menaces, réseau, incidents) en mode sobre | Branche `fix/frontend-security-performance` : `/security/threats` — auto-refresh **désactivé par défaut**, intervalle par défaut **60 s** (plus 5 s actif au boot), garde `document.visibilityState`, options 15–60 s en tête du sélecteur. `/security/network` et `/security/incidents` : polling **60 s** (au lieu de 45 s) uniquement si onglet visible. Validations sans `make` : `./node_modules/.bin/tsc --noEmit --pretty false` OK ; lints IDE OK. | [x] |
| P1C | Frontend CPU/RAM — Services en mode sobre par défaut | Branche `fix/frontend-services-performance` : nettoyage Git après HEAD détaché + caches `.tmp-jest`; page `/services` sans polling automatique par défaut, bouton **Auto-refresh pause/actif** (60 s si activé), déduplication des chargements concurrents, mini-séries limitées à **12** services visibles, **24** points et concurrence **2**. Fiche `/services/<service>` : auto-refresh désactivé par défaut, intervalle par défaut **60 s**, logs limités à **60** lignes, historique serveur **180** points et session **160**. Validations sans `make` : `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint direct ciblé OK ; lints IDE OK ; Jest fiche service **1 suite / 33 tests OK** ; Playwright ciblé Services sur `PLAYWRIGHT_BASE_URL=http://localhost:5003` **3/3 OK** (setup + liste + détail api-gateway). Les wrappers `npm run type-check` et `npm run lint -- --quiet` sortent encore code 1 sans sortie, dette déjà tracée. | [x] |
| P1C | Cadrage backlog — réduction CPU/RAM frontend + étude metrics-aggregator Rust | Demande porteur 15/06 consignée dans `docs/TODOS.md` : le chantier **Frontend mémoire/performance** cible désormais le constat `frontend` ~40 % CPU / ~1800 MiB RAM, notamment Services/backoffice, avec campagne de profiling toutes pages lourdes, mesure temps de chargement/CPU/RAM/volume API et objectif de réduction minimum **50 %**. Ajout aussi d’une tâche **Metrics-aggregator sobre / Rust éventuel** : décision conditionnée à baseline, prototype minimal, compatibilité endpoints/tests et comparaison RAM/CPU/latence/maintenance avant toute réécriture. | [x] |
| P1C | Lot A graphes — brush synchronisé page Performances Réseau | Branche `feat/monitoring-brush-sync-network` : hook partagé `useSyncedChartBrushRange` (Jest **1 suite / 2 tests OK**) ; `/performances/network` synchronise le zoom brush entre les graphes RX/TX cumul, débit Mo/min, temps de réponse (si présent) et corrélation CPU vs réseau (`SystemCpuNetworkCorrelationChart`). Bouton **Réinitialiser le zoom** quand une plage personnalisée est active. Validations sans `make` : `type-check` OK ; `lint --quiet` OK ; Playwright `npm run test:e2e:performances` avec `PLAYWRIGHT_BASE_URL=http://localhost:5003` → **7/7 OK** (**~1,1 min**, Réseau **6,5 s**). | [x] |
| P1B | Performances Corrélation — contexte endpoint incidents | Branche `fix/correlation-incident-context` : le tableau “Corrélation fine incidents ↔ points métriques” expose maintenant une colonne **Méthode** et renforce le parsing contexte depuis `method/httpMethod/requestMethod/request.method`, `endpoint/originalUrl/path/url/request.url/req.url`, URL complète (`protocol`, port 80/443), `host:port`, `port/localPort/serverPort/remotePort`. Le diagnostic de ligne inclut aussi **Méthode HTTP** avec source technique et correctif suggéré. Quand la donnée n’a jamais été persistée dans `aggregated_logs.metadata` ou le message, l’UI affiche `—` et le diagnostic explique la source manquante au lieu d’inventer. Validations sans `make` : Prettier ciblé OK ; `npm run type-check` OK ; `npm run lint -- --quiet` OK ; Jest `performanceCorrelationModel` **1 suite / 5 tests OK** ; lints IDE OK. Playwright Performances : échec initial car `PLAYWRIGHT_BASE_URL=http://localhost:3000` alors que le frontend Docker écoute sur **5003** (port 3000 fermé) ; relance via `playwright.standalone.config.ts` + `PLAYWRIGHT_BASE_URL=http://localhost:5003` (ou script `npm run test:e2e:performances`) → `performances-range-smoke.spec.ts` **7/7 OK** en **48,3 s** (dont Corrélation **7,9 s**). | [x] |
| P1C | Lot A graphes — mini-série historique liste Services + dette CPU global | Branche `feat/services-history-sparklines` : `/b4ck0ff1ce/services` remplace la jauge seule par une mini-série CPU historique SVG quand `/docker/service/:name/history` renvoie au moins 2 points ; fallback jauge instantanée conservé. Chargement borné : 24 services visibles, concurrence 3, 36 points max/service, sans Recharts sur la liste. Dette porteur notée dans `docs/TODOS.md` : ne pas présenter une somme CPU conteneurs >100 % comme CPU global machine ; distinguer CPU global 0–100 %, CPU conteneur multi-cœur, somme conteneurs. Validations sans `make` : `format:check` OK ; `type-check` OK ; `lint --quiet` OK ; Jest `serviceCpuSparklineModel` **1 suite / 3 tests OK**. | [x] |
| P1C | Lot A graphes — export CSV/JSON séries affichées | Branche `feat/monitoring-series-export` : utilitaire `seriesExport` testé + composant `SeriesExportButtons`. `/performances/network` exporte les séries visibles (RX/TX cumulés, débit Mo/min, CPU, mémoire, temps de réponse). `/services/[serviceName]` exporte l’historique service (CPU/mémoire/réseau/Block I/O cumul + débit estimé). Validations sans `make` : `npm run type-check` OK ; `npm run lint -- --quiet` OK ; Jest `seriesExport.test.ts` **1 suite / 3 tests OK** ; Jest fiche service **1 suite / 33 tests OK**. Playwright Performances **7/7 OK** via `PLAYWRIGHT_BASE_URL=http://localhost:5003` (`npm run test:e2e:performances`). | [x] |
| P1D | GitHub Actions / PR / préprod Portainer — suite lot | PR #8 (`chore/ci-playwright-performance-lot` → `dev`) verte puis mergée le 15/06 (`73dea552`). PR #7 conflictuelle fermée et remplacée par PR #9, elle-même verte puis mergée dans `dev` (`e6e5cb90`) : scan CVE full-scope, jobs backoffice pollés et rapports `summary.json`. Validations sans `make` : Jest CVE **2 suites / 4 tests OK**, smoke `cve-findings` OK (**33 findings**, limit 2 affichés), smoke `cve-scan.py` full-scope OK (**24 surfaces Node**, Rust, **2 surfaces Flutter**, rapport `tests/results/security/cve-reprise-smoke/cve-20260615-032823/summary.md`), `format:check`/`type-check`/`lint --quiet` OK, Jest CI frontend **42 suites / 179 tests OK**, Playwright login sécurité **5/5 OK**, Playwright `/tests-security` **2/2 OK**. Cadrage préprod Portainer : `deploy-preprod.yml` manuel/branche `preprod`, webhook `PREPROD_DEPLOY_URL` + `PREPROD_DEPLOY_TOKEN`, échec non masqué, doc `docs/deployment/VPS_PORTAINER_NPM_OVH.md` §5.1. Mail récap `[JobbingTrack] Recap P1D CI PR preprod 2026-06-15` via `notification-service` à `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh` : `EmailLog` **3/3 SENT**, `metadata.mirror.sent=true` **3/3**. Branche suite `chore/ci-pr-preprod-portainer` synchronisée sur `dev` post-merge PR #9. | [x] |
| P1B | Infra Postgres — saturation connexions | Correctif 15/06 : singleton `getPersistenceTableStats()` dans `persistence.service.js` ; route `/stats` déléguée ; `POSTGRES_MAX_CONNECTIONS=200` dans Compose + `.env.example`. Recreate `postgres` + `jobbingtrack-metrics-aggregator` : `max_connections=200`, repos **~5** conn., burst **20/20** `/persistence/stats` OK, post-burst **~18** conn. Backend Jest `persistenceStatsRoute` via TMPDIR local (si node_modules présent). Mail récap `[JobbingTrack] Recap infra Postgres saturation 2026-06-15` : **3/3 SENT**, miroir **3/3**. Commits `f86fd05b`, `8d3ec45a` poussés sur `chore/ci-pr-preprod-portainer`. | [x] |
| P1B | Performances — Réseau, Corrélation et panneau P1B temps de réponse | Correctifs 15/06 : Postgres retiré des **services prioritaires temps de réponse P1B** ; panneau Vue d’ensemble sans `postgres` / `Santé Docker`. Corrélation : squelette chargement puis graphes (`Synthèse chargée: 8/8`). Réseau : état explicite si RX/TX absents. **Revalidation 15/06 post-infra** : Playwright `performances-range-smoke.spec.ts` **7/7 OK** ; Jest ciblé **12 tests OK** (`serviceHealthOverview`, `PriorityResponseServicesSummary`) ; `format:check`, `type-check`, `lint --quiet` OK. Mail récap `[JobbingTrack] Recap P1B Performances Reseau Correlation 2026-06-15` : **3/3 SENT**, miroir **3/3**. | [x] |
| P1B | Analytics — moteur UI partagé shell (revalidation) | Branche `feat/p1b-analytics-ui-shell-validation` : `AnalyticsPageShell` via `ApplicationSubNav` sur hub + Application performance/activity/feedback. Jest `backoffice/analytics/__tests__` **5 suites / 20 tests OK** ; routes `/analytics/*` HTTP **307** sans session. Mail récap `[JobbingTrack] Recap P1B Analytics UI shell 2026-06-15` : **3/3 SENT**, miroir **3/3**. | [x] |
| P1B | Statistics overview + Performances Corrélation — correction retour porteur | Branche `fix/performance-correlation-fluency` : `StatisticsErrorAvailabilityCharts` empile maintenant disponibilité et taux d’erreur en deux cartes pleine largeur. Corrélation : bootstrap réduit à un seul service préféré, auto-refresh lourd en pause par défaut (toggle persistant session), clic nom service = focus + filtre, bouton tableau `Charger/Voir`, mode complet jusqu’à **24** services, cache historique par période pour ne pas refetch tous les services déjà chargés à chaque clic. Dette notée dans `docs/TODOS.md` : sécurité historique long terme au-delà de 30 j / 2000 lignes, snapshots score horodatés, analytics utilisateur/app/mobile + rapports d’erreurs exploitables. Validations sans `make` : frontend `format:check`, `type-check`, `lint --quiet` OK ; Jest monitoring/metrics **2 suites / 12 tests OK** ; Playwright `statistics-smoke` + `performances-range-smoke` **11/11 OK**. Browser MCP direct bloqué par session `/login`, sans contournement manuel. Mail récap `[JobbingTrack] Recap P1B Corrélation Performances fluidité 2026-06-15` : **3/3 SENT**, miroir **3/3 true**. | [x] |
| P0 | Cohérence du nouveau système de pilotage | Liens mis à jour vers `PILOTAGE.md`, `TODOS_A_VALIDER.md`, `TODOS_A_VERIFIER.md`; ancienne référence `A_VALIDER_VERIFIER.md` supprimée ou remplacée. | [x] |
| P0 | Règle Cursor de pilotage | `.cursor/rules/pilotage-validation.mdc` existe et impose la lecture du pilotage avant action. | [x] |
| P0 | Docs de suivi accessibles | `docs/README.md`, `docs/TODOS.md`, `docs/security/README.md`, `TRAITER_IMMEDIATEMENT.md` pointent vers les bons fichiers. | [x] |
| P0 | Correctif Firewall backoffice | Déblocage IP + garde-fous règles globales couverts par `env npm test -- --runTestsByPath tests/firewall-unblock-ip.test.js --verbose --runInBand` OK (6/6) ; `env npm run type-check` OK ; `env npm run lint` OK avec warnings historiques uniquement. | [x] |
| P0 | Rapport sécurité frais pour backoffice | `python3 scripts/security/cve-scan.py --output-dir tests/results/security --timeout-sec 60` OK ; API `/api/test-reports/all` liste `security-results-cve-20260521-201336` en premier ; `/api/test-reports/view` OK ; `/api/test-reports/download` OK (`cve-20260521-201336-summary.md`). | [x] |
| P0 | Comparaison rapports sécurité CVE | Route `/api/test-reports/compare` utilise `frontend/src/lib/test-reports/resolveReport.ts` (IDs `security-results-*`, `security-reports-*`, `summary.md` parsé en lignes par surface). UI dédiée : cartes Critical/High/Medium/Low/Info par rapport, tableau par surface, écarts, notes/payloads non renvoyés. Validation 21/05 : `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé sur `compare/route.ts`, `resolveReport.ts`, `test-reports/page.tsx` OK avec warnings historiques uniquement. `npm run type-check` / `npm run lint` sortent `1` sans sortie dans ce terminal, mais les commandes sous-jacentes passent. | [x] |
| P0 | Cadrage nouvelles demandes sécurité 08/06 | `TODOS_A_VALIDER.md` et `docs/TODOS.md` contiennent : alertes email critiques JobbingTrack vers adresses dev/admin porteur, CVE applicatives localisées (ex. `CVE-2026-49975`), tests offensifs contrôlés par conteneur (remote host/shell/URL/headers), réduction d’exposition/leurres VPS-Portainer, comparaison CVE exploitable malgré gros volumes, et accès notes/payloads bruts uniquement via réauth forte ; `A_VALIDER_AVANT_PRODUCTION.md` contient les gates préprod/prod associés. | [x] |
| P0 | Inventaire menaces lab/privées (lecture seule) | Script `scripts/security/security-threats-inventory-dry-run.cjs` : classe sans DELETE — `lab_test` (RFC 5737 `198.51.100.x`, `203.0.113.x`, `192.0.2.x`, `10.0.0.x`, metadata lab/test), `private_network` (`172.16–172.31.x`, `192.168.x`, autres `10.x`). Dry-run 21/05 (réf. `docs/TODOS.md`) : **228** lab_test (135 high/critical, 0 blocked), **11** private_network (2 high/critical, 1 blocked). **Aucune purge** tant que porteur n’a pas validé la ligne P0 dans `TODOS_A_VALIDER.md`. Rejouer localement : `node scripts/security/security-threats-inventory-dry-run.cjs --limit=25` (nécessite conteneur `jobbingtrack-postgres` up). | [x] |
| P1 | UX rendu rapports sécurité | Rendu CVE Markdown amélioré dans `/api/test-reports/view` : cartes synthèse, priorités à trier, tableau responsive, markdown brut repliable ; `env npm run type-check` OK ; `env npm run lint` OK avec warnings historiques ; smoke API view OK. | [x] |
| P1 | Validation frontend si fichier UI touché | `env npm run type-check` OK ; `env npm run lint` OK avec warnings historiques uniquement ; pas de Jest frontend ciblé existant pour `firewall/page.tsx`. | [x] |
| P0 | Tests sécurité backoffice — filesystem writable | `docker-compose.yml` : `TESTS_RESULTS_DIR=/tmp/tests/results` + volume `./tests/results:/tmp/tests/results` (car `./tests:/app/tests:ro`). `generate-test-report.sh` bascule vers `/tmp/tests/results` si mkdir échoue. Recreate conteneur `frontend` requis après merge. | [x] |
| P0 | Comparaison CVE — exploitabilité et filtres | `resolveReport.ts` : statut `skip` pour surfaces skipped (plus de faux « Échoué »). `compare/route.ts` : libellés écart sécurité, tri par score exploitabilité. UI : filtres type docker/node, critical/high only, masquer absents, badge « Ignoré ». `./node_modules/.bin/tsc --noEmit` OK. | [x] |
| P0 | Détails sensibles sous réauth | APIs `POST /api/test-reports/sensitive-step-up` (vérif mot de passe via auth login) + `GET /api/test-reports/sensitive-details` (jeton court usage unique, audit `tests/results/security-audit/step-up.jsonl`, `Cache-Control: no-store`). UI modal sur comparaison sécurité. `./node_modules/.bin/tsc --noEmit` OK. | [x] |
| P0 | Rapport tests sécurité backoffice — stats cohérentes | `generate-test-report.sh` : pattern 3 désactivé pour `security`, parsing `security-report.json` via **node** (pas jq requis), statut `warning` si medium/low seuls, labels Critical/High vs Medium/Low. `METRICS_AGGREGATOR_INTERNAL_URL` pour logs services. | [x] |
| P0 | Scan CVE + localisation depuis backoffice | `POST /api/test/run-cve-scan`, `GET /api/security/cve-locate?cve=…`, `scripts/security/cve-locate.mjs`, page tests-security : boutons séparés applicatifs / CVE + recherche CVE. `frontend/Dockerfile` : `python3` + `jq` (rebuild frontend requis). `./node_modules/.bin/tsc --noEmit` OK. | [x] |
| P0 | Correctifs retours porteur tests sécurité 16h | `run-security/route.ts` lit `security-report.json` pour décider `critical/high` et strip ANSI ; `view/route.ts` ouvre un rapport standard via fallback `report.html → summary.json → security.json → security-report.json`; `cve-scan.py` marque `vulnerable` si critical/high et `warning` si medium/low ; page `tests-security` strip ANSI côté terminal. Smoke `python3 scripts/security/cve-scan.py --output-dir /tmp/jt-cve-smoke --timeout-sec 30` OK avec surfaces `vulnerable`; `./node_modules/.bin/tsc --noEmit` OK. | [x] |
| P2 | Rapports tests — taille affichée | `/api/test-reports/all` calcule la taille complète des dossiers de rapports `tests/results/*` et `tests/results/security/*`; `/b4ck0ff1ce/test-reports` affiche la taille Ko/Mo par carte et le volume total de la liste filtrée. Compression/archivage noté dans `docs/TODOS.md`, sans purge automatique. | [x] |
| P2 | Agent email / tâches recherche emploi — cadrage documentaire | Cadrage ajouté sans implémentation : `docs/features/EMAIL_TRIAGE_AGENT.md` détaille objectif, sources Gmail/boîte candidatures, digest 18h, interface utilisateur hors backoffice, Google Tasks/Calendar, IA locale/optionnelle, sécurité OAuth/RGPD et critères d’acceptation. Liens ajoutés dans `docs/TODOS.md`, `docs/BACKLOG.md`, `docs/PLAN.md`, `docs/STATUS.md`, `docs/project/FONCTIONNALITES.md` et `TODOS_A_VALIDER.md`. Branche documentaire dédiée : `docs/email-triage-agent-roadmap`; rappel : reprendre ensuite `fix/security-cve-scan-full-scope` / PR #7 pour le P0 CVE. | [x] |
| P1A | Frontend tests — suppression des credentials codés en dur | Les tests `.ts` frontend et utilitaires ADB ne doivent plus retomber sur `admin@jobbingtrack.test`, `password123`, `user1@jobbingtrack.test` ou équivalents. Les credentials admin/user/mobile/email-verification sont lus via variables d’environnement (`TEST_ADMIN_EMAIL`, `TEST_ADMIN_PASSWORD`, `TEST_USER_EMAIL(S)`, `TEST_USER_PASSWORD`, `E2E_GENERATED_USER_PASSWORD`, `NEXT_PUBLIC_*`, `TEST_EMAIL_RECIPIENT`) et échouent explicitement si absents. Recherche ciblée `rg` sur les anciens secrets `.ts` : aucun match restant hors données mock non credential. | [x] |
| P1A | Nettoyage adresses email réelles dans l’état courant | Remplacement des adresses personnelles/projet routables par des placeholders non routables (`example.invalid`, `jobbingtrack.test`) dans les fichiers suivis. Preuve agent : scan scripté sur `git ls-files` + regex email → `TOTAL_ROUTABLE=0`. À distinguer de la purge historique Git, qui nécessitera une réécriture d’historique et force-push explicite. | [x] |
| P2 | Agent email — configuration d’envoi digest clarifiée | Le cadrage ne présente plus d’adresse factice spécifique comme adresse utilisable. Apps Script lit désormais `TRIAGE_DIGEST_RECIPIENT` depuis les propriétés du script et échoue si absent ; le futur digest natif JobbingTrack doit passer par le socle SMTP existant (`SMTP_*`, `SMTP_FROM`, `SMTP_REPLY_TO`, `EmailLog`) comme reset/validation/notifications. | [x] |
| P2 | Agent email — périmètre interface et suivi enrichi | Cadrage ajouté dans `docs/features/EMAIL_TRIAGE_AGENT.md`, `docs/PLAN.md` lot I, `docs/TODOS.md`, `docs/BACKLOG.md`, `docs/STATUS.md`, `docs/project/FONCTIONNALITES.md` et `TODOS_A_VALIDER.md` : interface web/mobile responsive, revalidation PIN, autocomplete accessible, boîte de réception agent, envoi email contrôlé, calendrier agrégé, programmation manuelle d’appels/tâches/rappels/événements même sans email déclencheur, appels préremplis, relances depuis fiche candidature, détail entreprise enrichi, suivi intérim, import Google Contacts CSV/vCard, sauvegarde PDF offre, enrichissement entreprise et veille salons/job dating par ville/région. | [x] |
| P2 | Agent email — architecture frontend utilisateur/backoffice | Cadrage ajouté : espace utilisateur connecté sur `/`, backoffice admin sur `/b4ck0ff1ce`, base de composants partagée, possibilité future de séparer `user-frontend` et `backoffice-frontend`. `.env.example` / `.env.production.example` clarifient `USER_FRONTEND_URL`, `BACKOFFICE_FRONTEND_URL`, `NEXT_PUBLIC_USER_FRONTEND_URL`, `NEXT_PUBLIC_BACKOFFICE_URL` et `ALLOWED_ORIGINS` (origines uniquement, pas chemins). Validation 09/06 : scan emails suivis `CURRENT_ROUTABLE_EMAILS=0`; `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé `tests/e2e/email-verification-monitor.spec.ts` OK ; diagnostics IDE OK. `npm run type-check` / `npm run lint` sortent encore `1` sans sortie exploitable dans ce terminal, comme constaté précédemment. | [x] |
| P2 | Agent email / tâches recherche emploi — cadrage porteur renforcé | `docs/features/EMAIL_TRIAGE_AGENT.md`, `docs/TODOS.md`, `docs/BACKLOG.md`, `docs/PLAN.md`, `docs/STATUS.md` et `TODOS_A_VALIDER.md` actent : JobbingTrack comme socle, pas Make.com/Zapier pour la logique métier, worker planifié, Gmail OAuth lecture seule, boîtes configurées hors Git, stockage interne emails utiles, règles déterministes, digest 18h, Google Tasks/Calendar obligatoires, IA locale en renfort, interface privée dédiée séparée du backoffice email transactionnel. | [x] |
| P1C | Backoffice Développement → Tests — navigation à réorganiser | Demande porteur ajoutée sans implémentation : clic direct sur Tests = vue d’ensemble ; sous-menu Rapports pour Rapports de tests + Rapports de parcours ; menu Tests moins long et mieux catégorisé. Suivi dans `TODOS_A_VALIDER.md` P1C et `docs/TODOS.md`, après P0 CVE. | [x] |
| P1D | Frontend validation scripts — sortie npm inexploitable | À diagnostiquer : `npm run type-check` et `npm run lint` peuvent sortir code 1 sans stdout/stderr utile alors que les commandes directes sous-jacentes passent. Vérifier scripts `package.json`, shell/env, redirections, wrappers, hooks, comportement CI et terminal Cursor ; l’objectif est d’obtenir une erreur lisible ou un code 0 cohérent. Reproduction 09/06 : `npm run type-check` = 1 sans sortie, `npm run lint` = 1 sans sortie ; `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé `src/lib/adb/adb-steps.ts` OK. | [ ] |
| P1A | Tests hors frontend — credentials codés en dur historiques | Dette ouverte après scan `tests/` : plusieurs anciens specs/helpers/fixtures contiennent encore des fallbacks `admin@jobbingtrack.test`, `password123`, `SecureP@ss123!` ou `admin123`. Première correction appliquée aux specs mobile email Gmail/Proton/BlueMail et au script `tests/email/run-inscription-gmail-email-check.js`; poursuivre fichier par fichier sans casser les fixtures explicitement mockées. | [ ] |
| P1A | Sécurité réseau avancée — cadrage menaces | Demande porteur notée dans `docs/security/SECURITY_TESTING_MATRIX.md`, `docs/security/ROADMAP_SECURITE_API_ET_BACKOFFICE.md`, `docs/TODOS.md` et `TODOS_A_VALIDER.md` : HTTP forgé, DNS poisoning, UPnP abuse, session hijack, IP spoofing, ICMP redirect, BGP hijack, ARP spoofing, MAC flooding, VLAN hopping, port scan/SYN scan/SYN flood contrôlé ; tests uniquement lab/préprod autorisée, bornés et non destructifs. | [x] |
| P0 | Comparaison CVE — statut dérivé des compteurs | `resolveReport.ts` + `securityStatus.ts` : statut `vulnerable/warning/skipped` recalculé depuis critical/high/medium/low (plus de `ok` si high>0) ; UI comparaison affiche libellé « À traiter » / « À surveiller ». `./node_modules/.bin/tsc --noEmit` OK. | [x] |
| P0 | Comparaison CVE — tests unitaires ciblés rapports | Tests ajoutés : `securityStatus.test.ts` verrouille critical/high → `vulnerable` / « À traiter », skipped conservé, medium/low → `warning` ; `resolveReport.test.ts` fabrique un rapport `security-results-cve-*` avec uniquement `summary.json` et vérifie que `loadCompareReport` produit des lignes comparables fail/pass/skip au lieu d’un rapport vide. `./node_modules/.bin/jest --runTestsByPath "src/lib/test-reports/securityStatus.test.ts" "src/lib/test-reports/resolveReport.test.ts" --runInBand --verbose` OK : 2 suites, 4 tests. `./node_modules/.bin/tsc --noEmit` OK ; ESLint ciblé OK ; diagnostics IDE OK. `npm run type-check` et `npm run lint -- --quiet` retestés : échec sans sortie dans cet environnement, compensé par les binaires directs. | [x] |
| P0 | Fiches finding CVE critical/high | `scripts/security/cve-audit-parse.mjs`, `cve-findings.mjs`, `GET /api/security/cve-findings`, enrichissement `cve-locate.mjs` ; UI `/b4ck0ff1ce/tests-security` : bouton « Charger findings critical/high » + cartes package/version/service/lockfile/exposition/exploitabilité/correctif. Smoke `/usr/bin/node scripts/security/cve-findings.mjs --limit 2` OK (35 findings détectés). `./node_modules/.bin/tsc --noEmit` OK. | [x] |
| P0 | Localisation CVE runtime Node (`CVE-2026-21710`) | `scripts/security/cve-locate.mjs` scanne désormais aussi les Dockerfiles Node connus ; `CVE-2026-21710` remonte `node-runtime` sur les images `node:20.18.0`, avec `patchedVersion=20.20.2`, surface HTTP Node et correctif. Test `/usr/bin/node tests/node_modules/jest/bin/jest.js --config tests/jest.config.js tests/security/cve-locate-runtime.test.js --runInBand` OK (1/1). UI `tests-security/page.tsx` affiche image, version corrigée, surface et correctif ; `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé OK avec warnings historiques uniquement. | [x] |
| P1 | Guide validation porteur P1A/P1B/P1C | `TODOS_A_VALIDER.md` : mode opératoire pas à pas avec URLs `localhost:5003`, menus backoffice, commandes terminal copiables, critères OK/KO et textes de réponse pour chaque ligne P1A/P1B/P1C. | [x] |
| P1A | Alertes email critiques — archivage validation porteur | Porteur confirme réception réelle `admin@delhomme.ovh` (smoke batch, validation P1A, E2E security-service), non-spam. Ligne retirée de `TODOS_A_VALIDER.md`, archivée dans `TODOS_DONE.md` (11/06). Notes backlog : sync Google Tasks bidirectionnelle, `security@jobbingtrack.com` en fin de programme. Commits `800e9d08`, `b6ff92d0`. | [x] |
| P1A | Alertes email — non-régression post-archivage | Jest SMTP **5/5**, payload+disponibilité **6/6** ; `tsc --noEmit` OK ; conteneurs `notification-service`, `security-service`, `frontend`, `postgres` healthy. | [x] |
| P1A | Tests offensifs contrôlés — pages de consultation accessibles | Après archivage P1A email, première ligne ouverte = **Tests offensifs contrôlés par conteneur JobbingTrack**. Vérification navigateur sans scan offensif : `/b4ck0ff1ce/tests-security` charge la page **Tests Sécurité** (tests applicatifs, scan CVE, couverture WAF/auth/injection/headers) ; `/b4ck0ff1ce/test-reports` charge les rapports. Documents de cadrage relus : `docs/security/SECURITY_TESTING_MATRIX.md` et `docs/security/COMPOSE_RUNTIME_HARDENING.md`. | [x] |
| P1A | Tests offensifs contrôlés — préflight non destructif | `scripts/security/controlled-offensive-preflight.cjs` ajouté : classe la cible `allowed` / `needs_approval` / `blocked`, bloque la prod sans fenêtre explicite, demande approbation pour cible publique externe et ne lance aucun payload (`dryRun=true`). Preuves : `/usr/bin/node scripts/security/controlled-offensive-preflight.cjs --target=http://localhost:5002 --environment=local --json` → `allowed`; Jest direct `./frontend/node_modules/.bin/jest --config tests/jest.config.js --runTestsByPath tests/security/controlled-offensive-preflight.test.js` → **5/5 OK** ; `frontend npm run type-check` OK ; `frontend npm run lint` OK ; diagnostics IDE sans erreur sur fichiers touchés. | [x] |
| P1A | Tests offensifs contrôlés — manifeste périmètre lab | `scripts/security/controlled-offensive-lab-scope.cjs` ajouté : combine le préflight et une matrice services/scénarios (`api-gateway`, `auth-service`, `application-service`, `notification-service`, `security-service`, `monitoring-agent-rs`) en mode `plan-only`, `dryRun=true`, `willRunPayload=false`. Objectif : validation porteur du périmètre avant tout runner actif. Preuves : script local `--json` → `ready_for_review`; Jest direct `controlled-offensive-preflight.test.js` + `controlled-offensive-lab-scope.test.js` OK. | [x] |
| P1A | Archive logs sécurité sans purge (preuve agent) | `/usr/bin/env node scripts/security/security-logs-archive-export.cjs --class=noise --limit=50` OK → `data/archives/security-logs/2026-06-10/manifest.json` + `noise.jsonl.gz` (50 lignes, sha256 `04194f6b7cee…`) ; `security_logs` **42311** avant et après ; note manifest « Export only — no rows deleted ». | [x] |
| P1A | Restauration staging logs sécurité (preuve agent) | `/usr/bin/env node scripts/security/security-logs-archive-restore.cjs --class=noise --load-staging` OK : `verified=50`, `staging_loaded=50`, `No write to public.security_logs` ; `security_logs_restore_staging` = **50** ; `security_logs` inchangé **42311**. | [x] |
| P0 | Correctif `make up-full` — SMTP_FROM docker-compose | `docker-compose.yml` L751 : `${SMTP_FROM:redacted@…}` → `${SMTP_FROM:-redacted@…}` (syntaxe défaut Compose). `docker compose config --quiet` OK ; stack relancée via `docker compose` (postgres, full, https) ; `notification-service` healthy. | [x] |
| P1A | Gate validation — 6 commits alertes email (`8a6b38b2`→`ac057bf5`) | Jest SMTP **5/5**, payload+disponibilité **6/6** ; `tsc` + ESLint ciblés OK ; conteneurs clés healthy ; mount `notification-service/src` confirmé ; smoke `BATCH VALIDATION 6 COMMITS 2026-06-10T19:46:11Z` → API `202`, `metadata.mirror.sent=true`, `messageId 20b94457-…@maily.ovh`. Détail dans `docs/STATUS.md` § gate validation 6 commits. | [x] |
| P1A | Alertes email — diagnostic admin retiré après OK porteur | Après validation réelle des alertes critiques, `SecurityAlertEmailDiagnostics` ne met plus en avant une adresse admin dédiée : suppression de la requête `q=admin...`, de l’encart « Dernier envoi vers admin » et du surlignage ciblé. Le panneau conserve la liste claire des derniers `EmailLog` `NOTIFICATION`, statut, badge miroir SMTP et `messageId`. | [x] |
| P1A | Alertes email — diagnostic derniers envois page Sécurité | Composant `SecurityAlertEmailDiagnostics` sur `/b4ck0ff1ce/security/alerts` : 8 derniers `EmailLog` type `NOTIFICATION`, statut, badge miroir SMTP, `messageId`, lien Email Monitor. Test E2E `securityAlertEmailNotifier.notifySecurityAlert` OK (`3/3` destinataires `sent:true`). `tsc` + ESLint ciblés OK. | [x] |
| P1A | notification-service — montage src Compose dev | Cause racine : `notification-service` n'avait pas de volume `./backend/notification-service/src:/app/src` (contrairement à `security-service`), donc le conteneur exécutait une image figée sans `awaitSecurityAlertMirror` ni `getCrashReportIdentity`. Correctif `docker-compose.yml` + `docker compose up -d --force-recreate notification-service` : conteneur aligné (`emailService.js` 302 lignes), `metadata.mirror` persisté en BDD (`TEST ADMIN P1A MIRROR META …` → `sent:true`, `messageId @maily.ovh`). | [x] |
| P1A | Alertes email — contenu enrichi + statut miroir Email Monitor | `securityAlertEmailNotifier.buildPayload()` enrichi : service touché, type, horodatage, erreur/statut, liens Email Monitor / sécurité, métadonnées redigées (`smtp_pass`, `token`, etc.). `notification-service` attend désormais le miroir SMTP pour les alertes internes et persiste `metadata.mirror` (`sent`, `messageId`, `from`, `replyTo`, `error`) dans `EmailLog`. Email Monitor affiche badges `Miroir SMTP OK/KO` et panneau détail dans la modal. Jest payload `3/3` + SMTP `5/5` OK ; `tsc`/`eslint` ciblés OK. | [x] |
| P1A | Alertes email sécurité — réauth + test + navigation + diagnostic admin | Page `/b4ck0ff1ce/security/alerts` ; liens ajoutés menu latéral **Sécurité → Alertes email**, popup **Paramètres → Notifications** (bouton rouge), `/settings` onglet Notifications. Fix hydratation : `SecurityAlertEmailSettings` — `Badge` hors `<p>`. Cause KO MailHog : `notification-service` héritait du SMTP réel ; correction : transport principal local `mailhog:1025` + miroir SMTP réel async pour alertes sécurité. Destinataires actifs alignés : `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Diagnostic ajouté : `/b4ck0ff1ce/email-monitor?type=NOTIFICATION`, filtre **Notification** actif, emails d’alerte visibles avec statut/destinataire/contenu, lien **Gestion des emails → Historique** corrigé vers cette route. Email Monitor : pagination serveur branchée sur le composant réutilisable `Pagination`, recherche `q` sur destinataire/expéditeur/sujet avec badge de recherche ; API `q=admin@delhomme.ovh` OK (`total=8`, 5 lignes page 1), UI OK (champ + badge + pagination). Nettoyage local EmailLog demandé porteur : `8971` lignes / `19 MB` → suppression anciens `FAILED`/`PENDING` (`8722`) + `VACUUM FULL ANALYZE` → `249` lignes / `720 kB`, notifications récentes `admin/dev/security` conservées. Correctif KO boîte admin 20:15 : `.env` local avait `SECURITY_ALERT_SMTP_MIRROR_ENABLED=false`; miroir réactivé, `SECURITY_ALERT_MIRROR_SMTP_FROM` aligné sur le compte SMTP authentifié, `Reply-To` conservé sur alias sécurité ; `emailService` force par défaut le `From` miroir vers l’utilisateur SMTP pour limiter les rejets silencieux. Validation 10/06 : API test admin isolé `202` sujet `TEST ADMIN OVH FROM AUTH 2026-06-10T20:15:03+02:00`, MailHog OK, miroir OVH planifié puis accepté (`messageId <59b166d5-1191-9465-2fd3-0c89ea94c2c5@maily.ovh>`), règle STARTTLS corrigée (`587 => secure:false`, `465 => secure:true`), test navigateur nettoyage utilisateurs `/b4ck0ff1ce/users` OK (`2441` utilisateurs de test supprimés, compteur BDD `0` après), `docker compose config --quiet` OK, `node --check` OK, test Jest SMTP `4/4` OK, `tsc --noEmit` OK, ESLint ciblé OK avec warnings historiques uniquement. | [x] |
| P1C | Responsive Email Monitor petit/moyen écran | Correctif ciblé `/b4ck0ff1ce/email-monitor?type=NOTIFICATION` : header/actions empilables, filtres sans `min-width` bloquant, cartes emails `min-w-0` + retours de ligne sur sujets/adresses longues, metadata en une colonne sur petit écran, modal contenu protégée par `overflow-x-auto`, pagination réutilisable qui wrappe, et `AdminLayout` corrigé pour ne plus appliquer `md:ml-80` / `md:pl-12` quand la sidebar est encore en mode mobile/overlay. Validation 10/06 : navigateur à `390x844` puis `820x844`, page visible sans contenu poussé/coupé sur le côté ; `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé `email-monitor`, `Pagination`, `AdminLayout` OK avec warnings historiques uniquement ; lints IDE OK. | [x] |
| P1C | Responsive popup Paramètres | `SettingsPopup` repris pour petit/moyen écran : overlay `p-2`, conteneur `flex` limité à `92vh`, header compact, onglets horizontaux scrollables en mobile puis sidebar interne à partir de `md`, contenu `min-w-0`/scroll vertical, grilles système en 1 colonne mobile, toggles avec `flex-shrink-0`, champs/selects `min-w-0`. Validation 10/06 : navigateur `390x844`, popup ouverte depuis `/b4ck0ff1ce`, capture `/tmp/cursor/screenshots/settings-popup-mobile.png`, onglets et contenu visibles sans découpe latérale de la popup ; `tsc --noEmit` OK ; ESLint ciblé OK avec warnings historiques uniquement. | [x] |
| P1A | Faux positif alerte `metrics-aggregator` indisponible | Diagnostic 10/06 : conteneurs `jobbingtrack-metrics-aggregator`, `security-service`, `notification-service`, `monitoring-agent-rs`, `log-collector-rs`, `postgres`, `auth`, `deployment`, `call`, `followup`, `application` tous `Up (healthy)` ; logs `metrics-aggregator` `/api/v1/health` en `200`. Cause alerte : `security-service` appelait `/api/v1/docker/services/all` sans `X-API-Key` et recevait `401`. Correctif : `securityScheduler.checkServiceAvailabilityAlerts()` transmet `METRICS_API_KEY`; test `service-availability-alerts` mis à jour ; `security-service` redémarré ; smoke depuis le conteneur `security-service` vers `metrics-aggregator` OK (`200`, `23` services). | [x] |
| P1B | Cadrage stats temps de réponse endpoints | Demande porteur ajoutée dans `TODOS_A_VALIDER.md` / `docs/TODOS.md` : reprendre la vue temps de réponse instantané/moyen pour couvrir `auth-service`, `deployment-service`, `call-service`, `notification-service`, `followup-service`, `application-service`, `postgres`, avec source claire `monitoring-agent-rs` / `metrics-aggregator` et distinction endpoint HTTP vs dépendance santé. | [x] |
| P1A | Crash-report SMTP — From authentifié / Reply-To alias | `.env.example` et `docs/deployment/environment-variables/README.md` alignés ; code `emailService.getCrashReportIdentity()` dans `notification-service` : `From` aligné sur `SMTP_USER` par défaut, `Reply-To` sur `CRASH_REPORT_REPLY_TO`. Jest SMTP `5/5` OK. Test isolé admin P1A relancé (`TEST ADMIN OVH P1A …`) : API `202`, MailHog OK, miroir OVH accepté ; confirmation boîte réelle porteur en attente. | [x] |
| P1A | Cadrage sécurité — ggshield / GitGuardian secrets | Demande porteur ajoutée dans `docs/TODOS.md` et `docs/STATUS.md` : installer/configurer `ggshield`, documenter `ggshield auth login` et scans repo, prévoir hooks Git `pre-commit` / `pre-push` / `pre-receive`, scans images Docker, puis étudier GitGuardian API/webhooks pour incidents temps réel dans le backoffice sécurité + emails d’alerte, sans stocker les secrets bruts. Implémentation non démarrée tant que la validation email P1A reste bloquante. | [x] |
| P1A | Emails crash/sécurité — alias publics et identité d’envoi | `notification-service` supporte désormais `CRASH_REPORT_FROM`, `CRASH_REPORT_REPLY_TO`, `SECURITY_ALERT_FROM`, `SECURITY_ALERT_REPLY_TO` en plus des destinataires `CRASH_REPORT_EMAIL` / `SECURITY_ALERT_EMAIL(S)`. Les exemples `.env*` utilisent des alias non routables `jobbingtrack.test`; les vraies redirections alias public -> boîte privée restent chez le fournisseur mail et hors Git. Validation 09/06 : diagnostics IDE OK ; scan `CURRENT_ROUTABLE_EMAILS=0` ; `rg` ciblé sans adresse privée réelle. Non validé par Jest/Node local car `npm test`, `node --check` et même `node --version` sortent code 1 sans sortie dans ce terminal. | [x] |
| P2 | Agent email — Gmail multi-comptes, Calendar sans minuit, recherche v2 | Cadrage renforcé : OAuth Gmail multi-comptes, liste/statut/révocation des comptes connectés, digest sans IA obligatoire, IA locale gratuite en renfort plus tard, barre de recherche v2 réutilisable espace utilisateur puis backoffice/admin, et garde-fou Calendar/Tasks : ne jamais créer d’événement à `00:00` sans heure explicite ; date seule = journée entière proposée ou tâche “horaire à confirmer”. Validation 10/06 : `CURRENT_ROUTABLE_EMAILS=0`, scan ciblé sans adresse privée réelle, `./node_modules/.bin/tsc --noEmit --pretty false` OK ; `npm run type-check` / `npm run lint` sortent encore code 1 sans sortie exploitable. | [x] |
| P2 | Agent email — accès compte personnel non-admin | Cadrage ajouté : premier usage via compte personnel non-admin explicitement autorisé du porteur, pas activation automatique pour tout compte inscrit ; droit/feature flag utilisateur, activation/révocation admin auditée, séparation stricte entre rôle admin/backoffice et lecture des emails personnels. | [x] |
| P2 | Agent email — fenêtres Calendar et suite de tests | Cadrage ajouté : pas d’événement Calendar automatique à `00:00`, avant `05:00` ou après `23:00`; date seule ou horaire hors fenêtre = tâche/proposition à confirmer. Suite de tests future cadrée : règles de tri, permissions, Gmail/IMAP lecture seule, digest SMTP/mock, Calendar/Tasks, rapports `tests/results/email-triage/<timestamp>`, secrets uniquement via `.env` gitignoré et skip explicite si absents. | [x] |
| P2 | Agent email — socle tests Calendar unitaire | `tests/email-triage/lib/calendar-time-policy.js` + `calendar-time-policy.test.js` + `run-with-report.sh` + README ; variables documentées dans `.env.example` et `docs/deployment/environment-variables/README.md`. Validation 10/06 : `/usr/bin/node tests/node_modules/jest/bin/jest.js --config tests/jest.config.js tests/email-triage/calendar-time-policy.test.js --runInBand` **7/7 OK** ; `npm run` / `npx jest` peuvent encore sortir code 1 sans sortie dans ce terminal. | [x] |
| P1B | Alignement sondes temps de réponse metrics-aggregator | Source unique `backend/metrics-aggregator-service/src/config/serviceHealthEndpoints.js` ; `server.js` + `docker.routes.js` importent la config ; sondes via nom conteneur Docker (pas `localhost:800x`) ; postgres/redis sans sonde HTTP ; mount dev `./backend/metrics-aggregator-service/src:/app/src` dans `docker-compose.yml` + recreate conteneur. Smoke `GET /api/v1/docker/services/all` (depuis conteneur) : auth **5ms**, notification **13ms**, followup **11ms**, application **6ms**, call **6ms**, deployment **10ms** (après fix chemin `/health`), postgres `responseTime=null` (attendu). `/usr/bin/node --check` OK sur `server.js`, `docker.routes.js`, `serviceHealthEndpoints.js`. | [x] |
| P1B | monitoring-agent-rs — chemins health alignés | `monitoring/rust/crates/monitoring-agent/src/constants.rs` : notification **3008** `/health`, profile/workflow/deployment/security **`/health`**, log-collector **3019** `/health`. Rebuild image requis pour prise en compte runtime (hors scope smoke immédiat metrics-aggregator). | [x] |
| P1B | UI Statistics / Performances — temps de réponse P1B | `responseTimePresentation.ts` : services prioritaires, label « Santé Docker » postgres/redis, note source ; `serviceHealthOverview.ts` : `responseTimeLabel`, `nonHttpDependency` ; encarts source sur `/b4ck0ff1ce/statistics` et `/b4ck0ff1ce/performances/latency`. `./node_modules/.bin/tsc --noEmit` OK ; ESLint ciblé 6 fichiers OK (warnings historiques `statistics/page.tsx` uniquement). | [x] |
| P1C | Menu Développement → Tests / Rapports | `AdminLayout.tsx` : sous-menu **Rapports** (`Rapports de tests`, `Rapports de parcours`) séparé ; entrée « Rapports de tests » retirée du sous-menu Tests. Validation navigateur porteur en attente. | [x] |
| P1A | Tests offensifs — encart cadrage UI | `/b4ck0ff1ce/tests-security` : encart « Tests offensifs contrôlés (cadrage P1A) » avec liens matrice `SECURITY_TESTING_MATRIX.md`, durcissement Compose, rapports sécurité — **aucun scan offensif lancé**. | [x] |
| P1B | Statistics — panneau services prioritaires temps de réponse | Composant `PriorityResponseServicesSummary` sur vue d’ensemble Statistics + onglet services ; `responseTimeLabel` par carte ; lien Performances → Latence. `./node_modules/.bin/tsc --noEmit` OK. | [x] |
| P1C | Menu Tests — sections Automatisés / Sécurité & charge | `AdminLayout` : `sectionLabel` pour en-têtes non cliquables ; doublon « Rapports de parcours » retiré du menu Parcours (accès via sous-menu Rapports). | [x] |
| P1A | Leurres VPS — design réduction d’exposition | `docs/security/VPS_EXPOSURE_REDUCTION.md` : priorité réduction ports/réseaux, leurres en phase ultérieure isolée, critères validation porteur. | [x] |
| P1A | Tests offensifs — tableau périmètre UI | `/b4ck0ff1ce/tests-security` : `<details>` tableau lecture seule (WAF, CVE, réseau lab, ZAP, leurres) — aucun bouton scan agressif ajouté. | [x] |
| P1B | monitoring-agent-rs — rebuild image health paths | `docker compose build monitoring-agent-rs` + `--force-recreate` OK ; conteneur healthy ; smoke deployment/security/auth/notification via metrics-aggregator (rt 7–8 ms, http=healthy). | [x] |
| P1B | Hub backoffice — panneau latence prioritaire | `/b4ck0ff1ce` : `PriorityResponseServicesSummary` sous KPI santé services. `tsc --noEmit` OK. | [x] |
| P1B | Suite Jest P1B temps de réponse | Tests ajoutés : `PriorityResponseServicesSummary.test.tsx` + enrichissement `serviceHealthOverview.test.ts` (prioritaires, ordre, Postgres « Santé Docker », lien Latence, état vide). Commande directe : `./node_modules/.bin/jest --runTestsByPath src/lib/metrics/__tests__/serviceHealthOverview.test.ts src/components/monitoring/PriorityResponseServicesSummary.test.tsx --runInBand --silent` → **2 suites / 12 tests OK**. Script ajouté : `frontend` `test:metrics-p1b` (mais `npm run` reste impacté par la dette sortie vide). | [x] |
| P1B | Bouton backoffice suite P1B latence | Hub `/b4ck0ff1ce/tests` : bouton **Lancer suite P1B latence** + carte **Suite P1B latence** ; API `POST /api/test/run-metrics-p1b`. Smoke HTTP OK → rapport `tests/results/20260610-231609` (`frontend.json`, `summary.json`, `report.html`). Correctif `generate-test-report.sh` : suppression du here-string bash pour rester compatible `sh` dans le conteneur frontend. | [x] |
| P1A | Emails — non-régression continue | Jest notification SMTP **5/5 OK** ; Jest security payload + disponibilité **6/6 OK** ; MailHog `total=56`, derniers emails P1A visibles (`BATCH VALIDATION 6 COMMITS`, `VALIDATION PORTEUR P1A`, E2E security-service) ; Postgres `EmailLog` derniers `NOTIFICATION` en `SENT` pour `admin@delhomme.ovh` / `dev@delhomme.ovh`. | [x] |
| P1 | Validation A-Z agent — programme | `docs/operations/VALIDATION_A_Z_AGENT.md` ajouté : ordre A→I (Docker, API, backend, emails, frontend, backoffice, mobile, sécurité non destructive, doc), critères de fin de lot, interdits prod/purge/scans agressifs. | [x] |
| P1 | Validation A-Z agent — passe 11/06 01h20 | Stack : conteneurs `jobbingtrack-*` healthy sauf `monitoring-agent-rs` encore `starting` au `docker ps` mais `/health` OK ; HTTP `gateway/frontend/metrics/monitoring` OK ; P1B frontend `tsc` OK + Jest **12/12** ; notification SMTP **5/5** ; security email **6/6** ; metrics-aggregator backend **6/6** ; services prioritaires metrics OK (`auth`, `deployment`, `call`, `notification`, `followup`, `application`, `postgres`). | [x] |
| P1 | Email validation agent — lot P1 tests | Email envoyé depuis `security-service` sujet `VALIDATION AGENT P1 LOT TESTS 2026-06-10T23:21:52.461Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh` ; MailHog `total=59` ; `EmailLog` `SENT`; miroir SMTP réel `metadata.mirror.sent=true` avec `messageId @maily.ovh` pour les 3 destinataires. | [x] |
| P2 | Mobile Flutter — smoke widget sans appareil | `flutter test test/widget_test.dart` dans `mobile/` : **1/1 OK** (écran connexion). | [x] |
| P2 | ADB appareil physique S21 FE | **11/06 01h25** : appareil visible `R5CT7263YJL`, modèle `SM-G990B2`, Android `16`; `emulator-controller` `:5055` voit `devices:[R5CT7263YJL]`; UI dump brut OK, écran connexion mobile visible (`Connexion — Cloudity Photos`). Helper `tools/adb-lib/examples/inspect-ui.js` sort encore code 1 sans sortie exploitable à diagnostiquer. | [x] |
| P1 | monitoring-agent-rs — boucle health `starting` | Cause : `BrokenPipe` sur requête healthcheck (`curl` ferme la connexion) remontait via `?` dans `serve()` et tuait le processus (`RestartCount` > 2600). Correctif `monitoring/rust/crates/monitoring-agent/src/http.rs` : erreurs client bénignes ignorées par requête ; `start_period: 30s` sur healthcheck Compose. Preuve post-recreate : `Up … (healthy)`, `RestartCount=0`, `GET http://localhost:5100/health` → `200` JSON `status: ok` (défaut hôte `MONITORING_RS_PORT=5100`, ancien 5118). | [x] |
| P1 | HTTPS dev LAN téléphone — IP PC 5443 | Accès dev `https://192.168.1.134:5443` pris en charge sans modifier la prod : `ports.config.ts` bascule l’API en same-origin sur IP LAN HTTPS (`/api/v1` via Nginx), `production/nginx/dev-https/default.conf` proxifie `/api/*` vers gateway, `api-gateway` accepte les origines LAN privées HTTP/HTTPS hors production, `dev-https-certs.sh` ajoute l’IP LAN dans les SAN. Preuves : cert SAN `IP Address:192.168.1.134`, `curl -k` login LAN `200`, API health LAN `200`, préflight CORS `Access-Control-Allow-Origin: https://192.168.1.134:5443`. ADB encore vide, donc validation écran téléphone à confirmer porteur. | [x] |
| P2 | Mobile physique — setup ADB + API LAN | Branche `feat/mobile-adb-physical-device` : script `scripts/mobile/setup-physical-device.sh` (attente ADB, `adb reverse`, build APK avec `MOBILE_DEV_LAN_HOST`, install/launch) ; smoke `scripts/mobile/smoke-login-adb.js` (creds `.env`) ; `ApiService` persiste l'URL API (`shared_preferences`) et sonde IP LAN build ; `flows.js` lit `TEST_USER_*` / `TEST_ADMIN_*`. Validations : `flutter test test/widget_test.dart` **1/1 OK** ; APK debug buildé (`app-debug.apk`, dart-define `192.168.1.134`). **Blocage session 11/06 ~12h** : `adb devices` vide depuis l'agent (câble/autorisation USB à confirmer côté poste) — relancer `bash scripts/mobile/setup-physical-device.sh` une fois l'appareil visible. | [ ] |
| P2 | Agent email — digest programmé et identité JobbingTrack | Socle tests étendu : `digest-schedule-policy` (quotidien 18h, hebdo optionnel, horaires 05:00–23:00) + `digest-identity-policy` (expéditeur visible `@jobbingtrack.com`, défaut `JobbingTrack <noreply@jobbingtrack.com>`, refus expéditeur Gmail personnel et destinataires placeholders). Variables `.env.example` ajoutées : `EMAIL_TRIAGE_DIGEST_FROM`, `EMAIL_TRIAGE_DIGEST_DAILY_TIME`, hebdo, timezone. Suite complétée par `classification-rules` : refus, entretien, 2e entretien, test technique, salon emploi, relance après 7j, newsletter/bruit, revue manuelle. Preuves : Jest email-triage **4 suites / 28 tests OK** ; `bash tests/email-triage/run-with-report.sh` OK → `tests/results/email-triage/20260612-001512` (`28 passed`). | [x] |
| P2 | Backoffice Tests — suite Agent email / triage lançable | Interface `/b4ck0ff1ce/tests` : ajout carte sélectionnable **Agent email / triage**, bouton rapide **Lancer suite agent email**, endpoint `POST /api/test/run-email-triage`, rapport listé dans `/b4ck0ff1ce/test-reports` via catégorie **Agent email / triage** et ID `email-triage-<timestamp>`. `tests/email-triage/run-with-report.sh` respecte maintenant `TESTS_RESULTS_DIR` pour le volume writable du frontend. Test UI catalogue ajouté : `testsCatalog.test.ts` garantit `email-triage` lançable + endpoint API. Validations : Jest catalogue **2/2 OK** ; `bash tests/email-triage/run-with-report.sh` → `tests/results/email-triage/20260612-004238` **28/28 OK** ; `frontend` `npm run type-check` OK ; `frontend` `npm run lint` OK (warnings historiques uniquement) ; lints IDE OK. | [x] |
| P2 | Suite CLI globale — intégration email-triage | `scripts/run-all-tests-with-reports.sh` section **34b** : exécute les 4 suites Jest email-triage (`classification-rules`, `calendar-time-policy`, `digest-schedule-policy`, `digest-identity-policy`) et écrit `email-triage.json` dans le dossier rapport horodaté. Preuve : `bash tests/email-triage/run-with-report.sh` → `20260612-004238` **28/28 OK** ; Jest catalogue hub **2/2 OK** ; `npm run type-check` OK ; `npm run lint` exit 0 (warnings historiques). | [x] |
| P2 | Emails — séparation MailHog local et SMTP préprod/prod | Docs/env alignés : `docs/emails/SMTP_CONFIGURATION.md`, `docs/emails/MAIL.md`, `docs/deployment/environment-variables/README.md`, `.env.example`, `.env.production.example`, `docker-compose.yml`, `docs/operations/PREPROD_PRODUCTION_CHECKLIST.md`, `A_VALIDER_AVANT_PRODUCTION.md`, `DEPLOIEMENT_PRODUCTION.md`. Règle écrite et automatisée : MailHog local/dev/test uniquement ; préprod/prod via SMTP réel, `SMTP_HOST` jamais `mailhog`/`localhost`, `NOTIFICATION_SMTP_HOST` ne force pas MailHog, smoke réception réelle + `EmailLog` `SENT`. Preuves : `docker compose config --quiet` OK ; `/usr/bin/node scripts/env/env-validate-runtime.cjs --production-example` OK ; Jest `tests/env/env-validate-runtime.test.js` **3/3 OK** ; `npm run type-check` OK ; `npm run lint` exit 0 (warnings historiques). Mail récap `RAPPORT AGENT JOBBINGTRACK — MailHog local / SMTP prod` envoyé vers `pauldelhomme.pro@gmail.com`, `dev@delhomme.ovh`, `test@delhomme.ovh` : HTTP **202** x3, `EmailLog` **SENT** x3. | [x] |
| P2 | Alertes sécurité — expéditeur visible JobbingTrack Security | `notification-service` : `getSecurityAlertIdentity()` + normalisation si seul `noreply@maily.ovh` (ou équivalent SMTP auth) est configuré → expéditeur visible `JobbingTrack Security <compte-auth>`, `Reply-To` alias sécurité conservé. Tests `email-service-smtp-config.test.js` + `internal-security-alert-email.test.js` **8/8 OK** ; `notification-service` recréé. Mail récap `RAPPORT AGENT JOBBINGTRACK — identité JobbingTrack Security` : HTTP **202** x3, `EmailLog` **SENT** x3. | [x] |
| P2 | Agent email — permissions, digest rendu, connexion boîte mail | Libs `tests/email-triage/lib/agent-access-policy.js`, `digest-renderer.js`, `mail-connection-policy.js` + tests Jest dédiés. Preuves relancées 12/06 01h33 : `/usr/bin/node tests/node_modules/jest/bin/jest.js --config tests/jest.config.js tests/email-triage/ --runInBand --silent` → **8 suites / 45 passed, 1 skipped** ; `bash tests/email-triage/run-with-report.sh` OK → `tests/results/email-triage/20260612-013307` ; `run-with-report.sh` + `scripts/run-all-tests-with-reports.sh` (34b) incluent les 8 fichiers ; `.env.example` documente `JOB_SEARCH_AGENT_ENABLED` (droit utilisateur). Intégration Gmail/IMAP réelle reste skip tant que `TEST_EMAIL_TRIAGE_ENABLED=false` ou credentials placeholder. | [x] |
| P2 | Agent email Apps Script — récupération v4 Perplexity | Récupération depuis `origin/docs/email-triage-agent-roadmap` du fichier `docs/emails/triage_email_agent.gs` v4 : boutons email `FAIT` / `PAS PERTINENT` / `REPORTER +3j`, Web App `doGet`, recherche offres Rennes via flux publics France Travail / Indeed / Hellowork / Meteojob, section offres dans le récap. Durcissement agent : suppression de l’email personnel et du PIN fixe du script ; configuration via propriétés Apps Script `TRIAGE_AGENT_EMAIL`, `TRIAGE_DIGEST_RECIPIENT`, `JBT_AGENT_PIN`. README `docs/emails/TRIAGE_AGENT_README.md` mis à jour. Preuves : recherche ciblée sans email personnel/PIN `1234` dans `docs/emails`; `/usr/bin/node` parse le `.gs` et vérifie les fonctions/features clés OK. | [x] |
| P1A | Backoffice Tests — périmètre offensif contrôlé lançable | Hub `/b4ck0ff1ce/tests` : carte **Tests offensifs contrôlés**, bouton **Périmètre offensif lab**, `POST /api/test/run-controlled-offensive-lab-scope`, script `scripts/security/run-controlled-offensive-lab-scope-with-report.sh` (Jest preflight+lab-scope **9/9 OK**, manifeste `lab-scope.json`, `dryRun=true`, `willRunPayload=false`). Rapports listés `/b4ck0ff1ce/test-reports` ID `controlled-offensive-<timestamp>`. Validations relancées 12/06 01h33 : `bash scripts/security/run-controlled-offensive-lab-scope-with-report.sh` → `tests/results/controlled-offensive/20260612-013308` ; Jest catalogue hub **3/3 OK** ; `frontend npm run type-check` OK ; `frontend npm run lint` OK (**0 erreur**, warnings historiques) ; smoke HTTP `/b4ck0ff1ce/tests`, `/b4ck0ff1ce/test-reports`, `/api/test-reports/all` et vues `email-triage-20260612-013307` / `controlled-offensive-20260612-013308` OK ; email récap validation envoyé vers `pauldelhomme.pro@gmail.com`, `dev@delhomme.ovh`, `test@delhomme.ovh`, `EmailLog` **SENT** x3. | [x] |
| P1A | Leurres / désinformation VPS-Portainer — design sans déploiement | Relecture agent 12/06 : `docs/security/VPS_EXPOSURE_REDUCTION.md`, `docs/security/COMPOSE_RUNTIME_HARDENING.md`, `docs/deployment/VPS_PORTAINER_NPM_OVH.md`, `docs/security/SECURITY_TESTING_MATRIX.md`, `A_VALIDER_AVANT_PRODUCTION.md`. Conclusion : la réduction d’exposition prime sur les leurres ; Postgres/Redis/MailHog/ports services `500x` du `docker-compose.yml` sont des mappings dev et ne doivent pas être exposés tels quels en préprod/prod ; NPM doit être sur réseau partagé uniquement avec `frontend`/`api-gateway`; Portainer/NPM admin doivent rester IP allowlist/VPN/tunnel ; tout leurre/honeypot est reporté à une préprod isolée validée, jamais sur le domaine utilisateur réel. Aucune commande offensive, aucun nmap, aucun déploiement de leurre lancé. | [x] |
| P1B | Corrélation performances — logs et KPI runtime | Script `scripts/ops/run-performance-correlation-validation-with-report.sh` ajouté pour produire un rapport reproductible. Validation 12/06 : `bash scripts/ops/run-performance-correlation-validation-with-report.sh` → `tests/results/performance-correlation/20260612-015102`, **5 étapes / 5 OK** ; `validate-central-logging-compose.cjs` OK **15 services** ; `smoke-central-logging-runtime.cjs` OK **15/15** avec lignes persistées dans `aggregated_logs` ; BDD `aggregated_logs` = **27604** lignes, **18** services distincts, dernier log `2026-06-11 23:51:08.018` ; endpoints metrics `/docker/services/all`, `/persistence/logs`, `/persistence/system/metrics`, `/persistence/services/jobbingtrack-api-gateway/availability`, `/persistence/stats` OK ; Jest `performanceCorrelationModel.test.ts` **5/5 OK** ; `/b4ck0ff1ce/performances/correlation` HTTP **200** ; rapport listé/ouvert via `/api/test-reports/all` + `performance-correlation-20260612-015102`; `frontend npm run type-check` OK ; `frontend npm run lint` OK (**0 erreur**, warnings historiques). Mail récap `VALIDATION AGENT JOBBINGTRACK — P1B corrélation performances 2026-06-12 01:52` envoyé vers `pauldelhomme.pro@gmail.com`, `dev@delhomme.ovh`, `test@delhomme.ovh`, `EmailLog` **SENT** x3. | [x] |
| P1B | Statistics / Performances — temps de réponse endpoints | Correctif `metrics-aggregator` : `responseTime.per_service` alimenté depuis `servicesMetrics` (23 entrées). Validation 12/06 13h49 : `bash scripts/ops/run-statistics-endpoint-latency-validation-with-report.sh` → `tests/results/statistics-endpoint-latency/20260612-134940`, **3/3 étapes OK** ; 7 services prioritaires P1B présents (`auth`, `deployment`, `call`, `notification`, `followup`, `application`, `postgres`) ; HTTP services avec `responseTimeMs` > 0, postgres libellé **Santé Docker** ; `per_service` = **23** ; Jest `performanceCorrelationModel` + `serviceHealthOverview` + `PriorityResponseServicesSummary` OK ; pages `/b4ck0ff1ce/performances`, `/performances/latency`, `/statistics` HTTP **200** ; rapport `statistics-endpoint-latency-20260612-134940` ; `frontend npm run type-check` OK. | [x] |
| P1B/P1C | Backoffice metrics — proxy frontend et pages Performances | Correctif `centralMetricsService` : en navigateur, appels live `/api/v1/metrics`, `/docker/services/all` et logs service passent par le proxy Next `/api/metrics-aggregator` comme `analyticsService`, pour éviter les erreurs directes `localhost:5004` / auth / CORS et garder `METRICS_API_KEY` côté serveur. Mapping historique `services` corrigé (`item.services || item.containers`). Validation 12/06 14h35 : Jest ciblé **5 suites / 22 tests OK** (`centralMetricsService`, statistics séries, service health, latence, corrélation) ; `frontend npm run type-check` OK ; `frontend npm run lint` OK (**0 erreur**, warnings historiques 1458) ; rapport `bash scripts/ops/run-statistics-endpoint-latency-validation-with-report.sh` → `tests/results/statistics-endpoint-latency/20260612-143538` **3/3 OK** ; navigateur authentifié : `/b4ck0ff1ce/performances`, `/performances/latency`, `/performances/containers` chargent les métriques via `/api/metrics-aggregator` en HTTP **200**, graphes visibles, conteneurs **23** et **199** points affichés. | [x] |
| P1B | Statistics — vue d’ensemble (graphes dispo/erreur) | Suite 15/06 : le smoke Playwright `/b4ck0ff1ce/statistics` vérifie maintenant explicitement les graphes **Disponibilité dans le temps**, la source `Persistance system_metrics`, la période visible et la mention **taux d’erreur dérivé (100 − disponibilité)**. API metrics directe : `/persistence/system/metrics` 24h et 7j OK, **500** points chaque, disponibilité **500/500**, `errorRate` explicite **0/500** donc dérivation depuis disponibilité confirmée. Validations sans `make` : frontend `format:check`, `type-check`, `lint --quiet` OK ; Jest `statisticsTimeSeries` **1 suite / 3 tests OK** ; Jest CI frontend **45 suites / 187 tests OK** ; Playwright Statistics **5/5 OK** ; perf HTTP moy. `/statistics` ~97 ms, `/statistics/security` ~69 ms, `/statistics/app-data` ~66 ms. Mail récap `[JobbingTrack] Recap P1B Statistics overview 2026-06-15` à `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh` : `EmailLog` **3/3 SENT**, `metadata.mirror.sent=true` **3/3**. | [x] |
| P1B | Statistics — onglet Données applicatives (`app-data`) | Correctif 15/06 : `statisticsService.getStatisticsTimelineResult()` conserve la note API de timeline fallback et `/b4ck0ff1ce/statistics/app-data` l’affiche quand la timeline ne renvoie qu’un snapshot courant. Runtime : `/api/v1/statistics` HTTP **200** (`applications=1043`, `users=146`, `companies=761`, `contacts=2`, `interviews=128`, `calls=200`, `followups=140`, `events=973`) ; `/api/v1/statistics/timeline?time_range=7d&limit=500` HTTP **200**, **1** point, note fallback présente, aucun `undefined` brut dans les payloads. Validations sans `make` : `node --check` controller statistics OK ; frontend `format:check`, `type-check`, `lint --quiet` OK ; Jest app-data/statistics **2 suites / 5 tests OK** ; Jest CI frontend **45 suites / 187 tests OK** ; Playwright Statistics **5/5 OK** ; perf HTTP moy. `/statistics/app-data` ~222 ms, `/statistics` ~49 ms, `/statistics/log-stats` ~54 ms. Mail récap `[JobbingTrack] Recap P1B Statistics app-data 2026-06-15` à `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh` : `EmailLog` **3/3 SENT**, `metadata.mirror.sent=true` **3/3**. | [x] |
| P1B | Statistics — onglet Logs (`log-stats`) | Correctif 15/06 : les listes Niveau/Service de `/b4ck0ff1ce/statistics/log-stats` sont construites depuis un échantillon non filtré pour éviter qu’un filtre actif masque les autres choix. Correctif backend lié : `/api/v1/persistence/stats` ferme explicitement le `PrismaClient` en `finally`, après détection d’une saturation Postgres `too many clients` pendant les smokes ; test backend dédié vérifie l’appel à `$disconnect`. Runtime post-restart : `metrics-aggregator` healthy, `/persistence/stats` `aggregatedLogs=28121`, `/persistence/logs` 14 j **800**, filtre `WARN` **800**, filtre `jobbingtrack-api-gateway` **66**, boucle `/persistence/stats` **20/20 OK**. Validations sans `make` : `node --check` route/tests OK ; backend Jest persistence **2 suites / 3 tests OK** ; frontend `format:check`, `type-check`, `lint --quiet` OK ; Jest log-stats/statistics **2 suites / 6 tests OK** ; Jest CI frontend **44 suites / 185 tests OK** ; Playwright Statistics **5/5 OK** ; perf HTTP moy. `/statistics/log-stats` ~135 ms, `/statistics` ~244 ms, `/services/logs` ~105 ms. Mail récap final `[JobbingTrack] Recap P1B Statistics log-stats FINAL 2026-06-15` à `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh` : `EmailLog` **3/3 SENT**, `metadata.mirror.sent=true` **3/3**. | [x] |
| P1B | Statistics — onglet Sécurité cohérent avec `/security` | Correctif 15/06 : ajout du modèle `securityStatisticsComparison` et d’un bloc UI **Cohérence avec la console Sécurité live** dans `/b4ck0ff1ce/statistics/security`. La page compare maintenant la persistance 7 j (`persistence/security/summary`, `dataPoints=2000`, score persisté ~99.1) et `/security` live 30 j (`criticalEvents=4`, `ddosAttacks=258`, score live 0) sans laisser croire qu’un score persisté élevé vaut absence d’incident live. Tests sans `make` : API metrics/security summary/metrics/stats OK ; API `/security` stats/logs OK ; backend metrics-aggregator Jest **1 suite / 2 tests OK** ; frontend `format:check` OK, `type-check` OK, `lint --quiet` OK ; Jest Statistics **2 suites / 6 tests OK** ; Jest CI frontend **43 suites / 182 tests OK** ; Playwright Statistics **5/5 OK** ; Playwright Sécurité titres **9 passed + 1 flaky retry OK** (`/security/threats` `ERR_EMPTY_RESPONSE`, retry OK). Perf HTTP moyenne locale : `/statistics/security` ~104 ms, `/security` ~57 ms, `/statistics` ~86 ms. Mail récap `[JobbingTrack] Recap P1B Statistics Securite 2026-06-15` à `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh` : `EmailLog` **3/3 SENT**, `metadata.mirror.sent=true` **3/3**. | [x] |
| Infra | Postgres — saturation `too many clients` | Diagnostic 15/06 : `max_connections=100`, baseline **~80–86** connexions `idle` au repos (`dashboard-service` ~15, `security-service` ~13, `call`/`followup` ~9) ; erreurs `FATAL: sorry, too many clients already` lors des pages Statistics (`/persistence/stats`, `/persistence/logs`, `/persistence/security/metrics`). Cause : ~15 microservices Prisma sans `connection_limit` sur une seule Postgres. Correctifs code : `/persistence/stats` refactoré vers `persistence.service.getPersistenceTableStats()` (singleton, plus de `new PrismaClient()` par requête) ; Jest route **1/1 OK**. Correctif Compose : `POSTGRES_MAX_CONNECTIONS=200` sur service `postgres` (recreate requis). Burst 30× `/stats` post-fix : connexions stables (~81). Reste : `connection_limit` par service, recreate postgres + metrics-aggregator, audit `new PrismaClient()` ad hoc. Versionnement noté `docs/BACKLOG.md`. | [x] |
| P1C | Responsive backoffice complet — smartphone/moyen | Correctifs layout sur `AdminLayout`, pages sécurité/services/tests/parcours/API tester/emails/events/mobile/corbeille/utilisateurs. Spec Playwright `frontend/tests/e2e/backoffice-responsive-audit.spec.ts` couvre les routes backoffice statiques et 4 viewports. Validation 11/06 : `./node_modules/.bin/playwright test tests/e2e/backoffice-responsive-audit.spec.ts --project=chromium --workers=1 --grep smartphone` **2/2 OK** ; `--grep moyen` **2/2 OK** ; audit complet précédent : petit PC + grand écran OK, smartphone/moyen corrigés ensuite. `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé 19 fichiers **0 erreur**, warnings historiques uniquement ; lints IDE OK. Email suivi `VALIDATION AGENT RESPONSIVE BACKOFFICE 2026-06-11T09:21:51.634Z` : MailHog OK, `EmailLog` `SENT`, miroir SMTP réel `metadata.mirror.sent=true` (`messageId @maily.ovh`). | [x] |
| P1C | Interfaces backoffice frontend — smoke élargi sans erreur runtime | Correction test Playwright `backoffice-interactions.spec.ts` : le check Statistics cible désormais le `h1` exact pour éviter un faux échec strict-mode quand un `h2` interne contient aussi “Statistiques”. Validation 12/06 14h45 : `PLAYWRIGHT_BASE_URL=http://localhost:5003 ./node_modules/.bin/playwright test tests/e2e/statistics-smoke.spec.ts tests/e2e/performances-range-smoke.spec.ts tests/e2e/backoffice.spec.ts tests/e2e/backoffice-extended.spec.ts tests/e2e/backoffice-interactions.spec.ts --project=chromium --workers=1 --reporter=list` → **151/151 OK** ; couvre dashboard, Statistics, Performances, réseau/disque/conteneurs/latence, sécurité, services, emails, data-management, tests/rapports, parcours, recherche et interactions principales. `frontend npm run type-check` OK ; `frontend npm run lint` OK (**0 erreur**, warnings historiques 1458) ; lints IDE OK. | [x] |
| P1C | Mode sombre persistant après refresh | Tests Jest `theme.test.ts` + `apply.test.ts` : `setStoredTheme` synchronise `theme`, `jobbingtrack-ui-preferences-v1` et `customization-settings` ; `applyTheme` applique `dark`/`light` ; script init `layout.tsx` lit `localStorage.theme` avant hydratation. Validation 12/06 13h56 : `bash scripts/ops/run-p1c-ux-theme-settings-validation-with-report.sh` → `tests/results/p1c-ux-theme-settings/20260612-135642`, **4/4 étapes OK** ; Jest thème **9/9 OK** ; smoke HTTP `/login` et `/b4ck0ff1ce` avec script init présent ; rapport `p1c-ux-theme-settings-20260612-135642` listé test-reports ; `frontend npm run type-check` OK. Validation navigateur porteur (icône lune → F5) encore attendue. | [x] |
| P1C | Popup paramètres fermeture | `SettingsPopup` : `Escape`, `mousedown` backdrop et bouton fermer appellent `onClose` ; `stopPropagation` sur le dialogue. Tests Jest `SettingsPopup.test.tsx` **5/5 OK** (inclus dans le rapport `p1c-ux-theme-settings/20260612-135642`). Validation navigateur porteur (clic extérieur / Escape depuis barre haute) encore attendue. Mail récap `VALIDATION AGENT JOBBINGTRACK — P1C mode sombre + popup paramètres 2026-06-12 13:56` : HTTP **202** x3 vers `pauldelhomme.pro@gmail.com`, `dev@delhomme.ovh`, `test@delhomme.ovh`, `EmailLog` **SENT** x3. | [x] |
| P1B/P1C | Performances — graphes réels + plages + UX sous-pages | Diagnostic porteur « graphes vides » : les données metrics-aggregator étaient bien présentes ; causes principales = échelle Y CPU conteneurs explosée quand un service dépasse 100 % multi-cœurs (ex. `frontend` ~199 %), graphes Corrélation masqués sous la longue liste services sur écrans < XL, et smoke Playwright trop faible (wrappers Recharts sans courbes visibles). Correctifs : `AnalyticsContainersChartsBundle` domaine Y adaptatif (cap 400 %) + ticks arrondis ; Corrélation réordonnée (détail graphique avant la liste sur mobile/tablette) ; Latence alignée avec sous-nav Performances + lien retour ; spec `performances-range-smoke.spec.ts` étendue à Synthèse + Corrélation, compte les SVG Recharts avec courbes/barres visibles, gère les plages `7d` vs `168`. Validation 12/06 15h30 : `frontend npm run type-check` OK ; Playwright `performances-range-smoke.spec.ts` **7/7 OK** (Synthèse, Réseau, Disque, Conteneurs, Latence, Corrélation) avec bascule 24h↔7j ; ESLint ciblé fichiers modifiés **0 erreur** (2 warnings historiques non liés dans `correlation/page.tsx`). | [x] |
| P1B/P1C | Performances HTTPS dev — proxy metrics-aggregator | Cause racine porteur (Conteneurs vides + Corrélation Axios 404) : en `https://jobbingtrack.localhost:5443`, Nginx routait `/api/metrics-aggregator/*` vers api-gateway au lieu du proxy Next → **404**. Correctif `production/nginx/dev-https/default.conf` : bloc `location ^~ /api/metrics-aggregator/` avant `/api/`. Module partagé `metricsAggregatorClient.ts` ; `analytics.service` + `centralMetricsService` unifiés ; page Conteneurs affiche erreur HTTP explicite (hint proxy HTTPS) au lieu de « Aucun conteneur » silencieux. Validation 12/06 16h00 : après `docker restart jobbingtrack-dev-https-proxy`, `curl -sk` `/api/metrics-aggregator/docker/services/all` et `/persistence/system/metrics` → **200** sur **5443** ; HTTP **5003** inchangé **200** ; Jest `metricsAggregatorClient` + `centralMetricsService` **4/4 OK** ; `tsc --noEmit` OK ; doc `docs/operations/DEV_HTTPS.md` mise à jour. | [x] |
| P1C | Performances — liens sous-navigation + tooltips graphes | `PerformancesSubNav` : l’onglet **Temps de réponse** pointe désormais vers `/b4ck0ff1ce/performances/latency` (plus `#latence`). Validation navigateur 12/06 : sur `https://jobbingtrack.localhost:5443/b4ck0ff1ce/performances/latency`, clic sur le lien sous-nav **Temps de réponse** reste sur `/performances/latency`; console sans Axios 404 (seul `CanceledError` bénin). Tooltips Recharts : `rechartsTooltipProps` ne force plus `itemStyle.color`, donc chaque item reprend la couleur de sa série ; test Jest `rechartsTooltipTheme.test.ts` inclus dans le lot **6/6 OK**. | [x] |
| P1C | Logo JobbingTrack web/mobile | Logo source fourni enregistré dans `frontend/public/brand/jobbingtrack-logo.png` et `mobile/assets/branding/jobbingtrack-logo.png`. Intégration : sidebar backoffice, login/register web, favicon/apple-touch/PWA manifest, splash/login Flutter (`pubspec.yaml` assets). Validations : Jest ciblé **6/6 OK**, `tsc --noEmit --pretty false` OK, ESLint ciblé **0 erreur** (warnings historiques), `flutter test test/widget_test.dart` **1/1 OK** avec chargement de l’écran connexion. | [x] |
| P1C | Console React 19 — script thème layout | Cause : `<script dangerouslySetInnerHTML>` dans `layout.tsx` déclenche l’avertissement Next.js 16 / React 19 « Encountered a script tag while rendering React component ». Correctif : script extrait vers `frontend/public/theme-init.js`, chargé via `next/script` `strategy="beforeInteractive"` dans le `<body>`. Même logique (classes toxiques, variables CSS, thème localStorage, meta theme-color). Script validation P1C `run-p1c-ux-theme-settings-validation-with-report.sh` aligné. Lints IDE `layout.tsx` OK ; `tsc`/`eslint` npm sortent encore `1` sans sortie dans ce terminal (dette P1D). | [x] |
| P1C | Mémoire JobbingTrack — budget stack et limites UI | `docker-compose.yml` : `mem_limit` sur les 22 services du profil `full`, total rendu **8192 MB** (`COMPOSE_PROFILES=full /usr/bin/docker compose --env-file .env.example config`) ; `production/docker-compose.yml` : 18 limites, total rendu **5568 MB** avec warnings attendus de variables prod absentes dans l’exemple. `metrics-aggregator` normalise les limites Docker : si `docker stats` expose la RAM hôte (ex. 48046 MB), l’API remplace par le budget service (`jobbingtrack-api-gateway` 384 MB, frontend 2048 MB) et recalcule `memory_percent`. Test Jest ciblé `serviceHealthModel.test.js` **6/6 OK** ; frontend `tsc --noEmit --pretty false` OK ; Jest `centralMetricsService.test.ts` **2/2 OK** ; ESLint ciblé **0 erreur**, warnings historiques uniquement. Recreate conteneurs requis pour que Docker applique les `mem_limit` réels. | [x] |
| P1C/B10 | Sécurité backoffice — libellés FR, sous-nav et Logs sécurité | Module `frontend/src/lib/security/securityLabels.ts` ajouté pour centraliser sévérités, types de menaces, événements, origines blocage et actions firewall. Pages alignées : vue d’ensemble sécurité, Analyse, Logs, Menaces, fiche menace, Incidents, détail alerte, Firewall, Politiques et `SecuritySubNav`. Route `/b4ck0ff1ce/security/logs` restaurée avec titre `Logs sécurité`, pagination URL, filtres niveau/catégorie/type/recherche et support `highlight`. Validation : `./node_modules/.bin/tsc --noEmit --pretty false` OK ; Jest `securityLabels.test.ts` **6/6 OK** ; ESLint ciblé **0 erreur** sur la nouvelle page/helper/spec, warnings historiques uniquement sur anciennes pages sécurité ; `npm run type-check` / `npm run lint` sortent encore code 1 sans sortie (dette P1D déjà ouverte). | [x] |
| P1C/B10 | Sécurité backoffice — archivage validation porteur et suite forensics | Validation porteur reçue 12/06 : `OK Sécurité - titres FR, libellés et navigation`. Ligne P1C retirée de `TODOS_A_VALIDER.md`, ajoutée à `TODOS_DONE.md`. Suite notée dans `docs/TODOS.md` : tri logs explicite (`timestamp desc`), corrélation fiches menace avec logs/endpoints/comptes/requestId, score max non trompeur, enrichissement VPN/proxy/Tor/ASN et politique de blocage high/critical bornée. | [x] |
| P1C/B10 | Sécurité logs/fiche menace — tri serveur, autocomplétion et score retenu | Branche `fix/security-b10-forensics-score` : `/api/v1/security/logs` accepte `eventType`, `q` et `order` borné (`desc` par défaut) ; la page Logs affiche le tri dans la barre de filtres et dans l’en-tête `Date` cliquable, utilise les filtres serveur, et propose une autocomplétion pour catégories, types d’événements, IP/endpoints/messages. Fiche menace : `effectiveRiskScore` + `riskSource` évitent un `Risque max 0` trompeur quand aucun log n’est corrélé mais que la menace est `HIGH/CRITICAL`. Tests ajoutés/renforcés : `security-logs-filters.test.js`, `firewall-threat-forensics.test.js`, `security-logs-filters.spec.ts`. Validation navigateur MCP 12/06 : `https://jobbingtrack.localhost:5443/b4ck0ff1ce/security/logs` affiche le tri, les champs autocomplétés, l’état vide ; remplissage `Catégorie=network` OK ; bascule `Plus ancien d’abord` OK ; console récente sans erreur applicative (HMR/React DevTools seulement). Validations shell possibles : `node --check` sur JS modifiés/tests OK ; `git diff --check` OK ; lints IDE OK. Non exécuté : Jest/TS/ESLint/Playwright shell complets, car `npm ci`, `npm --version`, `npm run type-check` et `npm run lint` sortent code 1 sans sortie exploitable (dette P1D/npm locale). | [x] |
| P1C/B10 | Refonte filtres backoffice — base réutilisable et migration sécurité | Branche `refactor/backoffice-filter-facets` : `useAppliedFilters`, `FilterBar`, `FilterSelectField`, `FacetAutocompleteField`, `facetUtils`, `fetchSecurityLogFacets`, variant `plain` sur `AutocompleteInput` avec suggestions `{value,label}`. Pages migrées : `/b4ck0ff1ce/security/logs` et `/b4ck0ff1ce/security/threats` (filtres brouillon/appliqué, suggestions dynamiques, options sévérité/type centralisées dans `securityLabels`). Tests : Jest `facetUtils` **4/4**, `useAppliedFilters` **2/2**, `securityLabels` **7/7** ; `tsc --noEmit` OK ; ESLint ciblé **0 erreur** (warnings `any` historiques sur menaces). Reste : étendre aux autres listes backoffice avant Lot M. | [x] |
| P1C/B10 | Refonte filtres backoffice — extension Email Monitor, Incidents, log-stats | Suite branche `refactor/backoffice-filter-facets` : options centralisées `emailMonitorOptions`, `incidentFilterOptions`, `periodOptions` ; helper `filterIncidentRows`. Pages migrées : `/b4ck0ff1ce/email-monitor`, `/b4ck0ff1ce/security/incidents`, `/b4ck0ff1ce/statistics/log-stats`. Tests : Jest **9/9 OK**, `tsc --noEmit` OK. | [x] |
| P1C/B10 | Refonte filtres backoffice — réseau, users, rapports, logs services | Branche `refactor/backoffice-filter-facets-next` mergée dans `dev` (`809f38c6`) : Réseau sécurité (ports + ports suspects), Utilisateurs (`userFilters` + rôle/test), Rapports tests (`testReportFilterOptions`), Logs services (`service-logs/page.tsx` + rewrite middleware `/services/logs`, filtres détail `[serviceName]`). Tests Jest : `userFilters` **3/3**, `serviceLogFilters` **3/3**, total **15/15** ; `tsc --noEmit` OK. Reste : liste Services (CPU/mémoire), Playwright patron partagé. | [x] |
| P1C/B10 | Logs sécurité — liens menace et raisons explicites | Branche `fix/security-logs-threat-links` (suite 13/06) : validation CUID `isPersistedThreatId` — rejet `lab-autocomplete-threat` et IDs synthétiques ; API vérifie existence BDD ; création auto `NetworkThreat` pour logs `critical`/`error`/`warning` ; raisons `lab_non_rattache` / `menace_introuvable` ; fusion menu **Incidents & menaces** + onglets `SecurityIncidentsTabs` ; suppression `SecuritySubNav` et paragraphes d’aide non demandés. Tests : Jest backend corrélation **6/6**, frontend `securityLogLinks` **3/3**, `tsc --noEmit` OK. **Reste validation porteur** : clic lien log lab → pas de 404 ; redémarrer `security-service` ; tests E2E Playwright ; seed lab avec vrais CUID. | [x] |
| P1C/B10 | Nettoyage UI sécurité + proxy rapports HTTPS | Suite 13/06 : suppression des textes demandés sur Incidents/Menaces, Alertes email, Firewall et Réseau ; suppression entrée navigation email **Historique** qui doublonnait Email Monitor ; ajout page backoffice `/b4ck0ff1ce/emails/mailhog` avec iframe MailHog ; menu MailHog pointe vers cette page ; Nginx dev HTTPS route désormais `/api/test-reports/*`, `/api/test/*`, `/api/security/cve-*` vers le frontend Next au lieu de l’API Gateway pour corriger le 404 `/api/test-reports/all`. Notes ajoutées dans `docs/TODOS.md` : WAF réel, Politiques sécurité large, réseau VPN/proxy/Tor, emails/déliverabilité/SMTP réel, navigation Tests/Rapports, historiques agrégés, audit cAdvisor/Prometheus. Validations : `npm run type-check` OK ; `npm run lint` OK avec **0 erreur / 1444 warnings historiques** ; `ReadLints` OK sur fichiers UI modifiés. Playwright `auth.setup.ts` : `loadRootEnv` charge bien `ADMIN_EMAIL`/`ADMIN_PASSWORD` depuis `.env` racine ; échec **401** confirmé aussi via `curl` login → mot de passe `.env` ≠ BDD (relancer seed-auth ou réaligner creds avant E2E). | [x] |
| P1C/B10 | Logs sécurité — facettes DB, filtre appliqué et données lab | Ajout `/api/v1/security/logs/facets` : suggestions catégories/types/IP/endpoints/messages calculées depuis les logs récents, plus de constantes catégories/types dans la page Logs. UX : champs filtres en état brouillon, bouton **Appliquer les filtres**, bouton **Réinitialiser**, badge “Filtres modifiés” ; taper dans `Catégorie` ne déclenche plus de fetch tableau. Correction runtime : `createSecurityLog` n’écrit plus `requestId` en champ top-level inexistant Prisma, il le conserve dans `metadata.requestId`. Seed local : 6 logs lab créés via API (`auth`, `waf`, `firewall`, `intrusion`, `network`, `ddos`) ; `/logs` retourne `count=6,total=6`, `/logs/facets` retourne les facettes attendues. Navigateur MCP : `/b4ck0ff1ce/security/logs`, `Catégorie=network` ne filtre pas avant clic, puis **Appliquer les filtres** affiche badge `catégorie network` + ligne “Menace réseau”. Validations : `node --check` backend OK, `git diff --check` OK, lints IDE frontend OK. `npm run type-check`/`npm run lint` restent code 1 sans sortie ; Playwright ciblé non concluant car serveur Next déjà actif. | [x] |
| P1C/B8 | Fiche menace — corrélation intrusion partielle et score runtime | `firewallController` agrège désormais les endpoints/méthodes/risques depuis `intrusionAttempt` et `dDoSAttack` en plus des `security_logs`, avec `riskSource` (`security_logs`, `intrusion_attempts`, `ddos_attacks`, `threat_severity`). Test forensics renforcé : une tentative intrusion sans log sécurité remonte `/api/v1/admin`, méthode `POST`, score `82` et source `intrusion_attempts`. Validation runtime 12/06 après `docker compose restart security-service` : fiche lab `cmpbtgo9v0002lwlv74po2fh6` affiche `Risque retenu 75` + explication “Estimé depuis la sévérité…” au lieu de `0`; endpoints/comptes restent `Non corrélé` pour cette menace précise faute de logs/app/auth liés. `node --check` backend OK ; lints IDE UI OK ; `git diff --check` OK. | [x] |
| P2 | Ports observabilité — monitoring-agent-rs hôte cohérent | Alignement défaut `MONITORING_RS_PORT` **5118 → 5100** (bloc 5098 monitoring-c, 5099 log-collector-rs, 5100 agent Rust, 5109 log-collector-c) ; `5018` reste réservé à deployment-service. Fichiers : `docker-compose.yml`, `.env.example`, `setup-ports.sh`, Makefile, scripts health/compare, docs ports. Après recreate : `curl http://localhost:5100/health` → **200**. Si `.env` local garde `5118`, mettre à jour ou exporter la variable avant recreate. | [x] |
| P1C/M | Cadrage prochain lot Performances + mini-modèles | Demande porteur 13/06 inscrite sans démarrer de nouvelle feature pendant la file de validation ouverte : `docs/TODOS.md` précise la future page **Performances → CPU & Mémoire** (`/b4ck0ff1ce/performances/cpu-memory`, global CPU/mémoire, vues détaillées par service, période partagée) et repositionne le **Lot M mini-modèles locaux** comme chantier proche après clôture/reclassement P1 ; `docs/BACKLOG.md` garde le rappel long terme. Suite précisée 13/06 : le **vrai moteur graphique dédié** vient après validations P1B/P1C + page CPU/Mémoire + stabilisation A5/A1h, pour extraire `ChartShell`/séries/périodes/tooltips/états sans mauvaise abstraction prématurée. | [x] |
| P1C | Performances — page CPU & Mémoire dédiée | Branche `feat/performance-cpu-memory-charts` : entrée navigation Performances **CPU & Mémoire** sous Synthèse et avant Temps de réponse ; page `/b4ck0ff1ce/performances/cpu-memory` avec cartes CPU/mémoire live, période partagée, vues **Vue globale / CPU détaillé / Mémoire détaillée**, séries par service activables/masquables, couleurs stables via `buildStableSeriesColorMap`, tooltips partagés. Optimisation 13/06 après retour porteur : la vue globale ne lance plus les historiques de tous les conteneurs, elle charge `getSystemMetricsHistory()` en une requête ; les vues détaillées chargent seulement les services cochés (6 par défaut, tout afficher possible). Correctif “aucune information” : `getContainersList()` conserve/aplatit maintenant les champs live CPU/mémoire renvoyés dans `service.metrics`; la page affiche un fallback graphique live si l’historique persisté est vide ; points visibles sur fallback live ; hints de période masqués sur les pages Performances. Suite retour porteur “Disque / Conteneurs / historiques agrégés” : vérification API = données **non perdues** (`systemMetrics` ~244k, `containerMetrics` ~5,4M, plage 06/03→13/06) ; renfort pages **Conteneurs** et **Disque** avec fallback live quand une plage historique est vide/illisible, sans requête supplémentaire lourde. Contrat : réutilise `analyticsService.getContainersList()`, `getSystemMetricsHistory()` et `getContainerMetricsHistory()` sans nouvelle API. Validations : `frontend npm run type-check` OK ; `frontend npm run lint` OK (**0 erreur**, 1444 warnings historiques) ; Jest `test:chart-system-metrics` **4/4 OK** ; routes Performances synthèse/cpu-memory/containers/latency/network/disk → HTTP **200** ; API `docker/services/all` → **23 services / 23 avec métriques live CPU ou mémoire** ; API système expose `diskUsagePercent`, `diskUsedBytes`, `networkRxBytes` ; API conteneur expose `cpuUsagePercent`, `memoryUsagePercent`, `blockReadBytes`, `blockWriteBytes`. | [x] |
| P1B | Statistics — onglet Sécurité cohérent `/security` | Suite 13/06 : `/statistics/security` n’affiche plus un résumé persisté vide comme un score 100 ; si `dataPoints=0` ou `source=empty`, bandeau explicite + pas de cartes synthèse trompeuses. Les cartes persistées précisent fenêtre/source 168 h et rappellent que `/security` est live 30 j (max UI 2000 logs). L’onglet interne de `/statistics` est renommé **Santé technique** et explique qu’il affiche disponibilité/requêtes/erreurs infra, avec liens vers sécurité persistée et sécurité live. Validations : `frontend npm run type-check` OK ; `frontend npm run lint` OK (**0 erreur**, 1444 warnings historiques) ; routes `/b4ck0ff1ce/statistics`, `/b4ck0ff1ce/statistics/security`, `/b4ck0ff1ce/security` → HTTP **200** ; `persistence/security/summary?hours=168` → HTTP **200**, `dataPoints=2000` ; `persistence/security/metrics?hours=168` → HTTP **200**. Playwright `performances-range-smoke` retenté avec `PLAYWRIGHT_BASE_URL=:5003`, bloqué par le 401 admin connu (`auth.setup.ts`) avant tests. | [x] |
| P1B | Statistics — onglet Logs (`log-stats`) | Suite 13/06 : `/statistics/log-stats` applique maintenant `periodDays`, `level` et `serviceName` côté API (`persistence/logs`) au clic **Appliquer**, puis garde le filtrage UI comme garde-fou sur l’échantillon 800. La page distingue mieux compteurs globaux par table vs graphes filtrés, et affiche un bandeau rouge si `persistence/stats` est indisponible au lieu d’un empty state ambigu. Validations : `frontend npm run type-check` OK ; `frontend npm run lint` OK (**0 erreur**, 1444 warnings historiques) ; lints IDE ciblés OK ; route `/b4ck0ff1ce/statistics/log-stats` → HTTP **200** ; API `persistence/logs?level=WARN` → HTTP **200**, lignes filtrées valides ; API `persistence/logs?serviceName=jobbingtrack-deployment-service` → HTTP **200**, lignes filtrées valides. | [x] |
| P1B | Statistics — onglet Données applicatives (`app-data`) | Suite 13/06 : `/statistics/app-data` affiche désormais un **snapshot applicatif courant** quand la timeline API ne renvoie qu’un point, au lieu d’un empty state ambigu “pas assez de points”. Les cartes/totaux restent la source principale tant que l’historique applicatif dédié n’est pas alimenté. Validations : `frontend npm run type-check` OK ; `frontend npm run lint` OK (**0 erreur**, 1444 warnings historiques) ; lints IDE ciblés OK ; route `/b4ck0ff1ce/statistics/app-data` → HTTP **200** ; API `/api/v1/statistics` → HTTP **200**, clés `users/applications/companies/contacts/interviews/calls/followups/events/summary` ; API `/api/v1/statistics/timeline?time_range=7d&limit=500` → HTTP **200**, `points=1`, note fallback présente. | [x] |
| P1B/P1C | Email récap agent — Performances + Statistics | Après merge/push `dev` du lot Performances + Statistics : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Performances + Statistics 2026-06-13T02:14:23.433Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1B | Statistics — graphes disponibilité / erreur (vue d’ensemble) | Branche `fix/statistics-availability-error-graphs` : composant `StatisticsErrorAvailabilityCharts` renforcé pour rendre la source du graphe erreur explicite (persistée ou dérivée depuis disponibilité) et borner dynamiquement l’axe Y du taux d’erreur quand les valeurs sont basses. Validation données : `/b4ck0ff1ce/statistics` HTTP **200** ; `/api/metrics-aggregator/persistence/system/metrics?limit=100` HTTP **200**, `rows=100`, `availability=100`, `explicitErrorRate=0`, `derivedErrorRate=100`. Validations : `frontend npm run type-check` OK ; `frontend npm run lint` OK (**0 erreur**, warnings historiques) ; Jest `src/lib/metrics/__tests__/statisticsTimeSeries.test.ts` **3/3 OK** ; lints IDE ciblés OK. | [x] |
| P1B | Email récap agent — Statistics disponibilité / erreur | Après merge/push `dev` du lot `fix/statistics-availability-error-graphs` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Statistics disponibilité erreur 2026-06-13T02:24:37.750Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1B | Statistics — moteur UI partagé shell | Suite 15/06 : les états chargement/erreur de la vue d’ensemble utilisent maintenant `StatisticsPageShell`, donc la sous-navigation et l’en-tête restent cohérents aussi hors état nominal. Le smoke Playwright vérifie la sous-navigation partagée `Vue d’ensemble / App data / Sécurité / Logs (stats)` sur les 4 routes Statistics. API smokes : `/statistics` OK, `/statistics/timeline` **1** point OK, `/persistence/system/metrics` **50** points OK. Validations sans `make` : frontend `format:check`, `type-check`, `lint --quiet` OK ; Jest ciblé **4 suites / 11 tests OK** ; Jest CI frontend **45 suites / 187 tests OK** ; Playwright Statistics **5/5 OK** ; perf HTTP moy. `/statistics` ~70 ms, `/statistics/security` ~48 ms, `/statistics/log-stats` ~48 ms, `/statistics/app-data` ~85 ms. Mail récap `[JobbingTrack] Recap P1B Statistics shell 2026-06-15` à `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh` : `EmailLog` **3/3 SENT**, `metadata.mirror.sent=true` **3/3**. | [x] |
| P1B | Email récap agent — Moteur UI Statistics shell | Après merge/push `dev` du lot `refactor/statistics-ui-engine-shell` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Statistics shell 2026-06-13T17:35:44.082Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1B | Analytics — moteur UI partagé shell | Branche `refactor/analytics-ui-engine-shell` : premier lot moteur UI Analytics. Ajout de `AnalyticsPageShell` dans le module Application pour porter `AdminLayout`, le retour hub, l’en-tête, les actions et la sous-navigation Application optionnelle. Migration de `/analytics`, `/analytics/application/performance`, `/analytics/application/activity` et `/analytics/application/feedback` sans modifier les appels `centralMetricsService`, `statisticsService`, le `TimeRangeSelector`, les cartes métriques ni les textes métier. Validations : `frontend ./node_modules/.bin/tsc --noEmit` OK ; `frontend ./node_modules/.bin/eslint` ciblé pages Analytics **0 erreur** ; lints IDE ciblés OK ; Jest Analytics **3 suites / 13 tests OK** ; `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable (wrappers/dette globale) ; routes `/b4ck0ff1ce/analytics`, `/analytics/application/performance`, `/analytics/application/activity`, `/analytics/application/feedback` HTTP **200** ; APIs `/api/v1/statistics` et `/api/metrics-aggregator/metrics` HTTP **200** ; temps HTTP locaux : hub **~63 ms**, performance **~73 ms**, activity **~57 ms**, feedback **~58 ms**. | [x] |
| P1B | Email récap agent — Moteur UI Analytics shell | Après merge/push `dev` du lot `refactor/analytics-ui-engine-shell` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Analytics shell 2026-06-13T17:47:06.266Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Performances — moteur UI partagé pages réseau/latence | Branche `refactor/performance-ui-engine-shell` : premier lot du moteur UI Performances avec `PerformancePageShell` (layout admin + retour + sous-nav + titre/actions), `PerformanceChartCard` (carte graphe + période) et états `PerformanceLoadingState` / `PerformanceEmptyState` / `PerformanceInfoNotice`. Migration limitée à `/performances/network` et `/performances/latency` pour éviter de toucher les hooks/périodes/data. Validations : `frontend npm run type-check` OK ; `frontend npm run lint` OK (**0 erreur**, warnings historiques) ; Jest `src/components/performances/PerformanceUi.test.tsx` **3/3 OK** ; régressions charts/metrics ciblées **5 suites / 15 tests OK** ; routes `/b4ck0ff1ce/performances/network`, `/b4ck0ff1ce/performances/latency`, `/b4ck0ff1ce/performances` HTTP **200**. Playwright `performances-range-smoke` retenté, bloqué par le 401 admin connu en `auth.setup.ts` avant les tests de page. | [x] |
| P1C | Email récap agent — Moteur UI Performances lot 1 | Après merge/push `dev` du lot `refactor/performance-ui-engine-shell` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Performances lot 1 2026-06-13T02:46:19.942Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Performances — moteur UI partagé pages disque/conteneurs | Branche `refactor/performance-ui-engine-disk-containers` : lot 2 du moteur UI Performances appliqué à `/performances/disk` et `/performances/containers`. Les pages utilisent maintenant `PerformancePageShell`, `PerformanceChartCard`, `PerformanceLoadingState` et `PerformanceEmptyState` au lieu de dupliquer `AdminLayout`/retour/sous-nav/états, sans modifier les fetchs, hooks de période, calculs disque/Block I/O ou séries CPU/mémoire conteneurs. Validations : `frontend ./node_modules/.bin/tsc --noEmit` OK ; `frontend npm run type-check` sort encore code 1 sans sortie exploitable (wrapper connu) ; `frontend npm run lint` KO global à cause de 8 erreurs préexistantes hors fichiers touchés (`jest.config.js`, `next.config.js`, `scripts/test-mobile-cli.js`) ; ESLint ciblé pages modifiées **0 erreur**, 1 warning hook préexistant sur `selectedContainer` ; Jest UI/charts/metrics **4 suites / 19 tests OK** ; routes `/b4ck0ff1ce/performances`, `/performances/disk`, `/performances/containers` HTTP **200** ; API `persistence/system/metrics?limit=20` HTTP **200**, 20 points ; API `docker/services/all` HTTP **200**, 23 services ; temps HTTP local moyen sur 3 appels : Disque **~70 ms**, Conteneurs **~70 ms**, système metrics **~49 ms**, services Docker **~790 ms** (max observé 2229 ms côté API live Docker). | [x] |
| P1C | Email récap agent — Moteur UI Performances lot 2 | Après merge/push `dev` du lot `refactor/performance-ui-engine-disk-containers` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Performances lot 2 2026-06-13T17:06:23.584Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Performances — moteur UI partagé vue d’ensemble | Branche `refactor/performance-ui-engine-overview` : lot 3 du moteur UI Performances appliqué à `/performances`. `PerformancePageShell` accepte maintenant `backHref`, `backLabel` et `topLinks` pour préserver le retour Tableau de bord + lien Analytics ; `PerformanceChartCard` accepte `id` pour conserver l’ancre `#latence` ; les états partagés acceptent aussi `id`. La vue d’ensemble réutilise les cartes/états partagés pour CPU/mémoire, latence, réseau/corrélation, endpoints live, sans modifier `analyticsService`, `centralMetricsService`, hooks de période, compression ou calculs. Validations : `frontend ./node_modules/.bin/tsc --noEmit` OK ; `frontend ./node_modules/.bin/eslint` ciblé vue d’ensemble + composants UI **0 erreur** ; lints IDE ciblés OK ; Jest UI/charts/metrics **4 suites / 19 tests OK** ; `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable (wrappers/dette globale) ; route `/b4ck0ff1ce/performances` HTTP **200** ; API `persistence/system/metrics?limit=20` HTTP **200**, 20 points ; API `docker/services/all` HTTP **200**, 23 services ; temps HTTP local moyen vue d’ensemble **~142 ms**, système metrics **~99 ms**. | [x] |
| P1C | Email récap agent — Moteur UI Performances lot 3 | Après merge/push `dev` du lot `refactor/performance-ui-engine-overview` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Performances lot 3 2026-06-13T17:16:49.694Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Performances — moteur UI partagé CPU & Mémoire | Branche `refactor/performance-ui-engine-cpu-memory` : lot 4 du moteur UI Performances appliqué à `/performances/cpu-memory`. La page utilise maintenant `PerformancePageShell`, `PerformanceChartCard`, `PerformanceLoadingState` et `PerformanceEmptyState` au lieu de dupliquer `AdminLayout`/retour/sous-nav/états, sans modifier les fetchs système/conteneurs, la sélection de services, les vues `overview/cpu/memory`, les fallbacks live ou les calculs de séries. Validations : `frontend ./node_modules/.bin/tsc --noEmit` OK ; `frontend ./node_modules/.bin/eslint` ciblé CPU & Mémoire + composants UI **0 erreur** ; lints IDE ciblés OK ; Jest UI/charts/metrics **4 suites / 19 tests OK** ; `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable (wrappers/dette globale) ; route `/b4ck0ff1ce/performances/cpu-memory` HTTP **200** ; API `persistence/system/metrics?limit=20` HTTP **200**, 20 points ; API `docker/services/all` HTTP **200**, 23 services ; temps HTTP local moyen CPU & Mémoire **~187 ms**, système metrics **~129 ms**, services Docker **~868 ms** (max observé 2428 ms côté API live Docker). | [x] |
| P1C | Email récap agent — Moteur UI Performances CPU & Mémoire | Après merge/push `dev` du lot `refactor/performance-ui-engine-cpu-memory` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Performances CPU Mémoire 2026-06-13T17:26:37.593Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Sécurité — moteur UI partagé shell | Branche `refactor/security-ui-engine-shell` : premier lot moteur UI Sécurité. Ajout de `SecurityPageShell` + `SecuritySubNav` pour porter `AdminLayout`, la sous-navigation des 8 sections Sécurité, l’en-tête, les actions et les onglets Incidents/Menaces optionnels (`showIncidentsTabs`). Migration de `/security`, `/security/analysis`, `/security/logs`, `/security/incidents`, `/security/threats`, `/security/firewall`, `/security/network`, `/security/alerts`, `/security/policies` et fiches détail menace/alerte incident (`showSubNav=false` + retour) sans modifier les appels API, filtres, tableaux, exports ni calculs. Validations : `frontend ./node_modules/.bin/tsc --noEmit` OK ; `frontend ./node_modules/.bin/eslint` ciblé pages Sécurité **0 erreur**, 56 warnings historiques ; Jest sécurité lib **3 suites / 14 tests OK** ; `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable (wrappers/dette globale) ; routes `/b4ck0ff1ce/security/*` HTTP **307** sans session (redirect login, stack up). | [x] |
| P1C | Email récap agent — Moteur UI Sécurité shell | Après merge/push `dev` du lot `refactor/security-ui-engine-shell` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Sécurité shell 2026-06-13T18:03:24.609Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Administration — moteur UI partagé Services | Branche `refactor/administration-services-ui-shell` : premier lot moteur UI Administration appliqué au domaine Services. Ajout de `ServicesPageShell` + `ServicesSubNav` pour porter `AdminLayout`, la sous-navigation Liste des services / Services & Logs, l’en-tête, les actions et le retour détail. Migration de `/services`, `/services/logs` (rewrite vers `service-logs`) et `/services/[serviceName]` sans modifier les appels metrics-aggregator, filtres logs, tableaux, stop/restart, auto-refresh ni graphes détail. Validations : `frontend ./node_modules/.bin/tsc --noEmit` OK ; `frontend ./node_modules/.bin/eslint` ciblé pages Services **0 erreur**, 24 warnings historiques ; lints IDE ciblés OK ; Jest fiche service **1 suite / 33 tests OK** ; `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable (wrappers/dette globale) ; routes `/b4ck0ff1ce/services`, `/services/logs`, `/services/api-gateway` HTTP **307** sans session (redirect login, stack up). | [x] |
| P1C | Email récap agent — Moteur UI Administration Services | Après merge/push `dev` du lot `refactor/administration-services-ui-shell` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Administration Services 2026-06-13T18:22:21.335Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Administration — moteur UI partagé Archives / Corbeille | Branche `refactor/administration-data-ui-shell` : second lot moteur UI Administration appliqué aux pages Archives et Corbeille. Ajout de `AdministrationDataPageShell` + `AdministrationDataSubNav` pour porter `AdminLayout`, la sous-navigation Données, l’en-tête et les actions. Migration de `/archives` et `/trash` sans modifier `adminService`, filtres, confirmations, restaurations, désarchivage, vidage corbeille ni suppression définitive. Validations : `frontend ./node_modules/.bin/tsc --noEmit` OK ; `frontend ./node_modules/.bin/eslint` ciblé pages Archives/Corbeille **0 erreur**, 6 warnings historiques ; lints IDE ciblés OK ; `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable (wrappers/dette globale) ; routes `/b4ck0ff1ce/archives` et `/trash` HTTP **307** sans session (redirect login, stack up). Jest ciblé non lancé : pas de test unitaire Archives/Corbeille existant ; E2E existants reportés au réalignement credentials admin Playwright. | [x] |
| P1C | Email récap agent — Moteur UI Administration Archives Corbeille | Après merge/push `dev` du lot `refactor/administration-data-ui-shell` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Administration Archives Corbeille 2026-06-13T18:33:05.415Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Administration — moteur UI partagé Données utilitaires | Branche `refactor/administration-data-utilities-ui-shell` : troisième lot moteur UI Administration appliqué aux pages Données de test, Facturation, Stats utilisateur et Suivi intérim. Les wrappers `/test-data`, `/billing`, `/user-stats` et `/suivi-interim` utilisent `AdministrationDataPageShell` / `AdministrationDataSubNav`, sans modifier les appels API, loaders auth, composants internes, actions sensibles de génération/nettoyage, formulaires ni contenus métier. Validations : `frontend ./node_modules/.bin/tsc --noEmit` OK ; `frontend ./node_modules/.bin/eslint` ciblé pages utilitaires **0 erreur**, 6 warnings historiques sur `test-data`; lints IDE ciblés OK ; `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable (wrappers/dette globale) ; routes `/b4ck0ff1ce/test-data`, `/billing`, `/user-stats`, `/suivi-interim` HTTP **307** sans session (redirect login, stack up). Jest ciblé non lancé : pas de test unitaire dédié à ces wrappers ; E2E existants reportés au réalignement credentials admin Playwright. | [x] |
| P1C | Email récap agent — Moteur UI Administration Données utilitaires | Après merge/push `dev` du lot `refactor/administration-data-utilities-ui-shell` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Administration Données utilitaires 2026-06-13T18:43:59.690Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Administration — moteur UI partagé Utilisateurs | Branche `refactor/administration-users-ui-shell` : quatrième lot moteur UI Administration appliqué à `/users` et `/users/[id]`. Ajout de `UsersPageShell` pour porter `AdminLayout`, l’en-tête, les actions et le retour fiche ; migration des états loading/erreur/création/détail sans modifier les appels auth API, cache, filtres, pagination, emails reset/vérification, changements de rôle, suppressions ni formulaires. Validations : `frontend ./node_modules/.bin/tsc --noEmit` OK ; `frontend ./node_modules/.bin/eslint` ciblé pages Users **0 erreur**, 18 warnings historiques ; lints IDE ciblés OK ; Jest `userFilters` **1 suite / 3 tests OK** ; `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable (wrappers/dette globale) ; routes `/b4ck0ff1ce/users`, `/users/new`, `/users/test-id` HTTP **307** sans session (redirect login, stack up). | [x] |
| P1C | Email récap agent — Moteur UI Administration Utilisateurs | Après merge/push `dev` du lot `refactor/administration-users-ui-shell` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Administration Utilisateurs 2026-06-13T18:55:52.544Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Administration — moteur UI partagé Hub Données | Branche `refactor/administration-datas-hub-ui-shell` : cinquième lot moteur UI Administration appliqué à `/datas` et à la route legacy `/data-management`. Les deux pages utilisent `AdministrationDataPageShell` / `AdministrationDataSubNav`, sans modifier les redirections `?tab=test-data|billing|user-stats`, les onglets lazy, les composants métiers, les actions export/import/nettoyage ni les appels API. Validations : `frontend ./node_modules/.bin/tsc --noEmit` OK ; `frontend ./node_modules/.bin/eslint` ciblé hub Données **0 erreur** ; lints IDE ciblés OK ; `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable (wrappers/dette globale) ; routes `/b4ck0ff1ce/datas`, `/datas?tab=applications`, `/data-management` HTTP **307** sans session (redirect login, stack up). Jest ciblé non lancé : pas de test unitaire dédié au hub Données ; E2E existants reportés au réalignement credentials admin Playwright. | [x] |
| P1C | Email récap agent — Moteur UI Administration Hub Données | Après merge/push `dev` du lot `refactor/administration-datas-hub-ui-shell` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Administration Hub Données 2026-06-13T19:07:21.060Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Email récap agent — Synthèse moteur UI Administration complet | Mail global de synthèse envoyé après les lots Services, Archives/Corbeille, Données utilitaires, Utilisateurs et Hub Données : sujet `RAPPORT AGENT JOBBINGTRACK — Synthèse moteur UI Administration complet 2026-06-13T19:11:00.862Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Développement — moteur UI partagé Tests automatisés | Branche `refactor/development-tests-runners-ui-shell` : premier lot moteur UI Développement appliqué aux runners automatisés `/tests-api`, `/tests-backend`, `/tests-frontend` et `/tests-backoffice`. Ajout de `DevelopmentPageShell` + `DevelopmentTestsSubNav` pour porter `AdminLayout`, la sous-navigation Tests, l’en-tête et les actions Lancer/Arrêter, sans modifier les endpoints `/api/test/run-*`, les sélections de tests, logs, progression, états ni liens rapports. Validations : `frontend ./node_modules/.bin/tsc --noEmit` OK ; `frontend ./node_modules/.bin/eslint` ciblé pages runners **0 erreur**, 18 warnings historiques ; lints IDE ciblés OK ; Jest `testsCatalog` **1 suite / 3 tests OK** ; `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable (wrappers/dette globale) ; routes `/b4ck0ff1ce/tests-api`, `/tests-backend`, `/tests-frontend`, `/tests-backoffice` HTTP **307** sans session (redirect login, stack up). | [x] |
| P1C | Email récap agent — Moteur UI Développement Tests automatisés | Après merge/push `dev` du lot `refactor/development-tests-runners-ui-shell` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Développement Tests automatisés 2026-06-13T19:28:09.311Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Emails — moteur UI partagé Configuration / MailHog | Branche `refactor/emails-settings-mailhog-ui-shell` : premier lot moteur UI Gestion des emails appliqué à `/emails/settings` et `/emails/mailhog`. Ajout de `EmailBackofficePageShell` + `EmailBackofficeSubNav` pour porter `AdminLayout`, la sous-navigation Emails, l’en-tête et les actions `Vérifier` / `Ouvrir dans un onglet`, sans modifier l’appel SMTP `/api/v1/emails/test-smtp`, la récupération token, le fallback MailHog `NEXT_PUBLIC_MAILHOG_UI_URL`, le lien externe ni l’iframe. Validations : `frontend ./node_modules/.bin/tsc --noEmit` OK ; `frontend ./node_modules/.bin/eslint` ciblé pages Emails **0 erreur**, 2 warnings historiques `any` sur `settings`; lints IDE ciblés OK ; `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable (wrappers/dette globale) ; routes `/b4ck0ff1ce/emails/settings`, `/emails/mailhog`, `/emails`, `/email-monitor` HTTP **307** sans session (redirect login, stack up). Playwright ciblé `backoffice.spec.ts --grep 'page Configuration SMTP'` tenté avec `PLAYWRIGHT_BASE_URL=http://localhost:5003` : setup admin KO `401`, test page non exécuté, blocage credentials E2E déjà connu. Jest ciblé non lancé : aucun test unitaire dédié au shell Emails ou à ces wrappers. | [x] |
| P1C | Email récap agent — Moteur UI Emails Configuration MailHog | Après merge/push `dev` du lot `refactor/emails-settings-mailhog-ui-shell` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Emails Configuration MailHog 2026-06-13T19:48:03.212Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `messageId @maily.ovh`. | [x] |
| P1C | Emails — moteur UI partagé Dashboard / Templates | Branche `refactor/emails-dashboard-templates-ui-shell` : second lot moteur UI Gestion des emails appliqué à `/emails` et `/emails/templates`. Réutilisation de `EmailBackofficePageShell` pour porter la sous-navigation Emails, les en-têtes, l’action Actualiser et le lien Email Monitor, sans modifier les appels stats/envoi test/reset/templates, les formulaires, les onglets, la preview HTML, l’édition de templates ni la sauvegarde variables. Validations : `frontend ./node_modules/.bin/tsc --noEmit` OK ; `frontend ./node_modules/.bin/eslint` ciblé Dashboard/Templates **0 erreur**, 19 warnings historiques sur ces grandes pages ; lints IDE ciblés OK ; `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable (wrappers/dette globale) ; routes `/b4ck0ff1ce/emails`, `/emails/templates`, `/email-monitor` HTTP **307** sans session (redirect login, stack up). Playwright ciblé `page Gestion des emails|onglets Email de Test|page Templates emails|onglets Templates` tenté avec `PLAYWRIGHT_BASE_URL=http://localhost:5003` : setup admin KO `401`, 4 tests page non exécutés, blocage credentials E2E déjà connu. Jest ciblé non lancé : aucun test unitaire dédié à ces wrappers. | [x] |
| P1C | Email récap agent — Moteur UI Emails Dashboard Templates | Après merge/push `dev` du lot `refactor/emails-dashboard-templates-ui-shell` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Emails Dashboard Templates 2026-06-13T20:05:31.549Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `messageId @maily.ovh`. | [x] |
| P1C | Emails — moteur UI partagé Email Monitor / Déliverabilité | Branche `refactor/emails-monitor-deliverability-ui-shell` : troisième lot moteur UI Gestion des emails appliqué à `/email-monitor` et `/emails/deliverability`. Réutilisation de `EmailBackofficePageShell` pour porter la sous-navigation Emails et les en-têtes, sans modifier polling temps réel, filtres appliqués, pagination, export JSON, suppressions de logs, tests DNS/SMTP, envois test/reset/vérification ni états de résultat. Validations : `frontend ./node_modules/.bin/tsc --noEmit` OK ; `frontend ./node_modules/.bin/eslint` ciblé Monitor/Déliverabilité **0 erreur**, 9 warnings historiques ; lints IDE ciblés OK ; `npm run type-check` et `npm run lint` sortent encore code 1 sans sortie exploitable (wrappers/dette globale) ; routes `/b4ck0ff1ce/email-monitor`, `/emails/deliverability`, `/emails` HTTP **307** sans session (redirect login, stack up). Playwright ciblé `page Email Monitor|page Délivrabilité|page Emails - Configuration avec vérifications` tenté avec `PLAYWRIGHT_BASE_URL=http://localhost:5003` : setup admin KO `401`, 5 tests page non exécutés, blocage credentials E2E déjà connu. Jest ciblé non lancé : aucun test unitaire dédié à ces wrappers. | [x] |
| P1C | Email récap agent — Moteur UI Emails Monitor Deliverabilite | Après merge/push `dev` du lot `refactor/emails-monitor-deliverability-ui-shell` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Moteur UI Emails Monitor Deliverabilite 2026-06-13T20:18:22.931Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `messageId @maily.ovh`. | [x] |
| P1C | Performances — réactivation points historiques et charge réduite | Suite retours porteur “pas de points CPU/conteneurs/réseau/mémoire/disque” puis “aucun historique agrégé temps de réponse” : causes isolées côté `metrics-aggregator` et pages Performances. Correctifs : `getContainerMetricsHistory` lit désormais `container_metrics` en priorité et mappe CPU/mémoire/réseau/temps réponse sans inventer de Block I/O à `0`; `getServiceAvailabilityHistory` et `getServiceAvailabilityStats` acceptent aussi les alias courts (`api-gateway`, `frontend`, `metrics-aggregator`) en fallback vers `jobbingtrack-*`; Corrélation réduit sa charge par défaut (`maxHistoriesLoaded` 8 en mode léger, 16 en complet, concurrence 3) ; Disque limite le Block I/O à 8 conteneurs en affichant `8/23`. Validations : stack relancée via Docker Compose direct profils `backend`, `monitoring`, `https`; santé frontend/API/metrics OK ; audits 24h via API/proxy/direct : système **1440/1440** puis audit compact **180/180** points CPU/mémoire/temps réponse/réseau/disque ; **23/23 conteneurs** avec **1440/1440** puis échantillon **90/90** points CPU/mémoire/réseau ; alias disponibilité `api-gateway`, `frontend`, `metrics-aggregator` → **20/20** historiques avec temps de réponse et stats 24h non vides ; Postgres/Redis ont disponibilité mais pas de ms HTTP (normal non-HTTP) ; MailHog arrêté dans stack minimale ; proxy HTTPS sans historique disponibilité dédié. Runtime `node --check` dans conteneur OK ; lints IDE OK ; `npm run type-check` frontend OK ; `npm run lint` frontend OK (**0 erreur**, warnings historiques) ; Jest frontend Performances/metrics **4 suites / 14 tests OK** ; backend stable metrics-aggregator **3 suites / 18 tests OK**. Limite : validation navigateur visuelle non faite car MCP redirigé sur `/login` sans session admin. | [x] |
| P1C | Audit charge API mobile + mémoire frontend | Suite demande porteur “tenir très forte demande / éviter DDoS / réduire mémoire frontend” : audit statique read-only des pages frontend et endpoints mobiles, puis campagne locale bornée sans `make`. Stack `full` relancée via Docker Compose direct (`full`, `monitoring`, `https`) : microservices métier healthy. Préflight offensif contrôlé `controlled-offensive-preflight.cjs` OK (`status=allowed`) ; manifeste scope lab OK (`17` checks plan-only, aucun payload). WAF : appel direct localhost sur `/health?...<script>` retourne `200` car bypass interne local ; retest avec `X-Forwarded-For: 203.0.113.77` retourne **403** et corps `WAF_BLOCKED`, donc blocage OK pour trafic proxy/externe simulé. Charge API légère `PERF_LIGHT=1 tests/performance/test-performance.js` : **15/15** endpoints OK, charge **13/13**, score **100/100** ; limite metrics direct sans `X-API-Key` notée. Charge avancée légère : **46/46** requêtes OK. Charge avancée bornée non extreme `tests/performance/test-load-advanced.js` : **455/455** requêtes OK, débit global script **1590 req/s**, spike health **200/200** avec concurrence **50**. Snapshot backend : gateway ~52 MiB/384 MiB puis ~42 MiB/384 MiB, metrics-aggregator ~116–132 MiB/512 MiB, services métier ~59–81 MiB selon service. Faiblesse critique isolée : `jobbingtrack-frontend` Next dev/Turbopack reste à **1.98–2.00 GiB / 2 GiB**, CPU >200%, `restart=111`, process `next-server (v16.2.6)` ~1.4 GiB RSS + worker PostCSS ~89 MiB ; pages à risque identifiées : `performances/correlation`, `security`, `statistics`, `log-stats`, `email-monitor`, `useMetrics`. Limites : pas de test DDoS destructif ; rate-limit gateway non représentatif en `NODE_ENV=development` car skip dev/test ; endpoints mobile métier testés surtout au périmètre auth/401, pas en CRUD authentifié BDD faute de token de test ; profil navigateur connecté non lancé car auth admin MCP reste bloquée. | [x] |
| P1C | Frontend mémoire/performance — lot correctif dev server + polling | Suite audit précédent : correction du point chaud frontend local. `frontend/package.json` lance désormais `next dev --webpack -H 0.0.0.0` et `docker-compose.yml` n’ajoute plus un second `-H`, ce qui sort le conteneur Docker du mode Turbopack par défaut. `Email Monitor` passe son polling de **3 s** à **10 s** ; `Sécurité` réduit l’aperçu logs de **2000** à **500** lignes et le refresh de **5 s** à **15 s** uniquement onglet visible ; `useMetrics` annule maintenant aussi le timer bootstrap `checkServices` au démontage. Validation runtime : recreate `frontend` via Docker Compose direct `full/monitoring/https`, logs `Next.js 16.2.6 (webpack)`, `restart=0`, `oomKilled=false`, health `healthy`. Smokes HTTP : `/health` **200**, `/b4ck0ff1ce/email-monitor`, `/security`, `/performances/correlation` **200 → /login** sans session. Ressources : repos Webpack ~**499 MiB / 2 GiB**, après smokes/tests ~**1.41 GiB**, puis après court idle ~**984 MiB / 2 GiB**, sans OOM. Validations frontend : `npm run type-check` OK ; `npm run lint` OK **0 erreur**, 1430 warnings historiques ; ESLint ciblé pages/hooks **0 erreur**, 12 warnings historiques ; lints IDE OK ; Jest ciblé sécurité/metrics/charts **5 suites / 18 tests OK**. Charge API régression : `test-performance.js PERF_LIGHT=1` **15/15** endpoints, charge **13/13**, score **100/100** ; `test-load-advanced.js PERF_LIGHT=1` **46/46** requêtes, débit global **832 req/s**. Limite restante : le dev server reste lourd après compilation backoffice (~1 Go), et la vraie charge mobile CRUD authentifiée/prod-like rate-limit reste à faire avec token/environnement dédié. | [x] |
| P1C | Playwright backoffice — setup admin réaligné | Branche `fix/playwright-admin-auth` : correction du helper E2E admin pour respecter le seed auth. Quand `TEST_ADMIN_EMAIL` pointe vers le même compte que `ADMIN_EMAIL`, Playwright utilise désormais le mot de passe du compte seedé (`ADMIN_PASSWORD`) au lieu d’un `TEST_ADMIN_PASSWORD` éventuellement divergent ; si un vrai compte `TEST_ADMIN_EMAIL` distinct est configuré, `TEST_ADMIN_PASSWORD` reste obligatoire. Smoke sécurité mis à jour pour le titre produit actuel `/b4ck0ff1ce/security/incidents` = **Incidents & menaces**. Relance 14/06, sans `make` : stack utile healthy ; `docker exec jobbingtrack-auth-service npx prisma db seed` OK ; `npm run type-check` OK ; `npm run lint` OK **0 erreur** (warnings historiques) ; ESLint ciblé `test-data-helper.ts`, `security-titles-smoke.spec.ts`, `auth.setup.ts` OK ; lints IDE OK ; login admin API direct avec `.env` chargé OK (`status=200`, token présent) ; routes `/b4ck0ff1ce/security`, `/security/incidents`, `/security/alerts` HTTP **307** sans session (redirect login attendu). Limite de relance : Playwright navigateur ne démarre plus dans l’environnement local courant, même sur test minimal `about:blank` : Chromium/headless-shell quitte en **SIGTRAP** avant navigation. Les specs `auth.setup.ts` et `security-titles-smoke.spec.ts` restent donc bloquées par l’environnement navigateur, pas par un `401` admin. | [x] |
| P1C | Playwright backoffice — Chromium débloqué et smokes UI relancés | Branche `fix/playwright-chromium-launch` : cause du `SIGTRAP` isolée côté répertoire temporaire. Chromium direct fonctionne, mais Playwright crashait quand ses profils temporaires partaient dans `/tmp`, saturé/quota (`/tmp` ~20 Go, erreurs d’écriture). Correctif : `playwright.config.ts` crée `frontend/.tmp-playwright` et pose `TMPDIR` local si absent ; `.gitignore` ignore ce dossier. Régression UI trouvée/fixée au passage : `FollowupsTab` ne rend plus un objet `status/type` API comme enfant React ; `statistics/page.tsx` ne sauvegarde plus `timeRange` si déjà identique, ce qui coupe la boucle `Maximum update depth`. Validations sans `make` : admin setup **1/1** ; sécurité titres **10/10** ; Statistics smoke **4/4** ; Performances range **7/7** ; backoffice ciblé Relances/Événements/API tester **6/6** ; `npm run type-check` OK ; `npm run lint` OK **0 erreur** (warnings historiques) ; ESLint ciblé OK ; lints IDE OK. Suite backoffice complète après restart frontend à froid : **77/78** OK, échec unique `ERR_EMPTY_RESPONSE` sur `/api-tester` pendant restart Next dev `Server is approaching the used memory threshold`; route OK en isolé ensuite. Ressources après relance ciblée 14/06 : `jobbingtrack-frontend` healthy, `restart=0`, `oomKilled=false`, ~**1.56 GiB / 2 GiB**. | [x] |
| P1C | Email récap agent — Make logs recents par defaut | Après push `chore/logs-recent-default` du commit `abaa62b5` : email récap envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Make logs recents par defaut 2026-06-14` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**. | [x] |
| P1C | Ops — `make logs` démarre sur logs récents | Branche `chore/logs-recent-default` : `make logs` passe `--since=24h --tail=500` par défaut (`LOGS_SINCE` / `LOGS_TAIL` surchargeables) puis suivi live ; `make logs-history` conserve l'historique complet ; `scripts/ops/logs-watch.sh` aligné. Validations sans `make` : `bash -n scripts/ops/logs-watch.sh` OK ; `docker compose -f docker-compose.yml logs -t --since=24h --tail=20` OK. Doc : `makefiles/README.md`, `docs/STATUS.md`, `docs/TODOS.md`. | [x] |
| P1C | Email récap agent — Backoffice warmup 78/78 | Après push `fix/backoffice-dev-warmup` du commit `cc28bc98` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Backoffice warmup 78/78 2026-06-14` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**. | [x] |
| P1C | Playwright backoffice — suite complète 78/78 sans flaky | Branche `fix/backoffice-dev-warmup` : helper `gotoBackofficePage` retente les navigations coupées (`ERR_EMPTY_RESPONSE`, reset/refus connexion) ; `warmupBackofficeRoutes` précompile en `beforeAll` les routes lourdes (`/search`, `/tests-frontend`, `/user-journey/custom`, `/api-tester`, performance/tests-data, etc.) avant la campagne `backoffice.spec.ts` ; retry Playwright local porté à **2**. Validations sans `make` : `npm run type-check` OK ; ESLint ciblé `backoffice-navigation.ts`, `backoffice.spec.ts`, `playwright.config.ts` OK ; lints IDE OK ; relance complète après restart frontend propre : **78/78 passed**, **0 flaky**, **0 failed** (~5,3 min, workers=1, `PLAYWRIGHT_BASE_URL=http://localhost:5003`). Runtime final : `jobbingtrack-frontend` healthy, `restart=0`, `oomKilled=false`, ~**1.49 GiB / 2 GiB**. | [x] |
| P1C | Email récap agent — Backoffice smoke runner | Après push `fix/backoffice-smoke-memory` du commit `796d74b0` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Backoffice smoke runner 2026-06-14` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Email récap agent — Playwright Chromium backoffice | Après push `fix/playwright-chromium-launch` du commit `1aca8c0c` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Playwright Chromium backoffice 2026-06-14` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Email récap agent — Playwright admin auth | Après push `fix/playwright-admin-auth` du commit `8e416736` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Playwright admin auth 2026-06-14` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Responsive backoffice — audit 4 viewports agent | Branche `fix/backoffice-responsive-audit` : correctifs `EmailBackofficePageShell` + toolbar Email Monitor (grille responsive 1280px) ; spec `backoffice-responsive-audit.spec.ts` renforcé (warmup, attente loader auth, garde-fous DOM, navigation tolérante, erreurs réseau transitoires, mode serial, timeout 15 min/viewport) ; `/b4ck0ff1ce/tests` ajouté au warmup ; `PLAYWRIGHT_BROWSERS_PATH` local `frontend/.cache-playwright`. Validations sans `make` : `./node_modules/.bin/tsc --noEmit` OK ; ESLint ciblé **0 erreur** (1 warning historique) ; Playwright audit — petit-pc **2/2**, smartphone **2/2**, moyen **3/3**, grand-ecran **3/3** (~70 routes/viewport, workers=1, `PLAYWRIGHT_BASE_URL=http://localhost:5003`). Limite : timeout navigation `/tests` possible si dev server chaud — restart frontend + re-run viewport ciblé. | [x] |
| P1C | Email récap agent — Responsive backoffice 4 viewports | Après merge/push `dev` du lot `fix/backoffice-responsive-audit` (commit `e222cf6f`, merge `7799d12e`) : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Responsive backoffice 4 viewports 2026-06-14` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**. | [x] |
| P1C | Frontend mémoire + API mobile authentifiée | Branche `fix/frontend-memory-mobile-api-load` : `MAX_CHART_API_POINTS = 2000` dans `timeRangeUtils` + plage custom Statistics ; refresh Statistics seulement si onglet visible ; script `tests/performance/test-mobile-api-authenticated.js` (9 endpoints métier USER + charge légère). Validations sans `make` : `tsc --noEmit` OK ; ESLint ciblé **0 erreur** (warnings historiques) ; Jest `statisticsTimeSeries` **3/3** ; API mobile **9/9** + charge **10/10** (`PERF_LIGHT=1`) ; `test-performance.js PERF_LIGHT=1` **15/15** score **100/100** ; Playwright statistics **4/4**, backoffice **80 passed 1 flaky** (Performances applicatives infra). Frontend ~**1.79 GiB / 2 GiB** après campagne. | [x] |
| P1C | Email récap agent — Mémoire Statistics et API mobile auth | Après merge/push `dev` du lot `fix/frontend-memory-mobile-api-load` (commit `d8f7e5c7`, merge `4047091e`) : email récap envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Mémoire Statistics et API mobile auth 2026-06-14` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**. | [x] |
| P1C | Benchmark performance avant/après — preuve gains réels | Script `scripts/perf/benchmark-performance-commits.sh` + rapport `docs/performance/PERFORMANCE_BENCHMARK_2026-06-14.md`. Comparaison Git : `34fc84f4` avant (`next dev` Turbopack, Statistics 7j **10080** points) vs `69ec0b0d` après (`next dev --webpack`, Statistics 7j **2000** points max). Résultats : mémoire frontend idle **1.733 GiB → 454.9 MiB** ; après `test-performance.js PERF_LIGHT=1` **2.000 GiB / 2 GiB → 409.5 MiB / 2 GiB** ; API perf **15/15** score **100/100** avant et après ; API mobile auth après **9/9** + charge **10/10** ; smoke Statistics après **4 passed** en **33 s**, mémoire après smoke **1.577 GiB / 2 GiB**. Conclusion : gain réel frontend/payloads, backend sans régression ; reste Recharts dynamiques/rate-limit prod-like. | [x] |
| P1C | Email récap agent — Preuve performance avant après | Après merge/push `dev` du lot `docs/performance-benchmark-proof` (commit `433de167`, merge `4c38a4f3`) : email récap envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Preuve performance avant apres 2026-06-14` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**. | [x] |
| P1A | Sécurité login backoffice — campagne contrôlée | Branche `security/backoffice-login-hardening` : le login frontend ne passe plus par `cachedRequest`; `api-gateway` applique explicitement `authRateLimiter` sur `/api/v1/auth/login` quand `RATE_LIMIT_ENABLED !== false`; spec `frontend/tests/e2e/backoffice-login-security.spec.ts` ajouté. Validations sans `make` : restart `api-gateway` + `/health` **200** ; `tsc --noEmit` frontend OK ; ESLint ciblé **0 erreur** (warnings historiques `any` dans `api.ts`) ; `/usr/bin/node --check backend/api-gateway/src/server.js` OK ; Playwright sécurité login **5/5 passed** ; Playwright login no-auth existant **4 passed / 3 skipped**. Rapport : `docs/security/BACKOFFICE_LOGIN_SECURITY_AUDIT_2026-06-15.md`. | [x] |
| P1A | Email récap agent — Sécurité login backoffice | Après merge/push `dev` du lot `security/backoffice-login-hardening` (commit `03b7e8ab`, merge `1d6ad977`) : email récap envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Sécurité login backoffice 2026-06-15` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**. | [x] |
| P1A | WAF gateway — remote host, shell et URL injection | Branche `security/gateway-injection-host-audit` : ajout des règles WAF `REMOTE_HOST_ACCESS`, `URL_INJECTION`, `HOST_HEADER_SPOOFING` et renfort `COMMAND_INJECTION` encodée ; journalisation high/critical vers `security-service` en `eventType=waf_blocked_request` avec evidence redigée. Validations sans `make` : `/usr/bin/node --check backend/api-gateway/src/middleware/waf.js` OK ; tests WAF avec `src/tests/config` montés depuis le workspace dans l’image `jobbingtrack-api-gateway` **12/12 passed** ; payloads runtime bornés remote-host metadata, shell injection encodée et `X-Forwarded-Host` forgé → **403/403/403** ; `security_logs` contient **3/3** événements `waf_blocked_request`, `isBlocked=true` pour `203.0.113.52/53/54` ; frontend `tsc --noEmit` direct OK ; ESLint ciblé frontend 0 erreur, 5 warnings historiques sur `security-e2e.spec.ts`. Rapport : `docs/security/GATEWAY_INJECTION_HOST_AUDIT_2026-06-15.md`. Limites : `npm run type-check`/`npm run lint` muets code 1 (dette wrapper connue) ; ESLint api-gateway non concluant dans l’image faute de config ESLint sous `/app`. | [x] |
| P1A | Email récap agent — WAF remote host shell URL | Après merge/push `dev` du lot `security/gateway-injection-host-audit` (commit `b4485553`, merge `0e5f1444`) : email récap envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — WAF remote host shell URL 2026-06-15` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**. | [x] |
| P1C | Playwright local — procédure anti-SIGTRAP consolidée | Relance 15/06 : le `SIGTRAP` n’est pas un Chromium corrompu mais un combo `/tmp` saturé (~80 %) + `playwright.standalone.config.ts` sans `TMPDIR` local + invocations shell fragiles. Correctifs : module `frontend/tests/e2e/playwright-runtime-env.ts` partagé par `playwright.config.ts` et `playwright.standalone.config.ts` ; suppression du `webServer` `echo` quand `PLAYWRIGHT_BASE_URL` est défini ; script `scripts/testing/playwright-local.sh` (bash, chemins locaux, vérif `:5003`) ; `playwright-frontend-e2e.sh` exporte aussi `TMPDIR`/`PLAYWRIGHT_BROWSERS_PATH`. Validations sans `make` : `tsc --noEmit` frontend OK ; `playwright-local.sh` → `auth.setup.ts` **1/1** ; campagne sécurité `backoffice-login-security` **4/4** + `security-titles-smoke` **10/10** = **14/14** (~46 s, workers=1). Commande recommandée : `bash scripts/testing/playwright-local.sh test <spec> --config=playwright.standalone.config.ts --project=chromium --workers=1`. | [x] |
| P1C | Email récap agent — Frontend mémoire et charge API | Après push `dev` du commit `60c16928` : email récap complet envoyé via `notification-service` sujet `RAPPORT AGENT JOBBINGTRACK — Frontend mémoire et charge API 2026-06-13` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Vérification Postgres `EmailLog` : **3/3 SENT**, `metadata.mirror.sent=true`, expéditeur visible `JobbingTrack Security <noreply@maily.ovh>`, `Reply-To security@maily.ovh`, `messageId @maily.ovh`. | [x] |
| P1C | Menaces — statut blocage B12/B8 (recommandé vs auto) | Helper `threatBlockPresentation.ts` : distingue **Blocage recommandé**, **Bloqué automatiquement**, **Bloqué manuellement** ; explique IP privée/lab ou échec auto-blocage pour menaces high/critical non bloquées. Intégré liste `/security/threats` et fiche détail ; `metadata.blockOrigin` posé côté `networkThreatDetector` (auto) et `blockThreat` (manuel). Vue d’ensemble sécurité : mention `docs/ERRORS.md` dans la légende. Validations sans `make` : `tsc --noEmit` direct OK ; Jest `threatBlockPresentation` **3/3** ; `node --check` backend modifié OK ; lints IDE ciblés OK. | [x] |
| P1C | Menaces — timeline investigation B8 multi-sources | Helper `threatInvestigationTimeline.ts` fusionne menace, logs sécurité, intrusions, DDoS et connexions réseau (24 h) triés desc ; composant `ThreatInvestigationTimeline` sur fiche menace avec badges source, requestId, lien log. Validations sans `make` : `tsc --noEmit` direct OK ; Jest timeline **2/2** ; lints IDE ciblés OK. | [x] |
| P1C | Logs sécurité — filtre requestId B6/B8 | `securityService.getSecurityLogs()` accepte `requestId` exact (`metadata.requestId`) et la recherche libre `q` matche aussi `requestId`, `correlationId`, `xRequestId`, `x-request-id`. Les facettes logs exposent `requestIds`; le client frontend `securityService.getSecurityLogs()` transmet `eventType`, `requestId`, `q`, `order`. Validations sans `make` : frontend `tsc --noEmit` direct OK ; Jest backend `security-logs-filters` **4/4** ; `/usr/bin/env node --check` service/controller OK ; lints IDE ciblés OK. | [x] |
| P1C | Performances Corrélation — incidents sécurité + champs forensics | Page `/performances/correlation` : fusion `aggregated_logs` + `security_logs`, parsing forensics élargi, raison visible si champ manquant. **Erreurs Postgres `event_type`/`service_name`/`requestid` vues en logs Docker** : requêtes SQL de diagnostic agent (snake_case), **pas** l’application — grep code `event_type`/`service_name` = **0** ; schéma Prisma = camelCase (`"eventType"`, `"serviceName"`, `"requestId"`). **Pipeline HTTP gateway** : middleware `httpForensicsAccessLog` journalise WARN avec requestId/méthode/endpoint/HTTP/proto/port pour toutes les réponses **≥ 400** (hors `/health`, sans doublon WAF) ; proxy renseigne `upstreamServiceName` ; `pickCentralLogForensics` nettoie les métadonnées central logger. WAF + security_logs inchangés (lab `lab-correlation-perfect-002`). **Limite attendue** : logs hors requête HTTP (workflow, deployment, cron) restent sans méthode/endpoint — normal. Validations sans `make` : `tsc --noEmit` OK ; Jest corrélation **5/5**, WAF **12/12**, `httpForensics` **2/2**, security filters **4/4** ; `node --check` backend ciblé OK. | [x] |
| P1C | Lot B3/B4 — nature incidents + corrélation réseau | B3 : `classifySecurityEventNature`, `SecurityNatureBadge`, colonne **Nature** sur `/security/incidents`, libellé origine `unknown`. B4 : API `correlationHint` + bannière % conteneurs sur `/security/network`, erreurs `error+message`, liens tableau avec raisons explicites. Validations sans `make` : `tsc --noEmit` OK ; Jest `securityLabels` **8/8** ; `node --check` firewallController OK. | [x] |
| P1C | Lot B7/B8 — audit append-only + investigation | Modèle `AuditLog`, `GET/POST /api/v1/security/audit`, hooks déblocage IP + settings + auth login ; page `/b4ck0ff1ce/security/investigation` (export JSON + SHA-256) ; `getThreatDetails` fusionne `aggregated_logs` ; doc `docs/security/INVESTIGATION_PROCEDURE.md`. Validations sans `make` : Jest `audit-service` **3/3** ; `tsc` OK ; `node --check` auditService/controller/auth OK. **16/06 local** : `prisma db push` complet refusé car Prisma voulait supprimer des colonnes/tables non liées (`Application`, `Company`, métriques, log collector) ; création ciblée non destructive de `audit_logs` + index, `prisma generate` dans `security-service`, smoke interne `POST /api/v1/security/audit/events` OK (`agent_smoke_audit=1`). | [x] |
| P1C | Lot B6/B9 — corrélation mobile | Auth CORS `X-Request-Id`/`X-Correlation-Id` ; Flutter headers corrélation + `postSecurityEvent` ; gateway `POST /api/v1/mobile/security-events` rate-limit 30/min → `security_logs`. Validations sans `make` : `node --check` gateway/auth OK ; `tsc` frontend OK. | [x] |
| P1C | Lot B — non-régression + incréments ciblés 16/06 (suite) | **Tests** : security-service Jest **58/58** (fix `server.test.js` mocks DB/scheduler, expectations `metadata` blocage auto/manuel, mock `aggregatedLog.findMany`) ; api-gateway **40/40** ; frontend `tsc --noEmit` OK ; Jest sécurité **16/16** (`securityLabels`, `threatBlockPresentation`, `threatInvestigationTimeline`, `securityLogLinks`). **B3** : Analyse — `formatBlockOriginLabelOrUnknown` (fini fallback « Actif »). **B6** : morgan après `requestContextMiddleware` sur contact/notification/workflow/deployment (+ dépendance `morgan` workflow/deployment). **B7** : audit `role_change` (`auth.controller.updateUserRole`) + `clear_test_data` (`testdata.controller`). **B9** : `AuthProvider` envoie `auth_failure` / `mobile_logout` ; `ApiConfigStore.getOrCreateDeviceId` ; gateway accepte `mobile_logout` ; carte **Signaux sécurité mobile** sur vue d’ensemble. Validations via `/usr/bin/npm test` direct (pas `make`). | [x] |
| P1C | Lot B — redémarrage local post audit/corrélation | Services rebuild/recréés sans `make` : `security-service`, `api-gateway`, `auth-service`, `contact-service`, `notification-service`, `workflow-service`, `deployment-service`. Correction packaging gateway : `backend/api-gateway/Dockerfile.backend-context` embarque `backend/shared` vers `/shared` pour les imports `httpForensics` après rebuild. Vérification `docker compose ps` : les 7 services impactés sont **healthy** ; `security-service` `/health` OK ; smoke audit interne OK. | [x] |
| P1C/P1B | Corrélation fine + détail service — refresh ciblé et historique récent | Suite retour porteur 16/06 : `/performances/correlation` ne relance plus `load()` complet en auto-refresh ; l’intervalle ne rafraîchit que les logs/score du bloc **Corrélation incidents**, sans recharger les historiques lourds ni toute la page. Parsing forensics incident enrichi : champs top-level + `sourceIP/source_ip/clientIp/client_ip/ipAddress`, headers `x-forwarded-for`, `x-real-ip`, `cf-connecting-ip`, nested `request/req/context`. Fiche `/services/[serviceName]` : l’appel `/history` envoie explicitement `startTime/endTime/_`, `cache: no-store`, fenêtre 6 h et limite 320 pour éviter les graphes bloqués sur d’anciens points. Preuves sans `make` : frontend `tsc --noEmit` OK ; Jest ciblé `serviceHistorySources` + `performanceCorrelationModel` **2 suites / 8 tests OK** ; ESLint ciblé fichiers modifiés **0 erreur** (14 warnings historiques) ; lint global **1 erreur préexistante** `securityLabels.test.ts require()` ; smoke API historique `auth-service` 6 h → **20 points**, dernier `2026-06-16T15:17:33Z` ; Playwright Corrélation **2/2** ; Playwright détail `auth-service` **2/2**. | [x] |
| P1C/P1B | Backoffice — règle refresh ciblé + B3 Firewall | Règle portée dans `docs/TODOS.md` + `A_VALIDER_AVANT_PRODUCTION.md` : aucun auto-refresh backoffice ne doit recharger une page entière ; seuls les composants utiles doivent se rafraîchir avec `visibilityState`, annulation requêtes obsolètes, fréquence/concurrence sobres et audit CPU/RAM final avant préprod/prod. Incrément B3 : `/security/firewall` affiche `SecurityNatureBadge` nature+origine sur les IP bloquées et son timer ne remet plus les règles en skeleton global (`loadRules(false)`). Validations sans `make` : frontend `tsc --noEmit` OK ; Jest `securityLabels` **8/8** ; ESLint ciblé Firewall/labels/badge **0 erreur** (warnings historiques) ; lints IDE ciblés OK. | [x] |
| P1C/P1B | Corrélation fine — champs forensics manquants (requestId, méthode, endpoint, IP, HTTP, proto, port) | Cause identifiée : alertes `security_alert_created` (analyseur cron) sans contexte HTTP direct ; IP seulement dans `suspiciousIPs` ; forensics complets dans `security_logs` liés par `logId`. **Frontend** : module `incidentForensics.ts` — fusion `metadata.metadata` + `representativeForensics`, enrichissement via index `security_logs` (`logId`), filtre strict `isCorrelationTableEligibleRow` (7 champs HTTP obligatoires, exclusion agrégats analyseur). **Backend** : plus de central logging pour alertes `threat_analysis`/`security-analyzer` sans HTTP ; `analyzeAttackPatterns` produit `representativeForensics`. **Nettoyage BDD** : suppression **1741** lignes `aggregated_logs` « Activité d'attaque critique détectée » ; re-vérif `bad_analyzer=0`, `lab_complete=6`. **Preuve page** : `/b4ck0ff1ce/performances/correlation` focus `security-service` — lignes `lab-correlation-verify-*` avec requestId, GET/POST, endpoint, IP lab RFC5737, HTTP 403, http, port 3000/3017 ; **0** ligne « Activité d'attaque critique » incomplète. Validations sans `make` : `tsc --noEmit` OK ; Jest `incidentForensics` **7/7** ; rebuild `security-service` OK. | [x] |
| P1C | Lot B9/B4 — filtres mobile + empty state réseau | **B9** : preset **Signaux mobile** sur `/security/logs` (`category=mobile`), lien croisé Incidents ; carte vue d’ensemble → logs mobile ; filtre Incidents `?mobile=1` ; `isIncidentLog(category=mobile|mobile_*)` ; libellés FR mobile dans `securityLabels.ts`. **B4** : empty state connexions réseau guidé par `correlationHint` + message monitoring-agent/docker.sock. Validations sans `make` : `tsc --noEmit` OK ; Jest `securityLabels`+`incidentForensics` **15/15** ; ESLint ciblé **0 erreur** (1 warning historique network). | [x] |
| P1C | Lot B7/B8 — investigation multi-sources + export menaces | **Backend** : `investigationService` + `GET /api/v1/security/investigation/search` (menaces, `security_logs`, `aggregated_logs`, audit, comptes impactés auth) ; `POST /api/v1/security/investigation/export` (bundle JSON ou CSV menaces + audit `security_export`). **Frontend** : page `/b4ck0ff1ce/security/investigation` — onglets Audit / Menaces & logs / Comptes impactés ; filtres IP, requestId, service, type menace ; exports CSV menaces + bundle complet + SHA-256. **Validations complètes 16/06** : Jest `investigation-service` + `audit-service` + `firewall-threat-forensics` + `security-logs-filters` **17/17** ; `tsc --noEmit` OK ; smoke script `backend/security-service/scripts/smoke-investigation-b7-b8.sh` — login admin, audit 5 lignes, search (10 menaces / 20 agg / 15 sec / 10 audit / 1 compte), export JSON `auditRecorded=true`, CSV menaces header OK, 2× `security_export` en audit. **UI Playwright** : spec `security-investigation-b7-b8.spec.ts` ajoutée ; échec auth hydration si frontend Docker pointe `NEXT_PUBLIC_API_URL` HTTPS `:5443` — contournement test : `NEXT_PUBLIC_API_URL=http://127.0.0.1:5002` sur conteneur frontend ; validation visuelle porteur recommandée sur `/b4ck0ff1ce/security/investigation`. | [x] |

| P1C | Lot B9 — signaux mobile OTP + session révoquée | **Flutter** : `ApiService.onSessionRevoked` sur 401/403 des requêtes Bearer (hors `/mobile/security-events`) ; `AuthProvider` envoie `session_revoked` et purge la session ; échec `verifyEmail` → `otp_failed`. **Gateway** : types déjà acceptés (`otp_failed`, `session_revoked`). Smoke curl gateway **2/2** (`201`). | [x] |
| P1C | Backoffice — chemin canonique `/backoffice` (fin obscurité URL) | Demande porteur 16/06 : ne plus s’appuyer sur `/b4ck0ff1ce` + localhost pour la « sécurité » ; contrôle d’accès = login + token session (JWT auth-service) comme avant, vérifié côté middleware Next (cookie/header) et gateway API. Changements : `backoffice.config.ts` ; `middleware.ts` protège `/backoffice/*` et redirige **308** `/b4ck0ff1ce/*` → `/backoffice/*` ; remplacement des liens hardcodés frontend/src + tests. Emails alertes sécurité : liens `/backoffice/...`. Validations sans `make` : `npm run type-check` OK ; Jest `security-alert-email-payload` **3/3 OK**. | [x] |
| P1C | Lot B4/B8 — enrichissement IP réseau + comptes investigation | **B4** : `enrichIpBatch` (GeoIP top 12) sur `GET /firewall/network/stats` → `ipEnrichment` ; page Réseau : colonne Pays/réputation IPs surveillées + réputation tableau connexions. **B8** : `enrichAccountsWithUserProfiles` (table `User` : email, displayName, role). Validations sans `make` : Jest geoip **6/6**, investigation **4/4**, connectionSource **3/3** ; `tsc` OK. **Block I/O 16/06** : agent Rust `/proc/<pid>/io` + enrichissement `docker stats` ; Jest `blockIoEnrichment` **3/3** ; BDD snapshots non nuls. | [x] |
| P0 | HTTPS dev — `jobbingtrack.localhost` ports 443/80 + redirect | Cause racine : le proxy `dev-https-proxy` (profile Docker `https`) n’était pas démarré → aucun listener sur 443/5443/80, seul le frontend HTTP `:5003` répondait. Correctifs : `docker-compose.https.yml` mappe **443** et **80** en plus de **5443** ; Nginx `default.conf` redirige HTTP → HTTPS **308** ; `ports.config.ts` détecte port navigateur 443 (API same-origin sans `:5443`). Preuves 16/06 sans `make` : `docker compose … --profile https up -d dev-https-proxy` ; `http://jobbingtrack.localhost/` → **308** `https://jobbingtrack.localhost/` ; `https://jobbingtrack.localhost/login` → **200** ; `:5443/login` → **200** ; `https://api.jobbingtrack.localhost/health` → **200** ; `tsc --noEmit` OK. | [x] |
| P0 | ERR_TOO_MANY_REDIRECTS — boucle middleware backoffice | Cause racine : `BACKOFFICE_LEGACY_PATH` était par erreur `/backoffice` (identique à `BACKOFFICE_BASE_PATH`) → middleware renvoyait **308** `/backoffice` → `/backoffice` à l’infini (`ERR_TOO_MANY_REDIRECTS`). Correctif : `BACKOFFICE_LEGACY_PATH=/b4ck0ff1ce` ; matcher middleware inclut `/b4ck0ff1ce/:path*`. Preuves 16/06 : `/backoffice` sans cookie → **307** `/login` ; `/login` → **200** ; `/b4ck0ff1ce/security` → **308** `/backoffice/security` ; login + cookie JWT → **200** (plus de boucle) ; `tsc --noEmit` OK. | [x] |
| P0 | Backoffice post-login — `Service temporarily unavailable` (profil) | Cause : en HTTPS sur port **443**, l’Origin `https://jobbingtrack.localhost` n’était pas autorisée CORS (seul `:5443` dans `ALLOWED_ORIGINS`) → preflight **500** → Axios `Network Error` → `loadUserProfile` échoue. Correctifs : `ports.config.ts` — API **same-origin** via Nginx (`/api/v1/*`) pour `jobbingtrack.localhost` en dev HTTPS (443 et 5443) ; `api-gateway` — origines dev `https://jobbingtrack.localhost` / `api.*` + regex `isDevJobbingtrackHttpsOrigin` ; `.env.example` mis à jour. Preuves 16/06 : preflight CORS Origin 443 → **200** ; `POST/GET …/api/v1/auth/login|profile` same-origin **443** et **5443** → **200** ; `tsc --noEmit` OK ; gateway redémarré. | [x] |
| P1C | Lot B10 — filtres liste Services (CPU/mémoire/état) | Migration `/backoffice/services` vers patron **`FilterBar` + `useAppliedFilters`** : filtres État/CPU/Mémoire appliqués explicitement (plus de filtrage instantané), badges actifs, sync URL `?status=&cpu=&memory=`. Module `serviceListFilters.ts` + Jest **3/3** ; `tsc --noEmit` OK. | [x] |
| P1C | Git — branche `fix/frontend-security-performance` découpée et poussée | **16/06** : 11 commits logiques (`41e80bb9`…`c2272b50`) — dev HTTPS, CORS, `/backoffice`, investigation B7/B8, block I/O, forensics gateway, Lot B sécurité, mobile B6/B9, filtres Services B10, pilotage. Push `origin/fix/frontend-security-performance` OK ; PR [#10](https://github.com/PavelDelhomme/JobbingTrack/pull/10) vers `dev` ouverte (53 commits vs `dev`). Merge **non fait** : CI encore en cours au push ; validations porteur `TODOS_A_VALIDER.md` (Statistics P1B en tête) restent ouvertes. Validations sans `make` : `./node_modules/.bin/tsc --noEmit` frontend OK (wrappers npm `type-check` muets code 1, dette connue). | [x] |
| P1C | Merge PR #10 — revalidation post-merge sur `dev` | **16/06 soir** : PR #10 mergée (`70be4efd`). Branche locale `dev` fast-forward **55 commits**. Validations sans `make` : `./node_modules/.bin/tsc --noEmit` OK ; Jest ciblé `serviceListFilters` + `securityScore` + `securityStatisticsComparison` **9/9** ; security-service Jest investigation/audit/filters **11/11** ; Playwright `security-investigation-b7-b8` **3/3** (~15 s) ; Playwright Statistics smoke **5/5** (~30 s) via `PLAYWRIGHT_BASE_URL=http://localhost:5003` + `playwright-local.sh`. Nouvelle branche travail : `fix/playwright-investigation-post-merge`. | [x] |
| P1C | Lot B10 — pondération score sécurité persistée serveur | **16/06 soir** : `GET/PUT /api/v1/security/score-settings` (`securityScoreSettings.js`, audit `security_score_weights_update`) ; UI `/backoffice/security` charge/migrate depuis serveur, debounce 400 ms, repli localStorage si API KO. Validations sans `make` : Jest backend `score-settings` **3/3** ; frontend `tsc` OK ; Jest `securityScore` **3/3** ; runtime `GET score-settings` **200** après recreate `security-service`. | [x] |
| P1B | Statistics P1B — revalidation onglet Sécurité post-merge | **16/06 soir** : `smoke-persistence-stats.cjs` OK ; metrics `/api/v1/persistence/security/summary?hours=168` → `avgSecurityScore≈99.36` ; gateway `/api/v1/security/stats` → `criticalEvents=4`, `securityScore=65` ; Jest `securityStatisticsComparison`+`logStatsFilters` **6/6** ; backend persistence Jest **3/3** ; Playwright Statistics **5/5** (~13 s). File porteur ligne 306 `TODOS_A_VALIDER.md` enrichie — attend OK porteur. | [x] |
| P1C | Merge PR #11 — score serveur B10 sur `dev` | **16/06 soir** : PR #11 mergée sur `dev` ; CI **verte** (backend, frontend, sécurité). Validations agent : Jest `score-settings` **3/3** ; recreate `security-service` ; `GET /api/v1/security/score-settings` **200**. | [x] |
| P1C | Lot B4 — hints connexion réseau unknown | **16/06 soir** : `connectionSource.js` — ignore `containerName=unknown`, priorise hints port service, détail client interne ; Jest **4/4** ; recreate `monitoring-agent-rs` (Block I/O `/proc` déjà dans image). **PR #12 mergée** (`44e53b9f`). | [x] |
| P1C | Campagne tests agent — post-merge PR #11/#12 | **16/06 nuit** : `tsc --noEmit` OK ; security-service Jest ciblé **23/23** ; frontend Jest ciblé **15/15** ; `smoke-persistence-stats.cjs` OK (`securityMetrics=109813`, `aggregatedLogs=26595`) ; Playwright investigation **2/2** + Statistics **5/5** + Services filtres B10 **1/1** (~33 s campagne combinée) ; `GET /login` **200**. Performances range smoke lancé séparément (spec longue). | [x] |
| P1C | Merge PR #13 — Playwright filtres Services B10 | **16/06 nuit** : PR #13 mergée (`d0ae9370`) ; spec `services-list-filters-b10.spec.ts` ; CI **verte** (backend + frontend + sécurité). Playwright local **3/3** (setup + security-logs-filters + services-filters). | [x] |
| P1B | Statistics log-stats — revalidation script rapport | **16/06 23:21** : `run-statistics-log-stats-validation-with-report.sh` → `tests/results/statistics-log-stats/20260616-232133` **4/4 OK** ; `aggregatedLogs=26645`, 14 j API **800** lignes, filtre WARN **50**, filtre gateway **50** ; page smoke OK. Notes agent ligne 307 `TODOS_A_VALIDER.md` enrichie. | [x] |
| P1B | Statistics app-data — script validation Node (login + API) | **16/06 23:34** : remplacement login Python par `load-root-env.cjs` + `smoke-statistics-app-data-api.cjs` ; pages `/backoffice/statistics/...` via `smoke-backoffice-page-urls.cjs` ; précheck `ensure-dashboard-service-ready.sh` (profile compose `dashboard-service`). Rapport `tests/results/statistics-app-data/20260616-233410` **4/4 OK** ; `/api/v1/statistics` **200**, `undefinedHits=0`, timeline **1** point + note fallback ; BDD counts OK. Notes agent ligne 308 `TODOS_A_VALIDER.md` enrichie. | [x] |
| P1B | Statistics vue d’ensemble — revalidation script overview | **16/06 23:34** : `run-statistics-overview-validation-with-report.sh` → `tests/results/statistics-overview/20260616-233410` **4/4 OK** ; history 7 j **45** points ; ranges **24h=91**, **7d=174** ; Jest `statisticsTimeSeries` OK ; pages `/backoffice/statistics` + log-stats + app-data smoke OK. Notes agent ligne 309 `TODOS_A_VALIDER.md` enrichie. | [x] |
| P1C | Lot B10 — filtres FilterBar Menaces + Incidents | **16/06 23:34** : spec Playwright `security-filters-b10.spec.ts` — Menaces (sévérité Haute, apply explicite) + Incidents (gravité Critique, apply explicite). Playwright local **3/3 OK** (~16 s) via `PLAYWRIGHT_BASE_URL=http://localhost:5003` + `playwright-local.sh`. `tsc --noEmit` OK. Branche `fix/statistics-validation-scripts-b10`. | [x] |
| P1B | Statistics log-stats — migration API smoke Node | **16/06 23:38** : `smoke-statistics-log-stats-api.cjs` remplace bloc Python ; rapport `statistics-log-stats/20260616-233835` **4/4 OK**. Script récap `send-agent-recap-email.cjs` ajouté. **PR #15 mergée** (`09b7f138`). | [x] |
| P1C | Merge PR #15 + email récap agent Lot B/Statistics | **16/06 23h47** : PR #15 mergée sur `dev` (`09b7f138`) — CI **verte**. Email `[JobbingTrack] Recap agent complet Lot B + Statistics P1B — 16/06/2026 23h40` : **7/7 EmailLog SENT** (`test@delhomme.ovh`, `dev@delhomme.ovh`, `admin@delhomme.ovh`, `admin@jobbingtrack.com`, `security@jobbingtrack.com`, `crash-report@jobbingtrack.com`). | [x] |
| P1B | Backoffice vue d’ensemble — compteur services trompeur 10/10 | **17/06** : cause — API `docker/services/all` et UI ne comptaient que les conteneurs Docker existants (~10 running), pas le catalogue make status (~21 attendus). Correctif : merge `SERVICE_HEALTH_ENDPOINTS` manquants en `not_deployed` ; `ServiceHealthKpiCards` affiche Actifs X/Y + Non déployés ; hub `/backoffice` « N sains · X/Y actifs » ; résumé `make status` basé catalogue explicite. Jest `serviceHealthOverview` **12/12** ; `tsc` OK. **PR #16 mergée** (`d2101fbe`). | [x] |
| P1C | Email récap journée complet 16/06 — PR #10–#16 + pilotage | **17/06 00:10** : rapport détaillé (`/tmp/jt-recap-journee-20260616.txt`) — synthèse 7 PR, commits/fichiers par lot, validations agent, état TODOs, marche/ne marche pas, priorités. Envoi via `send-agent-recap-email.cjs` sujet `[JobbingTrack] Recap journée complet 16/06/2026 — PR #10-#16, Statistics P1B, pilotage` : **6/6 HTTP 202** (`test@delhomme.ovh`, `admin@jobbingtrack.com`, `security@jobbingtrack.com`, `crash-report@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`). Script programmation ajouté : `scripts/ops/schedule-agent-recap-email.sh` (envoi différé HH:MM). | [x] |
| P1D | CI/CD — Tests Frontend Playwright E2E bloquants | **17/06** : analyse `ci-reports-27648174610.zip` (summary only) + run GHA `27651325457` — échec réel = **Playwright E2E** (`Email admin E2E manquant`, `email-verification-monitor` import-time), pas lint (warnings analytics = bruit). Correctifs : `scripts/ci/prepare-playwright-e2e-stack.sh` (postgres+auth+gateway CI), env E2E job `test-frontend`, `playwright.ci.config.ts` (exclusions specs lourdes), `email-verification-monitor` lazy env, nettoyage imports analytics. Validations sans `make` : eslint analytics 10 warnings user **0** ; Jest CI **56/56 suites 223 tests OK** ; `tsc --noEmit` OK. | [x] |
| P1C | Block I/O conteneurs — cgroups agent + graphes Performances Conteneurs | **17/06** : retour porteur `docker stats Block I/O` vs agent à 0. Cause : agent lisait `/proc/<pid>/io` (permission refusée). Correctif Rust `io.rs` — lecture cgroup v2 `io.stat` (`rbytes`/`wbytes`), fallback `/proc`. Agrégateur : `blockIoEnrichment.js` **skip** `docker stats` CLI si `blockIO` déjà présent (évite appels lourds). Frontend : `/performances/containers` mode conteneur unique — graphes **Block I/O cumulé** + **débit Mo/min** (`containerBlockIoChartModel.ts`, `AnalyticsContainersChartsBundle`). Alignement runtime post-recreate : postgres agent **2,98 GiB / 5,14 GiB** vs `docker stats` **3,2 GB / 5,52 GB** ; security-service **5,19 MB / 1,22 MB** identique. Validations sans `make` : Rust `parse_cgroup_io_stat` **1/1** ; Jest backend `blockIoEnrichment` **4/4** ; Jest frontend `containerBlockIoChartModel` **4/4** ; `tsc --noEmit` OK. | [x] |
| P1C | Corrélation — sous-page Signaux conteneurs (pics CPU/mém/réseau/I/O) | **17/06** : demande porteur — repérer quel conteneur pic à quel moment sans modifier la page incidents. Nouvelle route `/backoffice/performances/correlation/containers` + `CorrelationSubNav` (Incidents & logs / Signaux conteneurs). Contenu : tableau synthèse pics (CPU/mém + horodatage, Δ réseau/I/O), comparaison superposée multi-conteneurs (6 graphes), détail focus avec brush synchronisé CPU/mémoire/réseau/Block I/O. Même file mémoire « Tout charger (filtre) » / plafond léger-complet que Corrélation. Validations sans `make` : Jest `correlationContainerMetrics` **2/2** ; `tsc --noEmit` OK. | [x] |
| P1C | Backoffice vue d’ensemble — cartes KPI cliquables | **17/06** : les 6 cartes synthèse du hub `/backoffice` pointent vers la page métier adéquate (navigation `Link` + flèche →). Mapping : **Sessions actives** → `/users?status=active` (filtre statut ajouté sur la page Utilisateurs) ; **Signaux sécurité** → `/security/incidents` ; **Santé système** → `/performances` ; **Temps de réponse** → `/performances/latency` ; **CPU / Mémoire conteneurs JobbingTrack** → `/performances/cpu-memory`. Validations sans `make` : Jest `userFilters` **4/4** ; `tsc --noEmit` OK. | [x] |
| P1C | Email récap agent session 17/06 — Block I/O + Corrélation + cartes KPI | **17/06 nuit** : rapport `/tmp/jt-recap-agent-20260617.txt` — synthèse 4 lots (Block I/O cgroups, sous-page Signaux conteneurs, cartes hub cliquables, correctifs CI Playwright non commités). Validations cumulées : `tsc` OK ; Jest frontend ciblé **10/10** ; Jest `blockIoEnrichment` **4/4**. Envoi via `send-agent-recap-email.cjs`. | [x] |
| P1C | Fin session 17/06 — récap complet + arrêt stack locale | **17/06 fin soir** : mail `[JobbingTrack] Recap session complete 17/06/2026 — Block I/O, Correlation, cartes KPI, CI — fin journee` **6/6 HTTP 202**. Rapport `/tmp/jt-recap-session-complete-20260617.txt`. Stack locale arrêtée (`docker compose down --remove-orphans` + stop résiduels). **29 fichiers non commités** sur `dev` — reprise demain : relancer stack, validation porteur Statistics P1B, commit/push. | [x] |
| P1C | Push dev b19198d7 — lot Performances/I/O + hub + CI Playwright | **17/06 matin** : commit direct sur `dev` (reprise après travail hors branche feature) — `b19198d7` pushé `origin/dev`. Contenu : Block I/O cgroup agent, graphes conteneurs, sous-page Corrélation signaux, cartes KPI cliquables, CI Playwright E2E, pilotage. Validations post-push : Jest ciblé **13/13** ; `tsc` OK ; smoke persistence **OK** (`securityMetrics=110038`). Stack relancée profils backend+monitoring+full pour suite Statistics P1B. | [x] |
| P1B/P1C | Branche `feat/p1b-validations-performances-e2e` — E2E hub KPI + Corrélation conteneurs + revalidation Statistics | **17/06** : specs Playwright `backoffice-hub-kpi-links.spec.ts` (**6/6**) et `correlation-containers-smoke.spec.ts` (**2/2**) via `PLAYWRIGHT_BASE_URL=http://localhost:5003` + `playwright.standalone.config.ts`. Revalidation Statistics : `smoke-persistence-stats.cjs` OK (`securityMetrics=110573`) ; scripts `run-statistics-log-stats-validation-with-report.sh` (`20260617-120015` **4/4**), `run-statistics-app-data-validation-with-report.sh` (`20260617-120331` **4/4**, `undefinedHits=0`), `run-statistics-overview-validation-with-report.sh` (`20260617-120331` **4/4**, history 7j **45** pts). Jest `securityStatisticsComparison` **4/4** + `userFilters` **3/3** ; `tsc --noEmit` OK. | [x] |
| P1D | Merge PR #17 — Statistics Sécurité + E2E hub/corrélation + ignore menaces | **17/06 après-midi** : PR [#17](https://github.com/PavelDelhomme/JobbingTrack/pull/17) mergée sur `dev` (`c1b1aab6`) — 4 commits (`19d94708`→`fd7b6015`). Revalidation post-merge branche `feat/p1b-statistics-post-merge-revalidation` : `smoke-persistence-stats.cjs` OK (`securityMetrics=110892`, `aggregatedLogs=26770`) ; scripts `run-statistics-log-stats-validation-with-report.sh` (`20260617-142627` **4/4**), `run-statistics-overview-validation-with-report.sh` (`20260617-142704` **4/4**, history 7j **43** pts, ranges **24h=135**, **7d=167**), `run-statistics-app-data-validation-with-report.sh` (`20260617-142705` **4/4**, `undefinedHits=0`) ; Jest frontend sécurité **10/10** ; backend `securityPersistenceSummary` **2/2** ; `tsc --noEmit` OK. | [x] |
| P1B | Statistics Sécurité — validation porteur OK | **17/06** : porteur `OK Statistics Sécurité`. Ligne archivée `TODOS_DONE.md`. | [x] |
| P1C | Merge PR #19 — filtres multi Incidents & Menaces | **17/06** : PR [#19](https://github.com/PavelDelhomme/JobbingTrack/pull/19) mergée sur `dev`. Porteur `OK filtres multi Incidents Menaces`. | [x] |
| P1B | Statistics log-stats — revalidation post-commit Performances Disque | **17/06 16:08** : après commit `b29db2cf` (fix volume disque), `run-statistics-log-stats-validation-with-report.sh` → `tests/results/statistics-log-stats/20260617-160815` **4/4 OK** ; `aggregatedLogs=26829`, **18** services ; persistence stats smoke OK. Ligne 310 `TODOS_A_VALIDER.md` enrichie — en attente `OK Statistics log-stats` porteur. | [x] |
| P1B | Statistics Sécurité vs Analyse — cohérence compteurs + recommandations live | **17/06 retour porteur KO** : somme snapshots persistés gonflait « activités suspectes » (ex. 1049) vs Analyse live (18). Correctifs : backend `getSecuritySummary` **max_per_snapshot** ; Statistics — tableau comparatif persisté/Analyse + lib `securityAnalysisSummary` ; Analyse — stats 30 j, IPs paginées (total meta), recommandations dynamiques, refresh 30 s ; hub Sécurité — total IPs via pagination. Jest `securityAnalysisSummary` + `securityStatisticsComparison` ; backend `securityPersistenceSummary` mis à jour. **Suite 17/06** : score global divergent (ex. 30 Analyse vs 20 Vue d’ensemble) — unification `calculateSecurityScore` + constantes partagées `securityLiveConstants` (30 j, 2000 logs, 200 menaces, refresh 15 s), fetch WAF + pondération locale identique. **Demande porteur** : ignorer faux positifs menaces — API `POST .../threats/:id/ignore|unignore`, exclusion compteurs/score, UI fiche + filtre liste. Jest `firewall-threat-ignore` **2/2**, `threatIgnore` **2/2**. | [x] |

## Vérifications récurrentes

- Lire `PILOTAGE.md` avant de choisir une prochaine tâche.
- Contrôler `TODOS_A_VALIDER.md` avant de commencer une feature.
- Ne travailler que sur la première ligne ouverte de `TODOS_A_VALIDER.md`; attendre validation porteur avant la suivante.
- Si une validation porteur échoue, créer une tâche dans `docs/TODOS.md` et traiter ce problème avant la suite.
- Après chaque livrable, mettre à jour `TODOS_A_VERIFIER.md`, `TODOS_A_VALIDER.md` ou `TODOS_DONE.md` selon le cas.
