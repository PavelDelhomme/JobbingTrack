# TODOs à vérifier par l’agent

Dernière mise à jour : 11 juin 2026

## Rôle

Ce fichier liste ce que l’agent doit vérifier techniquement avant de demander une validation porteur. Une ligne vérifiée par l’agent ne vaut pas validation produit.

## Vérifications ouvertes

| Priorité | Vérification agent | Preuve attendue | Statut |
|----------|--------------------|-----------------|--------|
| P0 | Cohérence du nouveau système de pilotage | Liens mis à jour vers `PILOTAGE.md`, `TODOS_A_VALIDER.md`, `TODOS_A_VERIFIER.md`; ancienne référence `A_VALIDER_VERIFIER.md` supprimée ou remplacée. | [x] |
| P0 | Règle Cursor de pilotage | `.cursor/rules/pilotage-validation.mdc` existe et impose la lecture du pilotage avant action. | [x] |
| P0 | Docs de suivi accessibles | `docs/README.md`, `docs/TODOS.md`, `docs/security/README.md`, `TRAITER_IMMEDIATEMENT.md` pointent vers les bons fichiers. | [x] |
| P0 | Correctif Firewall backoffice | Déblocage IP + garde-fous règles globales couverts par `env npm test -- --runTestsByPath tests/firewall-unblock-ip.test.js --verbose --runInBand` OK (6/6) ; `env npm run type-check` OK ; `env npm run lint` OK avec warnings historiques uniquement. | [x] |
| P0 | Rapport sécurité frais pour backoffice | `python3 scripts/security/cve-scan.py --output-dir tests/results/security --timeout-sec 60` OK ; API `/api/test-reports/all` liste `security-results-cve-20260521-201336` en premier ; `/api/test-reports/view` OK ; `/api/test-reports/download` OK (`cve-20260521-201336-summary.md`). | [x] |
| P0 | Comparaison rapports sécurité CVE | Route `/api/test-reports/compare` utilise `frontend/src/lib/test-reports/resolveReport.ts` (IDs `security-results-*`, `security-reports-*`, `summary.md` parsé en lignes par surface). UI dédiée : cartes Critical/High/Medium/Low/Info par rapport, tableau par surface, écarts, notes/payloads non renvoyés. Validation 21/05 : `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé sur `compare/route.ts`, `resolveReport.ts`, `test-reports/page.tsx` OK avec warnings historiques uniquement. `npm run type-check` / `npm run lint` sortent `1` sans sortie dans ce terminal, mais les commandes sous-jacentes passent. | [x] |
| P0 | Cadrage nouvelles demandes sécurité 08/06 | `TODOS_A_VALIDER.md` et `docs/TODOS.md` contiennent : alertes email critiques JobbingTrack vers adresses dev/admin porteur, CVE applicatives localisées (ex. `CVE-2026-49975`), tests offensifs contrôlés par conteneur (remote host/shell/URL/headers), réduction d’exposition/leurres VPS-Portainer, comparaison CVE exploitable malgré gros volumes, et accès notes/payloads bruts uniquement via réauth forte ; `A_VALIDER_AVANT_PRODUCTION.md` contient les gates préprod/prod associés. | [x] |
| P0 | Inventaire menaces lab/privées (lecture seule) | Script `scripts/security/security-threats-inventory-dry-run.cjs` : classe sans DELETE — `lab_test` (RFC 5737 `198.51.100.x`, `203.0.113.x`, `192.0.2.x`, `10.0.0.x`, metadata lab/test), `private_network` (`172.16–172.31.x`, `192.168.x`, autres `10.x`). Dry-run 21/05 (réf. `docs/TODOS.md`) : **228** lab_test (135 high/critical, 0 blocked), **11** private_network (2 high/critical, 1 blocked). **Aucune purge** tant que porteur n’a pas validé la ligne P0 dans `TODOS_A_VALIDER.md`. Rejouer localement : `node scripts/security/security-threats-inventory-dry-run.cjs --limit=25` (nécessite conteneur `jobbingtrack-postgres` up). | [x] |
| P1 | UX rendu rapports sécurité | Rendu CVE Markdown amélioré dans `/api/test-reports/view` : cartes synthèse, priorités à trier, tableau responsive, markdown brut repliable ; `env npm run type-check` OK ; `env npm run lint` OK avec warnings historiques ; smoke API view OK. | [x] |
| P1 | Validation frontend si fichier UI touché | `env npm run type-check` OK ; `env npm run lint` OK avec warnings historiques uniquement ; pas de Jest frontend ciblé existant pour `firewall/page.tsx`. | [x] |
| P0 | Tests sécurité backoffice — filesystem writable | `docker-compose.yml` : `TESTS_RESULTS_DIR=/tmp/tests/results` + volume `./tests/results:/tmp/tests/results` (car `./tests:/app/tests:ro`). `generate-test-report.sh` bascule vers `/tmp/tests/results` si mkdir échoue. Recreate conteneur `frontend` requis après merge. | [x] |
| P0 | Comparaison CVE — exploitabilité et filtres | `resolveReport.ts` : statut `skip` pour surfaces skipped (plus de faux « Échoué »). `compare/route.ts` : libellés écart sécurité, tri par score exploitabilité. UI : filtres type docker/node, critical/high only, masquer absents, badge « Ignoré ». `./node_modules/.bin/tsc --noEmit` OK. | [x] |
| P0 | Détails sensibles sous réauth | APIs `POST /api/test-reports/sensitive-step-up` (vérif mot de passe via auth login) + `GET /api/test-reports/sensitive-details` (jeton court usage unique, audit `tests/results/security-audit/step-up.jsonl`, `Cache-Control: no-store`). UI modal sur comparaison sécurité. `./node_modules/.bin/tsc --noEmit` OK. | [x] |
| P0 | Rapport tests sécurité backoffice — stats cohérentes | `generate-test-report.sh` : pattern 3 désactivé pour `security`, parsing `security-report.json` via **node** (pas jq requis), statut `warning` si medium/low seuls, labels Critical/High vs Medium/Low. `METRICS_AGGREGATOR_INTERNAL_URL` pour logs services. | [x] |
| P0 | Scan CVE + localisation depuis backoffice | `POST /api/test/run-cve-scan`, `GET /api/security/cve-locate?cve=…`, `scripts/security/cve-locate.mjs`, page tests-security : boutons séparés applicatifs / CVE + recherche CVE. `frontend/Dockerfile` : `python3` + `jq` (rebuild frontend requis). `./node_modules/.bin/tsc --noEmit` OK. | [x] |
| P0 | Correctifs retours porteur tests sécurité 16h | `run-security/route.ts` lit `security-report.json` pour décider `critical/high` et strip ANSI ; `view/route.ts` ouvre un rapport standard via fallback `report.html → summary.json → security.json → security-report.json`; `cve-scan.py` marque `vulnerable` si critical/high et `warning` si medium/low ; page `tests-security` strip ANSI côté terminal. Smoke `python3 scripts/security/cve-scan.py --output-dir /tmp/jt-cve-smoke --timeout-sec 30` OK avec surfaces `vulnerable`; `./node_modules/.bin/tsc --noEmit` OK. | [x] |
| P2 | Rapports tests — taille affichée | `/api/test-reports/all` calcule la taille complète des dossiers de rapports `tests/results/*` et `tests/results/security/*`; `/b4ck0ff1ce/test-reports` affiche la taille Ko/Mo par carte et le volume total de la liste filtrée. Compression/archivage noté dans `docs/TODOS.md`, sans purge automatique. | [x] |
| P2 | Agent email / tâches recherche emploi — cadrage documentaire | Cadrage ajouté sans implémentation : `docs/features/EMAIL_TRIAGE_AGENT.md` détaille objectif, sources Gmail/boîte candidatures, digest 18h, interface utilisateur hors backoffice, Google Tasks/Calendar, IA locale/optionnelle, sécurité OAuth/RGPD et critères d’acceptation. Liens ajoutés dans `docs/TODOS.md`, `docs/BACKLOG.md`, `docs/PLAN.md`, `docs/STATUS.md`, `docs/project/FONCTIONNALITES.md` et `TODOS_A_VALIDER.md`. Branche documentaire dédiée : `docs/email-triage-agent-roadmap`; rappel : reprendre ensuite `fix/security-cve-scan-full-scope` / PR #7 pour le P0 CVE. | [x] |
| P1A | Frontend tests — suppression des credentials codés en dur | Les tests `.ts` frontend et utilitaires ADB ne doivent plus retomber sur `admin@jobbingtrack.test`, `password123`, `user1@jobbingtrack.test` ou équivalents. Les credentials admin/user/mobile/email-verification sont lus via variables d’environnement (`TEST_ADMIN_EMAIL`, `TEST_ADMIN_PASSWORD`, `TEST_USER_EMAIL(S)`, `TEST_USER_PASSWORD`, `E2E_GENERATED_USER_PASSWORD`, `NEXT_PUBLIC_*`, `TEST_EMAIL_RECIPIENT`) et échouent explicitement si absents. Recherche ciblée `rg` sur les anciens secrets `.ts` : aucun match restant hors données mock non credential. | [x] |
| P1A | Nettoyage adresses email réelles dans l’état courant | Remplacement des adresses personnelles/projet routables par des placeholders non routables (`example.invalid`, `jobbingtrack.test`) dans les fichiers suivis. Preuve agent : scan scripté sur `git ls-files` + regex email → `TOTAL_ROUTABLE=0`. À distinguer de la purge historique Git, qui nécessitera une réécriture d’historique et force-push explicite. | [x] |
| P2 | Agent email — configuration d’envoi digest clarifiée | Le cadrage ne présente plus d’adresse factice spécifique comme adresse utilisable. Apps Script lit désormais `TRIAGE_DIGEST_RECIPIENT` depuis les propriétés du script et échoue si absent ; le futur digest natif JobbingTrack doit passer par le socle SMTP existant (`SMTP_*`, `SMTP_FROM`, `SMTP_REPLY_TO`, `EmailLog`) comme reset/validation/notifications. | [x] |
| P2 | Agent email — périmètre interface et suivi enrichi | Cadrage ajouté dans `docs/features/EMAIL_TRIAGE_AGENT.md`, `docs/PLAN.md` lot I, `docs/TODOS.md`, `docs/BACKLOG.md`, `docs/STATUS.md`, `docs/project/FONCTIONNALITES.md` et `TODOS_A_VALIDER.md` : interface web/mobile responsive, revalidation PIN, autocomplete accessible, boîte de réception agent, envoi email contrôlé, calendrier agrégé, programmation manuelle d’appels/tâches/rappels/événements même sans email déclencheur, appels préremplis, relances depuis fiche candidature, détail entreprise enrichi, suivi intérim, import Google Contacts CSV/vCard, sauvegarde PDF offre, enrichissement entreprise et veille salons/job dating par ville/région. | [x] |
| P2 | Agent email — architecture frontend utilisateur/backoffice | Cadrage ajouté : espace utilisateur connecté sur `/`, backoffice admin sur `/b4ck0ff1ce`, base de composants partagée, possibilité future de séparer `user-frontend` et `backoffice-frontend`. `.env.example` / `.env.production.example` clarifient `USER_FRONTEND_URL`, `BACKOFFICE_FRONTEND_URL`, `NEXT_PUBLIC_USER_FRONTEND_URL`, `NEXT_PUBLIC_BACKOFFICE_URL` et `ALLOWED_ORIGINS` (origines uniquement, pas chemins). Validation 09/06 : scan emails suivis `CURRENT_ROUTABLE_EMAILS=0`; `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé `tests/e2e/email-verification-monitor.spec.ts` OK ; diagnostics IDE OK. `npm run type-check` / `npm run lint` sortent encore `1` sans sortie exploitable dans ce terminal, comme constaté précédemment. | [x] |
| P2 | Agent email / tâches recherche emploi — cadrage porteur renforcé | `docs/features/EMAIL_TRIAGE_AGENT.md`, `docs/TODOS.md`, `docs/BACKLOG.md`, `docs/PLAN.md`, `docs/STATUS.md` et `TODOS_A_VALIDER.md` actent : JobbingTrack comme socle, pas Make.com/Zapier pour la logique métier, worker planifié, Gmail OAuth lecture seule, boîtes configurées hors Git, stockage interne emails utiles, règles déterministes, digest 18h, Google Tasks/Calendar obligatoires, IA locale en renfort, interface privée dédiée séparée du backoffice email transactionnel. | [x] |
| P1C | Backoffice Développement → Tests — navigation à réorganiser | Demande porteur ajoutée sans implémentation : clic direct sur Tests = vue d’ensemble ; sous-menu Rapports pour Rapports de tests + Rapports de parcours ; menu Tests moins long et mieux catégorisé. Suivi dans `TODOS_A_VALIDER.md` P1C et `docs/TODOS.md`, après P0 CVE. | [x] |
| P1D | Frontend validation scripts — sortie npm inexploitable | À diagnostiquer : `npm run type-check` et `npm run lint` peuvent sortir code 1 sans stdout/stderr utile alors que les commandes directes sous-jacentes passent. Vérifier scripts `package.json`, shell/env, redirections, wrappers, hooks, comportement CI et terminal Cursor ; l’objectif est d’obtenir une erreur lisible ou un code 0 cohérent. Reproduction 09/06 : `npm run type-check` = 1 sans sortie, `npm run lint` = 1 sans sortie ; `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé `src/lib/adb/adb-steps.ts` OK. | [ ] |
| P1A | Tests hors frontend — credentials codés en dur historiques | Dette ouverte après scan `tests/` : plusieurs anciens specs/helpers/fixtures contiennent encore des fallbacks `admin@jobbingtrack.test`, `password123`, `SecureP@ss123!` ou `admin123`. Première correction appliquée aux specs mobile email Gmail/Proton/BlueMail et au script `tests/email/run-inscription-gmail-email-check.js`; poursuivre fichier par fichier sans casser les fixtures explicitement mockées. | [ ] |
| P1A | Sécurité réseau avancée — cadrage menaces | Demande porteur notée dans `docs/security/SECURITY_TESTING_MATRIX.md`, `docs/security/ROADMAP_SECURITE_API_ET_BACKOFFICE.md`, `docs/TODOS.md` et `TODOS_A_VALIDER.md` : HTTP forgé, DNS poisoning, UPnP abuse, session hijack, IP spoofing, ICMP redirect, BGP hijack, ARP spoofing, MAC flooding, VLAN hopping, port scan/SYN scan/SYN flood contrôlé ; tests uniquement lab/préprod autorisée, bornés et non destructifs. | [x] |
| P0 | Comparaison CVE — statut dérivé des compteurs | `resolveReport.ts` + `securityStatus.ts` : statut `vulnerable/warning/skipped` recalculé depuis critical/high/medium/low (plus de `ok` si high>0) ; UI comparaison affiche libellé « À traiter » / « À surveiller ». `./node_modules/.bin/tsc --noEmit` OK. | [x] |
| P0 | Fiches finding CVE critical/high | `scripts/security/cve-audit-parse.mjs`, `cve-findings.mjs`, `GET /api/security/cve-findings`, enrichissement `cve-locate.mjs` ; UI `/b4ck0ff1ce/tests-security` : bouton « Charger findings critical/high » + cartes package/version/service/lockfile/exposition/exploitabilité/correctif. Smoke `/usr/bin/node scripts/security/cve-findings.mjs --limit 2` OK (35 findings détectés). `./node_modules/.bin/tsc --noEmit` OK. | [x] |
| P0 | Localisation CVE runtime Node (`CVE-2026-21710`) | `scripts/security/cve-locate.mjs` scanne désormais aussi les Dockerfiles Node connus ; `CVE-2026-21710` remonte `node-runtime` sur les images `node:20.18.0`, avec `patchedVersion=20.20.2`, surface HTTP Node et correctif. Test `/usr/bin/node tests/node_modules/jest/bin/jest.js --config tests/jest.config.js tests/security/cve-locate-runtime.test.js --runInBand` OK (1/1). UI `tests-security/page.tsx` affiche image, version corrigée, surface et correctif ; `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé OK avec warnings historiques uniquement. | [x] |
| P1 | Guide validation porteur P1A/P1B/P1C | `TODOS_A_VALIDER.md` : mode opératoire pas à pas avec URLs `localhost:5003`, menus backoffice, commandes terminal copiables, critères OK/KO et textes de réponse pour chaque ligne P1A/P1B/P1C. | [x] |
| P1A | Alertes email critiques — archivage validation porteur | Porteur confirme réception réelle `admin@delhomme.ovh` (smoke batch, validation P1A, E2E security-service), non-spam. Ligne retirée de `TODOS_A_VALIDER.md`, archivée dans `TODOS_DONE.md` (11/06). Notes backlog : sync Google Tasks bidirectionnelle, `security@jobbingtrack.com` en fin de programme. Commits `800e9d08`, `b6ff92d0`. | [x] |
| P1A | Alertes email — non-régression post-archivage | Jest SMTP **5/5**, payload+disponibilité **6/6** ; `tsc --noEmit` OK ; conteneurs `notification-service`, `security-service`, `frontend`, `postgres` healthy. | [x] |
| P1A | Tests offensifs contrôlés — pages de consultation accessibles | Après archivage P1A email, première ligne ouverte = **Tests offensifs contrôlés par conteneur JobbingTrack**. Vérification navigateur sans scan offensif : `/b4ck0ff1ce/tests-security` charge la page **Tests Sécurité** (tests applicatifs, scan CVE, couverture WAF/auth/injection/headers) ; `/b4ck0ff1ce/test-reports` charge les rapports. Documents de cadrage relus : `docs/security/SECURITY_TESTING_MATRIX.md` et `docs/security/COMPOSE_RUNTIME_HARDENING.md`. | [x] |
| P1A | Tests offensifs contrôlés — préflight non destructif | `scripts/security/controlled-offensive-preflight.cjs` ajouté : classe la cible `allowed` / `needs_approval` / `blocked`, bloque la prod sans fenêtre explicite, demande approbation pour cible publique externe et ne lance aucun payload (`dryRun=true`). Preuves : `/usr/bin/node scripts/security/controlled-offensive-preflight.cjs --target=http://localhost:5002 --environment=local --json` → `allowed`; Jest direct `./frontend/node_modules/.bin/jest --config tests/jest.config.js --runTestsByPath tests/security/controlled-offensive-preflight.test.js` → **5/5 OK** ; `frontend npm run type-check` OK ; `frontend npm run lint` OK ; diagnostics IDE sans erreur sur fichiers touchés. | [x] |
| P1A | Tests offensifs contrôlés — manifeste périmètre lab | `scripts/security/controlled-offensive-lab-scope.cjs` ajouté : combine le préflight et une matrice services/scénarios (`api-gateway`, `auth-service`, `application-service`, `notification-service`, `security-service`, `monitoring-agent-rs`) en mode `plan-only`, `dryRun=true`, `willRunPayload=false`. Objectif : validation porteur du périmètre avant tout runner actif. Preuves : script local `--json` → `ready_for_review`; Jest direct `controlled-offensive-preflight.test.js` + `controlled-offensive-lab-scope.test.js` OK. | [x] |
| P1A | Archive logs sécurité sans purge (preuve agent) | `/usr/bin/env node scripts/security/security-logs-archive-export.cjs --class=noise --limit=50` OK → `data/archives/security-logs/2026-06-10/manifest.json` + `noise.jsonl.gz` (50 lignes, sha256 `04194f6b7cee…`) ; `security_logs` **42311** avant et après ; note manifest « Export only — no rows deleted ». | [x] |
| P1A | Restauration staging logs sécurité (preuve agent) | `/usr/bin/env node scripts/security/security-logs-archive-restore.cjs --class=noise --load-staging` OK : `verified=50`, `staging_loaded=50`, `No write to public.security_logs` ; `security_logs_restore_staging` = **50** ; `security_logs` inchangé **42311**. | [x] |
| P0 | Correctif `make up-full` — SMTP_FROM docker-compose | `docker-compose.yml` L751 : `${SMTP_FROM:redacted@…}` → `${SMTP_FROM:-redacted@…}` (syntaxe défaut Compose). `docker compose config --quiet` OK ; stack relancée via `docker compose` (postgres, full, https) ; `notification-service` healthy. | [x] |
| P1A | Gate validation — 6 commits alertes email (`8a6b38b2`→`ac057bf5`) | Jest SMTP **5/5**, payload+disponibilité **6/6** ; `tsc` + ESLint ciblés OK ; conteneurs clés healthy ; mount `notification-service/src` confirmé ; smoke `BATCH VALIDATION 6 COMMITS 2026-06-10T19:46:11Z` → API `202`, `metadata.mirror.sent=true`, `messageId 20b94457-…@maily.ovh`. Détail dans `docs/STATUS.md` § gate validation 6 commits. | [x] |
| P1A | Alertes email — diagnostic admin retiré après OK porteur | Après validation réelle des alertes critiques, `SecurityAlertEmailDiagnostics` ne met plus en avant une adresse admin dédiée : suppression de la requête `q=admin...`, de l’encart « Dernier envoi vers admin » et du surlignage ciblé. Le panneau conserve la liste claire des derniers `EmailLog` `NOTIFICATION`, statut, badge miroir SMTP et `messageId`. | [x] |
| P1A | Alertes email — diagnostic derniers envois page Sécurité | Composant `SecurityAlertEmailDiagnostics` sur `/b4ck0ff1ce/security/alerts` : 8 derniers `EmailLog` type `NOTIFICATION`, statut, badge miroir SMTP, `messageId`, lien Email Monitor. Test E2E `securityAlertEmailNotifier.notifySecurityAlert` OK (`3/3` destinataires `sent:true`). `tsc` + ESLint ciblés OK. | [x] |
| P1A | notification-service — montage src Compose dev | Cause racine : `notification-service` n'avait pas de volume `./backend/notification-service/src:/app/src` (contrairement à `security-service`), donc le conteneur exécutait une image figée sans `awaitSecurityAlertMirror` ni `getCrashReportIdentity`. Correctif `docker-compose.yml` + `docker compose up -d --force-recreate notification-service` : conteneur aligné (`emailService.js` 302 lignes), `metadata.mirror` persisté en BDD (`TEST ADMIN P1A MIRROR META …` → `sent:true`, `messageId @maily.ovh`). | [x] |
| P1A | Alertes email — contenu enrichi + statut miroir Email Monitor | `securityAlertEmailNotifier.buildPayload()` enrichi : service touché, type, horodatage, erreur/statut, liens Email Monitor / sécurité, métadonnées redigées (`smtp_pass`, `token`, etc.). `notification-service` attend désormais le miroir SMTP pour les alertes internes et persiste `metadata.mirror` (`sent`, `messageId`, `from`, `replyTo`, `error`) dans `EmailLog`. Email Monitor affiche badges `Miroir SMTP OK/KO` et panneau détail dans la modal. Jest payload `3/3` + SMTP `5/5` OK ; `tsc`/`eslint` ciblés OK. | [x] |
| P1A | Alertes email sécurité — réauth + test + navigation + diagnostic admin | Page `/b4ck0ff1ce/security/alerts` ; liens ajoutés menu latéral **Sécurité → Alertes email**, popup **Paramètres → Notifications** (bouton rouge), `/settings` onglet Notifications. Fix hydratation : `SecurityAlertEmailSettings` — `Badge` hors `<p>`. Cause KO MailHog : `notification-service` héritait du SMTP réel ; correction : transport principal local `mailhog:1025` + miroir SMTP réel async pour alertes sécurité. Destinataires actifs alignés : `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh`. Diagnostic ajouté : `/b4ck0ff1ce/email-monitor?type=NOTIFICATION`, filtre **Notification** actif, emails d’alerte visibles avec statut/destinataire/contenu, lien **Gestion des emails → Historique** corrigé vers cette route. Email Monitor : pagination serveur branchée sur le composant réutilisable `Pagination`, recherche `q` sur destinataire/expéditeur/sujet avec badge de recherche ; API `q=admin@delhomme.ovh` OK (`total=8`, 5 lignes page 1), UI OK (champ + badge + pagination). Nettoyage local EmailLog demandé porteur : `8971` lignes / `19 MB` → suppression anciens `FAILED`/`PENDING` (`8722`) + `VACUUM FULL ANALYZE` → `249` lignes / `720 kB`, notifications récentes `admin/dev/security` conservées. Correctif KO boîte admin 20:15 : `.env` local avait `SECURITY_ALERT_SMTP_MIRROR_ENABLED=false`; miroir réactivé, `SECURITY_ALERT_MIRROR_SMTP_FROM` aligné sur le compte SMTP authentifié, `Reply-To` conservé sur alias sécurité ; `emailService` force par défaut le `From` miroir vers l’utilisateur SMTP pour limiter les rejets silencieux. Validation 10/06 : API test admin isolé `202` sujet `TEST ADMIN OVH FROM AUTH 2026-06-10T20:15:03+02:00`, MailHog OK, miroir OVH planifié puis accepté (`messageId <59b166d5-1191-9465-2fd3-0c89ea94c2c5@maily.ovh>`), règle STARTTLS corrigée (`587 => secure:false`, `465 => secure:true`), test navigateur nettoyage utilisateurs `/b4ck0ff1ce/users` OK (`2441` utilisateurs de test supprimés, compteur BDD `0` après), `docker compose config --quiet` OK, `node --check` OK, test Jest SMTP `4/4` OK, `tsc --noEmit` OK, ESLint ciblé OK avec warnings historiques uniquement. | [x] |
| P1C | Responsive Email Monitor petit/moyen écran | Correctif ciblé `/b4ck0ff1ce/email-monitor?type=NOTIFICATION` : header/actions empilables, filtres sans `min-width` bloquant, cartes emails `min-w-0` + retours de ligne sur sujets/adresses longues, metadata en une colonne sur petit écran, modal contenu protégée par `overflow-x-auto`, pagination réutilisable qui wrappe, et `AdminLayout` corrigé pour ne plus appliquer `md:ml-80` / `md:pl-12` quand la sidebar est encore en mode mobile/overlay. Validation 10/06 : navigateur à `390x844` puis `820x844`, page visible sans contenu poussé/coupé sur le côté ; `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé `email-monitor`, `Pagination`, `AdminLayout` OK avec warnings historiques uniquement ; lints IDE OK. | [x] |
| P1C | Responsive popup Paramètres | `SettingsPopup` repris pour petit/moyen écran : overlay `p-2`, conteneur `flex` limité à `92vh`, header compact, onglets horizontaux scrollables en mobile puis sidebar interne à partir de `md`, contenu `min-w-0`/scroll vertical, grilles système en 1 colonne mobile, toggles avec `flex-shrink-0`, champs/selects `min-w-0`. Validation 10/06 : navigateur `390x844`, popup ouverte depuis `/b4ck0ff1ce`, capture `/tmp/cursor/screenshots/settings-popup-mobile.png`, onglets et contenu visibles sans découpe latérale de la popup ; `tsc --noEmit` OK ; ESLint ciblé OK avec warnings historiques uniquement. | [x] |
| P1A | Faux positif alerte `metrics-aggregator` indisponible | Diagnostic 10/06 : conteneurs `jobbingtrack-metrics-aggregator`, `security-service`, `notification-service`, `monitoring-agent-rs`, `log-collector-rs`, `postgres`, `auth`, `deployment`, `call`, `followup`, `application` tous `Up (healthy)` ; logs `metrics-aggregator` `/api/v1/health` en `200`. Cause alerte : `security-service` appelait `/api/v1/docker/services/all` sans `X-API-Key` et recevait `401`. Correctif : `securityScheduler.checkServiceAvailabilityAlerts()` transmet `METRICS_API_KEY`; test `service-availability-alerts` mis à jour ; `security-service` redémarré ; smoke depuis le conteneur `security-service` vers `metrics-aggregator` OK (`200`, `23` services). | [x] |
| P1B | Cadrage stats temps de réponse endpoints | Demande porteur ajoutée dans `TODOS_A_VALIDER.md` / `docs/TODOS.md` : reprendre la vue temps de réponse instantané/moyen pour couvrir `auth-service`, `deployment-service`, `call-service`, `notification-service`, `followup-service`, `application-service`, `postgres`, avec source claire `monitoring-agent-rs` / `metrics-aggregator` et distinction endpoint HTTP vs dépendance santé. | [x] |
| P1A | Crash-report SMTP — From authentifié / Reply-To alias | `.env.example` et `docs/deployment/environment-variables/README.md` alignés ; code `emailService.getCrashReportIdentity()` dans `notification-service` : `From` aligné sur `SMTP_USER` par défaut, `Reply-To` sur `CRASH_REPORT_REPLY_TO`. Jest SMTP `5/5` OK. Test isolé admin P1A relancé (`TEST ADMIN OVH P1A …`) : API `202`, MailHog OK, miroir OVH accepté ; confirmation boîte réelle porteur en attente. | [x] |
| P1A | Cadrage sécurité — ggshield / GitGuardian secrets | Demande porteur ajoutée dans `docs/TODOS.md` et `docs/STATUS.md` : installer/configurer `ggshield`, documenter `ggshield auth login` et scans repo, prévoir hooks Git `pre-commit` / `pre-push` / `pre-receive`, scans images Docker, puis étudier GitGuardian API/webhooks pour incidents temps réel dans le backoffice sécurité + emails d’alerte, sans stocker les secrets bruts. Implémentation non démarrée tant que la validation email P1A reste bloquante. | [x] |
| P1A | Emails crash/sécurité — alias publics et identité d’envoi | `notification-service` supporte désormais `CRASH_REPORT_FROM`, `CRASH_REPORT_REPLY_TO`, `SECURITY_ALERT_FROM`, `SECURITY_ALERT_REPLY_TO` en plus des destinataires `CRASH_REPORT_EMAIL` / `SECURITY_ALERT_EMAIL(S)`. Les exemples `.env*` utilisent des alias non routables `jobbingtrack.test`; les vraies redirections alias public -> boîte privée restent chez le fournisseur mail et hors Git. Validation 09/06 : diagnostics IDE OK ; scan `CURRENT_ROUTABLE_EMAILS=0` ; `rg` ciblé sans adresse privée réelle. Non validé par Jest/Node local car `npm test`, `node --check` et même `node --version` sortent code 1 sans sortie dans ce terminal. | [x] |
| P2 | Agent email — Gmail multi-comptes, Calendar sans minuit, recherche v2 | Cadrage renforcé : OAuth Gmail multi-comptes, liste/statut/révocation des comptes connectés, digest sans IA obligatoire, IA locale gratuite en renfort plus tard, barre de recherche v2 réutilisable espace utilisateur puis backoffice/admin, et garde-fou Calendar/Tasks : ne jamais créer d’événement à `00:00` sans heure explicite ; date seule = journée entière proposée ou tâche “horaire à confirmer”. Validation 10/06 : `CURRENT_ROUTABLE_EMAILS=0`, scan ciblé sans adresse privée réelle, `./node_modules/.bin/tsc --noEmit --pretty false` OK ; `npm run type-check` / `npm run lint` sortent encore code 1 sans sortie exploitable. | [x] |
| P2 | Agent email — accès compte personnel non-admin | Cadrage ajouté : premier usage via compte personnel non-admin explicitement autorisé du porteur, pas activation automatique pour tout compte inscrit ; droit/feature flag utilisateur, activation/révocation admin auditée, séparation stricte entre rôle admin/backoffice et lecture des emails personnels. | [x] |
| P2 | Agent email — fenêtres Calendar et suite de tests | Cadrage ajouté : pas d’événement Calendar automatique à `00:00`, avant `05:00` ou après `23:00`; date seule ou horaire hors fenêtre = tâche/proposition à confirmer. Suite de tests future cadrée : règles de tri, permissions, Gmail/IMAP lecture seule, digest SMTP/mock, Calendar/Tasks, rapports `tests/results/email-triage/<timestamp>`, secrets uniquement via `.env` gitignoré et skip explicite si absents. | [x] |
| P2 | Agent email — socle tests Calendar unitaire | `tests/email-triage/lib/calendar-time-policy.js` + `calendar-time-policy.test.js` + `run-with-report.sh` + README ; variables documentées dans `.env.example` et `docs/deployment/environment-variables/README.md`. Validation 10/06 : `/usr/bin/node tests/node_modules/jest/bin/jest.js --config tests/jest.config.js tests/email-triage/calendar-time-policy.test.js --runInBand` **7/7 OK** ; `npm run` / `npx jest` peuvent encore sortir code 1 sans sortie dans ce terminal. | [x] |
| P1B | Alignement sondes temps de réponse metrics-aggregator | Source unique `backend/metrics-aggregator-service/src/config/serviceHealthEndpoints.js` ; `server.js` + `docker.routes.js` importent la config ; sondes via nom conteneur Docker (pas `localhost:800x`) ; postgres/redis sans sonde HTTP ; mount dev `./backend/metrics-aggregator-service/src:/app/src` dans `docker-compose.yml` + recreate conteneur. Smoke `GET /api/v1/docker/services/all` (depuis conteneur) : auth **5ms**, notification **13ms**, followup **11ms**, application **6ms**, call **6ms**, deployment **10ms** (après fix chemin `/health`), postgres `responseTime=null` (attendu). `/usr/bin/node --check` OK sur `server.js`, `docker.routes.js`, `serviceHealthEndpoints.js`. | [x] |
| P1B | monitoring-agent-rs — chemins health alignés | `monitoring/rust/crates/monitoring-agent/src/constants.rs` : notification **3008** `/health`, profile/workflow/deployment/security **`/health`**, log-collector **3019** `/health`. Rebuild image requis pour prise en compte runtime (hors scope smoke immédiat metrics-aggregator). | [x] |
| P1B | UI Statistics / Performances — temps de réponse P1B | `responseTimePresentation.ts` : services prioritaires, label « Santé Docker » postgres/redis, note source ; `serviceHealthOverview.ts` : `responseTimeLabel`, `nonHttpDependency` ; encarts source sur `/b4ck0ff1ce/statistics` et `/b4ck0ff1ce/performances/latency`. `./node_modules/.bin/tsc --noEmit` OK ; ESLint ciblé 6 fichiers OK (warnings historiques `statistics/page.tsx` uniquement). | [x] |
| P1C | Menu Développement → Tests / Rapports | `AdminLayout.tsx` : sous-menu **Rapports** (`Rapports de tests`, `Rapports de parcours`) séparé ; entrée « Rapports de tests » retirée du sous-menu Tests. Validation navigateur porteur en attente. | [x] |
| P1A | Tests offensifs — encart cadrage UI | `/b4ck0ff1ce/tests-security` : encart « Tests offensifs contrôlés (cadrage P1A) » avec liens matrice `SECURITY_TESTING_MATRIX.md`, durcissement Compose, rapports sécurité — **aucun scan offensif lancé**. | [x] |
| P1B | Statistics — panneau services prioritaires temps de réponse | Composant `PriorityResponseServicesSummary` sur vue d’ensemble Statistics + onglet services ; `responseTimeLabel` par carte ; lien Performances → Latence. `./node_modules/.bin/tsc --noEmit` OK. | [x] |
| P1C | Menu Tests — sections Automatisés / Sécurité & charge | `AdminLayout` : `sectionLabel` pour en-têtes non cliquables ; doublon « Rapports de parcours » retiré du menu Parcours (accès via sous-menu Rapports). | [x] |
| P1A | Leurres VPS — design réduction d’exposition | `docs/security/VPS_EXPOSURE_REDUCTION.md` : priorité réduction ports/réseaux, leurres en phase ultérieure isolée, critères validation porteur. | [x] |
| P1A | Tests offensifs — tableau périmètre UI | `/b4ck0ff1ce/tests-security` : `<details>` tableau lecture seule (WAF, CVE, réseau lab, ZAP, leurres) — aucun bouton scan agressif ajouté. | [x] |
| P1B | monitoring-agent-rs — rebuild image health paths | `docker compose build monitoring-agent-rs` + `--force-recreate` OK ; conteneur healthy ; smoke deployment/security/auth/notification via metrics-aggregator (rt 7–8 ms, http=healthy). | [x] |
| P1B | Hub backoffice — panneau latence prioritaire | `/b4ck0ff1ce` : `PriorityResponseServicesSummary` sous KPI santé services. `tsc --noEmit` OK. | [x] |
| P1B | Suite Jest P1B temps de réponse | Tests ajoutés : `PriorityResponseServicesSummary.test.tsx` + enrichissement `serviceHealthOverview.test.ts` (prioritaires, ordre, Postgres « Santé Docker », lien Latence, état vide). Commande directe : `./node_modules/.bin/jest --runTestsByPath src/lib/metrics/__tests__/serviceHealthOverview.test.ts src/components/monitoring/PriorityResponseServicesSummary.test.tsx --runInBand --silent` → **2 suites / 12 tests OK**. Script ajouté : `frontend` `test:metrics-p1b` (mais `npm run` reste impacté par la dette sortie vide). | [x] |
| P1B | Bouton backoffice suite P1B latence | Hub `/b4ck0ff1ce/tests` : bouton **Lancer suite P1B latence** + carte **Suite P1B latence** ; API `POST /api/test/run-metrics-p1b`. Smoke HTTP OK → rapport `tests/results/20260610-231609` (`frontend.json`, `summary.json`, `report.html`). Correctif `generate-test-report.sh` : suppression du here-string bash pour rester compatible `sh` dans le conteneur frontend. | [x] |
| P1A | Emails — non-régression continue | Jest notification SMTP **5/5 OK** ; Jest security payload + disponibilité **6/6 OK** ; MailHog `total=56`, derniers emails P1A visibles (`BATCH VALIDATION 6 COMMITS`, `VALIDATION PORTEUR P1A`, E2E security-service) ; Postgres `EmailLog` derniers `NOTIFICATION` en `SENT` pour `admin@delhomme.ovh` / `dev@delhomme.ovh`. | [x] |
| P1 | Validation A-Z agent — programme | `docs/operations/VALIDATION_A_Z_AGENT.md` ajouté : ordre A→I (Docker, API, backend, emails, frontend, backoffice, mobile, sécurité non destructive, doc), critères de fin de lot, interdits prod/purge/scans agressifs. | [x] |
| P1 | Validation A-Z agent — passe 11/06 01h20 | Stack : conteneurs `jobbingtrack-*` healthy sauf `monitoring-agent-rs` encore `starting` au `docker ps` mais `/health` OK ; HTTP `gateway/frontend/metrics/monitoring` OK ; P1B frontend `tsc` OK + Jest **12/12** ; notification SMTP **5/5** ; security email **6/6** ; metrics-aggregator backend **6/6** ; services prioritaires metrics OK (`auth`, `deployment`, `call`, `notification`, `followup`, `application`, `postgres`). | [x] |
| P1 | Email validation agent — lot P1 tests | Email envoyé depuis `security-service` sujet `VALIDATION AGENT P1 LOT TESTS 2026-06-10T23:21:52.461Z` vers `security@jobbingtrack.com`, `dev@delhomme.ovh`, `admin@delhomme.ovh` ; MailHog `total=59` ; `EmailLog` `SENT`; miroir SMTP réel `metadata.mirror.sent=true` avec `messageId @maily.ovh` pour les 3 destinataires. | [x] |
| P2 | Mobile Flutter — smoke widget sans appareil | `flutter test test/widget_test.dart` dans `mobile/` : **1/1 OK** (écran connexion). | [x] |
| P2 | ADB appareil physique S21 FE | **11/06 01h25** : appareil visible `R5CT7263YJL`, modèle `SM-G990B2`, Android `16`; `emulator-controller` `:5055` voit `devices:[R5CT7263YJL]`; UI dump brut OK, écran connexion mobile visible (`Connexion — Cloudity Photos`). Helper `tools/adb-lib/examples/inspect-ui.js` sort encore code 1 sans sortie exploitable à diagnostiquer. | [x] |
| P1 | monitoring-agent-rs — boucle health `starting` | Cause : `BrokenPipe` sur requête healthcheck (`curl` ferme la connexion) remontait via `?` dans `serve()` et tuait le processus (`RestartCount` > 2600). Correctif `monitoring/rust/crates/monitoring-agent/src/http.rs` : erreurs client bénignes ignorées par requête ; `start_period: 30s` sur healthcheck Compose. Preuve post-recreate : `Up … (healthy)`, `RestartCount=0`, `GET http://localhost:5118/health` → `200` JSON `status: ok`. | [x] |
| P1 | HTTPS dev LAN téléphone — IP PC 5443 | Accès dev `https://192.168.1.134:5443` pris en charge sans modifier la prod : `ports.config.ts` bascule l’API en same-origin sur IP LAN HTTPS (`/api/v1` via Nginx), `production/nginx/dev-https/default.conf` proxifie `/api/*` vers gateway, `api-gateway` accepte les origines LAN privées HTTP/HTTPS hors production, `dev-https-certs.sh` ajoute l’IP LAN dans les SAN. Preuves : cert SAN `IP Address:192.168.1.134`, `curl -k` login LAN `200`, API health LAN `200`, préflight CORS `Access-Control-Allow-Origin: https://192.168.1.134:5443`. ADB encore vide, donc validation écran téléphone à confirmer porteur. | [x] |
| P2 | Mobile physique — setup ADB + API LAN | Branche `feat/mobile-adb-physical-device` : script `scripts/mobile/setup-physical-device.sh` (attente ADB, `adb reverse`, build APK avec `MOBILE_DEV_LAN_HOST`, install/launch) ; smoke `scripts/mobile/smoke-login-adb.js` (creds `.env`) ; `ApiService` persiste l'URL API (`shared_preferences`) et sonde IP LAN build ; `flows.js` lit `TEST_USER_*` / `TEST_ADMIN_*`. Validations : `flutter test test/widget_test.dart` **1/1 OK** ; APK debug buildé (`app-debug.apk`, dart-define `192.168.1.134`). **Blocage session 11/06 ~12h** : `adb devices` vide depuis l'agent (câble/autorisation USB à confirmer côté poste) — relancer `bash scripts/mobile/setup-physical-device.sh` une fois l'appareil visible. | [ ] |
| P2 | Agent email — digest programmé et identité JobbingTrack | Socle tests étendu : `digest-schedule-policy` (quotidien 18h, hebdo optionnel, horaires 05:00–23:00) + `digest-identity-policy` (expéditeur visible `@jobbingtrack.com`, défaut `JobbingTrack <noreply@jobbingtrack.com>`, refus expéditeur Gmail personnel et destinataires placeholders). Variables `.env.example` ajoutées : `EMAIL_TRIAGE_DIGEST_FROM`, `EMAIL_TRIAGE_DIGEST_DAILY_TIME`, hebdo, timezone. Suite complétée par `classification-rules` : refus, entretien, 2e entretien, test technique, salon emploi, relance après 7j, newsletter/bruit, revue manuelle. Preuves : Jest email-triage **4 suites / 28 tests OK** ; `bash tests/email-triage/run-with-report.sh` OK → `tests/results/email-triage/20260612-001512` (`28 passed`). | [x] |
| P2 | Backoffice Tests — suite Agent email / triage lançable | Interface `/b4ck0ff1ce/tests` : ajout carte sélectionnable **Agent email / triage**, bouton rapide **Lancer suite agent email**, endpoint `POST /api/test/run-email-triage`, rapport listé dans `/b4ck0ff1ce/test-reports` via catégorie **Agent email / triage** et ID `email-triage-<timestamp>`. `tests/email-triage/run-with-report.sh` respecte maintenant `TESTS_RESULTS_DIR` pour le volume writable du frontend. Test UI catalogue ajouté : `testsCatalog.test.ts` garantit `email-triage` lançable + endpoint API. Validations : Jest catalogue **2/2 OK** ; `bash tests/email-triage/run-with-report.sh` → `tests/results/email-triage/20260612-004238` **28/28 OK** ; `frontend` `npm run type-check` OK ; `frontend` `npm run lint` OK (warnings historiques uniquement) ; lints IDE OK. | [x] |
| P2 | Suite CLI globale — intégration email-triage | `scripts/run-all-tests-with-reports.sh` section **34b** : exécute les 4 suites Jest email-triage (`classification-rules`, `calendar-time-policy`, `digest-schedule-policy`, `digest-identity-policy`) et écrit `email-triage.json` dans le dossier rapport horodaté. Preuve : `bash tests/email-triage/run-with-report.sh` → `20260612-004238` **28/28 OK** ; Jest catalogue hub **2/2 OK** ; `npm run type-check` OK ; `npm run lint` exit 0 (warnings historiques). | [x] |
| P2 | Emails — séparation MailHog local et SMTP préprod/prod | Docs/env alignés : `docs/emails/SMTP_CONFIGURATION.md`, `docs/emails/MAIL.md`, `docs/deployment/environment-variables/README.md`, `.env.example`, `.env.production.example`, `docker-compose.yml`, `docs/operations/PREPROD_PRODUCTION_CHECKLIST.md`, `A_VALIDER_AVANT_PRODUCTION.md`, `DEPLOIEMENT_PRODUCTION.md`. Règle écrite et automatisée : MailHog local/dev/test uniquement ; préprod/prod via SMTP réel, `SMTP_HOST` jamais `mailhog`/`localhost`, `NOTIFICATION_SMTP_HOST` ne force pas MailHog, smoke réception réelle + `EmailLog` `SENT`. Preuves : `docker compose config --quiet` OK ; `/usr/bin/node scripts/env/env-validate-runtime.cjs --production-example` OK ; Jest `tests/env/env-validate-runtime.test.js` **3/3 OK** ; `npm run type-check` OK ; `npm run lint` exit 0 (warnings historiques). Mail récap `RAPPORT AGENT JOBBINGTRACK — MailHog local / SMTP prod` envoyé vers `pauldelhomme.pro@gmail.com`, `dev@delhomme.ovh`, `test@delhomme.ovh` : HTTP **202** x3, `EmailLog` **SENT** x3. | [x] |
| P2 | Alertes sécurité — expéditeur visible JobbingTrack Security | `notification-service` : `getSecurityAlertIdentity()` + normalisation si seul `noreply@maily.ovh` (ou équivalent SMTP auth) est configuré → expéditeur visible `JobbingTrack Security <compte-auth>`, `Reply-To` alias sécurité conservé. Tests `email-service-smtp-config.test.js` + `internal-security-alert-email.test.js` **8/8 OK** ; `notification-service` recréé. Mail récap `RAPPORT AGENT JOBBINGTRACK — identité JobbingTrack Security` : HTTP **202** x3, `EmailLog` **SENT** x3. | [x] |
| P2 | Agent email — permissions, digest rendu, connexion boîte mail | Libs `tests/email-triage/lib/agent-access-policy.js`, `digest-renderer.js`, `mail-connection-policy.js` + tests Jest dédiés. Preuves : `/usr/bin/node tests/node_modules/jest/bin/jest.js --config tests/jest.config.js tests/email-triage/ --runInBand` → **8 suites / 45 passed, 1 skipped** ; `bash tests/email-triage/run-with-report.sh` OK ; `run-with-report.sh` + `scripts/run-all-tests-with-reports.sh` (34b) incluent les 8 fichiers ; `.env.example` documente `JOB_SEARCH_AGENT_ENABLED` (droit utilisateur) ; README email-triage mis à jour. Intégration Gmail/IMAP réelle reste skip tant que `TEST_EMAIL_TRIAGE_ENABLED=false` ou credentials placeholder. | [x] |
| P1A | Backoffice Tests — périmètre offensif contrôlé lançable | Hub `/b4ck0ff1ce/tests` : carte **Tests offensifs contrôlés**, bouton **Périmètre offensif lab**, `POST /api/test/run-controlled-offensive-lab-scope`, script `scripts/security/run-controlled-offensive-lab-scope-with-report.sh` (Jest preflight+lab-scope **9/9 OK**, manifeste `lab-scope.json`, `dryRun=true`, `willRunPayload=false`). Rapports listés `/b4ck0ff1ce/test-reports` ID `controlled-offensive-<timestamp>`. Validations : `bash scripts/security/run-controlled-offensive-lab-scope-with-report.sh` → `tests/results/controlled-offensive/20260612-012648` ; Jest catalogue hub **3/3 OK** ; `frontend npm run type-check` OK. | [x] |
| P1C | Responsive backoffice complet — smartphone/moyen | Correctifs layout sur `AdminLayout`, pages sécurité/services/tests/parcours/API tester/emails/events/mobile/corbeille/utilisateurs. Spec Playwright `frontend/tests/e2e/backoffice-responsive-audit.spec.ts` couvre les routes backoffice statiques et 4 viewports. Validation 11/06 : `./node_modules/.bin/playwright test tests/e2e/backoffice-responsive-audit.spec.ts --project=chromium --workers=1 --grep smartphone` **2/2 OK** ; `--grep moyen` **2/2 OK** ; audit complet précédent : petit PC + grand écran OK, smartphone/moyen corrigés ensuite. `./node_modules/.bin/tsc --noEmit --pretty false` OK ; ESLint ciblé 19 fichiers **0 erreur**, warnings historiques uniquement ; lints IDE OK. Email suivi `VALIDATION AGENT RESPONSIVE BACKOFFICE 2026-06-11T09:21:51.634Z` : MailHog OK, `EmailLog` `SENT`, miroir SMTP réel `metadata.mirror.sent=true` (`messageId @maily.ovh`). | [x] |

## Vérifications récurrentes

- Lire `PILOTAGE.md` avant de choisir une prochaine tâche.
- Contrôler `TODOS_A_VALIDER.md` avant de commencer une feature.
- Ne travailler que sur la première ligne ouverte de `TODOS_A_VALIDER.md`; attendre validation porteur avant la suivante.
- Si une validation porteur échoue, créer une tâche dans `docs/TODOS.md` et traiter ce problème avant la suite.
- Après chaque livrable, mettre à jour `TODOS_A_VERIFIER.md`, `TODOS_A_VALIDER.md` ou `TODOS_DONE.md` selon le cas.
